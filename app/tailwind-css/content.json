{"html": "<!DOCTYPE html>\n<html lang=\"en\">\n<head>\n<meta charset=\"UTF-8\">\n<title>CSS &amp; Tailwind Interview Mastery</title>\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1\">\n<link rel=\"preconnect\" href=\"https://fonts.googleapis.com\">\n<link rel=\"preconnect\" href=\"https://fonts.gstatic.com\" crossorigin>\n<link href=\"https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500;600;700&family=IBM+Plex+Sans:wght@400;500;600;700&display=swap\" rel=\"stylesheet\">\n<style>\n  :root{\n    --bg:#F6F4EF;\n    --panel:#FFFFFF;\n    --panel-2:#FBF9F4;\n    --border:#E4E0D4;\n    --text:#211F1B;\n    --muted:#716B5E;\n    --accent:#2F7A72;\n    --accent-ink:#F3FBF9;\n    --accent-soft:#E3F1EE;\n    --gold:#B8792F;\n    --gold-soft:#F5E9D8;\n    --code-bg:#EFECE2;\n    --danger:#B5502F;\n    --shadow: 0 1px 2px rgba(30,26,18,0.05);\n    --mono: 'IBM Plex Mono', ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;\n    --sans: 'IBM Plex Sans', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;\n  }\n  @media (prefers-color-scheme: dark){\n    :root:not([data-theme=\"light\"]){\n      --bg:#15181A;\n      --panel:#1B1F21;\n      --panel-2:#202426;\n      --border:#2C3133;\n      --text:#E9E6DE;\n      --muted:#9A968A;\n      --accent:#5FBFB2;\n      --accent-ink:#0B1716;\n      --accent-soft:#1E3230;\n      --gold:#D9A15C;\n      --gold-soft:#332918;\n      --code-bg:#20262A;\n      --danger:#D98866;\n      --shadow: 0 1px 2px rgba(0,0,0,0.4);\n    }\n  }\n  :root[data-theme=\"dark\"]{\n    --bg:#15181A;\n    --panel:#1B1F21;\n    --panel-2:#202426;\n    --border:#2C3133;\n    --text:#E9E6DE;\n    --muted:#9A968A;\n    --accent:#5FBFB2;\n    --accent-ink:#0B1716;\n    --accent-soft:#1E3230;\n    --gold:#D9A15C;\n    --gold-soft:#332918;\n    --code-bg:#20262A;\n    --danger:#D98866;\n    --shadow: 0 1px 2px rgba(0,0,0,0.4);\n  }\n\n  *{box-sizing:border-box;}\n  html,body{margin:0;padding:0;}\n  body{\n    background:var(--bg);\n    color:var(--text);\n    font-family:var(--sans);\n    line-height:1.55;\n    -webkit-font-smoothing:antialiased;\n  }\n  a{color:var(--accent);}\n\n  .app{\n    display:flex;\n    min-height:100vh;\n  }\n\n  /* ---------- Sidebar ---------- */\n  .sidebar{\n    width:260px;\n    flex:0 0 260px;\n    border-right:1px solid var(--border);\n    padding:28px 16px 24px;\n    position:sticky;\n    top:0;\n    height:100vh;\n    overflow-y:auto;\n  }\n  .brand{\n    font-family:var(--mono);\n    font-size:13px;\n    letter-spacing:0.02em;\n    color:var(--muted);\n    padding:0 8px 18px;\n    border-bottom:1px solid var(--border);\n    margin-bottom:14px;\n  }\n  .brand b{color:var(--text);font-weight:600;}\n  .nav-item{\n    display:flex;\n    align-items:center;\n    justify-content:space-between;\n    gap:10px;\n    width:100%;\n    text-align:left;\n    padding:10px 10px;\n    border-radius:8px;\n    border:1px solid transparent;\n    background:transparent;\n    color:var(--text);\n    font-family:var(--sans);\n    font-size:14.5px;\n    cursor:pointer;\n    margin-bottom:2px;\n  }\n  .nav-item:hover{background:var(--panel-2);}\n  .nav-item.active{\n    background:var(--accent-soft);\n    border-color:var(--accent);\n    color:var(--text);\n    font-weight:600;\n  }\n  .nav-num{\n    font-family:var(--mono);\n    font-size:12px;\n    color:var(--muted);\n    width:18px;\n    flex:0 0 18px;\n  }\n  .nav-item.active .nav-num{color:var(--accent);}\n  .nav-label{flex:1;}\n  .nav-count{\n    font-family:var(--mono);\n    font-size:11px;\n    color:var(--muted);\n    background:var(--panel-2);\n    border:1px solid var(--border);\n    padding:1px 6px;\n    border-radius:999px;\n  }\n  .progress-box{\n    margin-top:20px;\n    padding:14px 12px;\n    border:1px solid var(--border);\n    border-radius:10px;\n    background:var(--panel-2);\n  }\n  .progress-box .label{\n    font-family:var(--mono);\n    font-size:11px;\n    color:var(--muted);\n    margin-bottom:8px;\n  }\n  .progress-track{\n    height:6px;\n    border-radius:999px;\n    background:var(--border);\n    overflow:hidden;\n  }\n  .progress-fill{\n    height:100%;\n    background:var(--accent);\n    width:0%;\n    transition:width .25s ease;\n  }\n  .progress-num{\n    margin-top:8px;\n    font-family:var(--mono);\n    font-size:12px;\n    color:var(--text);\n  }\n  .reset-btn{\n    margin-top:10px;\n    width:100%;\n    font-family:var(--sans);\n    font-size:12px;\n    color:var(--muted);\n    background:transparent;\n    border:1px solid var(--border);\n    border-radius:7px;\n    padding:6px 8px;\n    cursor:pointer;\n  }\n  .reset-btn:hover{color:var(--danger);border-color:var(--danger);}\n\n  /* ---------- Main ---------- */\n  .main{\n    flex:1;\n    min-width:0;\n    padding:36px 40px 80px;\n    max-width:1200px;\n  }\n  .hero{\n    margin-bottom:34px;\n  }\n  .hero .eyebrow-free{\n    font-family:var(--mono);\n    font-size:13px;\n    color:var(--accent);\n    margin:0 0 10px;\n  }\n  .hero h1{\n    font-family:var(--mono);\n    font-size:30px;\n    line-height:1.25;\n    margin:0 0 12px;\n    font-weight:600;\n    letter-spacing:-0.01em;\n  }\n  .hero p{\n    color:var(--muted);\n    max-width:62ch;\n    margin:0 0 6px;\n    font-size:15px;\n  }\n  .hero .source-note{\n    font-size:13px;\n    color:var(--muted);\n    border-left:2px solid var(--border);\n    padding-left:12px;\n    margin-top:16px;\n  }\n\n  .section-head{\n    display:flex;\n    align-items:baseline;\n    gap:10px;\n    margin:44px 0 16px;\n    padding-bottom:10px;\n    border-bottom:1px solid var(--border);\n  }\n  .section-head .num{\n    font-family:var(--mono);\n    color:var(--accent);\n    font-size:14px;\n  }\n  .section-head h2{\n    font-family:var(--mono);\n    font-size:19px;\n    margin:0;\n    font-weight:600;\n  }\n  .section-head .count{\n    margin-left:auto;\n    font-family:var(--mono);\n    font-size:12px;\n    color:var(--muted);\n  }\n\n  .qcard{\n    border:1px solid var(--border);\n    border-radius:10px;\n    background:var(--panel);\n    margin-bottom:12px;\n    overflow:hidden;\n  }\n  .qhead{\n    display:flex;\n    align-items:flex-start;\n    gap:12px;\n    padding:14px 16px;\n    cursor:pointer;\n    user-select:none;\n  }\n  .qcheck{\n    margin-top:3px;\n    flex:0 0 auto;\n    width:18px;height:18px;\n    border-radius:5px;\n    border:1.5px solid var(--border);\n    display:flex;align-items:center;justify-content:center;\n    cursor:pointer;\n    background:var(--panel);\n  }\n  .qcheck.done{\n    background:var(--accent);\n    border-color:var(--accent);\n  }\n  .qcheck.done::after{\n    content:\"\u2713\";\n    color:var(--accent-ink);\n    font-size:12px;\n    font-weight:700;\n  }\n  .qtext{\n    flex:1;\n    font-size:15px;\n    font-weight:500;\n  }\n  .qtext.done-text{color:var(--muted);text-decoration:line-through;text-decoration-color:var(--border);}\n  .qchevron{\n    flex:0 0 auto;\n    color:var(--muted);\n    font-family:var(--mono);\n    font-size:13px;\n    transform:rotate(0deg);\n    transition:transform .15s ease;\n    margin-top:2px;\n  }\n  .qcard.open .qchevron{transform:rotate(90deg);}\n  .qbody{\n    display:none;\n    padding:2px 16px 20px 46px;\n    border-top:1px solid var(--border);\n    font-size:14.5px;\n  }\n  .qcard.open .qbody{display:block; padding-top:14px;}\n  .qbody p{margin:0 0 12px;}\n  .qbody ul{margin:0 0 12px; padding-left:20px;}\n  .qbody li{margin-bottom:6px;}\n  .qbody strong{color:var(--text);}\n  code{\n    font-family:var(--mono);\n    background:var(--code-bg);\n    padding:1px 5px;\n    border-radius:4px;\n    font-size:0.92em;\n  }\n  pre{\n    font-family:var(--mono);\n    background:var(--code-bg);\n    padding:12px 14px;\n    border-radius:8px;\n    overflow-x:auto;\n    font-size:13px;\n    margin:0 0 14px;\n    border:1px solid var(--border);\n  }\n  .diagram{\n    background:var(--panel-2);\n    border:1px solid var(--border);\n    border-radius:8px;\n    padding:14px;\n    margin:0 0 14px;\n    overflow-x:auto;\n  }\n  .diagram svg{display:block; margin:0 auto; max-width:100%; height:auto;}\n  .tag{\n    display:inline-block;\n    font-family:var(--mono);\n    font-size:10.5px;\n    color:var(--gold);\n    background:var(--gold-soft);\n    border:1px solid var(--gold);\n    border-radius:5px;\n    padding:1px 6px;\n    margin-bottom:10px;\n  }\n  table.cmp{\n    width:100%;\n    border-collapse:collapse;\n    margin:0 0 14px;\n    font-size:13.5px;\n  }\n  table.cmp th, table.cmp td{\n    border:1px solid var(--border);\n    padding:7px 10px;\n    text-align:left;\n    vertical-align:top;\n  }\n  table.cmp th{\n    background:var(--panel-2);\n    font-family:var(--mono);\n    font-size:12px;\n    font-weight:600;\n  }\n\n  ::selection{background:var(--accent-soft);}\n\n  @media (max-width: 820px){\n    .sidebar{\n      width:100%;\n      flex:none;\n      height:auto;\n      position:relative;\n      border-right:none;\n      border-bottom:1px solid var(--border);\n    }\n    .app{flex-direction:column;}\n    .main{padding:24px 18px 60px; max-width:100%;}\n  }\n\n  .tbadge{display:inline-block; font-family:var(--mono); font-size:11px; letter-spacing:.04em; color:var(--gold); background:var(--gold-soft); border:1px solid var(--gold); border-radius:99px; padding:2px 10px; margin-bottom:10px;}\n  .deeper{margin-top:14px; padding-top:10px; border-top:1px dashed var(--border); font-size:13.5px; line-height:1.9;}\n  a.go{color:var(--accent); text-decoration:none; border-bottom:1px solid var(--accent-soft); white-space:nowrap;}\n  a.go:hover{border-bottom-color:var(--accent);}\n\n  .search{width:100%; box-sizing:border-box; margin:0 0 14px; padding:9px 11px; font:inherit; font-size:13.5px; color:var(--text); background:var(--panel); border:1px solid var(--border); border-radius:8px;}\n  .search:focus{outline:2px solid var(--accent); outline-offset:1px;}\n  .qmain{flex:1; min-width:0; display:flex; flex-direction:column; gap:4px;}\n  .qchips{display:flex; gap:6px; flex-wrap:wrap;}\n  .qsec,.qcore{font-family:var(--mono); font-size:10.5px; padding:1px 7px; border-radius:99px; border:1px solid var(--border); color:var(--muted); background:var(--panel-2);}\n  .qcore{color:var(--gold); border-color:var(--gold); background:var(--gold-soft);}\n  .qsum{font-size:13.5px; color:var(--muted); line-height:1.5;}\n  .qcard.open .qsum{display:none;}\n  .qcard.focused{box-shadow:0 0 0 2px var(--accent);}\n  .qcheck:focus-visible{outline:2px solid var(--accent); outline-offset:2px;}\n  mark{background:var(--gold-soft); color:var(--text); border-radius:3px; padding:0 1px;}\n  .toolbar{display:flex; flex-wrap:wrap; align-items:center; gap:10px 16px; margin:-4px 0 16px; font-size:13px; color:var(--muted);}\n  .toolbar .tmeta{font-family:var(--mono); font-size:12px; margin-right:auto;}\n  .toolbar .tgroup{display:flex; gap:10px; align-items:center; flex-wrap:wrap;}\n  .toolbar button{font:inherit; font-size:12.5px; color:var(--text); background:var(--panel); border:1px solid var(--border); border-radius:7px; padding:5px 10px; cursor:pointer;}\n  .toolbar button:hover{border-color:var(--accent);}\n  .toolbar label{display:flex; gap:5px; align-items:center; cursor:pointer; white-space:nowrap;}\n  .toolbar input{accent-color:var(--accent);}\n  .empty{color:var(--muted); font-size:14px; padding:18px; border:1px dashed var(--border); border-radius:10px;}\n  .pager{display:flex; justify-content:space-between; gap:12px; margin-top:26px;}\n  .pager button{font:inherit; font-size:13.5px; color:var(--accent); background:var(--panel); border:1px solid var(--border); border-radius:8px; padding:10px 14px; cursor:pointer; max-width:48%; text-align:left;}\n  .pager button:last-child{text-align:right;}\n  .pager button:hover{border-color:var(--accent);}\n  .back-btn{position:fixed; right:22px; bottom:22px; z-index:10; font:inherit; font-size:13.5px; font-weight:600; color:var(--accent-ink); background:var(--accent); border:0; border-radius:99px; padding:10px 16px; cursor:pointer; box-shadow:0 4px 14px rgba(0,0,0,.18);}\n  .nav-item.complete .nav-count{color:var(--accent); border-color:var(--accent);}\n  .keys{display:inline-block; margin-top:6px;}\n  kbd{font-family:var(--mono); font-size:11px; padding:0 5px; border:1px solid var(--border); border-bottom-width:2px; border-radius:4px; background:var(--panel);}\n  @media (max-width: 820px){ .keys{display:none;} .toolbar .tmeta{width:100%;} }\n  .hero-actions{margin-top:14px;}\n  .fc-start,.toolbar .flash-btn{font:inherit; font-weight:600; color:var(--accent-ink); background:var(--accent); border:1px solid var(--accent); border-radius:8px; cursor:pointer;}\n  .fc-start{font-size:14px; padding:9px 16px;}\n  .toolbar .flash-btn{color:var(--accent-ink);}\n  .fc-start:hover,.toolbar .flash-btn:hover{filter:brightness(1.08);}\n  body.fc-lock{overflow:hidden;}\n  .fc{position:fixed; inset:0; z-index:50; background:rgba(10,12,14,.55); display:flex; align-items:center; justify-content:center; padding:24px;}\n  .fc[hidden]{display:none;}\n  .fc-dialog{width:min(920px,100%); max-height:100%; display:flex; flex-direction:column; background:var(--bg); border:1px solid var(--border); border-radius:14px; box-shadow:0 20px 60px rgba(0,0,0,.35); outline:none;}\n  .fc-head{display:flex; justify-content:space-between; align-items:flex-start; gap:12px; padding:16px 20px 10px;}\n  .fc-title{font-family:var(--mono); font-size:14px; font-weight:600;}\n  .fc-count{font-family:var(--mono); font-size:12px; color:var(--muted); margin-top:3px;}\n  .fc-tools{display:flex; gap:12px; align-items:center; font-size:13px; color:var(--muted);}\n  .fc-tools label{display:flex; gap:5px; align-items:center; cursor:pointer;}\n  .fc-tools input{accent-color:var(--accent);}\n  .fc-tools button{font:inherit; font-size:15px; color:var(--muted); background:transparent; border:1px solid var(--border); border-radius:7px; width:32px; height:32px; cursor:pointer;}\n  .fc-bar{height:4px; background:var(--border); margin:0 20px;}\n  .fc-bar i{display:block; height:100%; width:0; background:var(--accent); transition:width .2s ease;}\n  .fc-body{flex:1; overflow-y:auto; padding:22px 24px; min-height:220px;}\n  .fc-q{font-family:var(--sans); font-size:21px; line-height:1.4; margin:10px 0 16px; font-weight:600;}\n  .fc-prompt{color:var(--muted); font-size:14px; border:1px dashed var(--border); border-radius:10px; padding:26px 18px; text-align:center;}\n  .fc .qbody.fc-a{display:block; border-top:1px solid var(--border); padding:16px 0 0; font-size:14.5px;}\n  .fc-done h3{font-family:var(--mono); margin:0 0 8px;}\n  .fc-done .fc-sub{color:var(--muted); margin:14px 0 6px;}\n  .fc-done ul{padding-left:20px; font-size:14px;}\n  .fc-actions{display:flex; gap:10px; justify-content:flex-end; padding:12px 20px; border-top:1px solid var(--border); flex-wrap:wrap;}\n  .fc-actions button{font:inherit; font-size:14px; color:var(--text); background:var(--panel); border:1px solid var(--border); border-radius:8px; padding:9px 16px; cursor:pointer;}\n  .fc-actions button.primary{color:var(--accent-ink); background:var(--accent); border-color:var(--accent); font-weight:600;}\n  .fc-actions button.again{border-color:var(--gold); color:var(--gold);}\n  .fc-actions button:first-child{margin-right:auto;}\n  .fc-actions kbd{margin-left:6px; background:transparent; border-color:currentColor; opacity:.75;}\n  .fc-keys{font-size:11.5px; color:var(--muted); padding:0 20px 14px; text-align:right;}\n  @media (max-width: 820px){ .fc{padding:0;} .fc-dialog{height:100%; border-radius:0;} .fc-keys{display:none;} .fc-q{font-size:18px;} }\n\n  .lede{font-size:15px; color:var(--muted);}\n  .callout{background:var(--accent-soft); border:1px solid var(--accent); border-radius:9px; padding:11px 14px; margin:0 0 14px;}\n  .note{font-size:13px; color:var(--muted);}\n  .codelabel{font-family:var(--mono); font-size:12px; color:var(--accent); margin:10px 0 4px;}\n  .verified{font-size:12.5px; color:var(--muted); border-top:1px dashed var(--border); padding-top:8px; margin-top:12px;}\n  .diagram.lightbg{background:#F8FAFC; border-color:#CBD5E1;}\n  .diagram.lightbg .codelabel{color:#334155;}\n  .quizopts{display:block; font-weight:400; font-size:13px; color:var(--muted); margin-top:4px; line-height:1.55;}\n  ol.quiz{padding-left:26px;} ol.quiz li{margin-bottom:5px;}\n  ol.quiz li.right{color:var(--accent); font-weight:600;}\n  hr{border:0; border-top:1px dashed var(--border); margin:14px 0;}\n  a.fc-start.alt{display:inline-block; text-decoration:none; margin-left:8px; background:var(--panel); color:var(--accent); border:1px solid var(--accent);}\n</style>\n</head>\n<body>\n<div class=\"app\">\n  <nav class=\"sidebar\" id=\"sidebar\">\n    <div class=\"brand\"><b>CSS &amp; Tailwind</b><br>layout \u00b7 responsive \u00b7 utility-first</div>\n    <input class=\"search\" id=\"searchInput\" type=\"search\" placeholder=\"Search every question   /\" aria-label=\"Search all questions\">\n    <div id=\"navList\"></div>\n    <div class=\"progress-box\">\n      <div class=\"label\">REVIEWED</div>\n      <div class=\"progress-track\"><div class=\"progress-fill\" id=\"progressFill\"></div></div>\n      <div class=\"progress-num\" id=\"progressNum\">0 / 0</div>\n      <button class=\"reset-btn\" id=\"resetBtn\">Reset progress</button>\n    </div>\n  </nav>\n\n  <main class=\"main\">\n    <div class=\"hero\">\n      <p class=\"eyebrow-free\">Prep guide</p>\n      <h1>CSS and Tailwind interview mastery</h1>\n      <p>CSS fundamentals, Flexbox and Grid, responsive design, and Tailwind concepts (utility-first, JIT, variants, theming). Start with the <strong>Quick Reference</strong>, then practise with flashcards.</p>\n      <div class=\"hero-actions\"><button id=\"fcCore\" class=\"fc-start\" type=\"button\">\u25b6 Flashcards: all \u2605 core questions</button></div>\n      <div class=\"source-note\"><span id=\"heroMeta\"></span><br><span class=\"keys\"><kbd>/</kbd> search \u00b7 <kbd>j</kbd> <kbd>k</kbd> move \u00b7 <kbd>o</kbd> open \u00b7 <kbd>x</kbd> mark reviewed \u00b7 <kbd>n</kbd> <kbd>p</kbd> next / previous section \u00b7 <kbd>Esc</kbd> clear</span></div>\n    </div>\n    <div id=\"content\"></div>\n    <button id=\"backBtn\" class=\"back-btn\" hidden></button>\n    <div id=\"fc\" class=\"fc\" hidden>\n      <div class=\"fc-dialog\" role=\"dialog\" aria-modal=\"true\" aria-label=\"Flashcards\" tabindex=\"-1\">\n        <div class=\"fc-head\">\n          <div><div class=\"fc-title\"></div><div class=\"fc-count\"></div></div>\n          <div class=\"fc-tools\"><label><input type=\"checkbox\" id=\"fcShuffle\"> Shuffle</label><button data-fc=\"close\" aria-label=\"Close flashcards\">\u2715</button></div>\n        </div>\n        <div class=\"fc-bar\"><i></i></div>\n        <div class=\"fc-body\"></div>\n        <div class=\"fc-actions\"></div>\n        <div class=\"fc-keys\"><kbd>Space</kbd> reveal \u00b7 <kbd>1</kbd> again \u00b7 <kbd>2</kbd> got it (marks reviewed) \u00b7 <kbd>\u2190</kbd> <kbd>\u2192</kbd> back / skip \u00b7 <kbd>Esc</kbd> close</div>\n      </div>\n    </div>\n  </main>\n</div>\n\n<script>\nconst DATA = [\n{\n  id:\"quickref\", title:\"Quick Reference\",\n  questions:[\n    {\n      q:\"Cheat sheet: CSS Fundamentals\",\n      a:`<table class=\"cmp\"><tr><th>Question</th><th>One-line answer</th></tr><tr><td><a class=\"go\" href=\"#s1:0\" data-go=\"s1:0\">Explain the CSS box model, and the difference between content-box and border-box.</a></td><td>Every element is a box made of content, padding, border, and margin.</td></tr><tr><td><a class=\"go\" href=\"#s1:1\" data-go=\"s1:1\">How is CSS specificity calculated, and how does the cascade resolve conflicting rules?</a></td><td>Specificity is a four-part score: (inline styles, IDs, classes/attributes/pseudo-classes, elements/pseudo-elements).</td></tr><tr><td><a class=\"go\" href=\"#s1:2\" data-go=\"s1:2\">What is the difference between em and rem units?</a></td><td>em is relative to the font-size of its own element (compounding when nested); rem is relative to the root (&lt;html&gt;) font-size only \u2014 always the same absolute value no matter how deeply nested.</td></tr></table>`\n    },\n    {\n      q:\"Cheat sheet: Flexbox & Grid\",\n      a:`<table class=\"cmp\"><tr><th>Question</th><th>One-line answer</th></tr><tr><td><a class=\"go\" href=\"#s2:0\" data-go=\"s2:0\">When would you reach for Flexbox vs CSS Grid?</a></td><td>Flexbox is one-dimensional (a single row or column) and content-driven \u2014 great for distributing items along one axis, like a navbar or a button group.</td></tr><tr><td><a class=\"go\" href=\"#s2:1\" data-go=\"s2:1\">How do the Tailwind utilities map onto Flexbox properties?</a></td><td>Each Tailwind utility is a thin, memorable alias for the underlying CSS property/value.</td></tr><tr><td><a class=\"go\" href=\"#s2:2\" data-go=\"s2:2\">How do you build a responsive grid that goes from 1 to 2 to 4 columns with Tailwind?</a></td><td>Stack Tailwind's mobile-first responsive prefixes onto grid-cols-* \u2014 each breakpoint only overrides what changes above it.</td></tr></table>`\n    },\n    {\n      q:\"Cheat sheet: Responsive Design\",\n      a:`<table class=\"cmp\"><tr><th>Question</th><th>One-line answer</th></tr><tr><td><a class=\"go\" href=\"#s3:0\" data-go=\"s3:0\">What does \"mobile-first\" responsive design mean, and how does it show up in Tailwind's breakpoint syntax?</a></td><td>Mobile-first means your base (unprefixed) styles target the smallest screen, and each breakpoint prefix (sm:, md:, lg:, xl:, 2xl:) adds an override for \"this size and up\" \u2014 not \"only at this size.\"</td></tr><tr><td><a class=\"go\" href=\"#s3:1\" data-go=\"s3:1\">What is a CSS container query, and how does it differ from a media query?</a></td><td>A media query responds to the viewport size.</td></tr></table>`\n    },\n    {\n      q:\"Cheat sheet: Tailwind Core Concepts\",\n      a:`<table class=\"cmp\"><tr><th>Question</th><th>One-line answer</th></tr><tr><td><a class=\"go\" href=\"#s4:0\" data-go=\"s4:0\">What does \"utility-first\" actually mean, and what problem does it solve compared to traditional semantic CSS?</a></td><td>Instead of naming a class (.card, .btn-primary) and writing custom CSS for it, you apply small utilities (p-4, rounded-lg, bg-blue-500) directly in the markup.</td></tr><tr><td><a class=\"go\" href=\"#s4:1\" data-go=\"s4:1\">How does Tailwind's JIT (Just-In-Time) engine generate CSS, and why does the content/content-path config matter?</a></td><td>Tailwind doesn't ship a giant precompiled CSS file with every possible utility.</td></tr><tr><td><a class=\"go\" href=\"#s4:2\" data-go=\"s4:2\">What are arbitrary values in Tailwind, and when are they appropriate?</a></td><td>Square-bracket syntax lets you use a one-off value outside the design system's predefined scale, without writing custom CSS.</td></tr></table>`\n    },\n    {\n      q:\"Cheat sheet: Responsive & State Variants\",\n      a:`<table class=\"cmp\"><tr><th>Question</th><th>One-line answer</th></tr><tr><td><a class=\"go\" href=\"#s5:0\" data-go=\"s5:0\">How do hover:, focus:, and group-hover: differ?</a></td><td>hover: and focus: apply a utility only when that specific element is in that pseudo-class state.</td></tr><tr><td><a class=\"go\" href=\"#s5:1\" data-go=\"s5:1\">How does dark: mode work in Tailwind, and what are the two strategies for triggering it?</a></td><td>dark: is a variant, applied the same way as hover: or lg: \u2014 it just activates based on dark mode being \"on\" instead of a pseudo-class or breakpoint.</td></tr></table>`\n    },\n    {\n      q:\"Cheat sheet: Customization & Theming\",\n      a:`<table class=\"cmp\"><tr><th>Question</th><th>One-line answer</th></tr><tr><td><a class=\"go\" href=\"#s6:0\" data-go=\"s6:0\">What does @apply do, and when should (and shouldn't) you reach for it?</a></td><td>@apply lets you compose multiple existing utility classes into a single custom CSS class, inside an actual CSS file.</td></tr><tr><td><a class=\"go\" href=\"#s6:1\" data-go=\"s6:1\">How do you extend Tailwind's default theme (colors, spacing, fonts) without losing the defaults?</a></td><td>Use theme.extend in the config, not theme directly \u2014 extend merges with the defaults, while directly overriding theme.colors/theme.spacing replaces the entire default scale.</td></tr></table>`\n    },\n    {\n      q:\"Cheat sheet: Performance & Best Practices\",\n      a:`<table class=\"cmp\"><tr><th>Question</th><th>One-line answer</th></tr><tr><td><a class=\"go\" href=\"#s7:0\" data-go=\"s7:0\">Why does Tailwind's production CSS bundle stay small regardless of how large the app grows?</a></td><td>Because of the JIT content-scanning model: the final CSS only contains the utility classes that literally appear as strings somewhere in the scanned source files.</td></tr><tr><td><a class=\"go\" href=\"#s7:1\" data-go=\"s7:1\">What is the trade-off of extracting repeated utility strings into a React component vs. using @apply?</a></td><td>Both solve 'don't repeat this long className string everywhere,' but a component extraction (e.g.</td></tr><tr><td><a class=\"go\" href=\"#s7:2\" data-go=\"s7:2\">How would you keep a long list of conditional Tailwind classes readable and correct?</a></td><td>Use a small utility library (clsx or tailwind-merge/cva) instead of manually concatenating strings and ternaries \u2014 string concatenation is error-prone (extra/missing spaces) and doesn't resolve conflicting utilities (e.g.</td></tr></table>`\n    }\n  ]\n},\n{\n  id:\"s1\", title:\"CSS Fundamentals\",\n  questions:[\n    {\n      q:\"Explain the CSS box model, and the difference between content-box and border-box.\",\n      a:`<p class=\"lede\">Before Tailwind, the fundamentals: the box model, the cascade, and specificity. Interviewers use these to check whether you understand what a utility class is actually doing under the hood.</p><p>Every element is a box made of content, padding, border, and margin. box-sizing controls how width/height are calculated relative to those layers.</p><ul><li><strong>content-box (default):</strong> width/height apply only to the content area. Padding and border are added ON TOP, so the element visually grows larger than the width you set.</li><li><strong>border-box:</strong> width/height include padding and border. Setting width: 200px with 20px padding still renders a 200px-wide box \u2014 padding eats into the content area instead of growing it.</li></ul><p><strong>Interview tip:</strong> Tailwind sets <code>box-sizing: border-box</code> globally via Preflight (its CSS reset), which is why <code>w-48 p-4</code> behaves predictably instead of growing past 48 units \u2014 this is a very common \"why does Tailwind do this\" interview question.</p>`\n    },\n    {\n      q:\"How is CSS specificity calculated, and how does the cascade resolve conflicting rules?\",\n      a:`<p>Specificity is a four-part score: (inline styles, IDs, classes/attributes/pseudo-classes, elements/pseudo-elements). The rule with the higher score wins regardless of source order; if scores are equal, the rule that appears later in the stylesheet wins.</p><pre>#nav .item { color: blue; }   /* specificity: 0-1-1-0 */\n.item.active { color: red; }  /* specificity: 0-0-2-0 */\n/* #nav .item wins \u2014 one ID beats any number of classes */</pre><p><strong>Interview tip:</strong> !important overrides specificity entirely (highest priority short of inline styles + !important). Tailwind avoids ever needing !important in normal usage because every utility class has equal, low specificity (0-0-1-0) \u2014 conflicts are resolved by CSS source order instead, which is why utility order in your stylesheet's generated output matters more than the order you write classNames in JSX.</p>`\n    },\n    {\n      q:\"What is the difference between em and rem units?\",\n      a:`<p>em is relative to the font-size of its own element (compounding when nested); rem is relative to the root (&lt;html&gt;) font-size only \u2014 always the same absolute value no matter how deeply nested.</p><p><strong>Interview tip:</strong> Nested em values compound unexpectedly (a 1.2em inside a 1.2em inside a 1.2em element keeps growing), which is why rem is generally preferred for consistent sizing \u2014 and exactly why Tailwind's default spacing/sizing scale (p-4, text-lg, etc.) is defined in rem, not em.</p>`\n    },\n    {\n      q:\"Quiz: With box-sizing: border-box and width: 200px; padding: 20px, how wide does the element actually render? <span class=\\\"quizopts\\\">(A) 200px &nbsp;\u00b7&nbsp; (B) 240px &nbsp;\u00b7&nbsp; (C) 160px &nbsp;\u00b7&nbsp; (D) 220px</span>\",\n      a:`<ol class=\"quiz\" type=\"A\"><li class=\"right\">200px</li><li>240px</li><li>160px</li><li>220px</li></ol><p><strong>Answer: A.</strong> border-box includes padding and border inside the specified width \u2014 the content area shrinks to fit, but the box stays exactly 200px wide.</p>`\n    }\n  ]\n},\n{\n  id:\"s2\", title:\"Flexbox & Grid\",\n  questions:[\n    {\n      q:\"When would you reach for Flexbox vs CSS Grid?\",\n      a:`<p>Flexbox is one-dimensional (a single row or column) and content-driven \u2014 great for distributing items along one axis, like a navbar or a button group. Grid is two-dimensional and layout-driven \u2014 great for defining an overall page/component structure with explicit rows and columns.</p><ul><li><strong>Flexbox:</strong> justify-content/align-items for distributing space along the main/cross axis; flex-grow/flex-shrink for how items resize.</li><li><strong>Grid:</strong> grid-template-columns/rows define explicit tracks; items can span multiple tracks, overlap, and be placed by line number or named area.</li></ul><p><strong>Interview tip:</strong> A practical rule interviewers like: 'if I'm laying out the items inside one component, Flexbox; if I'm laying out the page/major regions, Grid.'</p>`\n    },\n    {\n      q:\"How do the Tailwind utilities map onto Flexbox properties?\",\n      a:`<p>Each Tailwind utility is a thin, memorable alias for the underlying CSS property/value.</p><ul><li>**justify-* \u2192 justify-content** (main-axis alignment: justify-start/center/between/around/evenly)</li><li>**items-* \u2192 align-items** (cross-axis alignment: items-start/center/end/stretch)</li><li><strong>flex-1 \u2192 flex: 1 1 0%</strong> (grow and shrink equally, ignoring initial size)</li></ul><pre>&lt;div class=\"flex items-center justify-between gap-4\"&gt;\n  &lt;!-- display: flex; align-items: center; justify-content: space-between; gap: 1rem; --&gt;\n  &lt;span&gt;Logo&lt;/span&gt;\n  &lt;nav class=\"flex gap-2\"&gt;...&lt;/nav&gt;\n&lt;/div&gt;</pre>`\n    },\n    {\n      q:\"How do you build a responsive grid that goes from 1 to 2 to 4 columns with Tailwind?\",\n      a:`<p>Stack Tailwind's mobile-first responsive prefixes onto grid-cols-* \u2014 each breakpoint only overrides what changes above it.</p><pre>&lt;div class=\"grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6\"&gt;\n  &lt;!-- 1 column below 640px, 2 columns from 640px, 4 columns from 1024px --&gt;\n&lt;/div&gt;</pre>`\n    }\n  ]\n},\n{\n  id:\"s3\", title:\"Responsive Design\",\n  questions:[\n    {\n      q:\"What does \\\"mobile-first\\\" responsive design mean, and how does it show up in Tailwind's breakpoint syntax?\",\n      a:`<p>Mobile-first means your base (unprefixed) styles target the smallest screen, and each breakpoint prefix (sm:, md:, lg:, xl:, 2xl:) adds an override for \"this size and up\" \u2014 not \"only at this size.\"</p><pre>&lt;p class=\"text-sm md:text-base lg:text-lg\"&gt;\n  &lt;!-- text-sm applies below 768px.\n       md:text-base applies from 768px up (overriding text-sm).\n       lg:text-lg applies from 1024px up (overriding md:text-base). --&gt;\n&lt;/p&gt;</pre><p><strong>Interview tip:</strong> A common mistake: assuming md: means \"only on medium screens.\" It actually means \"medium and everything larger,\" unless you explicitly narrow it with something like md:max-lg:.</p>`\n    },\n    {\n      q:\"What is a CSS container query, and how does it differ from a media query?\",\n      a:`<p>A media query responds to the viewport size. A container query responds to the size of a specific containing element, regardless of the viewport \u2014 letting the same component adapt differently depending on where it's placed (e.g. a card in a narrow sidebar vs. a wide main column).</p><pre>&lt;div class=\"@container\"&gt;\n  &lt;div class=\"flex flex-col @lg:flex-row\"&gt;\n    &lt;!-- Switches to a row layout once the CONTAINER (not the viewport) is &gt;= the lg container size --&gt;\n  &lt;/div&gt;\n&lt;/div&gt;</pre><p><strong>Interview tip:</strong> Tailwind's @container/@lg: syntax is the container-query equivalent of sm:/lg: for viewport media queries \u2014 a genuinely modern CSS feature, good to know it exists distinct from viewport-based responsiveness.</p>`\n    }\n  ]\n},\n{\n  id:\"s4\", title:\"Tailwind Core Concepts\",\n  questions:[\n    {\n      q:\"What does \\\"utility-first\\\" actually mean, and what problem does it solve compared to traditional semantic CSS?\",\n      a:`<p>Instead of naming a class (.card, .btn-primary) and writing custom CSS for it, you apply small utilities (p-4, rounded-lg, bg-blue-500) directly in the markup. This avoids the classic problem of a growing, hard-to-maintain global stylesheet where unrelated components accidentally share (and break each other via) the same custom class names.</p><ul><li>No more inventing/naming a new class for every visual variation.</li><li>No CSS specificity fights between component stylesheets \u2014 every utility has equal, minimal specificity.</li><li>Styles live next to the markup that uses them, so deleting a component's JSX also deletes its styling \u2014 no orphaned, unused CSS accumulating over time.</li></ul><p><strong>Interview tip:</strong> The standard pushback interviewers raise: 'doesn't this make the markup ugly/verbose?' \u2014 a good answer acknowledges the trade-off and mentions component extraction (a &lt;Button&gt; React component) as how Tailwind projects avoid repeating the same utility string everywhere.</p>`\n    },\n    {\n      q:\"How does Tailwind's JIT (Just-In-Time) engine generate CSS, and why does the content/content-path config matter?\",\n      a:`<p>Tailwind doesn't ship a giant precompiled CSS file with every possible utility. Instead, it scans your actual source files (per the <code>content</code> config) for class name strings, and generates only the CSS for classes that are actually used \u2014 on demand, at build time.</p><pre>// tailwind.config.js\nexport default {\n  content: ['./src/**/*.{js,ts,jsx,tsx}'], // Tailwind only generates CSS for classes found in these files\n  theme: { extend: {} },\n};</pre><p><strong>Interview tip:</strong> This is why a class name built via string concatenation (<code>bg-\\${color}-500</code>) often silently fails to generate any CSS \u2014 the JIT scanner does a static text search for whole class name strings, it does not execute your JavaScript, so it never sees the interpolated result. The fix is to write out the complete class name literally, or map to a lookup table of full strings.</p>`\n    },\n    {\n      q:\"What are arbitrary values in Tailwind, and when are they appropriate?\",\n      a:`<p>Square-bracket syntax lets you use a one-off value outside the design system's predefined scale, without writing custom CSS.</p><pre>&lt;div class=\"top-[117px] bg-[#1da1f2] grid-cols-[1fr_2fr_1fr]\"&gt;\n  &lt;!-- one-off pixel offset, an exact brand hex color, and a custom grid track layout --&gt;\n&lt;/div&gt;</pre><p><strong>Interview tip:</strong> Treat these as an escape hatch, not a default habit \u2014 reaching for arbitrary values everywhere defeats the point of having a constrained design scale (consistent spacing/colors across the app). If the same arbitrary value shows up repeatedly, that's a sign it belongs in tailwind.config.js's theme instead.</p>`\n    },\n    {\n      q:\"Quiz: Why does `className={`bg-${color}-500`}` often fail to produce any styling in a real Tailwind build? <span class=\\\"quizopts\\\">(A) Template literals are not supported in JSX &nbsp;\u00b7&nbsp; (B) The JIT scanner does a static text search for full class names and never evaluates the interpolated string &nbsp;\u00b7&nbsp; (C) Tailwind does not support dynamic colors at all &nbsp;\u00b7&nbsp; (D) It only fails in production, not in development</span>\",\n      a:`<ol class=\"quiz\" type=\"A\"><li>Template literals are not supported in JSX</li><li class=\"right\">The JIT scanner does a static text search for full class names and never evaluates the interpolated string</li><li>Tailwind does not support dynamic colors at all</li><li>It only fails in production, not in development</li></ol><p><strong>Answer: B.</strong> The build-time scanner looks for literal class name substrings in your source files \u2014 it does not execute your code, so an interpolated class name it never sees as a complete string is never generated.</p>`\n    }\n  ]\n},\n{\n  id:\"s5\", title:\"Responsive & State Variants\",\n  questions:[\n    {\n      q:\"How do hover:, focus:, and group-hover: differ?\",\n      a:`<p>hover: and focus: apply a utility only when that specific element is in that pseudo-class state. group-hover: applies a utility to a CHILD element when its ancestor (marked with the group class) is hovered \u2014 letting one hover trigger a style change on a different element.</p><pre>&lt;div class=\"group border p-4\"&gt;\n  &lt;h3 class=\"text-slate-900\"&gt;Card title&lt;/h3&gt;\n  &lt;p class=\"text-slate-500 group-hover:text-slate-700\"&gt;\n    &lt;!-- This paragraph's color changes when the PARENT card is hovered, not itself --&gt;\n  &lt;/p&gt;\n&lt;/div&gt;</pre><p><strong>Interview tip:</strong> peer works the same way but for SIBLING elements instead of a parent/child relationship \u2014 e.g. showing a validation message when a sibling input is peer-invalid:.</p>`\n    },\n    {\n      q:\"How does dark: mode work in Tailwind, and what are the two strategies for triggering it?\",\n      a:`<p>dark: is a variant, applied the same way as hover: or lg: \u2014 it just activates based on dark mode being \"on\" instead of a pseudo-class or breakpoint.</p><ul><li><strong>darkMode: 'media':</strong> Automatically follows the OS/browser's prefers-color-scheme \u2014 zero JS needed, but the user can't override it within your app.</li><li><strong>darkMode: 'class':</strong> Dark mode is toggled by adding a <code>.dark</code> class to a parent element (usually &lt;html&gt;) via your own JS \u2014 required if you want an in-app light/dark toggle that overrides system preference.</li></ul><pre>// tailwind.config.js\nexport default {\n  darkMode: 'class', // vs. 'media' (follows OS preference automatically)\n};\n\n// Usage \u2014 identical class-prefix pattern to every other variant\n&lt;div class=\"bg-white dark:bg-slate-900 text-slate-900 dark:text-white\"&gt;</pre>`\n    }\n  ]\n},\n{\n  id:\"s6\", title:\"Customization & Theming\",\n  questions:[\n    {\n      q:\"What does @apply do, and when should (and shouldn't) you reach for it?\",\n      a:`<p>@apply lets you compose multiple existing utility classes into a single custom CSS class, inside an actual CSS file.</p><pre>/* globals.css */\n.btn-primary {\n  @apply px-4 py-2 rounded-lg bg-indigo-600 text-white font-semibold hover:bg-indigo-700;\n}</pre><p><strong>Interview tip:</strong> Reach for @apply when the same long utility string is genuinely repeated across many places you can't easily componentize (e.g. inside markdown-generated HTML, or a design system's base CSS) \u2014 not as a default habit, since it reintroduces the maintenance problem utility-first CSS was meant to avoid (a growing custom stylesheet). Prefer extracting a React component over @apply whenever you're already in JSX.</p>`\n    },\n    {\n      q:\"How do you extend Tailwind's default theme (colors, spacing, fonts) without losing the defaults?\",\n      a:`<p>Use theme.extend in the config, not theme directly \u2014 extend merges with the defaults, while directly overriding theme.colors/theme.spacing replaces the entire default scale.</p><pre>// tailwind.config.js\nexport default {\n  theme: {\n    extend: {\n      colors: {\n        brand: { DEFAULT: '#6366f1', dark: '#4338ca' }, // adds bg-brand, text-brand-dark, etc.\n      },\n      spacing: {\n        18: '4.5rem', // adds p-18, m-18, gap-18, alongside the existing scale\n      },\n    },\n  },\n};</pre><p><strong>Interview tip:</strong> Putting colors directly under theme: { colors: {...} } (not theme.extend) REMOVES every default Tailwind color \u2014 a very common mistake that suddenly makes bg-red-500 stop working across the whole codebase.</p>`\n    },\n    {\n      q:\"Quiz: What happens if you define colors directly under `theme: { colors: {...} }` instead of `theme: { extend: { colors: {...} } }`? <span class=\\\"quizopts\\\">(A) Nothing different \u2014 they behave identically &nbsp;\u00b7&nbsp; (B) It merges your colors with the defaults &nbsp;\u00b7&nbsp; (C) It REPLACES Tailwind's entire default color palette with only what you defined &nbsp;\u00b7&nbsp; (D) It only affects dark mode colors</span>\",\n      a:`<ol class=\"quiz\" type=\"A\"><li>Nothing different \u2014 they behave identically</li><li>It merges your colors with the defaults</li><li class=\"right\">It REPLACES Tailwind's entire default color palette with only what you defined</li><li>It only affects dark mode colors</li></ol><p><strong>Answer: C.</strong> theme.colors (without extend) is a full override, not a merge \u2014 every default color utility (bg-red-500, text-blue-600, etc.) stops working unless you re-declare it yourself.</p>`\n    }\n  ]\n},\n{\n  id:\"s7\", title:\"Performance & Best Practices\",\n  questions:[\n    {\n      q:\"Why does Tailwind's production CSS bundle stay small regardless of how large the app grows?\",\n      a:`<p>Because of the JIT content-scanning model: the final CSS only contains the utility classes that literally appear as strings somewhere in the scanned source files. Adding more components doesn't linearly grow the CSS bundle the way traditional per-component stylesheets do \u2014 it only grows by the set of NEW unique utility combinations introduced.</p>`\n    },\n    {\n      q:\"What is the trade-off of extracting repeated utility strings into a React component vs. using @apply?\",\n      a:`<p>Both solve 'don't repeat this long className string everywhere,' but a component extraction (e.g. &lt;Button variant=\"primary\" /&gt;) keeps the styling co-located with markup AND gives you a typed, composable API (props, variants) \u2014 whereas @apply just gives you a new CSS class name, with none of a component's logic/composition benefits.</p><p><strong>Interview tip:</strong> In a React/Next.js codebase specifically, component extraction is almost always the better default over @apply \u2014 @apply mainly earns its keep in non-component contexts, like styling raw HTML from a CMS or markdown.</p>`\n    },\n    {\n      q:\"How would you keep a long list of conditional Tailwind classes readable and correct?\",\n      a:`<p>Use a small utility library (clsx or tailwind-merge/cva) instead of manually concatenating strings and ternaries \u2014 string concatenation is error-prone (extra/missing spaces) and doesn't resolve conflicting utilities (e.g. two different padding classes both present at once).</p><pre>import clsx from 'clsx';\nimport { twMerge } from 'tailwind-merge';\n\nfunction Button({ variant, className }) {\n  return (\n    &lt;button\n      className={twMerge(\n        clsx(\n          'px-4 py-2 rounded-lg font-semibold',\n          variant === 'primary' &amp;&amp; 'bg-indigo-600 text-white',\n          variant === 'ghost' &amp;&amp; 'bg-transparent text-indigo-600'\n        ),\n        className // caller-provided overrides win, thanks to twMerge resolving conflicts\n      )}\n    /&gt;\n  );\n}</pre><p><strong>Interview tip:</strong> twMerge specifically resolves conflicting Tailwind classes (e.g. \"px-4\" and a caller-provided \"px-8\") by keeping only the last one for that CSS property \u2014 plain string concatenation would just apply both classes and let CSS source order decide, which is much less predictable.</p>`\n    }\n  ]\n}\n];\n\n// ---------- render ----------\nconst navList = document.getElementById('navList');\nconst content = document.getElementById('content');\nlet activeId = DATA[0].id;\nconst STORAGE_KEY = 'tailwind-css-reviewed-v1';\nconst PREFS_KEY = 'tailwind-css-prefs-v1';\n\nfunction readJSON(key, fallback){ try{ const raw = localStorage.getItem(key); return raw ? JSON.parse(raw) : fallback; }catch(e){ return fallback; } }\nfunction writeJSON(key, val){ try{ localStorage.setItem(key, JSON.stringify(val)); }catch(e){} }\n\nlet reviewed = readJSON(STORAGE_KEY, {});\nlet prefs = Object.assign({ summaries:true, coreOnly:false, hideDone:false, shuffle:false }, readJSON(PREFS_KEY, {}));\nfunction savePrefs(){ writeJSON(PREFS_KEY, prefs); }\nfunction qKey(sectionId, qIdx){ return sectionId + ':' + qIdx; }\n\n// ---- derived data: plain text, one-line summaries, core set, reading time\nconst stripTags = (s)=> s.replace(/<pre[\\s\\S]*?<\\/pre>/g,' ').replace(/<svg[\\s\\S]*?<\\/svg>/g,' ').replace(/<[^>]+>/g,' ')\n  .replace(/&nbsp;/g,' ').replace(/&amp;/g,'&').replace(/&lt;/g,'<').replace(/&gt;/g,'>').replace(/&mdash;/g,'\u2014').replace(/&rarr;/g,'\u2192').replace(/&[a-z]+;/g,' ')\n  .replace(/\\s+/g,' ').trim();\nconst INDEX = [];\nDATA.forEach(section=>{\n  section.questions.forEach((item, qIdx)=>{\n    // Summary: leading prose (no badges, tables, code or diagrams), whole sentences until it says something useful.\n    const prose = stripTags(item.a.replace(/<span class=\"tbadge\">[\\s\\S]*?<\\/span>/,'').replace(/<table[\\s\\S]*?<\\/table>/g,' ').replace(/<div class=\"diagram\">[\\s\\S]*?<\\/div>/g,' '));\n    const sentences = prose.split(/(?<=[.!?])\\s+(?=[A-Z0-9\"'(])/);\n    let summary = '';\n    for(const s of sentences){ if(summary.length >= 90) break; summary = (summary ? summary + ' ' : '') + s; }\n    summary = summary.replace(/^(Common follow-ups|Model answer:|Answer:)\\s*/i, '');\n    if(summary.length > 240) summary = summary.slice(0, 237).replace(/\\s+\\S*$/,'') + '\u2026';\n    const text = stripTags(item.q + ' ' + item.a);\n    INDEX.push({ section, qIdx, item, summary, text: text.toLowerCase(), words: text.split(' ').length });\n  });\n});\nconst CORE = new Set();\nconst review = DATA.find(s=>s.id==='review');\nif(review) review.questions.forEach(q=>{ (q.a.match(/data-go=\"[^\"]+\"/g)||[]).forEach(m=>CORE.add(m.slice(9,-1))); });\nconst entry = (sid, i)=> INDEX.find(e=>e.section.id===sid && e.qIdx===i);\nfunction minutes(words){ return Math.max(1, Math.round(words / 200)); }\n\n// ---- totals and progress\nfunction totalQuestions(){ return INDEX.length; }\nfunction reviewedCount(){ return INDEX.filter(e=>reviewed[qKey(e.section.id,e.qIdx)]).length; }\nfunction sectionDone(section){ return section.questions.filter((_,i)=>reviewed[qKey(section.id,i)]).length; }\nfunction updateProgress(){\n  const total = totalQuestions(), done = reviewedCount();\n  document.getElementById('progressNum').textContent = done + ' / ' + total;\n  document.getElementById('progressFill').style.width = (total ? (done/total*100) : 0) + '%';\n}\n\n// ---- nav\nfunction renderNav(){\n  navList.innerHTML = '';\n  DATA.forEach((section, idx)=>{\n    const done = sectionDone(section), total = section.questions.length;\n    const btn = document.createElement('button');\n    btn.className = 'nav-item' + (section.id===activeId && !searchTerm ? ' active':'') + (done===total ? ' complete':'');\n    btn.innerHTML = '<span class=\"nav-num\">' + String(idx+1).padStart(2,'0') + '</span><span class=\"nav-label\">' + section.title + '</span><span class=\"nav-count\" title=\"' + done + ' of ' + total + ' reviewed\">' + (done ? done + '/' : '') + total + '</span>';\n    btn.addEventListener('click', ()=>{ clearSearch(false); activeId = section.id; hideBack(); renderNav(); renderContent(); window.scrollTo(0,0); });\n    navList.appendChild(btn);\n  });\n}\n\n// ---- cards\nfunction highlight(html, terms){\n  if(!terms.length) return html;\n  let out = html;\n  terms.forEach(t=>{\n    const safe = t.replace(/[.*+?^$(){}|[\\]\\\\]/g,'\\\\$&');\n    out = out.replace(new RegExp('(^|>)([^<]*)', 'g'), (m, a, b)=> a + b.replace(new RegExp('(' + safe + ')', 'ig'), '<mark>$1</mark>'));\n  });\n  return out;\n}\nfunction buildCard(e, opts){\n  opts = opts || {};\n  const key = qKey(e.section.id, e.qIdx);\n  const isDone = !!reviewed[key];\n  const card = document.createElement('div');\n  card.className = 'qcard' + (opts.open ? ' open' : '');\n  card.dataset.q = e.qIdx;\n  card.dataset.sec = e.section.id;\n  const core = CORE.has(key);\n  const chips = (opts.showSection ? '<span class=\"qsec\">' + e.section.title + '</span>' : '') + (core ? '<span class=\"qcore\" title=\"Core: linked from the 2-Hour Review\">\u2605 core</span>' : '');\n  const showSummary = prefs.summaries && e.section.id !== 'review' && e.summary;\n  const qhead = document.createElement('div');\n  qhead.className = 'qhead';\n  qhead.innerHTML =\n    '<span class=\"qcheck ' + (isDone?'done':'') + '\" role=\"checkbox\" aria-checked=\"' + isDone + '\" tabindex=\"0\" title=\"Mark reviewed (x)\"></span>' +\n    '<span class=\"qmain\"><span class=\"qtext ' + (isDone?'done-text':'') + '\">' + highlight(e.item.q, opts.terms || []) + '</span>' +\n    (chips ? '<span class=\"qchips\">' + chips + '</span>' : '') +\n    (showSummary ? '<span class=\"qsum\">' + highlight(e.summary.replace(/</g,'&lt;'), opts.terms || []) + '</span>' : '') + '</span>' +\n    '<span class=\"qchevron\">\u203a</span>';\n  const checkEl = qhead.querySelector('.qcheck');\n  const toggleDone = (ev)=>{ ev.stopPropagation(); setDone(card, !reviewed[key]); };\n  checkEl.addEventListener('click', toggleDone);\n  checkEl.addEventListener('keydown', (ev)=>{ if(ev.key===' '||ev.key==='Enter'){ ev.preventDefault(); toggleDone(ev); } });\n  qhead.addEventListener('click', ()=>{ card.classList.toggle('open'); setFocus(card, false); });\n  const body = document.createElement('div');\n  body.className = 'qbody';\n  body.innerHTML = e.item.a;\n  card.appendChild(qhead);\n  card.appendChild(body);\n  return card;\n}\nfunction setDone(card, val){\n  const key = qKey(card.dataset.sec, Number(card.dataset.q));\n  reviewed[key] = val;\n  writeJSON(STORAGE_KEY, reviewed);\n  const c = card.querySelector('.qcheck'), t = card.querySelector('.qtext');\n  c.classList.toggle('done', val); c.setAttribute('aria-checked', val); t.classList.toggle('done-text', val);\n  updateProgress(); renderNav();\n}\n\n// ---- section view\nfunction toolbarHTML(section, shown){\n  const words = section.questions.reduce((n,_,i)=> n + entry(section.id,i).words, 0);\n  const coreN = section.questions.filter((_,i)=>CORE.has(qKey(section.id,i))).length;\n  return '<div class=\"toolbar\">' +\n    '<span class=\"tmeta\">~' + minutes(words) + ' min full read' + (coreN && section.id!=='review' ? ' \u00b7 ' + coreN + ' core' : '') + (shown !== section.questions.length ? ' \u00b7 showing ' + shown : '') + '</span>' +\n    '<span class=\"tgroup\"><button data-act=\"flash\" class=\"flash-btn\">\u25b6 Flashcards</button><button data-act=\"expand\">Expand all</button><button data-act=\"collapse\">Collapse all</button></span>' +\n    '<span class=\"tgroup\">' +\n      '<label><input type=\"checkbox\" data-pref=\"summaries\"' + (prefs.summaries?' checked':'') + '> Summaries</label>' +\n      (CORE.size ? '<label><input type=\"checkbox\" data-pref=\"coreOnly\"' + (prefs.coreOnly?' checked':'') + '> \u2605 Core only</label>' : '') +\n      '<label><input type=\"checkbox\" data-pref=\"hideDone\"' + (prefs.hideDone?' checked':'') + '> Hide reviewed</label>' +\n    '</span></div>';\n}\nfunction renderContent(forceIdx){\n  content.innerHTML = '';\n  const section = DATA.find(s=>s.id===activeId);\n  const idx = DATA.findIndex(s=>s.id===activeId);\n  const visible = section.questions.map((_,i)=>i).filter(i=>{\n    if(i === forceIdx || section.id === 'review') return true;\n    const key = qKey(section.id, i);\n    if(prefs.coreOnly && !CORE.has(key)) return false;\n    if(prefs.hideDone && reviewed[key]) return false;\n    return true;\n  });\n  const head = document.createElement('div');\n  head.className = 'section-head';\n  head.innerHTML = '<span class=\"num\">' + String(idx+1).padStart(2,'0') + '</span><h2>' + section.title + '</h2><span class=\"count\">' + sectionDone(section) + ' / ' + section.questions.length + ' reviewed</span>';\n  content.appendChild(head);\n  const tb = document.createElement('div');\n  tb.innerHTML = toolbarHTML(section, visible.length);\n  content.appendChild(tb.firstChild);\n  if(!visible.length){\n    const empty = document.createElement('p');\n    empty.className = 'empty';\n    empty.textContent = prefs.coreOnly ? 'No core questions in this section with the current filters. Untick \"Core only\" to see all.' : 'Everything here is reviewed. Untick \"Hide reviewed\" to see it again.';\n    content.appendChild(empty);\n  }\n  visible.forEach(i=> content.appendChild(buildCard(entry(section.id, i), { open: section.id === 'review' })));\n  const pager = document.createElement('div');\n  pager.className = 'pager';\n  const prev = DATA[idx-1], next = DATA[idx+1];\n  pager.innerHTML = (prev ? '<button data-goto=\"' + prev.id + '\">\u2190 ' + prev.title + '</button>' : '<span></span>') + (next ? '<button data-goto=\"' + next.id + '\">' + next.title + ' \u2192</button>' : '<span></span>');\n  content.appendChild(pager);\n  focusIdx = -1;\n}\n\n// ---- search\nlet searchTerm = '';\nconst searchInput = document.getElementById('searchInput');\nfunction renderSearch(){\n  const terms = searchTerm.toLowerCase().split(/\\s+/).filter(t=>t.length > 1);\n  const hits = INDEX.filter(e=> terms.every(t=> e.text.includes(t)))\n    .map(e=>({ e, score: terms.reduce((n,t)=> n + (e.item.q.toLowerCase().includes(t) ? 10 : 1), 0) }))\n    .sort((a,b)=> b.score - a.score);\n  content.innerHTML = '';\n  const head = document.createElement('div');\n  head.className = 'section-head';\n  head.innerHTML = '<span class=\"num\">\u2315</span><h2>Search</h2><span class=\"count\">' + hits.length + ' result' + (hits.length===1?'':'s') + '</span>';\n  content.appendChild(head);\n  if(hits.length){\n    const tb = document.createElement('div'); tb.className = 'toolbar';\n    tb.innerHTML = '<span class=\"tmeta\">' + (hits.length > 80 ? 'showing the top 80' : 'best matches first') + '</span><span class=\"tgroup\"><button data-act=\"flash\" class=\"flash-btn\">\u25b6 Flashcards from these results</button></span>';\n    content.appendChild(tb);\n  }\n  if(!hits.length){\n    const p = document.createElement('p'); p.className = 'empty'; p.textContent = 'No questions match \"' + searchTerm + '\". Try fewer or shorter words.'; content.appendChild(p);\n  }\n  hits.slice(0, 80).forEach(h=> content.appendChild(buildCard(h.e, { showSection:true, terms })));\n  focusIdx = -1;\n  renderNav();\n}\nlet searchTimer;\nsearchInput.addEventListener('input', ()=>{\n  clearTimeout(searchTimer);\n  searchTimer = setTimeout(()=>{\n    searchTerm = searchInput.value.trim();\n    if(searchTerm.length >= 2){ hideBack(); renderSearch(); window.scrollTo(0,0); }\n    else if(!searchTerm){ renderNav(); renderContent(); }\n  }, 140);\n});\nfunction clearSearch(rerender){\n  if(!searchTerm && !searchInput.value) return;\n  searchTerm = ''; searchInput.value = '';\n  if(rerender !== false){ renderNav(); renderContent(); }\n}\n\n// ---- navigation between questions, with a Back button\nconst backBtn = document.getElementById('backBtn');\nlet returnTo = null;\nfunction hideBack(){ returnTo = null; backBtn.hidden = true; }\nfunction gotoQuestion(id, qIdx, fromLink){\n  if(!DATA.find(s=>s.id===id)) return;\n  if(fromLink){\n    returnTo = { id: activeId, search: searchTerm, y: window.scrollY };\n    const from = searchTerm ? 'search' : DATA.find(s=>s.id===activeId).title;\n    backBtn.textContent = '\u2190 Back to ' + from;\n    backBtn.hidden = false;\n  }\n  searchTerm = ''; searchInput.value = '';\n  activeId = id;\n  const n = (qIdx !== undefined && !isNaN(qIdx)) ? qIdx : undefined;\n  renderNav();\n  renderContent(n);\n  const card = n !== undefined ? content.querySelector('.qcard[data-q=\"' + n + '\"]') : null;\n  if(card){ card.classList.add('open'); setFocus(card, true); }\n  else window.scrollTo(0,0);\n}\nbackBtn.addEventListener('click', ()=>{\n  if(!returnTo) return;\n  const r = returnTo; hideBack();\n  if(r.search){ searchTerm = r.search; searchInput.value = r.search; renderSearch(); }\n  else { activeId = r.id; renderNav(); renderContent(); }\n  requestAnimationFrame(()=> window.scrollTo(0, r.y));\n});\nwindow.addEventListener('message', (e)=>{\n  if(e.data && e.data.type==='goto-section') gotoQuestion(e.data.id, e.data.qIdx, false);\n});\ncontent.addEventListener('click', (e)=>{\n  const a = e.target.closest('a[data-go]');\n  if(a){ e.preventDefault(); const [id, q] = a.dataset.go.split(':'); gotoQuestion(id, Number(q), true); return; }\n  const g = e.target.closest('button[data-goto]');\n  if(g){ activeId = g.dataset.goto; hideBack(); renderNav(); renderContent(); window.scrollTo(0,0); return; }\n  const act = e.target.closest('button[data-act]');\n  if(act){\n    if(act.dataset.act === 'flash'){ startDeck(deckFromView(), searchTerm ? 'Search: ' + searchTerm : DATA.find(s=>s.id===activeId).title); return; }\n    content.querySelectorAll('.qcard').forEach(c=> c.classList.toggle('open', act.dataset.act === 'expand'));\n  }\n});\ncontent.addEventListener('change', (e)=>{\n  const p = e.target.dataset && e.target.dataset.pref;\n  if(!p) return;\n  prefs[p] = e.target.checked; savePrefs();\n  if(searchTerm) renderSearch(); else renderContent();\n});\n\n// ---- keyboard: / search, j/k move, o or Enter open, x reviewed, n/p section, Esc\nlet focusIdx = -1;\nfunction cards(){ return Array.from(content.querySelectorAll('.qcard')); }\nfunction setFocus(card, scroll){\n  cards().forEach(c=> c.classList.remove('focused'));\n  card.classList.add('focused');\n  focusIdx = cards().indexOf(card);\n  if(scroll) card.scrollIntoView({ block:'start', behavior:'smooth' });\n}\ndocument.addEventListener('keydown', (e)=>{\n  if(fc.open){ fcKey(e); return; }\n  const typing = /INPUT|TEXTAREA|SELECT/.test(document.activeElement.tagName);\n  if(e.key === 'Escape'){ if(searchTerm || searchInput.value){ clearSearch(); } searchInput.blur(); return; }\n  if(typing || e.metaKey || e.ctrlKey || e.altKey) return;\n  const list = cards();\n  if(e.key === '/'){ e.preventDefault(); searchInput.focus(); searchInput.select(); return; }\n  if(e.key === 'j' || e.key === 'k'){\n    if(!list.length) return;\n    focusIdx = Math.max(0, Math.min(list.length - 1, focusIdx + (e.key === 'j' ? 1 : -1)));\n    setFocus(list[focusIdx], true); return;\n  }\n  const cur = list[focusIdx];\n  if((e.key === 'o' || e.key === 'Enter') && cur){ cur.classList.toggle('open'); return; }\n  if(e.key === 'x' && cur){ setDone(cur, !reviewed[qKey(cur.dataset.sec, Number(cur.dataset.q))]); return; }\n  if(e.key === 'n' || e.key === 'p'){\n    if(searchTerm) return;\n    const i = DATA.findIndex(s=>s.id===activeId) + (e.key === 'n' ? 1 : -1);\n    if(DATA[i]){ activeId = DATA[i].id; hideBack(); renderNav(); renderContent(); window.scrollTo(0,0); }\n  }\n});\n\n\n// ---- flashcards: question first, say the answer, reveal, rate yourself\nconst fcEl = document.getElementById('fc');\nconst fc = { open:false, deck:[], i:0, revealed:false, got:0, missed:[], again:new Map(), title:'', source:[] };\nfunction shuffleArr(a){ for(let i=a.length-1;i>0;i--){ const j=Math.floor(Math.random()*(i+1)); const t=a[i]; a[i]=a[j]; a[j]=t; } return a; }\nfunction startDeck(entries, title){\n  if(!entries.length) return;\n  fc.source = entries.slice();\n  fc.deck = prefs.shuffle ? shuffleArr(entries.slice()) : entries.slice();\n  fc.i = 0; fc.revealed = false; fc.got = 0; fc.missed = []; fc.again = new Map(); fc.title = title; fc.open = true;\n  fcEl.querySelector('#fcShuffle').checked = !!prefs.shuffle;\n  fcEl.hidden = false; document.body.classList.add('fc-lock');\n  renderFc();\n  fcEl.querySelector('.fc-dialog').focus();\n}\nfunction closeFc(){\n  fc.open = false; fcEl.hidden = true; document.body.classList.remove('fc-lock');\n  if(searchTerm) renderSearch(); else renderContent();\n}\nfunction markReviewed(key, val){ reviewed[key] = val; writeJSON(STORAGE_KEY, reviewed); updateProgress(); renderNav(); }\nfunction renderFc(){\n  const n = fc.deck.length;\n  fcEl.querySelector('.fc-title').textContent = fc.title;\n  fcEl.querySelector('.fc-count').textContent = (fc.i < n ? (fc.i + 1) : n) + ' / ' + n + (fc.got ? ' \u00b7 ' + fc.got + ' got it' : '') + (fc.missed.length ? ' \u00b7 ' + new Set(fc.missed).size + ' to revisit' : '');\n  fcEl.querySelector('.fc-bar i').style.width = (n ? fc.i / n * 100 : 0) + '%';\n  const body = fcEl.querySelector('.fc-body');\n  const actions = fcEl.querySelector('.fc-actions');\n  if(fc.i >= n){\n    const uniqMissed = Array.from(new Set(fc.missed));\n    body.innerHTML = '<div class=\"fc-done\"><h3>Deck finished</h3><p>' + fc.got + ' marked \"got it\" (and ticked as reviewed) \u00b7 ' + uniqMissed.length + ' to revisit.</p>' +\n      (uniqMissed.length ? '<p class=\"fc-sub\">To revisit:</p><ul>' + uniqMissed.map(e=>'<li>' + e.item.q + '</li>').join('') + '</ul>' : '<p class=\"fc-sub\">Nothing missed. Well done.</p>') + '</div>';\n    actions.innerHTML = (uniqMissed.length ? '<button data-fc=\"missed\" class=\"primary\">Practise the ' + uniqMissed.length + ' missed</button>' : '') +\n      '<button data-fc=\"restart\">Restart deck</button><button data-fc=\"close\">Close <kbd>Esc</kbd></button>';\n    return;\n  }\n  const e = fc.deck[fc.i];\n  const key = qKey(e.section.id, e.qIdx);\n  body.innerHTML = '<div class=\"qchips\"><span class=\"qsec\">' + e.section.title + '</span>' + (CORE.has(key) ? '<span class=\"qcore\">\u2605 core</span>' : '') + (reviewed[key] ? '<span class=\"qsec\">reviewed</span>' : '') + '</div>' +\n    '<h3 class=\"fc-q\">' + e.item.q + '</h3>' +\n    (fc.revealed ? '<div class=\"qbody fc-a\">' + e.item.a + '</div>' : '<p class=\"fc-prompt\">Answer it out loud first: the main point, one example, one trade-off. Then reveal.</p>');\n  body.scrollTop = 0;\n  actions.innerHTML = fc.revealed\n    ? '<button data-fc=\"prev\" title=\"Previous (\u2190)\">\u2190</button><button data-fc=\"again\" class=\"again\">Again <kbd>1</kbd></button><button data-fc=\"got\" class=\"primary\">Got it <kbd>2</kbd></button>'\n    : '<button data-fc=\"prev\" title=\"Previous (\u2190)\">\u2190</button><button data-fc=\"reveal\" class=\"primary\">Reveal answer <kbd>Space</kbd></button><button data-fc=\"skip\">Skip <kbd>\u2192</kbd></button>';\n}\nfunction fcAction(act){\n  const n = fc.deck.length, e = fc.deck[fc.i];\n  if(act === 'close') return closeFc();\n  if(act === 'restart') return startDeck(fc.source, fc.title);\n  if(act === 'missed') return startDeck(Array.from(new Set(fc.missed)), fc.title + ' \u00b7 missed');\n  if(fc.i >= n && act !== 'prev') return;\n  if(act === 'reveal'){ fc.revealed = !fc.revealed; }\n  else if(act === 'got'){ if(!fc.revealed){ fc.revealed = true; } else { markReviewed(qKey(e.section.id, e.qIdx), true); fc.got++; fc.i++; fc.revealed = false; } }\n  else if(act === 'again'){\n    if(!fc.revealed){ fc.revealed = true; }\n    else {\n      const k = qKey(e.section.id, e.qIdx), times = fc.again.get(k) || 0;\n      fc.missed.push(e);\n      if(times < 2){ fc.deck.push(e); fc.again.set(k, times + 1); }   // comes back later in this session\n      fc.i++; fc.revealed = false;\n    }\n  }\n  else if(act === 'skip'){ fc.i++; fc.revealed = false; }\n  else if(act === 'prev'){ if(fc.i > 0){ fc.i--; fc.revealed = false; } }\n  renderFc();\n}\nfcEl.addEventListener('click', (e)=>{\n  if(e.target === fcEl) return closeFc();\n  const b = e.target.closest('button[data-fc]');\n  if(b) return fcAction(b.dataset.fc);\n  const a = e.target.closest('a[data-go]');\n  if(a){ e.preventDefault(); closeFc(); const p = a.dataset.go.split(':'); gotoQuestion(p[0], Number(p[1]), true); }\n});\nfcEl.querySelector('#fcShuffle').addEventListener('change', (e)=>{\n  prefs.shuffle = e.target.checked; savePrefs();\n  const keep = fc.i + (fc.revealed ? 1 : 0);\n  const rest = fc.deck.slice(keep);\n  if(prefs.shuffle) shuffleArr(rest);\n  fc.deck = fc.deck.slice(0, keep).concat(rest);\n  renderFc();\n});\nfunction fcKey(e){\n  if(e.key === 'Escape'){ e.preventDefault(); closeFc(); return; }\n  if(e.target && e.target.id === 'fcShuffle') return;\n  const map = { ' ':'reveal', 'Enter':'reveal', '1':'again', '2':'got', 'ArrowRight':'skip', 'ArrowLeft':'prev' };\n  const act = map[e.key];\n  if(!act) return;\n  if(e.target && e.target.tagName === 'BUTTON' && (e.key === ' ' || e.key === 'Enter')) return;   // let a focused button click itself\n  e.preventDefault();\n  if(fc.i >= fc.deck.length && act !== 'prev') return;\n  fcAction(act);\n}\nfunction deckFromView(){ return cards().map(c=> entry(c.dataset.sec, Number(c.dataset.q))); }\ndocument.getElementById('fcCore').addEventListener('click', ()=>{\n  if(CORE.size) startDeck(Array.from(CORE).map(k=>{ const p = k.split(':'); return entry(p[0], Number(p[1])); }).filter(Boolean), 'All \u2605 core questions');\n  else startDeck(INDEX.filter(e=>e.section.id!=='quickref'), 'All questions');\n});\n\ndocument.getElementById('resetBtn').addEventListener('click', ()=>{\n  if(!confirm('Clear all reviewed checkmarks?')) return;\n  reviewed = {}; writeJSON(STORAGE_KEY, reviewed);\n  renderNav(); renderContent(); updateProgress();\n});\n\ndocument.getElementById('heroMeta').textContent = DATA.length + ' sections \u00b7 ' + totalQuestions() + ' questions' + (CORE.size ? ' \u00b7 ' + CORE.size + ' marked \u2605 core (linked from the 2-Hour Review)' : '');\ndocument.getElementById('fcCore').textContent = CORE.size ? '\u25b6 Flashcards: all \u2605 core questions' : '\u25b6 Flashcards: all ' + INDEX.filter(e=>e.section.id!=='quickref').length + ' questions';\nrenderNav();\nrenderContent();\nupdateProgress();\n</script>\n</body>\n</html>\n"}