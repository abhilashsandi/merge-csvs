{"html": "<!DOCTYPE html>\n<html lang=\"en\">\n<head>\n<meta charset=\"UTF-8\">\n<title>React Training \u2014 Interview Prep</title>\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1\">\n<link rel=\"preconnect\" href=\"https://fonts.googleapis.com\">\n<link rel=\"preconnect\" href=\"https://fonts.gstatic.com\" crossorigin>\n<link href=\"https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500;600;700&family=IBM+Plex+Sans:wght@400;500;600;700&display=swap\" rel=\"stylesheet\">\n<style>\n  :root{\n    --bg:#F6F4EF;\n    --panel:#FFFFFF;\n    --panel-2:#FBF9F4;\n    --border:#E4E0D4;\n    --text:#211F1B;\n    --muted:#716B5E;\n    --accent:#2F7A72;\n    --accent-ink:#F3FBF9;\n    --accent-soft:#E3F1EE;\n    --gold:#B8792F;\n    --gold-soft:#F5E9D8;\n    --code-bg:#EFECE2;\n    --danger:#B5502F;\n    --shadow: 0 1px 2px rgba(30,26,18,0.05);\n    --mono: 'IBM Plex Mono', ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;\n    --sans: 'IBM Plex Sans', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;\n  }\n  @media (prefers-color-scheme: dark){\n    :root:not([data-theme=\"light\"]){\n      --bg:#15181A;\n      --panel:#1B1F21;\n      --panel-2:#202426;\n      --border:#2C3133;\n      --text:#E9E6DE;\n      --muted:#9A968A;\n      --accent:#5FBFB2;\n      --accent-ink:#0B1716;\n      --accent-soft:#1E3230;\n      --gold:#D9A15C;\n      --gold-soft:#332918;\n      --code-bg:#20262A;\n      --danger:#D98866;\n      --shadow: 0 1px 2px rgba(0,0,0,0.4);\n    }\n  }\n  :root[data-theme=\"dark\"]{\n    --bg:#15181A;\n    --panel:#1B1F21;\n    --panel-2:#202426;\n    --border:#2C3133;\n    --text:#E9E6DE;\n    --muted:#9A968A;\n    --accent:#5FBFB2;\n    --accent-ink:#0B1716;\n    --accent-soft:#1E3230;\n    --gold:#D9A15C;\n    --gold-soft:#332918;\n    --code-bg:#20262A;\n    --danger:#D98866;\n    --shadow: 0 1px 2px rgba(0,0,0,0.4);\n  }\n\n  *{box-sizing:border-box;}\n  html,body{margin:0;padding:0;}\n  body{\n    background:var(--bg);\n    color:var(--text);\n    font-family:var(--sans);\n    line-height:1.55;\n    -webkit-font-smoothing:antialiased;\n  }\n  a{color:var(--accent);}\n\n  .app{\n    display:flex;\n    min-height:100vh;\n  }\n\n  /* ---------- Sidebar ---------- */\n  .sidebar{\n    width:260px;\n    flex:0 0 260px;\n    border-right:1px solid var(--border);\n    padding:28px 16px 24px;\n    position:sticky;\n    top:0;\n    height:100vh;\n    overflow-y:auto;\n  }\n  .brand{\n    font-family:var(--mono);\n    font-size:13px;\n    letter-spacing:0.02em;\n    color:var(--muted);\n    padding:0 8px 18px;\n    border-bottom:1px solid var(--border);\n    margin-bottom:14px;\n  }\n  .brand b{color:var(--text);font-weight:600;}\n  .nav-item{\n    display:flex;\n    align-items:center;\n    justify-content:space-between;\n    gap:10px;\n    width:100%;\n    text-align:left;\n    padding:10px 10px;\n    border-radius:8px;\n    border:1px solid transparent;\n    background:transparent;\n    color:var(--text);\n    font-family:var(--sans);\n    font-size:14.5px;\n    cursor:pointer;\n    margin-bottom:2px;\n  }\n  .nav-item:hover{background:var(--panel-2);}\n  .nav-item.active{\n    background:var(--accent-soft);\n    border-color:var(--accent);\n    color:var(--text);\n    font-weight:600;\n  }\n  .nav-num{\n    font-family:var(--mono);\n    font-size:12px;\n    color:var(--muted);\n    width:18px;\n    flex:0 0 18px;\n  }\n  .nav-item.active .nav-num{color:var(--accent);}\n  .nav-label{flex:1;}\n  .nav-count{\n    font-family:var(--mono);\n    font-size:11px;\n    color:var(--muted);\n    background:var(--panel-2);\n    border:1px solid var(--border);\n    padding:1px 6px;\n    border-radius:999px;\n  }\n  .progress-box{\n    margin-top:20px;\n    padding:14px 12px;\n    border:1px solid var(--border);\n    border-radius:10px;\n    background:var(--panel-2);\n  }\n  .progress-box .label{\n    font-family:var(--mono);\n    font-size:11px;\n    color:var(--muted);\n    margin-bottom:8px;\n  }\n  .progress-track{\n    height:6px;\n    border-radius:999px;\n    background:var(--border);\n    overflow:hidden;\n  }\n  .progress-fill{\n    height:100%;\n    background:var(--accent);\n    width:0%;\n    transition:width .25s ease;\n  }\n  .progress-num{\n    margin-top:8px;\n    font-family:var(--mono);\n    font-size:12px;\n    color:var(--text);\n  }\n  .reset-btn{\n    margin-top:10px;\n    width:100%;\n    font-family:var(--sans);\n    font-size:12px;\n    color:var(--muted);\n    background:transparent;\n    border:1px solid var(--border);\n    border-radius:7px;\n    padding:6px 8px;\n    cursor:pointer;\n  }\n  .reset-btn:hover{color:var(--danger);border-color:var(--danger);}\n\n  /* ---------- Main ---------- */\n  .main{\n    flex:1;\n    min-width:0;\n    padding:36px 40px 80px;\n    max-width:1200px;\n  }\n  .hero{\n    margin-bottom:34px;\n  }\n  .hero .eyebrow-free{\n    font-family:var(--mono);\n    font-size:13px;\n    color:var(--accent);\n    margin:0 0 10px;\n  }\n  .hero h1{\n    font-family:var(--mono);\n    font-size:30px;\n    line-height:1.25;\n    margin:0 0 12px;\n    font-weight:600;\n    letter-spacing:-0.01em;\n  }\n  .hero p{\n    color:var(--muted);\n    max-width:62ch;\n    margin:0 0 6px;\n    font-size:15px;\n  }\n  .hero .source-note{\n    font-size:13px;\n    color:var(--muted);\n    border-left:2px solid var(--border);\n    padding-left:12px;\n    margin-top:16px;\n  }\n\n  .section-head{\n    display:flex;\n    align-items:baseline;\n    gap:10px;\n    margin:44px 0 16px;\n    padding-bottom:10px;\n    border-bottom:1px solid var(--border);\n  }\n  .section-head .num{\n    font-family:var(--mono);\n    color:var(--accent);\n    font-size:14px;\n  }\n  .section-head h2{\n    font-family:var(--mono);\n    font-size:19px;\n    margin:0;\n    font-weight:600;\n  }\n  .section-head .count{\n    margin-left:auto;\n    font-family:var(--mono);\n    font-size:12px;\n    color:var(--muted);\n  }\n\n  .qcard{\n    border:1px solid var(--border);\n    border-radius:10px;\n    background:var(--panel);\n    margin-bottom:12px;\n    overflow:hidden;\n  }\n  .qhead{\n    display:flex;\n    align-items:flex-start;\n    gap:12px;\n    padding:14px 16px;\n    cursor:pointer;\n    user-select:none;\n  }\n  .qcheck{\n    margin-top:3px;\n    flex:0 0 auto;\n    width:18px;height:18px;\n    border-radius:5px;\n    border:1.5px solid var(--border);\n    display:flex;align-items:center;justify-content:center;\n    cursor:pointer;\n    background:var(--panel);\n  }\n  .qcheck.done{\n    background:var(--accent);\n    border-color:var(--accent);\n  }\n  .qcheck.done::after{\n    content:\"\u2713\";\n    color:var(--accent-ink);\n    font-size:12px;\n    font-weight:700;\n  }\n  .qtext{\n    flex:1;\n    font-size:15px;\n    font-weight:500;\n  }\n  .qtext.done-text{color:var(--muted);text-decoration:line-through;text-decoration-color:var(--border);}\n  .qchevron{\n    flex:0 0 auto;\n    color:var(--muted);\n    font-family:var(--mono);\n    font-size:13px;\n    transform:rotate(0deg);\n    transition:transform .15s ease;\n    margin-top:2px;\n  }\n  .qcard.open .qchevron{transform:rotate(90deg);}\n  .qbody{\n    display:none;\n    padding:2px 16px 20px 46px;\n    border-top:1px solid var(--border);\n    font-size:14.5px;\n  }\n  .qcard.open .qbody{display:block; padding-top:14px;}\n  .qbody p{margin:0 0 12px;}\n  .qbody ul{margin:0 0 12px; padding-left:20px;}\n  .qbody li{margin-bottom:6px;}\n  .qbody strong{color:var(--text);}\n  code{\n    font-family:var(--mono);\n    background:var(--code-bg);\n    padding:1px 5px;\n    border-radius:4px;\n    font-size:0.92em;\n  }\n  pre{\n    font-family:var(--mono);\n    background:var(--code-bg);\n    padding:12px 14px;\n    border-radius:8px;\n    overflow-x:auto;\n    font-size:13px;\n    margin:0 0 14px;\n    border:1px solid var(--border);\n  }\n  .diagram{\n    background:var(--panel-2);\n    border:1px solid var(--border);\n    border-radius:8px;\n    padding:14px;\n    margin:0 0 14px;\n    overflow-x:auto;\n  }\n  .diagram svg{display:block; margin:0 auto; max-width:100%; height:auto;}\n  .tag{\n    display:inline-block;\n    font-family:var(--mono);\n    font-size:10.5px;\n    color:var(--gold);\n    background:var(--gold-soft);\n    border:1px solid var(--gold);\n    border-radius:5px;\n    padding:1px 6px;\n    margin-bottom:10px;\n  }\n  table.cmp{\n    width:100%;\n    border-collapse:collapse;\n    margin:0 0 14px;\n    font-size:13.5px;\n  }\n  table.cmp th, table.cmp td{\n    border:1px solid var(--border);\n    padding:7px 10px;\n    text-align:left;\n    vertical-align:top;\n  }\n  table.cmp th{\n    background:var(--panel-2);\n    font-family:var(--mono);\n    font-size:12px;\n    font-weight:600;\n  }\n\n  ::selection{background:var(--accent-soft);}\n\n  @media (max-width: 820px){\n    .sidebar{\n      width:100%;\n      flex:none;\n      height:auto;\n      position:relative;\n      border-right:none;\n      border-bottom:1px solid var(--border);\n    }\n    .app{flex-direction:column;}\n    .main{padding:24px 18px 60px; max-width:100%;}\n  }\n\n  .tbadge{display:inline-block; font-family:var(--mono); font-size:11px; letter-spacing:.04em; color:var(--gold); background:var(--gold-soft); border:1px solid var(--gold); border-radius:99px; padding:2px 10px; margin-bottom:10px;}\n  .deeper{margin-top:14px; padding-top:10px; border-top:1px dashed var(--border); font-size:13.5px; line-height:1.9;}\n  a.go{color:var(--accent); text-decoration:none; border-bottom:1px solid var(--accent-soft); white-space:nowrap;}\n  a.go:hover{border-bottom-color:var(--accent);}\n\n  .search{width:100%; box-sizing:border-box; margin:0 0 14px; padding:9px 11px; font:inherit; font-size:13.5px; color:var(--text); background:var(--panel); border:1px solid var(--border); border-radius:8px;}\n  .search:focus{outline:2px solid var(--accent); outline-offset:1px;}\n  .qmain{flex:1; min-width:0; display:flex; flex-direction:column; gap:4px;}\n  .qchips{display:flex; gap:6px; flex-wrap:wrap;}\n  .qsec,.qcore{font-family:var(--mono); font-size:10.5px; padding:1px 7px; border-radius:99px; border:1px solid var(--border); color:var(--muted); background:var(--panel-2);}\n  .qcore{color:var(--gold); border-color:var(--gold); background:var(--gold-soft);}\n  .qsum{font-size:13.5px; color:var(--muted); line-height:1.5;}\n  .qcard.open .qsum{display:none;}\n  .qcard.focused{box-shadow:0 0 0 2px var(--accent);}\n  .qcheck:focus-visible{outline:2px solid var(--accent); outline-offset:2px;}\n  mark{background:var(--gold-soft); color:var(--text); border-radius:3px; padding:0 1px;}\n  .toolbar{display:flex; flex-wrap:wrap; align-items:center; gap:10px 16px; margin:-4px 0 16px; font-size:13px; color:var(--muted);}\n  .toolbar .tmeta{font-family:var(--mono); font-size:12px; margin-right:auto;}\n  .toolbar .tgroup{display:flex; gap:10px; align-items:center; flex-wrap:wrap;}\n  .toolbar button{font:inherit; font-size:12.5px; color:var(--text); background:var(--panel); border:1px solid var(--border); border-radius:7px; padding:5px 10px; cursor:pointer;}\n  .toolbar button:hover{border-color:var(--accent);}\n  .toolbar label{display:flex; gap:5px; align-items:center; cursor:pointer; white-space:nowrap;}\n  .toolbar input{accent-color:var(--accent);}\n  .empty{color:var(--muted); font-size:14px; padding:18px; border:1px dashed var(--border); border-radius:10px;}\n  .pager{display:flex; justify-content:space-between; gap:12px; margin-top:26px;}\n  .pager button{font:inherit; font-size:13.5px; color:var(--accent); background:var(--panel); border:1px solid var(--border); border-radius:8px; padding:10px 14px; cursor:pointer; max-width:48%; text-align:left;}\n  .pager button:last-child{text-align:right;}\n  .pager button:hover{border-color:var(--accent);}\n  .back-btn{position:fixed; right:22px; bottom:22px; z-index:10; font:inherit; font-size:13.5px; font-weight:600; color:var(--accent-ink); background:var(--accent); border:0; border-radius:99px; padding:10px 16px; cursor:pointer; box-shadow:0 4px 14px rgba(0,0,0,.18);}\n  .nav-item.complete .nav-count{color:var(--accent); border-color:var(--accent);}\n  .keys{display:inline-block; margin-top:6px;}\n  kbd{font-family:var(--mono); font-size:11px; padding:0 5px; border:1px solid var(--border); border-bottom-width:2px; border-radius:4px; background:var(--panel);}\n  @media (max-width: 820px){ .keys{display:none;} .toolbar .tmeta{width:100%;} }\n  .hero-actions{margin-top:14px;}\n  .fc-start,.toolbar .flash-btn{font:inherit; font-weight:600; color:var(--accent-ink); background:var(--accent); border:1px solid var(--accent); border-radius:8px; cursor:pointer;}\n  .fc-start{font-size:14px; padding:9px 16px;}\n  .toolbar .flash-btn{color:var(--accent-ink);}\n  .fc-start:hover,.toolbar .flash-btn:hover{filter:brightness(1.08);}\n  body.fc-lock{overflow:hidden;}\n  .fc{position:fixed; inset:0; z-index:50; background:rgba(10,12,14,.55); display:flex; align-items:center; justify-content:center; padding:24px;}\n  .fc[hidden]{display:none;}\n  .fc-dialog{width:min(920px,100%); max-height:100%; display:flex; flex-direction:column; background:var(--bg); border:1px solid var(--border); border-radius:14px; box-shadow:0 20px 60px rgba(0,0,0,.35); outline:none;}\n  .fc-head{display:flex; justify-content:space-between; align-items:flex-start; gap:12px; padding:16px 20px 10px;}\n  .fc-title{font-family:var(--mono); font-size:14px; font-weight:600;}\n  .fc-count{font-family:var(--mono); font-size:12px; color:var(--muted); margin-top:3px;}\n  .fc-tools{display:flex; gap:12px; align-items:center; font-size:13px; color:var(--muted);}\n  .fc-tools label{display:flex; gap:5px; align-items:center; cursor:pointer;}\n  .fc-tools input{accent-color:var(--accent);}\n  .fc-tools button{font:inherit; font-size:15px; color:var(--muted); background:transparent; border:1px solid var(--border); border-radius:7px; width:32px; height:32px; cursor:pointer;}\n  .fc-bar{height:4px; background:var(--border); margin:0 20px;}\n  .fc-bar i{display:block; height:100%; width:0; background:var(--accent); transition:width .2s ease;}\n  .fc-body{flex:1; overflow-y:auto; padding:22px 24px; min-height:220px;}\n  .fc-q{font-family:var(--sans); font-size:21px; line-height:1.4; margin:10px 0 16px; font-weight:600;}\n  .fc-prompt{color:var(--muted); font-size:14px; border:1px dashed var(--border); border-radius:10px; padding:26px 18px; text-align:center;}\n  .fc .qbody.fc-a{display:block; border-top:1px solid var(--border); padding:16px 0 0; font-size:14.5px;}\n  .fc-done h3{font-family:var(--mono); margin:0 0 8px;}\n  .fc-done .fc-sub{color:var(--muted); margin:14px 0 6px;}\n  .fc-done ul{padding-left:20px; font-size:14px;}\n  .fc-actions{display:flex; gap:10px; justify-content:flex-end; padding:12px 20px; border-top:1px solid var(--border); flex-wrap:wrap;}\n  .fc-actions button{font:inherit; font-size:14px; color:var(--text); background:var(--panel); border:1px solid var(--border); border-radius:8px; padding:9px 16px; cursor:pointer;}\n  .fc-actions button.primary{color:var(--accent-ink); background:var(--accent); border-color:var(--accent); font-weight:600;}\n  .fc-actions button.again{border-color:var(--gold); color:var(--gold);}\n  .fc-actions button:first-child{margin-right:auto;}\n  .fc-actions kbd{margin-left:6px; background:transparent; border-color:currentColor; opacity:.75;}\n  .fc-keys{font-size:11.5px; color:var(--muted); padding:0 20px 14px; text-align:right;}\n  @media (max-width: 820px){ .fc{padding:0;} .fc-dialog{height:100%; border-radius:0;} .fc-keys{display:none;} .fc-q{font-size:18px;} }\n\n  .lede{font-size:15px; color:var(--muted);}\n  .callout{background:var(--accent-soft); border:1px solid var(--accent); border-radius:9px; padding:11px 14px; margin:0 0 14px;}\n  .note{font-size:13px; color:var(--muted);}\n  .codelabel{font-family:var(--mono); font-size:12px; color:var(--accent); margin:10px 0 4px;}\n  .verified{font-size:12.5px; color:var(--muted); border-top:1px dashed var(--border); padding-top:8px; margin-top:12px;}\n  .diagram.lightbg{background:#F8FAFC; border-color:#CBD5E1;}\n  .diagram.lightbg .codelabel{color:#334155;}\n  .quizopts{display:block; font-weight:400; font-size:13px; color:var(--muted); margin-top:4px; line-height:1.55;}\n  ol.quiz{padding-left:26px;} ol.quiz li{margin-bottom:5px;}\n  ol.quiz li.right{color:var(--accent); font-weight:600;}\n  hr{border:0; border-top:1px dashed var(--border); margin:14px 0;}\n  a.fc-start.alt{display:inline-block; text-decoration:none; margin-left:8px; background:var(--panel); color:var(--accent); border:1px solid var(--accent);}\n</style>\n</head>\n<body>\n<div class=\"app\">\n  <nav class=\"sidebar\" id=\"sidebar\">\n    <div class=\"brand\"><b>React Training</b><br>hooks \u00b7 Router \u00b7 RTL \u00b7 React 19</div>\n    <input class=\"search\" id=\"searchInput\" type=\"search\" placeholder=\"Search every question   /\" aria-label=\"Search all questions\">\n    <div id=\"navList\"></div>\n    <div class=\"progress-box\">\n      <div class=\"label\">REVIEWED</div>\n      <div class=\"progress-track\"><div class=\"progress-fill\" id=\"progressFill\"></div></div>\n      <div class=\"progress-num\" id=\"progressNum\">0 / 0</div>\n      <button class=\"reset-btn\" id=\"resetBtn\">Reset progress</button>\n    </div>\n  </nav>\n\n  <main class=\"main\">\n    <div class=\"hero\">\n      <p class=\"eyebrow-free\">Prep guide</p>\n      <h1>React interview training</h1>\n      <p>React fundamentals, hooks, performance, React 19, React Router and testing, in the same format as the rest of the prep guides: summaries, search and flashcards. The original simulators (reducer trace, memo, RTL, forms, mock interviewer) are still available in the interactive labs.</p>\n      <div class=\"hero-actions\"><button id=\"fcCore\" class=\"fc-start\" type=\"button\">\u25b6 Flashcards</button><a class=\"fc-start alt\" href=\"/react-training/labs\" target=\"_top\">Open the interactive labs \u2192</a></div>\n      <div class=\"source-note\"><span id=\"heroMeta\"></span><br><span class=\"keys\"><kbd>/</kbd> search \u00b7 <kbd>j</kbd> <kbd>k</kbd> move \u00b7 <kbd>o</kbd> open \u00b7 <kbd>x</kbd> mark reviewed \u00b7 <kbd>n</kbd> <kbd>p</kbd> next / previous section \u00b7 <kbd>Esc</kbd> clear</span></div>\n    </div>\n    <div id=\"content\"></div>\n    <button id=\"backBtn\" class=\"back-btn\" hidden></button>\n    <div id=\"fc\" class=\"fc\" hidden>\n      <div class=\"fc-dialog\" role=\"dialog\" aria-modal=\"true\" aria-label=\"Flashcards\" tabindex=\"-1\">\n        <div class=\"fc-head\">\n          <div><div class=\"fc-title\"></div><div class=\"fc-count\"></div></div>\n          <div class=\"fc-tools\"><label><input type=\"checkbox\" id=\"fcShuffle\"> Shuffle</label><button data-fc=\"close\" aria-label=\"Close flashcards\">\u2715</button></div>\n        </div>\n        <div class=\"fc-bar\"><i></i></div>\n        <div class=\"fc-body\"></div>\n        <div class=\"fc-actions\"></div>\n        <div class=\"fc-keys\"><kbd>Space</kbd> reveal \u00b7 <kbd>1</kbd> again \u00b7 <kbd>2</kbd> got it (marks reviewed) \u00b7 <kbd>\u2190</kbd> <kbd>\u2192</kbd> back / skip \u00b7 <kbd>Esc</kbd> close</div>\n      </div>\n    </div>\n  </main>\n</div>\n\n<script>\nconst DATA = [\n{\n  id:\"react\", title:\"React\",\n  questions:[\n    {\n      q:\"What are Core Web Vitals, and how do you improve them?\",\n      a:`<p>Core Web Vitals measure key user-experience characteristics: <strong>LCP</strong> (loading performance), <strong>INP</strong> (interaction responsiveness), and <strong>CLS</strong> (visual stability). Improve them by reducing render-blocking resources, optimizing images, splitting bundles, avoiding unnecessary JS, reserving layout space, and profiling slow components.</p>\n      <p><strong>Common follow-ups</strong></p>\n      <ul>\n        <li><strong>What's the difference between TTFB and LCP?</strong> TTFB is how long the server takes to start responding (backend/network latency). LCP is how long the largest visible element takes to render \u2014 TTFB plus download, parse, and render time. A slow TTFB always hurts LCP, but a fast TTFB doesn't guarantee a fast LCP.</li>\n        <li><strong>How would you diagnose a slow first load?</strong> Use the browser's network/performance panels or Lighthouse to see the waterfall \u2014 slow TTFB, render-blocking CSS/JS, oversized images, or a blocking client-side fetch \u2014 then fix the single biggest contributor first and re-measure with real field data, not just lab data.</li>\n      </ul>\n      <p><strong>CLS specifically</strong> comes from elements shifting size or position after the initial render &mdash; the fix is to reserve space up front rather than let content \"pop in\":</p>\n<ul>\n<li>Set explicit <code>width</code>/<code>height</code> (or CSS <code>aspect-ratio</code>) on images and video &mdash; <code>next/image</code> does this automatically.</li>\n<li>Use <code>font-display: swap</code> and preload critical fonts to avoid a layout-shifting font swap.</li>\n<li>Render exact-sized skeleton screens instead of spinners that collapse to zero height, and give containers awaiting async content a <code>min-height</code>.</li>\n<li>Pre-allocate fixed-size space for ads, embeds, and other third-party widgets.</li>\n</ul>`\n    },\n    {\n      q:\"Which hooks have you used, and what does each one do?\",\n      a:`<table class=\"cmp\">\n        <tr><th>Hook</th><th>Purpose</th></tr>\n        <tr><td><code>useState</code></td><td>Local component state; triggers a re-render when it changes</td></tr>\n        <tr><td><code>useEffect</code></td><td>Side effects synced to render (fetches, subscriptions, DOM work)</td></tr>\n        <tr><td><code>useRef</code></td><td>A mutable box that survives re-renders <em>without</em> causing one when it changes; also used to hold DOM node references</td></tr>\n        <tr><td><code>useMemo</code></td><td>Caches a computed <em>value</em> between renders</td></tr>\n        <tr><td><code>useCallback</code></td><td>Caches a <em>function reference</em> between renders</td></tr>\n      </table>`\n    },\n    {\n      q:\"What is the component lifecycle, and how does useEffect map onto it?\",\n      a:`<p>Class components had three named phases; function components get the same behavior out of <code>useEffect</code>'s dependency array.</p>\n      <table class=\"cmp\">\n        <tr><th>Class lifecycle</th><th>Function component equivalent</th></tr>\n        <tr><td><code>componentDidMount</code></td><td><code>useEffect(fn, [])</code> \u2014 empty array, runs once after first render</td></tr>\n        <tr><td><code>componentDidUpdate</code></td><td><code>useEffect(fn, [dep1, dep2])</code> \u2014 runs when a listed dependency changes</td></tr>\n        <tr><td><code>componentWillUnmount</code></td><td>the function <em>returned</em> from the effect \u2014 React calls it as cleanup</td></tr>\n      </table>\n      <p>One nuance worth probing for: leaving the dependency array off entirely makes the effect run after <em>every</em> render, which is different from both <code>[]</code> and a populated array \u2014 a common source of bugs.</p>\n      <p><strong>Common follow-ups</strong></p>\n      <ul>\n        <li><strong>What causes an effect to run repeatedly?</strong> Most often a dependency that gets a new reference every render \u2014 an inline object, array, or function \u2014 so React sees it as \"changed\" even though its contents are the same. (Strict Mode's dev-only double-invoke is a different, expected thing.)</li>\n        <li><strong>How do you avoid race conditions in an effect?</strong> Use an <code>AbortController</code> (or a simple \"is this still current\" flag) created fresh each run and cleaned up in the effect's cleanup, so a stale response from a superseded request can't overwrite newer state.</li>\n        <li><strong>When should you avoid useEffect?</strong> For anything computable directly during render (derived/filtered values) \u2014 that doesn't need to \"synchronize with an external system\" at all \u2014 and for event-specific logic, which belongs in the handler, not in an effect reacting to the state it caused.</li>\n      </ul>`\n    },\n    {\n      q:\"What's the difference between useMemo and useCallback?\",\n      a:`<div class=\"diagram\"><svg viewBox=\"0 0 580 130\" width=\"580\">\n    <rect x=\"10\" y=\"15\" width=\"260\" height=\"95\" rx=\"8\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n    <text x=\"140\" y=\"38\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"12\" font-weight=\"600\" fill=\"var(--text)\">useMemo</text>\n    <text x=\"140\" y=\"60\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"11\" fill=\"var(--muted)\">caches a VALUE</text>\n    <text x=\"140\" y=\"80\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"10.5\" fill=\"var(--muted)\">e.g. an expensive sum</text>\n\n    <rect x=\"310\" y=\"15\" width=\"260\" height=\"95\" rx=\"8\" fill=\"var(--gold-soft)\" stroke=\"var(--gold)\"/>\n    <text x=\"440\" y=\"38\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"12\" font-weight=\"600\" fill=\"var(--text)\">useCallback</text>\n    <text x=\"440\" y=\"60\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"11\" fill=\"var(--muted)\">caches a FUNCTION reference</text>\n    <text x=\"440\" y=\"80\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"10.5\" fill=\"var(--muted)\">e.g. a stable onClick prop</text>\n  </svg></div>\n      <pre>const total = useMemo(() =&gt; expensiveSum(items), [items]);      // caches a VALUE\nconst onAdd = useCallback((item) =&gt; setItems(i =&gt; [...i, item]), []); // caches a FUNCTION</pre>\n      <p>Both exist to avoid unnecessary work: <code>useMemo</code> skips re-running an expensive calculation, <code>useCallback</code> keeps a stable function identity so it doesn't invalidate <code>React.memo</code> on a child or re-trigger another hook's dependency array.</p>\n      <p><strong>The nuance interviewers probe for:</strong> both add real cost (memory to store the cached value/comparison, and a dependency-array comparison on every render) &mdash; reaching for them by default is a common anti-pattern.</p>\n<ul>\n<li>Cheap computation? Just compute it inline &mdash; <code>const fullName = first + \" \" + last;</code> beats <code>useMemo</code> for something that cheap.</li>\n<li><code>useCallback</code> only pays off when the function is passed to a memoized child (<code>React.memo</code>) or sits in another hook's dependency array &mdash; otherwise a fresh function every render costs essentially nothing.</li>\n<li>Rule of thumb: memoize when the profiler shows a real cost, not preemptively.</li>\n</ul>`\n    },\n    {\n      q:\"How do you prevent unnecessary React re-renders?\",\n      a:`<p>First, identify the cause with React DevTools Profiler rather than adding memoization everywhere. Then keep state as local as possible, split large components, pass stable props, use <code>React.memo</code> for components that benefit from prop comparison, and reach for <code>useMemo</code>/<code>useCallback</code> only when they solve a measured problem.</p>\n      <p><strong>Common follow-ups</strong></p>\n      <ul>\n        <li><strong>What is referential equality?</strong> Two values pointing to the exact same object/array/function in memory, checked with <code>===</code> \u2014 not deep equality. A new inline object or arrow function passed as a prop breaks <code>React.memo</code> even when its \"value\" hasn't meaningfully changed.</li>\n        <li><strong>How does context cause re-renders?</strong> Every consumer of a context re-renders whenever the Provider's value changes, by reference \u2014 regardless of which part of that value it actually uses. Memoize the value, or split one large context into several smaller ones.</li>\n        <li><strong>How do you profile a slow component tree?</strong> React DevTools Profiler shows which components rendered, how long each took, and why \u2014 much faster than guessing and memoizing speculatively.</li>\n      </ul>\n      <p><strong>React.memo isn't free \u2014 know when it doesn't help.</strong></p>\n      <ul>\n        <li><strong>Cheap renders.</strong> <code>React.memo</code> runs a props comparison on every render regardless. For a component that renders almost nothing, that comparison can cost more than just re-rendering it.</li>\n        <li><strong>Inline object/array/function props.</strong> If the parent passes a new object or array literal every render, the shallow comparison always sees a difference \u2014 the child re-renders every time anyway, and you paid for the comparison for nothing. Stabilize the prop with <code>useMemo</code>/<code>useCallback</code> first, or memo does nothing.</li>\n        <li><strong>Parents that rarely re-render.</strong> If the parent only re-renders on genuine state changes (e.g. a route change), its children already re-render infrequently \u2014 memoizing them adds code with no measurable win.</li>\n        <li><strong>Context consumers.</strong> <code>React.memo</code> only blocks re-renders caused by the <em>parent</em> re-rendering with unchanged props. A component that reads from <code>useContext</code> still re-renders whenever that context value changes \u2014 memo has zero effect on that path.</li>\n      </ul>\n      <p>Rule of thumb: profile first with React DevTools, then memoize surgically \u2014 the sweet spot is a component with an expensive render, stable props, and a parent that re-renders often.</p>`\n    },\n    {\n      q:\"How do you stop one component's error from crashing the whole app?\",\n      a:`<p>An Error Boundary \u2014 a class component (function components can't do this yet) implementing <code>static getDerivedStateFromError</code> and <code>componentDidCatch</code>. It catches JS errors thrown anywhere in its child tree during render, logs them, and swaps in a fallback UI instead of unmounting the entire app.</p>\n      <div class=\"diagram\"><svg viewBox=\"0 0 560 190\" width=\"560\">\n    <rect x=\"200\" y=\"15\" width=\"160\" height=\"36\" rx=\"8\" fill=\"var(--gold-soft)\" stroke=\"var(--gold)\"/>\n    <text x=\"280\" y=\"38\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"11.5\" fill=\"var(--text)\">&lt;ErrorBoundary&gt;</text>\n    <line x1=\"280\" y1=\"51\" x2=\"120\" y2=\"90\" stroke=\"var(--border)\" stroke-width=\"1.5\"/>\n    <line x1=\"280\" y1=\"51\" x2=\"280\" y2=\"90\" stroke=\"var(--border)\" stroke-width=\"1.5\"/>\n    <line x1=\"280\" y1=\"51\" x2=\"440\" y2=\"90\" stroke=\"var(--border)\" stroke-width=\"1.5\"/>\n    <rect x=\"60\" y=\"90\" width=\"120\" height=\"36\" rx=\"8\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/><text x=\"120\" y=\"113\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"10.5\" fill=\"var(--text)\">&lt;Sidebar/&gt; ok</text>\n    <rect x=\"220\" y=\"90\" width=\"120\" height=\"36\" rx=\"8\" fill=\"var(--danger)\" opacity=\"0.25\" stroke=\"var(--danger)\"/><text x=\"280\" y=\"113\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"10.5\" fill=\"var(--text)\">&lt;Widget/&gt; throws</text>\n    <rect x=\"380\" y=\"90\" width=\"120\" height=\"36\" rx=\"8\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/><text x=\"440\" y=\"113\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"10.5\" fill=\"var(--text)\">&lt;Footer/&gt; ok</text>\n    <path d=\"M280 126 L280 150\" stroke=\"var(--danger)\" stroke-width=\"1.5\" marker-end=\"url(#arrow2)\"/>\n    <text x=\"280\" y=\"172\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"11\" fill=\"var(--danger)\">boundary catches it, renders a fallback UI</text>\n    <defs><marker id=\"arrow2\" markerWidth=\"8\" markerHeight=\"8\" refX=\"6\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L6,3 L0,6 z\" fill=\"var(--danger)\"/></marker></defs>\n  </svg></div>\n      <p>Worth knowing: Error Boundaries don't catch errors in event handlers, async code, or the boundary's own render \u2014 those still need a regular <code>try/catch</code>.</p>\n      <p><strong>In a larger app</strong>, pair the boundary with a normalized error shape so every caught error carries the same fields regardless of source:</p>\n<pre>class AppError extends Error {\n  constructor(message, code, statusCode, isRetryable) {\n    super(message);\n    this.code = code;\n    this.statusCode = statusCode;\n    this.isRetryable = isRetryable;\n  }\n}</pre>\n<p>Wrap a <code>Suspense</code> boundary alongside the error boundary at the route level (<code>&lt;ErrorBoundary&gt;&lt;Suspense fallback={...}&gt;&lt;Page /&gt;&lt;/Suspense&gt;&lt;/ErrorBoundary&gt;</code>) so a single route owns both its loading and error states, and log the caught error to Sentry/Datadog inside <code>componentDidCatch</code> rather than just swallowing it into the fallback UI.</p>`\n    },\n    {\n      q:\"Walk through the Redux workflow, and explain why you'd reach for it over useState.\",\n      a:`<div class=\"diagram\"><svg viewBox=\"0 0 560 200\" width=\"560\">\n    <rect x=\"30\" y=\"20\" width=\"120\" height=\"42\" rx=\"8\" fill=\"var(--panel)\" stroke=\"var(--border)\" stroke-width=\"1.5\"/><text x=\"90\" y=\"45\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"11.5\" fill=\"var(--text)\">Component</text>\n    <rect x=\"220\" y=\"20\" width=\"120\" height=\"42\" rx=\"8\" fill=\"var(--gold-soft)\" stroke=\"var(--gold)\" stroke-width=\"1.5\"/><text x=\"280\" y=\"45\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"11.5\" fill=\"var(--text)\">Action</text>\n    <rect x=\"410\" y=\"20\" width=\"120\" height=\"42\" rx=\"8\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\" stroke-width=\"1.5\"/><text x=\"470\" y=\"45\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"11.5\" fill=\"var(--text)\">Reducer</text>\n    <rect x=\"220\" y=\"130\" width=\"120\" height=\"42\" rx=\"8\" fill=\"var(--panel)\" stroke=\"var(--border)\" stroke-width=\"1.5\"/><text x=\"280\" y=\"155\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"11.5\" fill=\"var(--text)\">Store</text>\n\n    <path d=\"M150 41 L220 41\" stroke=\"var(--muted)\" stroke-width=\"1.5\" marker-end=\"url(#a3)\"/>\n    <text x=\"185\" y=\"34\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"9.5\" fill=\"var(--muted)\">dispatch</text>\n    <path d=\"M340 41 L410 41\" stroke=\"var(--muted)\" stroke-width=\"1.5\" marker-end=\"url(#a3)\"/>\n    <path d=\"M470 62 L470 151 L340 151\" stroke=\"var(--muted)\" stroke-width=\"1.5\" marker-end=\"url(#a3)\"/>\n    <text x=\"470\" y=\"105\" font-family=\"var(--mono)\" font-size=\"9.5\" fill=\"var(--muted)\">new state</text>\n    <path d=\"M220 151 L90 151 L90 62\" stroke=\"var(--muted)\" stroke-width=\"1.5\" marker-end=\"url(#a3)\"/>\n    <text x=\"90\" y=\"105\" font-family=\"var(--mono)\" font-size=\"9.5\" fill=\"var(--muted)\">re-render</text>\n    <defs><marker id=\"a3\" markerWidth=\"8\" markerHeight=\"8\" refX=\"6\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L6,3 L0,6 z\" fill=\"var(--muted)\"/></marker></defs>\n  </svg></div>\n      <ol>\n        <li>A component <strong>dispatches an action</strong> \u2014 a plain object describing what happened (<code>{type: \"cart/add\", payload}</code>).</li>\n        <li>The <strong>reducer</strong>, a pure function, takes <code>(state, action)</code> and returns a brand-new state object \u2014 never mutates the old one.</li>\n        <li>The <strong>store</strong> saves that new state and notifies subscribers.</li>\n        <li>Any component subscribed to the changed slice of state <strong>re-renders</strong>.</li>\n      </ol>\n      <p><code>useState</code> is fine for state one component (and maybe its direct children) cares about. Redux earns its keep when state needs to be read and written from many unrelated parts of the tree \u2014 a shopping cart, an authenticated user session \u2014 without wiring props through every layer in between.</p>\n      <p><strong>Common follow-ups</strong></p>\n      <ul>\n        <li><strong>Redux vs. Zustand?</strong> Redux Toolkit gives a strict single-store architecture with mature devtools/middleware and strong conventions \u2014 valuable at scale. Zustand is lighter and less opinionated \u2014 often a better fit for small/medium apps.</li>\n        <li><strong>Client state vs. server state?</strong> Client state (a modal being open) lives only in the UI and is fully owned by it. Server state (a list of users) is a cached, potentially stale copy of data that actually lives on the server, with its own caching/invalidation concerns \u2014 tools like TanStack Query are purpose-built for it rather than stuffing every API response into Redux.</li>\n        <li><strong>How does Redux middleware work?</strong> It sits between dispatch and the reducer, forming a chain where each middleware can inspect, delay, or short-circuit an action before calling <code>next()</code> \u2014 this is what lets <code>dispatch</code> accept an async thunk instead of only a plain action object.</li>\n      </ul>\n      <p><strong>In practice, almost nobody writes raw Redux anymore</strong> &mdash; Redux Toolkit (RTK) is the standard: <code>createSlice</code> generates action creators and a reducer from a single object, and lets you \"mutate\" state directly inside it (it's using Immer under the hood to produce the new object), which removes most of the classic Redux boilerplate.</p>\n<pre>const cartSlice = createSlice({\n  name: \"cart\",\n  initialState: { items: [] },\n  reducers: {\n    add(state, action) { state.items.push(action.payload); } // looks mutable, isn't\n  }\n});\nexport const { add } = cartSlice.actions;\n\nconst store = configureStore({ reducer: { cart: cartSlice.reducer } });</pre>\n<p>RTK also ships <strong>RTK Query</strong> for server state, with the same caching/dedup/invalidation model as TanStack Query &mdash; worth mentioning that even inside a Redux-based app, server data still belongs in a query cache, not hand-rolled into slices.</p>`\n    },\n    {\n      q:\"What is Zustand, and how does it decide when a component re-renders?\",\n      a:`<p>Zustand maintains a store and notifies subscribed components when state changes. Components normally <em>select</em> only the part of the store they need; when the selected value changes, the component re-renders \u2014 reading the whole store on every change would defeat the purpose.</p>\n      <pre>const useStore = create((set) => ({\n  count: 0,\n  increment: () => set((state) => ({ count: state.count + 1 }))\n}));\n\nfunction Counter() {\n  const count = useStore((state) => state.count);\n  return count;\n}</pre>\n      <p><strong>Good interview signal:</strong> keeping selectors small and stable, and being careful about returning newly-created objects from a selector (breaks the equality check Zustand uses to decide whether to re-render).</p>`\n    },\n    {\n      q:\"What are React Server Components, and how do they differ from traditional SSR?\",\n      a:`<p>Traditional SSR renders the whole tree to HTML on the server for the initial load, then ships the same component code to the client for hydration. React Server Components (RSC) let specific components run <em>only</em> on the server \u2014 including direct data access \u2014 and never ship their code to the client at all, composed in the same tree alongside regular Client Components that do hydrate.</p>\n      <p><strong>Good interview signal:</strong> \"RSC reduces client bundle size since server-only code never reaches the browser\" and \"it's a per-component choice, not a whole-page one\" \u2014 this is what Next.js's App Router builds on.</p>\n      <table class=\"cmp\">\n<tr><th>Strategy</th><th>When HTML is generated</th><th>Best for</th></tr>\n<tr><td>SSR</td><td>On every request</td><td>Personalized/auth-gated pages, dashboards</td></tr>\n<tr><td>SSG</td><td>At build time</td><td>Marketing pages, docs, blogs</td></tr>\n<tr><td>ISR</td><td>Build time, then revalidated on a timer (e.g. every 60s)</td><td>Semi-static data like product pages</td></tr>\n<tr><td>Server Components</td><td>Per-request, per-component &mdash; a finer grain than \"the page\"</td><td>Anything that doesn't need client interactivity or state</td></tr>\n</table>\n<p>In the Next.js App Router, every component is a Server Component by default; a leaf component opts into the client with <code>\"use client\"</code> only when it needs state, effects, or browser APIs &mdash; Server Components can import Client Components, but never the other way around.</p>\n<pre>// Server Component (default) &mdash; can query a database directly\nasync function ProductPage({ params }) {\n  const product = await db.query(\"SELECT * FROM products WHERE id = $1\", [params.id]);\n  return (&lt;div&gt;&lt;h1&gt;{product.name}&lt;/h1&gt;&lt;AddToCartButton productId={product.id} /&gt;&lt;/div&gt;);\n}\n\n// \"use server\" &mdash; a Server Action callable directly from a Client Component\n\"use server\";\nasync function addToCart(productId) {\n  await db.insert(\"cart_items\", { productId, userId: getCurrentUser() });\n  revalidatePath(\"/cart\");\n}</pre>\n<p>This also enables <strong>streaming SSR</strong>: the server sends the page shell immediately and streams in each <code>Suspense</code> boundary's content as it resolves (Next.js's <code>loading.tsx</code> wires this in automatically), instead of blocking the entire response on the slowest data fetch.</p>`\n    },\n    {\n      q:\"What are useTransition and useDeferredValue, and when would you reach for them?\",\n      a:`<p>Both are React 18+ concurrent-rendering APIs for keeping the UI responsive when a state update triggers expensive work. They don't make anything faster &mdash; they let React deprioritize the expensive part so urgent updates (typing, clicks) still render immediately.</p>\n<ul>\n<li><strong>useTransition:</strong> marks a state update as non-urgent. React renders it in the background and can interrupt it if a more urgent update comes in, while an <code>isPending</code> flag lets you show a loading indicator.</li>\n<li><strong>useDeferredValue:</strong> takes a value you don't control the setter for (e.g. a prop) and returns a version that \"lags behind\" during heavy renders, so whatever drives that value stays responsive.</li>\n</ul>\n<pre>const [isPending, startTransition] = useTransition();\n\nfunction handleChange(e) {\n  setQuery(e.target.value);            // urgent: input stays responsive\n  startTransition(() =&gt; {\n    setFilteredResults(filterHugeList(e.target.value)); // non-urgent\n  });\n}</pre>\n<p>Rule of thumb: <code>useTransition</code> when you control the state update; <code>useDeferredValue</code> when you only have the value (e.g. it's a prop or comes from a store).</p>\n<p><strong>Common follow-ups</strong></p>\n<ul><li><strong>Does this replace debouncing?</strong> No &mdash; debouncing delays work, transitions deprioritize it while still using the latest value. They solve different problems and are often combined.</li></ul>`\n    },\n    {\n      q:\"When is useReducer a better fit than useState?\",\n      a:`<p>Reach for <code>useReducer</code> when state has multiple sub-values that change together, when the next state depends on complex logic around the previous state, or when you want the transitions themselves to be testable as a pure function separate from the component.</p>\n<pre>type State = { status: \"idle\"|\"loading\"|\"success\"|\"error\"; data: unknown; error: string|null };\ntype Action =\n  | { type: \"FETCH_START\" }\n  | { type: \"FETCH_SUCCESS\"; payload: unknown }\n  | { type: \"FETCH_ERROR\"; error: string };\n\nfunction reducer(state: State, action: Action): State {\n  switch (action.type) {\n    case \"FETCH_START\": return { ...state, status: \"loading\", error: null };\n    case \"FETCH_SUCCESS\": return { status: \"success\", data: action.payload, error: null };\n    case \"FETCH_ERROR\": return { status: \"error\", data: null, error: action.error };\n  }\n}\n\nconst [state, dispatch] = useReducer(reducer, { status: \"idle\", data: null, error: null });</pre>\n<p>It isn't \"Redux in a component\" &mdash; there's no store or middleware &mdash; but it borrows the same idea: a single pure function owns every valid transition, which makes impossible states (like <code>status: \"success\"</code> with no data) much harder to end up in by accident.</p>\n<p><strong>Common follow-ups</strong></p>\n<ul><li><strong>Can you combine it with Context?</strong> Yes &mdash; <code>useReducer</code> + Context is a common lightweight alternative to Redux for state shared across a subtree, without pulling in a library.</li></ul>`\n    },\n    {\n      q:\"How do you write a custom hook with solid TypeScript typing?\",\n      a:`<p>A custom hook is just a function that calls other hooks &mdash; the discipline is in the return type. For a public hook, type the return explicitly rather than leaning on inference, and use <code>as const</code> when returning a tuple so consumers get positional types instead of a wide array type.</p>\n<pre>function useLocalStorage&lt;T&gt;(key: string, initialValue: T) {\n  const [value, setValue] = useState&lt;T&gt;(() =&gt; {\n    try {\n      const item = window.localStorage.getItem(key);\n      return item ? (JSON.parse(item) as T) : initialValue;\n    } catch {\n      return initialValue;\n    }\n  });\n\n  const setAndPersist = useCallback((next: T | ((prev: T) =&gt; T)) =&gt; {\n    setValue(prev =&gt; {\n      const resolved = next instanceof Function ? next(prev) : next;\n      window.localStorage.setItem(key, JSON.stringify(resolved));\n      return resolved;\n    });\n  }, [key]);\n\n  return [value, setAndPersist] as const; // a tuple, not (T | setter)[]\n}</pre>\n<ul>\n<li>Return a <strong>tuple</strong> (<code>as const</code>) for two values with a clear order (mirrors <code>useState</code>); return an <strong>object</strong> once you have three or more, so call sites don't have to remember positional order.</li>\n<li>Any hook touching <code>window</code>/<code>document</code> needs a guard or lazy initializer for environments (SSR) where those globals don't exist during the server render.</li>\n<li><code>useRef</code> is the other building block custom hooks lean on &mdash; it holds a value (a previous value, an interval id, a \"did this already run\" flag) across renders <em>without</em> triggering a re-render when it changes, unlike state.</li>\n</ul>`\n    },\n    {\n      q:\"Controlled vs uncontrolled components &mdash; how do you decide, and where does react-hook-form fit?\",\n      a:`<table class=\"cmp\">\n<tr><th></th><th>Controlled</th><th>Uncontrolled</th></tr>\n<tr><td>Source of truth</td><td>React state (<code>value</code> + <code>onChange</code>)</td><td>The DOM (read via a <code>ref</code>)</td></tr>\n<tr><td>Re-renders</td><td>One per keystroke</td><td>None until you read the value</td></tr>\n<tr><td>Good for</td><td>Live validation, conditional UI, derived state</td><td>Simple or large forms, perf-sensitive inputs</td></tr>\n</table>\n<pre>// Controlled\nconst [email, setEmail] = useState(\"\");\n&lt;input value={email} onChange={e =&gt; setEmail(e.target.value)} /&gt;\n\n// Uncontrolled\nconst emailRef = useRef&lt;HTMLInputElement&gt;(null);\n&lt;input ref={emailRef} defaultValue=\"initial\" /&gt;\n// read emailRef.current.value on submit</pre>\n<p>In practice, most production forms use <strong>react-hook-form</strong>, which keeps inputs uncontrolled (so typing doesn't re-render the form) but layers validation and error state on top via <code>register</code> &mdash; the performance of uncontrolled inputs with the ergonomics of controlled ones.</p>\n<pre>const { register, handleSubmit, formState: { errors } } = useForm();\n&lt;input {...register(\"email\", { required: true, pattern: /^\\\\S+@\\\\S+$/ })} /&gt;\n{errors.email &amp;&amp; &lt;span&gt;Invalid email&lt;/span&gt;}</pre>`\n    },\n    {\n      q:\"What is the Compound Component pattern, and when is it worth reaching for?\",\n      a:`<p>Compound components share implicit state through Context between a parent and a set of children, so consumers can compose the pieces freely instead of passing one big config object through a single prop.</p>\n<pre>const AccordionCtx = createContext(null);\n\nfunction Accordion({ children }) {\n  const [openId, setOpenId] = useState(null);\n  const toggle = id =&gt; setOpenId(prev =&gt; (prev === id ? null : id));\n  return &lt;AccordionCtx.Provider value={{ openId, toggle }}&gt;{children}&lt;/AccordionCtx.Provider&gt;;\n}\n\nfunction AccordionItem({ id, title, children }) {\n  const { openId, toggle } = useContext(AccordionCtx);\n  const isOpen = openId === id;\n  return (\n    &lt;div&gt;\n      &lt;button onClick={() =&gt; toggle(id)}&gt;{title}&lt;/button&gt;\n      {isOpen &amp;&amp; &lt;div&gt;{children}&lt;/div&gt;}\n    &lt;/div&gt;\n  );\n}\n\n// &lt;Accordion&gt;&lt;AccordionItem id=\"1\" title=\"Section 1\"&gt;...&lt;/AccordionItem&gt;&lt;/Accordion&gt;</pre>\n<p>It's the pattern behind most headless UI libraries (Radix, Reach UI, Headless UI) &mdash; it gives consumers control over markup and layout while the parent still owns the shared state. The older <strong>render props</strong> pattern (passing a function as a prop to expose internal state) solved a similar problem before hooks existed, but is now mostly superseded by custom hooks for logic reuse, and by compound components for this kind of flexible composition.</p>`\n    },\n    {\n      q:\"What is React Fiber, and how does it enable concurrent rendering?\",\n      a:`<p>Fiber is React's reconciliation engine (since React 16) &mdash; it replaced the older \"stack reconciler,\" which rendered synchronously and recursively, so a large component tree could block the main thread and drop frames.</p>\n<ul>\n<li><strong>Units of work:</strong> Fiber breaks rendering into small units, roughly one per component, each a node in a linked-list-like structure. React can pause after any unit and hand control back to the browser.</li>\n<li><strong>Interruptible rendering:</strong> because the work is chunked, React can pause a low-priority render to handle an urgent update (a keystroke) and resume &mdash; or discard in-progress work entirely if it's no longer needed.</li>\n<li><strong>Time slicing:</strong> React yields to the browser every few milliseconds during a render pass, so animations and input stay smooth even under a heavy update.</li>\n</ul>\n<p>Fiber is the underlying mechanism that makes <code>useTransition</code>, <code>useDeferredValue</code>, and Suspense possible &mdash; none of them would work with the old synchronous renderer, since there'd be nothing to interrupt.</p>`\n    },\n    {\n      q:\"How do you choose between Redux Toolkit, TanStack Query, Zustand, and Context for a given piece of state?\",\n      a:`<p>The senior framing: these solve different problems, and the most common mistake is using one tool for all of them &mdash; especially putting server data in Redux.</p>\n<ul>\n<li><strong>TanStack/React Query:</strong> the default for <em>server</em> state &mdash; anything that's a cached copy of data owned by an API. Gives you stale-while-revalidate caching, retries, dedup, pagination, and optimistic updates for free.</li>\n<li><strong>Redux Toolkit:</strong> global <em>client</em> state with non-trivial derived data and a need for strict, traceable, unidirectional flow &mdash; a shopping cart, a multi-step wizard, feature flags. Heavier setup, but strong devtools and conventions at scale.</li>\n<li><strong>Zustand:</strong> lightweight global client state with near-zero boilerplate and no Provider &mdash; a good default for things like theme, sidebar/modal state, or small shared state that doesn't need Redux's ceremony.</li>\n<li><strong>Context:</strong> best thought of as dependency injection, not a state manager &mdash; it re-renders every consumer when its value changes, regardless of which part they read. Fine for values that change rarely (theme, locale, the authenticated user).</li>\n</ul>\n<p><strong>Golden rule:</strong> server state &ne; client state. If it came from an API, it belongs in a query cache (React Query/SWR), not in Redux.</p>`\n    },\n    {\n      q:\"How do you keep a React app fast with large component trees and long lists?\",\n      a:`<p>Two complementary techniques: ship less code up front, and render less DOM at once.</p>\n<p><strong>Code-splitting with React.lazy:</strong></p>\n<pre>const AdminDashboard = React.lazy(() =&gt; import(\"./AdminDashboard\"));\n\n&lt;ErrorBoundary fallback={&lt;p&gt;Failed to load page&lt;/p&gt;}&gt;\n  &lt;Suspense fallback={&lt;PageSkeleton /&gt;}&gt;\n    &lt;AdminDashboard /&gt;\n  &lt;/Suspense&gt;\n&lt;/ErrorBoundary&gt;\n\n// prefetch on hover for instant transitions\n&lt;Link to=\"/admin\" onMouseEnter={() =&gt; import(\"./AdminDashboard\")}&gt;Admin&lt;/Link&gt;</pre>\n<p><strong>List virtualization</strong> for anything over roughly 100 rows &mdash; render only the rows in (or near) the viewport instead of the whole list:</p>\n<pre>import { FixedSizeList } from \"react-window\";\n\n&lt;FixedSizeList height={600} width=\"100%\" itemCount={items.length} itemSize={48}&gt;\n  {({ index, style }) =&gt; &lt;div style={style}&gt;{items[index].name}&lt;/div&gt;}\n&lt;/FixedSizeList&gt;</pre>\n<p>Rendering 10,000 rows unvirtualized means roughly 10,000 DOM nodes; virtualized, it's closer to 20. <code>@tanstack/react-virtual</code> is the more flexible modern alternative when rows have variable height or you need a grid.</p>`\n    },\n    {\n      q:\"How does Suspense work for data fetching, and how does it change loading-state code?\",\n      a:`<p>A component \"suspends\" by throwing a Promise during render. The nearest parent <code>&lt;Suspense&gt;</code> boundary catches it, shows its <code>fallback</code>, and swaps back to the real content once the promise resolves &mdash; loading state becomes a property of the tree's structure instead of a flag threaded through every component.</p>\n<pre>function UserProfile({ userId }) {\n  const { data } = useSuspenseQuery({\n    queryKey: [\"user\", userId],\n    queryFn: () =&gt; fetchUser(userId),\n  });\n  return &lt;h1&gt;{data.name}&lt;/h1&gt;; // no loading check needed &mdash; data is guaranteed here\n}\n\n&lt;ErrorBoundary fallback={&lt;ErrorPage /&gt;}&gt;\n  &lt;Suspense fallback={&lt;ProfileSkeleton /&gt;}&gt;\n    &lt;UserProfile userId=\"123\" /&gt;\n  &lt;/Suspense&gt;\n&lt;/ErrorBoundary&gt;</pre>\n<p>Boundaries can nest, so different parts of a page can reveal independently instead of the whole page waiting on the slowest fetch.</p>\n<p><strong>Common follow-ups</strong></p>\n<ul><li><strong>What if you're not using a Suspense-enabled query library?</strong> Fall back to manual fetch handling: an <code>AbortController</code> to cancel on unmount, retries with backoff only on 5xx (never blindly retry a 4xx), and runtime validation of the response (e.g. with Zod) since a TypeScript type checks nothing at runtime.</li></ul>`\n    },\n    {\n      q:\"Walk through your checklist for debugging a slow React page.\",\n      a:`<ol>\n<li><strong>Measure first:</strong> React DevTools Profiler to see which components render most/longest; Lighthouse for Core Web Vitals scores. Don't optimize on a guess.</li>\n<li><strong>Bundle size:</strong> a bundle analyzer to see what's shipping; code-split routes with <code>React.lazy</code>; tree-shake unused exports.</li>\n<li><strong>Network waterfalls:</strong> check whether requests fire sequentially when they could be parallel; prefetch on hover; cache with React Query.</li>\n<li><strong>Unnecessary re-renders:</strong> profile before reaching for <code>React.memo</code>/<code>useMemo</code>/<code>useCallback</code> &mdash; add them where the profiler shows the cost, not everywhere.</li>\n<li><strong>Long lists:</strong> virtualize anything over roughly 100 items.</li>\n<li><strong>Images:</strong> explicit <code>width</code>/<code>height</code> or <code>aspect-ratio</code>, modern formats (WebP/AVIF), lazy-load below the fold.</li>\n<li><strong>CSS:</strong> <code>content-visibility: auto</code> for off-screen sections; avoid layout thrashing (read all DOM measurements, then write, never interleaved).</li>\n</ol>\n<p>A structured checklist like this signals more than naming one optimization trick &mdash; it shows you'd diagnose before reaching for <code>memo</code>.</p>`\n    },\n    {\n      q:\"How does React prevent XSS, and where does that protection break down?\",\n      a:`<p>React escapes every string value it renders in JSX by default, so this is safe even with malicious input:</p>\n<pre>const userInput = '&lt;script&gt;alert(\"xss\")&lt;/script&gt;';\n&lt;div&gt;{userInput}&lt;/div&gt; // rendered as literal text, never executed</pre>\n<p>The protection breaks down at a few specific escape hatches:</p>\n<ul>\n<li><strong>dangerouslySetInnerHTML:</strong> bypasses escaping entirely &mdash; sanitize with a library like DOMPurify before ever passing user content through it.</li>\n<li><strong>URL-bearing attributes:</strong> an unvalidated <code>href</code> can contain a <code>javascript:</code> URL. Validate/allowlist the scheme (<code>https://</code>, relative paths) before rendering a user-supplied link.</li>\n<li><strong>Server-rendered user content</strong> injected outside React's rendering (e.g. straight into an HTML template) doesn't get React's escaping at all.</li>\n</ul>\n<p><strong>Common follow-ups</strong></p>\n<ul><li><strong>What headers back this up as defense-in-depth?</strong> A <code>Content-Security-Policy</code> to whitelist script/style sources, <code>X-Content-Type-Options: nosniff</code>, <code>X-Frame-Options: DENY</code> against clickjacking, HSTS to force HTTPS, and a tightened <code>Permissions-Policy</code> to disable unused browser features like camera/mic/geolocation.</li></ul>`\n    },\n    {\n      q:\"What are the core accessibility (WCAG) practices you build in by default?\",\n      a:`<p>Follow POUR &mdash; Perceivable, Operable, Understandable, Robust &mdash; and treat it as a default, not a pass at the end:</p>\n<ul>\n<li><strong>Semantic HTML:</strong> <code>&lt;button&gt;</code>, <code>&lt;nav&gt;</code>, <code>&lt;main&gt;</code>, <code>&lt;article&gt;</code> &mdash; not a <code>&lt;div onClick&gt;</code>. Screen readers rely on the element's actual role.</li>\n<li><strong>Color contrast:</strong> at least 4.5:1 for normal text, 3:1 for large text (WCAG AA).</li>\n<li><strong>Keyboard navigation:</strong> every interactive element reachable by Tab, with a visible focus indicator &mdash; never <code>outline: none</code> without a replacement.</li>\n<li><strong>Form labels:</strong> every input has an associated <code>&lt;label&gt;</code> via <code>htmlFor</code>/<code>id</code>; a placeholder is not a label.</li>\n<li><strong><code>aria-live</code> regions:</strong> for dynamic content like toasts &mdash; <code>polite</code> for routine updates, <code>assertive</code> only for critical ones.</li>\n<li><strong>Focus management:</strong> move focus deliberately after route changes or when a modal opens/closes.</li>\n</ul>\n<p><strong>Common follow-ups</strong></p>\n<ul><li><strong>How do you verify this?</strong> A keyboard-only pass, a screen reader (VoiceOver/NVDA), the axe-core browser extension, and a Lighthouse accessibility audit &mdash; automated tools catch maybe a third of real issues, so a manual pass still matters.</li></ul>`\n    },\n    {\n      q:\"Walk through making a custom dropdown accessible.\",\n      a:`<p>A custom dropdown has to reimplement everything a native <code>&lt;select&gt;</code> gives you for free: roles, keyboard handling, and focus management.</p>\n<pre>function AccessibleSelect({ options, value, onChange, label }) {\n  const [isOpen, setIsOpen] = useState(false);\n  const [activeIndex, setActiveIndex] = useState(-1);\n  const listboxId = useId();\n\n  function onKeyDown(e) {\n    if (e.key === \"ArrowDown\") setActiveIndex(i =&gt; Math.min(i + 1, options.length - 1));\n    if (e.key === \"ArrowUp\") setActiveIndex(i =&gt; Math.max(i - 1, 0));\n    if (e.key === \"Enter\") { onChange(options[activeIndex]); setIsOpen(false); }\n    if (e.key === \"Escape\") setIsOpen(false);\n  }\n\n  return (\n    &lt;&gt;\n      &lt;label id={listboxId + \"-label\"}&gt;{label}&lt;/label&gt;\n      &lt;button\n        role=\"combobox\" aria-expanded={isOpen} aria-haspopup=\"listbox\"\n        aria-controls={listboxId} aria-labelledby={listboxId + \"-label\"}\n        onKeyDown={onKeyDown} onClick={() =&gt; setIsOpen(!isOpen)}\n      &gt;{value ?? \"Select...\"}&lt;/button&gt;\n      {isOpen &amp;&amp; (\n        &lt;ul role=\"listbox\" id={listboxId}&gt;\n          {options.map((opt, i) =&gt; (\n            &lt;li key={opt} role=\"option\" aria-selected={opt === value}&gt;{opt}&lt;/li&gt;\n          ))}\n        &lt;/ul&gt;\n      )}\n    &lt;/&gt;\n  );\n}</pre>\n<p>The essentials: <code>role=\"combobox\"</code> on the trigger with <code>aria-expanded</code>/<code>aria-controls</code>, <code>role=\"listbox\"</code>/<code>role=\"option\"</code> with <code>aria-selected</code> on the list, and arrow-key/Enter/Escape handling. In real projects, reach for Radix UI or Headless UI instead of hand-rolling this &mdash; but the ARIA pattern is what the question is actually testing.</p>`\n    },\n    {\n      q:\"Vite vs Webpack vs Turbopack &mdash; how do you choose a build tool?\",\n      a:`<ul>\n<li><strong>Vite:</strong> native ESM in dev (no bundling needed for most of the dev cycle), Rollup for production builds. Very fast HMR &mdash; the default for new projects today.</li>\n<li><strong>Webpack:</strong> mature and enormously configurable, with a plugin ecosystem nothing else matches yet. Still the right call for Module Federation or complex legacy configs. Slower dev server/HMR by comparison.</li>\n<li><strong>Turbopack:</strong> Rust-based, built into Next.js, positioned as Webpack's eventual successor. Incremental, function-level caching means only changed modules rebuild. Still maturing outside the Next.js context.</li>\n</ul>\n<p>Practical answer: default to Vite for a new standalone app, use whatever Next.js ships (Turbopack) if you're on Next, and reach for Webpack only when a legacy config or Module Federation requires it.</p>`\n    },\n    {\n      q:\"Tailwind CSS vs CSS Modules vs CSS-in-JS &mdash; how do you choose a styling approach?\",\n      a:`<table class=\"cmp\">\n<tr><th></th><th>Tailwind</th><th>CSS Modules</th><th>CSS-in-JS</th></tr>\n<tr><td>Model</td><td>Utility classes</td><td>Scoped CSS files</td><td>Styles written in JS, colocated</td></tr>\n<tr><td>Runtime cost</td><td>None</td><td>None</td><td>Often some (varies by library)</td></tr>\n<tr><td>Design tokens</td><td>Built in</td><td>Manual</td><td>Manual (via a theme object)</td></tr>\n<tr><td>Main downside</td><td>Verbose class strings</td><td>No dynamic styling from JS state</td><td>Runtime/SSR overhead; several libraries are being deprecated</td></tr>\n</table>\n<p>A common senior default: Tailwind (often paired with a headless component library like Radix/Ark for interaction logic) for product UI, CSS Modules for shared library/design-system code, and CSS-in-JS avoided in new projects given the runtime and SSR complexity it adds relative to the zero-runtime alternatives.</p>`\n    },\n    {\n      q:\"What is useOptimistic, and how does optimistic UI rollback work?\",\n      a:`<p><code>useOptimistic</code> lets you update the UI immediately, before an async action (like a server mutation) has actually finished, so the app feels instant instead of waiting on the network.</p>\n      <pre>const [optimisticComments, addOptimistic] = useOptimistic(\n  comments,\n  (state, newComment) =&gt; [...state, newComment]\n);\n\nasync function action(formData) {\n  const text = formData.get('comment');\n  addOptimistic({ id: Date.now(), text, isPending: true }); // 1. instant UI update\n  await saveComment(text);                                  // 2. reconciles or rolls back automatically\n}</pre>\n      <p>It takes the current confirmed state plus an update function, and returns a temporary \"optimistic\" state that reflects your update right away. If the underlying async action succeeds, the real state (passed in as the first argument) eventually catches up and replaces the optimistic value. If it fails, React discards the optimistic update and the UI reverts to the last confirmed state \u2014 no manual rollback code required.</p>\n      <p><strong>Common follow-ups</strong></p>\n      <ul>\n        <li><strong>Where does the \"confirmed\" state come from?</strong> It's whatever state you pass as <code>useOptimistic</code>'s first argument \u2014 typically the real array from <code>useState</code>, which you update yourself once the request actually resolves.</li>\n        <li><strong>What should the UI show while a mutation is pending?</strong> Common practice is tagging the optimistic entry (<code>isPending: true</code>) so you can render it dimmed or with a spinner until the real state replaces it.</li>\n      </ul>`\n    },\n    {\n      q:\"What do React 19's useActionState, useFormStatus, and use() add to data flow?\",\n      a:`<p>Three related additions in React 19 target forms and reading async data during render.</p>\n      <ul>\n        <li><strong>useActionState</strong> \u2014 pairs a form with an async action function, and gives you back <code>[state, formAction, isPending]</code>. React manages the pending flag and the returned result/error state for you, so you no longer hand-roll <code>isLoading</code> flags or call <code>preventDefault()</code> yourself.</li>\n        <li><strong>useFormStatus</strong> \u2014 read inside a child rendered within a <code>&lt;form&gt;</code>, it reports whether that form's action is currently pending, without prop-drilling a loading flag down from the parent.</li>\n        <li><strong>use()</strong> \u2014 reads the value of a Promise (or Context) directly during render. Passing it a pending Promise <strong>suspends</strong> the component, integrating with <code>&lt;Suspense&gt;</code> the same way <code>React.lazy</code> does.</li>\n      </ul>\n      <pre>const [state, formAction, isPending] = useActionState(signUpAction, initialState);\n\nfunction SubmitButton() {\n  const { pending } = useFormStatus(); // no props needed\n  return &lt;button disabled={pending}&gt;Submit&lt;/button&gt;;\n}\n\nfunction Message({ promise }) {\n  const data = use(promise); // suspends until resolved\n  return &lt;div&gt;{data}&lt;/div&gt;;\n}</pre>\n      <p><strong>Common follow-ups</strong></p>\n      <ul>\n        <li><strong>How does <code>use()</code> differ from <code>await</code>?</strong> Both unwrap a Promise, but <code>use()</code> works inside ordinary Client Components by suspending the render tree, while <code>await</code> in a component body only works in async Server Components.</li>\n        <li><strong>Can you call <code>use()</code> conditionally?</strong> Yes \u2014 unlike other hooks, <code>use()</code> can be called inside loops and <code>if</code> blocks, since it isn't bound by the Rules of Hooks the way <code>useState</code>/<code>useEffect</code> are.</li>\n      </ul>`\n    },\n    {\n      q:\"How does a custom comparison function change React.memo's behavior, and what's the risk?\",\n      a:`<p><code>React.memo</code>'s second argument lets you override its default shallow-props comparison with your own function, so you decide exactly which prop changes justify a re-render.</p>\n      <pre>const PriceBadge = React.memo(\n  function PriceBadge({ name, price, category }) {\n    return &lt;div&gt;{name} \u2014 \\${price}&lt;/div&gt;;\n  },\n  // Only re-render when price changes; ignore name/category\n  (prevProps, nextProps) =&gt; prevProps.price === nextProps.price\n);</pre>\n      <p>Unlike a regular equality check, this comparator returns <code>true</code> when props are considered <em>equal</em> (skip the re-render) \u2014 the inverse of how comparator functions usually read. That inversion is a common source of confusion.</p>\n      <p><strong>Common follow-ups</strong></p>\n      <ul>\n        <li><strong>What happens if the comparator is wrong?</strong> If it returns <code>true</code> while props actually differ meaningfully, the component silently skips a render it should have done \u2014 a stale-UI bug that's easy to miss in code review since nothing throws or warns.</li>\n        <li><strong>When is a custom comparator worth it?</strong> When a component receives some props that change often but don't affect its output (like an object literal recreated every render) alongside props that genuinely matter \u2014 letting you ignore the noisy ones instead of stabilizing every single prop upstream with <code>useMemo</code>.</li>\n      </ul>`\n    },\n    {\n      q:\"Trace a useReducer dispatch sequence through a reducer with an unhandled action type.\",\n      a:`<p>A classic \"trace the output\" exercise: given a reducer with cases for <code>'increment'</code> (<code>state + 1</code>), <code>'double'</code> (<code>state * 2</code>), and a <code>default</code> that returns state unchanged \u2014 and <strong>no <code>'reset'</code> case</strong> \u2014 what's the final value after dispatching <code>['increment', 'double', 'reset', 'increment']</code> starting from <code>useReducer(reducer, 0)</code>?</p>\n      <pre>function reducer(state, action) {\n  switch (action.type) {\n    case 'increment': return state + 1;\n    case 'double':    return state * 2;\n    default:          return state; // no 'reset' case \u2014 falls through here\n  }\n}</pre>\n      <table class=\"cmp\">\n        <tr><th>Dispatch</th><th>Calculation</th><th>Value</th></tr>\n        <tr><td>init</td><td>\u2014</td><td>0</td></tr>\n        <tr><td>'increment'</td><td>0 + 1</td><td>1</td></tr>\n        <tr><td>'double'</td><td>1 \u00d7 2</td><td>2</td></tr>\n        <tr><td>'reset'</td><td>unhandled \u2192 default returns state unchanged</td><td>2</td></tr>\n        <tr><td>'increment'</td><td>2 + 1</td><td>3</td></tr>\n      </table>\n      <p>The final value is <strong>3</strong>. The trap is assuming a <code>'reset'</code> action does something just because it sounds like it should \u2014 a reducer only does exactly what its <code>switch</code> handles, and anything else silently falls through to <code>default</code>.</p>\n      <p><strong>Common follow-ups</strong></p>\n      <ul>\n        <li><strong>What if there were no <code>default</code> case at all?</strong> The reducer would return <code>undefined</code> for an unhandled action, which is a real bug \u2014 always include a <code>default</code> that returns the existing state unchanged.</li>\n      </ul>`\n    },\n    {\n      q:\"Why should a React functional component's name be capitalized?\",\n      a:`<p>This is really a JSX-compilation question in disguise. JSX doesn't know about your component at compile time \u2014 it just checks whether the tag name is capitalized to decide how to compile it.</p>\n      <pre>// Lowercase \u2014 compiles to a native DOM element lookup\nconst app = () =&gt; &lt;div&gt;Hi&lt;/div&gt;;\n&lt;app /&gt;  // compiles to React.createElement('app', ...)\n         // React looks for an HTML tag called \"app\" \u2014 doesn't exist, breaks\n\n// Capitalized \u2014 compiles to a component reference\nconst App = () =&gt; &lt;div&gt;Hi&lt;/div&gt;;\n&lt;App /&gt;  // compiles to React.createElement(App, ...)\n         // React calls your function as intended</pre>\n      <p>A lowercase tag like <code>&lt;app /&gt;</code> is compiled as a string \u2014 the name of an HTML element \u2014 not a reference to your variable. Since there's no built-in <code>&lt;app&gt;</code> element, this silently fails to render your component instead of throwing a helpful error.</p>\n      <p><strong>Common follow-ups</strong></p>\n      <ul>\n        <li><strong>Does this apply to custom hooks too?</strong> No \u2014 hooks follow the opposite convention (must start with lowercase <code>use</code>), which is an ESLint/naming convention, not a JSX-compilation requirement, since hooks are never used as JSX tags.</li>\n      </ul>`\n    },\n    {\n      q:\"What is useBlocker in React Router, and how do you prevent users from losing unsaved changes?\",\n      a:`<p><code>useBlocker</code> intercepts client-side React Router navigation \u2014 link clicks, <code>useNavigate</code> calls, in-app back/forward \u2014 so you can hold a transition and ask the user to confirm before it proceeds.</p>\n      <pre>const blocker = useBlocker(\n  ({ currentLocation, nextLocation }) =&gt;\n    isDirty &amp;&amp; currentLocation.pathname !== nextLocation.pathname\n);\n\nif (blocker.state === 'blocked') {\n  return (\n    &lt;Modal&gt;\n      &lt;button onClick={() =&gt; blocker.proceed()}&gt;Discard&lt;/button&gt;\n      &lt;button onClick={() =&gt; blocker.reset()}&gt;Cancel&lt;/button&gt;\n    &lt;/Modal&gt;\n  );\n}</pre>\n      <p>The blocker function runs synchronously on every navigation attempt. Returning <code>true</code> halts the transition and moves <code>blocker.state</code> to <code>'blocked'</code>; calling <code>blocker.proceed()</code> resumes it, <code>blocker.reset()</code> cancels it and keeps the user on the page.</p>\n      <p><strong>Common follow-ups</strong></p>\n      <ul>\n        <li><strong>Does this also stop the user from closing the tab?</strong> No \u2014 <code>useBlocker</code> only intercepts React Router's own client-side navigation. Closing the tab, refreshing, or navigating to a different origin needs the native <code>beforeunload</code> event as a second, separate guard, attached in a <code>useEffect</code> and cleaned up when the form is no longer dirty. Modern browsers also ignore any custom message you set on it \u2014 they always show their own generic confirmation text.</li>\n        <li><strong>Where should the blocker live in a multi-step flow?</strong> At the parent layout level, not inside individual steps, so it isn't torn down and reattached as the user moves between steps of the same flow.</li>\n      </ul>`\n    },\n    {\n      q:\"How does React Router's Form differ from useFetcher for data mutations?\",\n      a:`<p>Both submit data to a route action, but they differ in whether the page navigates.</p>\n      <table class=\"cmp\">\n        <tr><th></th><th>&lt;Form&gt;</th><th>useFetcher</th></tr>\n        <tr><td>Triggers navigation</td><td>Yes \u2014 full route transition</td><td>No \u2014 stays on the current page</td></tr>\n        <tr><td>Use for</td><td>Actions that should change the URL (create, then redirect to the new item)</td><td>In-place mutations (like/favorite, inline edits) where leaving the page makes no sense</td></tr>\n        <tr><td>State</td><td>Standard navigation state</td><td><code>fetcher.state</code> \u2014 <code>'idle' | 'submitting' | 'loading'</code>, scoped to that one fetcher</td></tr>\n      </table>\n      <pre>// &lt;Form&gt; \u2014 submits and navigates\n&lt;Form method=\"post\" action=\"/post/123?action=like\"&gt;\n  &lt;button type=\"submit\"&gt;Like Post&lt;/button&gt;\n&lt;/Form&gt;\n\n// useFetcher \u2014 mutates without leaving the page\nconst fetcher = useFetcher();\nconst isSubmitting = fetcher.state !== 'idle';\n&lt;fetcher.Form method=\"post\" action=\"/api/like\"&gt;\n  &lt;button disabled={isSubmitting}&gt;\n    {isSubmitting ? 'Saving...' : 'Like'}\n  &lt;/button&gt;\n&lt;/fetcher.Form&gt;</pre>\n      <p><strong>Common follow-ups</strong></p>\n      <ul>\n        <li><strong>What do useNavigate, useParams, and useLocation each give you?</strong> <code>useNavigate</code> lets you redirect programmatically after logic runs (e.g. on submit success); <code>useParams</code> returns the dynamic segments matched from the URL (e.g. <code>:userId</code>); <code>useLocation</code> returns the current location object \u2014 <code>pathname</code>, <code>search</code>, <code>hash</code>, <code>state</code>, and <code>key</code> \u2014 useful for analytics or reading query strings.</li>\n      </ul>`\n    },\n    {\n      q:\"How would you improve the frontend performance of a high-traffic site such as Amazon?\",\n      a:`<p>I'd measure before changing anything \u2014 real-user monitoring, browser performance tools, the React Profiler, network traces, and Core Web Vitals \u2014 to find out whether the bottleneck is server response time, JavaScript execution, rendering, network requests, images, or third-party scripts.</p>\n      <p>On the frontend, I'd check bundle size, route-level code splitting, unused JavaScript, image and font loading, long main-thread tasks, unnecessary React renders, and large non-virtualized lists. I'd put versioned static assets behind a CDN and pick a rendering strategy \u2014 static generation, server rendering, streaming, or client rendering \u2014 based on what each page actually needs.</p>\n      <p>Then I'd prioritize the largest measured bottleneck, ship one improvement at a time, and compare real-user metrics before and after each change rather than optimizing several things at once.</p>`\n    },\n    {\n      q:\"What are Core Web Vitals?\",\n      a:`<p>Core Web Vitals are user-experience metrics covering loading, responsiveness, and visual stability:</p>\n      <ul>\n        <li><strong>Largest Contentful Paint (LCP)</strong> \u2014 how quickly the main visible content loads. Good target: 2.5s or less.</li>\n        <li><strong>Interaction to Next Paint (INP)</strong> \u2014 responsiveness across user interactions. Good target: 200ms or less.</li>\n        <li><strong>Cumulative Layout Shift (CLS)</strong> \u2014 unexpected visual movement. Good target: 0.1 or less.</li>\n      </ul>\n      <p>I'd monitor these with both lab tools (Lighthouse, local profiling) and real-user field data, since actual devices and networks often behave differently from local tests.</p>`\n    },\n    {\n      q:\"What is the difference between local and global state management?\",\n      a:`<p>Local state belongs to a component or a small part of the component tree \u2014 an input value, an open menu, a local loading flag \u2014 and I normally manage it with <code>useState</code> or <code>useReducer</code>.</p>\n      <p>Global client state is shared across distant parts of the application, like authenticated-user context or a multi-page workflow. I'd reach for something like Zustand or Redux only when that shared state is complex enough to justify it. Server data should be treated as server state, with a tool like React Query, rather than copied unnecessarily into a global client store.</p>`\n    },\n    {\n      q:\"What is prop drilling?\",\n      a:`<p>Prop drilling is when data or callbacks have to pass through several intermediate components just so a deeply nested component can use them \u2014 the components in between don't need the value themselves, they just keep forwarding it.</p>\n      <p>For a short, clear component chain, passing props is fine. If the value is genuinely cross-cutting or widely shared, component composition, React Context, or a state-management store usually gives a cleaner design.</p>`\n    },\n    {\n      q:\"How do you pass data from a child component to its parent in React?\",\n      a:`<p>React uses one-way data flow, so the parent passes a callback down to the child as a prop. The child calls that callback with the new value or event data, and the parent updates its own state.</p>\n      <p>If the components are unrelated or far apart in the tree, I'd consider lifting the state to the closest common ancestor, or using Context or an appropriate shared store instead of threading a callback through several layers.</p>`\n    },\n    {\n      q:\"Do you have React Native experience?\",\n      a:`<p>I've experimented with React Native on personal projects, but haven't used it extensively in production \u2014 my main experience is React and Next.js web applications.</p>\n      <p>The component model, hooks, TypeScript, and state-management concepts transfer well, but I'd still need to get up to speed on a given project's native navigation, platform APIs, build tooling, and deployment process.</p>`\n    },\n    {\n      q:\"You're handed an existing React codebase and asked to improve its performance. What's your process?\",\n      a:`<p>Measure first, then work top-down from the biggest lever to the smallest \u2014 most of the highest-impact fixes never require touching a single component's render logic.</p>\n      <ol>\n        <li><strong>Audit the bundle.</strong> A bundle analyzer to find what's actually shipping \u2014 a surprisingly common finding is a full date/chart/icon library imported for one small piece of UI. Route-level code splitting and tree-shakeable imports are usually the single biggest win, before any component-level optimization.</li>\n        <li><strong>Cut client-side JavaScript with Server Components</strong> (Next.js App Router) \u2014 anything that doesn't need interactivity (a static layout shell, a data-fetching wrapper) can render on the server and ship zero JS to the client for that piece, rather than being optimized as client-side code at all.</li>\n        <li><strong>Profile with React DevTools</strong> to find components that re-render too often or too expensively \u2014 this comes after the bundle/architecture pass, because no amount of <code>memo</code> fixes a multi-megabyte bundle.</li>\n        <li><strong>Apply memoization deliberately, not reflexively:</strong> <code>React.memo</code> around a component that re-renders often with the same props, <code>useMemo</code> around a genuinely expensive computation (not a simple object literal), <code>useCallback</code> when a function is a dependency of another memoized value or passed to a memoized child. Wrapping everything by default adds comparison overhead without benefit, and is itself a common AI-generated-code smell (see the AI code-quality question in the Behavioral track).</li>\n        <li><strong>Fix list rendering</strong> \u2014 virtualize any list long enough to matter, and check for a stable, unique <code>key</code> (never array index on a reorderable list).</li>\n      </ol>\n      <p><strong>Common follow-up \u2014 how do you know when you're done?</strong> When the metric you started from (LCP/INP in the field, or a specific interaction's profiled duration) has actually moved, measured the same way the regression was measured \u2014 not when the code \"feels\" faster, and not by chasing flame graphs indefinitely once the user-facing metric has already recovered.</p>`\n    },\n    {\n      q:\"How do you pass data between sibling components, like a Search box and a ProductList?\",\n      a:`<p>Since <code>Search</code> and <code>ProductList</code> are siblings, one can't just hand a prop to the other &mdash; the value needs to live somewhere both can reach. There are four standard ways to do that, and picking the right one (not just knowing they exist) is what the question is really testing.</p>\n<div class=\"diagram\"><svg viewBox=\"0 0 560 170\" width=\"560\">\n  <rect x=\"200\" y=\"15\" width=\"160\" height=\"42\" rx=\"8\" fill=\"var(--panel)\" stroke=\"var(--border)\" stroke-width=\"1.5\"/><text x=\"280\" y=\"40\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"11.5\" fill=\"var(--text)\">Parent (App)</text>\n  <rect x=\"40\" y=\"110\" width=\"150\" height=\"42\" rx=\"8\" fill=\"var(--gold-soft)\" stroke=\"var(--gold)\" stroke-width=\"1.5\"/><text x=\"115\" y=\"135\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"11.5\" fill=\"var(--text)\">Search</text>\n  <rect x=\"370\" y=\"110\" width=\"150\" height=\"42\" rx=\"8\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\" stroke-width=\"1.5\"/><text x=\"445\" y=\"135\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"11.5\" fill=\"var(--text)\">ProductList</text>\n  <path d=\"M240 57 L145 110\" stroke=\"var(--muted)\" stroke-width=\"1.5\" marker-end=\"url(#mkSib1)\"/><text x=\"150\" y=\"85\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"9.5\" fill=\"var(--muted)\">value + onChange</text>\n  <path d=\"M320 57 L420 110\" stroke=\"var(--muted)\" stroke-width=\"1.5\" marker-end=\"url(#mkSib1)\"/><text x=\"410\" y=\"85\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"9.5\" fill=\"var(--muted)\">searchValue</text>\n  <defs><marker id=\"mkSib1\" markerWidth=\"8\" markerHeight=\"8\" refX=\"6\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L6,3 L0,6 z\" fill=\"var(--muted)\"/></marker></defs>\n</svg></div>\n<ol>\n<li><strong>Lift state up to the common parent (the default).</strong> App owns <code>searchValue</code>, passes it and a setter down to <code>Search</code>, and passes the value straight to <code>ProductList</code>. No new infrastructure, and it's exactly what \"lift state up\" means in the React docs.</li>\n<li><strong>React Context.</strong> Same idea, but skips prop drilling once the common ancestor is several levels away from one or both components &mdash; reach for it once drilling actually hurts, not preemptively.</li>\n<li><strong>URL / query params.</strong> Both components read/write the same URL (<code>?q=lipstick</code>) via <code>useSearchParams</code>. The search becomes bookmarkable and survives a refresh, which local state never does.</li>\n<li><strong>A global store</strong> (Zustand/Redux). Only justified once <code>searchValue</code> needs to be read or written from unrelated parts of the app, not just these two components.</li>\n</ol>\n<pre><code>function App() {\n  const [searchValue, setSearchValue] = useState(\"\");\n  return (\n    &lt;&gt;\n      &lt;Search value={searchValue} onChange={setSearchValue} /&gt;\n      &lt;ProductList searchValue={searchValue} /&gt;\n    &lt;/&gt;\n  );\n}</code></pre>\n<p><strong>Common follow-ups</strong></p>\n<ul>\n<li><strong>Why not just fetch on every keystroke?</strong> Keep the input's own value updating immediately (an \"onChange\" state), but only trigger the actual search/fetch on an explicit submit (button click or Enter) unless live-as-you-type search is a real requirement &mdash; otherwise debounce it (see the JavaScript track's debounce question).</li>\n<li><strong>How would you implement the fetch itself?</strong> A <code>try/catch/finally</code> around <code>fetch</code>, checking <code>response.ok</code>, setting loading/error/data state &mdash; see the \"Fetch data and render it in a table\" question in the Coding Exercise track for the full pattern, including cancellation with <code>AbortController</code>.</li>\n</ul>`\n    },\n    {\n      q:\"How would you show whichever of two independent API results arrives first, then append the second \u2014 without one failure blocking the other?\",\n      a:`<p>Start both requests concurrently and update state from each one's own completion handler, instead of waiting for an aggregate like <code>Promise.all</code> (which waits for both, and rejects the whole batch if either one fails). Functional state updates let each source append independently without clobbering the other's result, and an <code>AbortController</code> plus an \"is this still the active search\" check protects against a stale, slower request overwriting a newer one.</p>\n<pre><code>function useProgressiveSearch() {\n  const [products, setProducts] = useState([]);\n  const [errors, setErrors] = useState([]);\n  const [pending, setPending] = useState(0);\n  const activeRequest = useRef(null);\n\n  useEffect(() => () => activeRequest.current?.abort(), []);\n\n  function search(query) {\n    activeRequest.current?.abort();\n    const controller = new AbortController();\n    activeRequest.current = controller;\n\n    setProducts([]);\n    setErrors([]);\n    setPending(2);\n\n    const isCurrent = () => activeRequest.current === controller;\n\n    async function loadSource(source) {\n      try {\n        const url = \"/api/products/\" + source + \"?q=\" + encodeURIComponent(query);\n        const res = await fetch(url, { signal: controller.signal });\n        if (!res.ok) throw new Error(\"HTTP \" + res.status);\n        const items = await res.json();\n        if (isCurrent()) setProducts((prev) => [...prev, ...items.map((item) => ({ ...item, source }))]);\n      } catch (err) {\n        if (isCurrent()) setErrors((prev) => [...prev, err.message]);\n      } finally {\n        if (isCurrent()) setPending((count) => count - 1);\n      }\n    }\n\n    loadSource(\"source-a\");\n    loadSource(\"source-b\");\n  }\n\n  return { products, errors, loading: pending > 0, search };\n}</code></pre>\n<p><strong>Common follow-ups</strong></p>\n<ul>\n<li><strong>Why not just use <code>Promise.all</code>?</strong> It waits for the slowest of the two before rendering anything, and a single rejection fails the whole batch &mdash; the opposite of \"show whichever arrives first.\"</li>\n<li><strong>What does <code>Promise.allSettled</code> give you instead?</strong> The final outcome of every promise regardless of failures, but still only after ALL of them settle &mdash; it doesn't give progressive, as-they-arrive rendering either.</li>\n<li><strong>How do you avoid duplicate ids across the two sources?</strong> Tag each item with its source when appending (<code>{ ...item, source }</code>) and key rendered rows by <code>source + \":\" + id</code> rather than assuming ids are globally unique.</li>\n</ul>`\n    },\n    {\n      q:\"How do you handle asynchronous calls and their state in Redux?\",\n      a:`<p>Reducers must stay synchronous and free of side effects &mdash; they can't call <code>fetch</code> themselves. The async work happens in middleware (a thunk), which dispatches a sequence of plain actions marking each stage of the request, and the reducer just responds to those actions like any other.</p>\n<pre><code>// Redux Toolkit's createAsyncThunk generates pending/fulfilled/rejected\n// action types automatically from one async function.\nconst fetchProducts = createAsyncThunk(\"products/fetch\", async (query) => {\n  const res = await fetch(\"/api/products?q=\" + encodeURIComponent(query));\n  if (!res.ok) throw new Error(\"HTTP \" + res.status);\n  return res.json();\n});\n\nconst productsSlice = createSlice({\n  name: \"products\",\n  initialState: { items: [], status: \"idle\", error: null },\n  reducers: {},\n  extraReducers: (builder) => {\n    builder\n      .addCase(fetchProducts.pending, (state) => {\n        state.status = \"loading\";\n        state.error = null;\n      })\n      .addCase(fetchProducts.fulfilled, (state, action) => {\n        state.status = \"succeeded\";\n        state.items = action.payload;\n      })\n      .addCase(fetchProducts.rejected, (state, action) => {\n        state.status = \"failed\";\n        state.error = action.error.message;\n      });\n  },\n});</code></pre>\n<p><strong>Common follow-ups</strong></p>\n<ul>\n<li><strong>Thunk vs. Saga?</strong> A thunk is a plain async function &mdash; perfectly fine for straightforward request/response logic. Saga uses generator-based effects for more complex orchestration (debouncing, cancellation, coordinating multiple sagas) &mdash; more power, more ceremony. Most teams default to thunks and only reach for Saga once that orchestration complexity is real.</li>\n<li><strong>How do you avoid a stale response overwriting a newer one?</strong> Redux Toolkit's <code>createAsyncThunk</code> accepts an <code>AbortSignal</code> via <code>thunkAPI.signal</code> &mdash; cancel the previous request the same way you would with a plain <code>AbortController</code>.</li>\n<li><strong>Isn't this what RTK Query is for?</strong> Yes &mdash; RTK Query generates this exact pending/fulfilled/rejected lifecycle (plus caching, refetching, and cache invalidation) from just an endpoint definition, so most new Redux code doesn't hand-write thunks for data-fetching anymore.</li>\n</ul>`\n    },\n    {\n      q:\"How do pagination, sorting and filters work on a search results page backed by GraphQL?\",\n      a:`<p>The design principle: the <strong>server owns the result set, the facets and the pagination state</strong>; the client sends the current criteria and renders what comes back, so the grid and the available facets update together.</p>\n<pre>query Search($q: String!, $filters: [FilterInput!], $sort: SortOption, $page: Int, $pageSize: Int) {\n  search(query: $q, filters: $filters, sort: $sort, page: $page, pageSize: $pageSize) {\n    items { id ...ProductCard }\n    facets { name values { value label count selected } }\n    pageInfo { page pageSize totalPages totalItems }\n  }\n}</pre>\n<ul>\n<li><strong>Reset to page 1</strong> when the query, filters or sort change, otherwise a user on page 5 filters down to two pages and sees an empty result. Use the returned <code>pageInfo</code> (and clamp) rather than guessing the page count.</li>\n<li><strong>Facet counts come from the server</strong> and reflect the other active filters.</li>\n<li><strong>Offset vs cursor:</strong> page numbers suit shopping search where users jump to a page and want a total; cursor pagination is stable when data changes and cheaper for deep or infinite lists but cannot jump to page N.</li>\n<li><strong>Race conditions:</strong> a slow older response must not overwrite a newer one. Cancel with <code>AbortController</code> or ignore superseded requests; client libraries keyed by variables handle this.</li>\n<li><strong>Keep previous results on screen while loading</strong> (<code>keepPreviousData</code> or <code>useTransition</code>) to avoid the grid flashing empty.</li>\n<li><strong>Sort on the server.</strong> Sorting only the current page client-side is wrong. Debounce text input, apply checkboxes immediately.</li>\n<li><strong>Accessibility:</strong> announce \"24 results\" in an <code>aria-live=\"polite\"</code> region, keep focus stable after re-render, use fieldset/legend or disclosure buttons for facet groups.</li>\n<li><strong>SEO:</strong> paginated URLs should be crawlable with canonical tags; decide which facet combinations are indexable.</li>\n</ul>`\n    },\n    {\n      q:\"Why use Radix UI (headless primitives) instead of building components or using a styled library?\",\n      a:`<p>Radix gives accessible, largely unstyled behavior for interactive components. Shared components wrap its primitives and apply market- or page-specific styling (including Tailwind classes), without rebuilding the underlying interactions each time.</p>\n<p><strong>What a headless primitive gives you:</strong></p>\n<ul>\n<li><strong>Keyboard interaction</strong> following WAI-ARIA authoring practices (arrow keys, <code>Escape</code>, tab order).</li>\n<li><strong>Focus management:</strong> a Dialog traps focus, moves it in on open, restores it to the trigger on close. This is one of the hardest things to hand-roll correctly.</li>\n<li><strong>ARIA roles and attributes</strong> wired correctly, positioning and collision handling, and Portal layering.</li>\n<li><strong>State exposed as data attributes</strong> (<code>data-state=\"open\"</code>) for CSS or Tailwind styling, and controlled or uncontrolled use.</li>\n</ul>\n<table class=\"cmp\">\n<tr><th>Option</th><th>Trade-off</th></tr>\n<tr><td>Radix Primitives</td><td>Unstyled, total design control, more styling work</td></tr>\n<tr><td>React Aria</td><td>Very thorough accessibility and i18n, different hooks-style API</td></tr>\n<tr><td>Headless UI</td><td>Unstyled, smaller component set</td></tr>\n<tr><td>MUI / Chakra</td><td>Opinionated and quick to start, harder to match a strict brand</td></tr>\n<tr><td>shadcn/ui</td><td>Copy-in components built on Radix and Tailwind; you own the code</td></tr>\n<tr><td>Native HTML (<code>&lt;dialog&gt;</code>, popover)</td><td>Improving, but limited for complex widgets</td></tr>\n</table>\n<p><strong>Why it suits a multi-market retailer:</strong> behavior and accessibility are identical everywhere while each market restyles. Costs to admit: extra dependencies, learning the composition API, keeping wrappers consistent across versions. Be ready to name two Radix components you used and what you customized.</p>`\n    },\n    {\n      q:\"How do you install and customize a Radix component?\",\n      a:`<p>Install the package for the primitive (for example <code>@radix-ui/react-dialog</code>, or the unified <code>radix-ui</code> package), import its parts into a component in your codebase, then wrap and style them to match your design system. Never edit the installed package in <code>node_modules</code>; changes vanish on the next install.</p>\n<pre>'use client';   // Radix uses state and effects, so this is a client component\nimport * as Dialog from '@radix-ui/react-dialog';\n\nexport function Modal({ trigger, title, description, children }) {\n  return (\n    &lt;Dialog.Root&gt;\n      &lt;Dialog.Trigger asChild&gt;{trigger}&lt;/Dialog.Trigger&gt;\n      &lt;Dialog.Portal&gt;\n        &lt;Dialog.Overlay className=\"fixed inset-0 bg-black/50\" /&gt;\n        &lt;Dialog.Content className=\"fixed left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 rounded-lg bg-white p-6\"&gt;\n          &lt;Dialog.Title className=\"text-lg font-semibold\"&gt;{title}&lt;/Dialog.Title&gt;\n          &lt;Dialog.Description className=\"text-sm\"&gt;{description}&lt;/Dialog.Description&gt;\n          {children}\n          &lt;Dialog.Close asChild&gt;&lt;button aria-label=\"Close\"&gt;x&lt;/button&gt;&lt;/Dialog.Close&gt;\n        &lt;/Dialog.Content&gt;\n      &lt;/Dialog.Portal&gt;\n    &lt;/Dialog.Root&gt;\n  );\n}</pre>\n<ul>\n<li><strong><code>asChild</code></strong> merges Radix behavior and props onto your own element (via Slot) instead of rendering a new one, so you keep your own Button and avoid nested buttons.</li>\n<li><strong>Style by state</strong> with <code>data-state</code> attributes (<code>data-[state=open]:...</code> in Tailwind).</li>\n<li><strong>Title and Description matter for accessibility;</strong> Radix warns when missing.</li>\n<li><strong>Controlled use</strong> with <code>open</code> and <code>onOpenChange</code> when the parent must drive it. Forward refs and pass props through in wrappers.</li>\n<li><strong>Layering:</strong> primitives at the bottom, your shared design-system components above, feature code importing only the shared layer, so a Radix or branding change touches one place.</li>\n<li>In the App Router the wrapper needs <code>\"use client\"</code>, but server-rendered children can still be passed in.</li>\n</ul>`\n    },\n    {\n      q:\"How do you type forwardRef with generics?\",\n      a:`<p><strong>In React 19, <code>ref</code> is a normal prop for function components</strong>, so you type it like any other prop and generics just work; <code>forwardRef</code> is no longer needed and is expected to be deprecated. On React 18, <code>forwardRef</code> breaks generic inference, and the workaround is to cast or use a wrapper pattern:</p>\n<pre>import { forwardRef, useRef } from 'react';\n\n// Pattern: assertion to recover generics\ntype ListProps&lt;T&gt; = { items: T[]; renderItem: (item: T) =&gt; React.ReactNode };\n\nfunction ListInner&lt;T&gt;(props: ListProps&lt;T&gt; &amp; { ref?: React.Ref&lt;HTMLUListElement&gt; }) {\n  return (\n    &lt;ul ref={props.ref}&gt;\n      {props.items.map((item, i) =&gt; &lt;li key={i}&gt;{props.renderItem(item)}&lt;/li&gt;)}\n    &lt;/ul&gt;\n  );\n}\n\n// React 19+ supports ref as a prop directly \u2014 no forwardRef needed!\n// For React 18, use the module augmentation or casting pattern.</pre>\n<p><strong>Interview tip:</strong> This catches many candidates off guard. The key insight is that forwardRef's type signature doesn't preserve generics, so you need a pattern to work around it.</p>`\n    },\n    {\n      q:\"What is useRef used for beyond DOM references?\",\n      a:`<p>useRef stores any mutable value that persists across renders WITHOUT triggering re-renders. It's essentially an instance variable for function components.</p>\n<pre>// 1. DOM reference (most common)\nconst inputRef = useRef&lt;HTMLInputElement&gt;(null);\ninputRef.current?.focus();\n\n// 2. Storing previous value\nfunction usePrevious&lt;T&gt;(value: T): T | undefined {\n  const ref = useRef&lt;T&gt;();\n  useEffect(() =&gt; { ref.current = value; });\n  return ref.current;\n}\n\n// 3. Mutable flag that doesn't cause re-render\nconst isMounted = useRef(false);\nuseEffect(() =&gt; {\n  isMounted.current = true;\n  return () =&gt; { isMounted.current = false; };\n}, []);\n\n// 4. Storing interval/timeout IDs\nconst intervalRef = useRef&lt;ReturnType&lt;typeof setInterval&gt;&gt;();\nuseEffect(() =&gt; {\n  intervalRef.current = setInterval(() =&gt; tick(), 1000);\n  return () =&gt; clearInterval(intervalRef.current);\n}, []);</pre>\n<p><strong>Interview tip:</strong> Key insight: useRef.current changes don't cause re-renders. This makes it perfect for tracking values you need to read in callbacks/effects without adding dependencies.</p>`\n    },\n    {\n      q:\"Show React Query with optimistic updates + rollback.\",\n      a:`<p>Optimistic updates make the UI feel instant by updating the cache before the server responds, with rollback on failure.</p>\n<pre>const qc = useQueryClient();\n\nconst { mutate } = useMutation({\n  mutationFn: updateTodo,\n  onMutate: async (newTodo) =&gt; {\n    // 1. Cancel in-flight refetches so they don't overwrite our optimistic update\n    await qc.cancelQueries({ queryKey: ['todos', newTodo.id] });\n\n    // 2. Snapshot the previous value for rollback\n    const previousTodo = qc.getQueryData(['todos', newTodo.id]);\n\n    // 3. Optimistically update the cache\n    qc.setQueryData(['todos', newTodo.id], (old: Todo) =&gt; ({\n      ...old,\n      ...newTodo,\n    }));\n\n    return { previousTodo }; // context for onError\n  },\n  onError: (_err, newTodo, context) =&gt; {\n    // 4. Rollback on error\n    qc.setQueryData(['todos', newTodo.id], context?.previousTodo);\n    toast.error('Update failed, reverted changes');\n  },\n  onSettled: (_data, _err, newTodo) =&gt; {\n    // 5. Always refetch to ensure server/client sync\n    qc.invalidateQueries({ queryKey: ['todos', newTodo.id] });\n  },\n});</pre>\n<p><strong>Interview tip:</strong> This is a very common senior interview question. Remember the 5 steps: cancel, snapshot, optimistic update, rollback on error, refetch on settle.</p>`\n    },\n    {\n      q:\"Why is Context API bad for frequently changing state?\",\n      a:`<p>Context re-renders ALL consumers whenever the provider value changes, even if a consumer only uses a small part of the state.</p>\n<pre>// \u274c BAD: All consumers re-render when ANY part of this changes\nconst AppContext = createContext({ user: null, theme: 'dark', count: 0 });\n\n// \u2705 SOLUTION 1: Split into separate contexts\nconst UserContext = createContext(null);\nconst ThemeContext = createContext('dark');\n\n// \u2705 SOLUTION 2: Use useMemo on the provider value\nfunction AppProvider({ children }) {\n  const [user, setUser] = useState(null);\n  const value = useMemo(() =&gt; ({ user, setUser }), [user]);\n  return &lt;UserContext.Provider value={value}&gt;{children}&lt;/UserContext.Provider&gt;;\n}\n\n// \u2705 SOLUTION 3: Use Zustand instead (no provider, no re-render issue)\nconst useStore = create((set) =&gt; ({\n  count: 0,\n  increment: () =&gt; set((s) =&gt; ({ count: s.count + 1 })),\n}));</pre>\n<p><strong>Interview tip:</strong> This is a trap question. Many devs use Context for everything. Show you know its re-rendering limitation and when to reach for Zustand or jotai instead.</p>`\n    },\n    {\n      q:\"Explain React's reconciliation algorithm. What role does the 'key' prop play?\",\n      a:`<p>React uses a heuristic O(n) diffing algorithm. It compares trees level by level. The 'key' prop is the identity hint that tells React which elements are the 'same' between renders.</p>\n<ul>\n<li><strong>Different element types:</strong> React tears down the old tree entirely and builds a new one. <code>&lt;div&gt;</code> \u2192 <code>&lt;span&gt;</code> = full unmount + remount.</li>\n<li><strong>Same element type:</strong> React keeps the DOM node, only updates changed attributes/props.</li>\n<li><strong>Lists without keys:</strong> React re-renders ALL items when one changes. With unique keys, React knows exactly which item changed, was added, or removed.</li>\n<li><strong>Why index-as-key is dangerous:</strong> If you delete item at index 0, all subsequent items shift. React thinks item[0] changed content, item[1] changed content... causing bugs with stateful components (inputs, animations).</li>\n</ul>\n<p><strong>Interview tip:</strong> This is fundamental. Explain: (1) same type = update, different type = unmount/remount, (2) keys identify list items across renders. Using index as key is bad when list order changes.</p>`\n    },\n    {\n      q:\"Robust REST fetch with abort, retries, and Zod validation.\",\n      a:`<p>Production-grade API calls need request cancellation, retry logic for transient failures, and runtime type validation.</p>\n<pre>import { z } from 'zod';\n\nconst UserSchema = z.object({\n  id: z.number(),\n  name: z.string(),\n  email: z.string().email(),\n  role: z.enum(['admin', 'user', 'viewer']),\n});\n\ntype User = z.infer&lt;typeof UserSchema&gt;; // auto-generate TypeScript type\n\nasync function getUser(id: number, signal?: AbortSignal): Promise&lt;User&gt; {\n  for (let attempt = 0; attempt &lt; 3; attempt++) {\n    const res = await fetch(\\`/api/users/\\${id}\\`, { signal });\n\n    if (res.ok) {\n      const data = await res.json();\n      return UserSchema.parse(data); // runtime validation!\n    }\n\n    if (res.status &gt;= 400 &amp;&amp; res.status &lt; 500) {\n      throw new Error(\\`Client error: \\${res.status}\\`); // don't retry 4xx\n    }\n\n    // Exponential backoff for 5xx\n    await new Promise(r =&gt; setTimeout(r, 1000 * Math.pow(2, attempt)));\n  }\n  throw new Error('Retries exhausted');\n}\n\n// Usage with AbortController (cleanup on unmount)\nuseEffect(() =&gt; {\n  const controller = new AbortController();\n  getUser(1, controller.signal).then(setUser).catch(console.error);\n  return () =&gt; controller.abort(); // cancel if component unmounts\n}, []);</pre>\n<p><strong>Interview tip:</strong> Three things interviewers look for: (1) AbortController for cleanup, (2) only retry server errors, (3) runtime validation with Zod to bridge the TS/runtime gap.</p>`\n    },\n    {\n      q:\"How does React.lazy work and how do you handle loading/error states?\",\n      a:`<p>React.lazy enables code-splitting by dynamically importing components. They must be wrapped in Suspense.</p>\n<pre>// Basic code splitting\nconst AdminDashboard = React.lazy(() =&gt; import('./AdminDashboard'));\nconst UserSettings = React.lazy(() =&gt; import('./UserSettings'));\n\nfunction App() {\n  return (\n    &lt;ErrorBoundary fallback={&lt;p&gt;Failed to load page&lt;/p&gt;}&gt;\n      &lt;Suspense fallback={&lt;PageSkeleton /&gt;}&gt;\n        &lt;Routes&gt;\n          &lt;Route path=\"/admin\" element={&lt;AdminDashboard /&gt;} /&gt;\n          &lt;Route path=\"/settings\" element={&lt;UserSettings /&gt;} /&gt;\n        &lt;/Routes&gt;\n      &lt;/Suspense&gt;\n    &lt;/ErrorBoundary&gt;\n  );\n}\n\n// Prefetch on hover for instant page transitions\nconst prefetchAdmin = () =&gt; import('./AdminDashboard');\n&lt;Link to=\"/admin\" onMouseEnter={prefetchAdmin}&gt;Admin&lt;/Link&gt;\n\n// Named exports require a wrapper\nconst MyComponent = React.lazy(() =&gt;\n  import('./components').then(mod =&gt; ({ default: mod.MyComponent }))\n);</pre>`\n    },\n    {\n      q:\"What is the Render Props pattern and is it still relevant?\",\n      a:`<p>Render props pass a function as a prop to share logic. Largely replaced by custom hooks, but still useful for some UI abstraction patterns.</p>\n<pre>// Render Props pattern\nfunction MouseTracker({ render }: {\n  render: (pos: { x: number; y: number }) =&gt; React.ReactNode\n}) {\n  const [pos, setPos] = useState({ x: 0, y: 0 });\n  useEffect(() =&gt; {\n    const handler = (e: MouseEvent) =&gt; setPos({ x: e.clientX, y: e.clientY });\n    window.addEventListener('mousemove', handler);\n    return () =&gt; window.removeEventListener('mousemove', handler);\n  }, []);\n  return &lt;&gt;{render(pos)}&lt;/&gt;;\n}\n\n// Usage\n&lt;MouseTracker render={({ x, y }) =&gt; &lt;p&gt;Mouse: {x}, {y}&lt;/p&gt;} /&gt;\n\n// \u2705 Modern equivalent: custom hook (preferred)\nfunction useMousePosition() {\n  const [pos, setPos] = useState({ x: 0, y: 0 });\n  useEffect(() =&gt; { /* same listener logic */ }, []);\n  return pos;\n}\nconst { x, y } = useMousePosition();</pre>\n<p><strong>Interview tip:</strong> Say: 'Render props are mostly replaced by hooks, but the pattern is still useful in libraries like Downshift and when you need component-level code sharing that hooks can't provide (e.g., rendering the output in a specific DOM location).'</p>`\n    },\n    {\n      q:\"What security headers should every React app have?\",\n      a:`<p>Security headers are your first line of defense. Configure them in your web server or Next.js middleware.</p>\n<ul>\n<li><strong>Content-Security-Policy (CSP):</strong> Whitelist allowed script/style sources. Prevents inline script injection.</li>\n<li><strong>X-Content-Type-Options: nosniff:</strong> Prevents MIME type sniffing attacks.</li>\n<li><strong>X-Frame-Options: DENY:</strong> Prevents clickjacking by blocking iframe embedding.</li>\n<li><strong>Strict-Transport-Security (HSTS):</strong> Forces HTTPS for all future requests.</li>\n<li><strong>Referrer-Policy: strict-origin-when-cross-origin:</strong> Controls what referrer info is sent.</li>\n<li><strong>Permissions-Policy:</strong> Disable unused browser features (camera, microphone, geolocation).</li>\n</ul>`\n    },\n    {\n      q:\"What does the dependency array do in useEffect, and why does the cleanup function matter?\",\n      a:`<p><strong>The dependency array</strong> tells React when to re-run the effect: after the first render, and after any render where a listed value changed (compared with <code>Object.is</code>). No array means every render; an empty array means once on mount.</p>\n<div class=\"diagram\" data-sd=\"react-53\"><svg viewBox=\"0 0 800 190\" width=\"800\" xmlns=\"http://www.w3.org/2000/svg\" role=\"img\" aria-label=\"useEffect timeline: mount, dependency change, unmount\">\n<defs><marker id=\"fdEf1\" markerWidth=\"8\" markerHeight=\"8\" refX=\"6\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L6,3 L0,6 z\" fill=\"var(--muted)\"/></marker></defs>\n<path d=\"M10 60 L786 60\" fill=\"none\" stroke=\"var(--muted)\" stroke-width=\"1.5\" marker-end=\"url(#fdEf1)\"/>\n<circle cx=\"60\" cy=\"60\" r=\"6\" fill=\"var(--accent)\"/><text x=\"40\" y=\"40\"  font-family=\"var(--mono)\" font-size=\"11\" fill=\"var(--accent)\" font-weight=\"600\">mount</text><text x=\"40\" y=\"86\"  font-family=\"var(--mono)\" font-size=\"10\" fill=\"var(--text)\">render \u2192 commit \u2192 effect(A)</text><circle cx=\"320\" cy=\"60\" r=\"6\" fill=\"var(--accent)\"/><text x=\"300\" y=\"40\"  font-family=\"var(--mono)\" font-size=\"11\" fill=\"var(--accent)\" font-weight=\"600\">deps change</text><text x=\"300\" y=\"86\"  font-family=\"var(--mono)\" font-size=\"10\" fill=\"var(--text)\">render \u2192 cleanup(A) \u2192 effect(B)</text><circle cx=\"610\" cy=\"60\" r=\"6\" fill=\"var(--accent)\"/><text x=\"590\" y=\"40\"  font-family=\"var(--mono)\" font-size=\"11\" fill=\"var(--accent)\" font-weight=\"600\">unmount</text><text x=\"590\" y=\"86\"  font-family=\"var(--mono)\" font-size=\"10\" fill=\"var(--text)\">cleanup(B)</text>\n<text x=\"10\" y=\"124\"  font-family=\"var(--sans)\" font-size=\"10.5\" fill=\"var(--text)\">No array: after every render.  [] : once after mount (cleanup on unmount).  [a, b] : when a or b changes (Object.is).</text>\n<text x=\"10\" y=\"144\"  font-family=\"var(--sans)\" font-size=\"10.5\" fill=\"var(--muted)\">Cleanup always runs before the next effect and on unmount: abort fetches, clear timers, remove listeners, unsubscribe.</text>\n<text x=\"10\" y=\"164\"  font-family=\"var(--sans)\" font-size=\"10.5\" fill=\"var(--muted)\">Strict Mode (dev) runs mount \u2192 cleanup \u2192 mount once, to prove your cleanup works.</text>\n</svg></div>\n\n<ul>\n<li><strong>List everything the effect reads</strong> from props, state or the component scope. A missing dependency gives a <em>stale closure</em>: the effect keeps using the value from an earlier render. Use the <code>react-hooks/exhaustive-deps</code> lint rule.</li>\n<li><strong>Objects, arrays and functions created during render are new every time,</strong> so they re-run the effect. Move them inside the effect, memoise them, or depend on primitives.</li>\n<li><strong>Cleanup runs before the effect re-runs and on unmount.</strong> Use it to unsubscribe, clear timers, abort fetches, and remove listeners; otherwise you leak memory and get updates after unmount.</li>\n<li><strong>Strict Mode in development</strong> mounts, unmounts and re-mounts once to expose missing cleanups. An effect that breaks when run twice has a bug.</li>\n</ul>\n<pre>useEffect(() =&gt; {\n  const controller = new AbortController();\n  fetch('/api/search?q=' + query, { signal: controller.signal })\n    .then((r) =&gt; r.json())\n    .then(setResults)\n    .catch((e) =&gt; { if (e.name !== 'AbortError') setError(e); });\n  return () =&gt; controller.abort();      // cleanup: cancels the stale request when query changes or on unmount\n}, [query]);</pre>\n<p><strong>Interview tip:</strong> not every piece of logic belongs in an effect. Derive values during render, and handle user actions in event handlers; effects are for synchronising with something outside React.</p>`\n    },\n    {\n      q:\"When should you NOT use useMemo, useCallback or React.memo, and what goes wrong with bad dependencies?\",\n      a:`<p>Memoization has a cost (comparison work, memory, and complexity), so it only pays off when it prevents work that is actually expensive or stops a memoised child from re-rendering.</p>\n<p><strong>Skip it when:</strong></p>\n<ul>\n<li><strong>The component or calculation is cheap.</strong> Comparing props can cost more than re-rendering a simple component.</li>\n<li><strong>Props change on every render anyway</strong> (inline objects, arrays or functions passed down), so the memo check always fails.</li>\n<li><strong>The parent rarely re-renders,</strong> or the component is a singleton near the root.</li>\n<li><strong>You have not measured.</strong> Profile with the React DevTools Profiler first.</li>\n</ul>\n<p><strong>Use it when</strong> a calculation is genuinely heavy (large filter or sort), when a value or callback is passed to a <code>React.memo</code> child (so its reference must stay stable), or when a value is a dependency of another hook.</p>\n<ul>\n<li><strong>Wrong or missing dependencies</strong> in <code>useMemo</code>/<code>useCallback</code> return stale values, because the cached result is reused although an input changed. Extra dependencies just cause recomputation.</li>\n<li><strong><code>React.memo</code> cannot prevent every re-render.</strong> The component still re-renders when its own state changes, when a context it consumes changes, or when any prop reference changes.</li>\n<li><strong><code>useMemo</code> is a performance hint, not a semantic guarantee.</strong> Correctness must not depend on it never recomputing.</li>\n<li><strong>React Compiler</strong> can memoise automatically, reducing the need for manual hooks; still measure.</li>\n</ul>`\n    },\n    {\n      q:\"Why use useDeferredValue instead of debouncing a search input?\",\n      a:`<p>They solve different problems. <strong>Debounce</strong> delays <em>when work starts</em> by a fixed time, so every keystroke waits (for example 300 ms) even on a fast device. <strong><code>useDeferredValue</code></strong> lets the input update immediately at urgent priority, while the expensive render that depends on the value happens at lower priority and can be interrupted.</p>\n<ul>\n<li><strong>No artificial delay:</strong> on a fast machine the deferred render finishes quickly; on a slow one it lags behind without blocking typing. A debounce timer is the same fixed wait for everyone.</li>\n<li><strong>Interruptible:</strong> if the user types again mid-render, React abandons the stale render and starts over. A debounced handler that already started heavy work cannot be cancelled.</li>\n<li><strong>It does not reduce network requests.</strong> It defers <em>rendering</em>. If each keystroke triggers a fetch, you still want a debounce or an aborted request, and can combine both.</li>\n<li><strong>Use it</strong> for a child that receives a query prop, a large filtered list you own, or expensive tab content. For third-party state you cannot wrap, or things that must not run at all until the user pauses (API calls), debounce is the right tool.</li>\n</ul>\n<pre>function Search({ items }) {\n  const [query, setQuery] = useState('');\n  const deferred = useDeferredValue(query);              // lags behind during heavy renders\n  const results = useMemo(() =&gt; filter(items, deferred), [items, deferred]);\n  const stale = query !== deferred;\n  return (\n    &lt;&gt;\n      &lt;input value={query} onChange={(e) =&gt; setQuery(e.target.value)} /&gt;\n      &lt;div style={{ opacity: stale ? 0.6 : 1 }}&gt;&lt;List results={results} /&gt;&lt;/div&gt;\n    &lt;/&gt;\n  );\n}</pre>`\n    },\n    {\n      q:\"What are the main React Router hooks (useNavigate, useParams, useLocation, useSearchParams) and what does each give you?\",\n      a:`<ul>\n<li><strong><code>useNavigate()</code></strong> returns a function for programmatic navigation: <code>navigate('/users/42')</code>, <code>navigate(-1)</code> to go back, <code>navigate(path, { replace: true })</code> to replace the history entry (for example after login so Back does not return to the form). Prefer <code>&lt;Link&gt;</code> for ordinary links, since it is accessible and supports prefetching and open-in-new-tab.</li>\n<li><strong><code>useParams()</code></strong> reads dynamic segments from the matched route: <code>/users/:id</code> gives <code>{ id: '42' }</code>. Values are always strings, so convert and validate.</li>\n<li><strong><code>useLocation()</code></strong> gives the current <code>pathname</code>, <code>search</code>, <code>hash</code> and <code>state</code>. Use <code>state</code> to pass non-URL data across a navigation (such as the page to return to after login).</li>\n<li><strong><code>useSearchParams()</code></strong> reads and updates the query string, which is the right home for shareable filter and pagination state.</li>\n<li><strong><code>useMatches()</code>, <code>useRouteLoaderData()</code>, <code>useLoaderData()</code></strong> expose data from route loaders in the data router APIs (v6.4+ and v7).</li>\n</ul>\n<pre>&lt;Routes&gt;\n  &lt;Route path=\"/users/:id\" element={&lt;UserPage /&gt;} /&gt;\n&lt;/Routes&gt;\n\nfunction UserPage() {\n  const { id } = useParams();\n  const navigate = useNavigate();\n  const { state } = useLocation();\n  const [params, setParams] = useSearchParams();\n  // navigate(state?.from ?? '/', { replace: true });\n}</pre>\n<p><strong>Interview tip:</strong> mention that data routers (loaders and actions) move fetching out of components into the route definition, which avoids fetch waterfalls, and that hooks like <code>useBlocker</code> and <code>useFetcher</code> cover unsaved-changes prompts and non-navigating mutations (see the related questions).</p>`\n    }\n  ]\n},\n{\n  id:\"testing\", title:\"Testing (RTL, Jest, Supertest)\",\n  questions:[\n    {\n      q:\"How do you write an integration test for an API route with Supertest?\",\n      a:`<p>Integration tests verify that routing, middleware, validation, and the database all work together correctly \u2014 closer to what a real client experiences than a unit test of a single function.</p>\n<pre><code>import request from 'supertest';\nimport { app } from '../app';\nimport { db } from '../db';\n\ndescribe('POST /api/users', () => {\n  beforeEach(async () => { await db.query('DELETE FROM users'); }); // clean state\n  afterAll(async () => { await db.end(); });\n\n  it('creates a user with valid data', async () => {\n    const res = await request(app)\n      .post('/api/users')\n      .send({ name: 'Dana', email: 'dana@test.com' })\n      .expect(201)\n      .expect('Content-Type', /json/);\n\n    expect(res.body).toMatchObject({ id: expect.any(Number), name: 'Dana', email: 'dana@test.com' });\n\n    const dbUser = await db.query('SELECT * FROM users WHERE email = $1', ['dana@test.com']);\n    expect(dbUser.rows).toHaveLength(1); // actually landed in the database\n  });\n\n  it('returns 400 for invalid email', async () => {\n    const res = await request(app).post('/api/users').send({ name: 'Dana', email: 'not-an-email' }).expect(400);\n    expect(res.body.code).toBe('VALIDATION_ERROR');\n  });\n\n  it('returns 409 for a duplicate email', async () => {\n    await request(app).post('/api/users').send({ name: 'Dana', email: 'dana@test.com' });\n    await request(app).post('/api/users').send({ name: 'Dana2', email: 'dana@test.com' }).expect(409);\n  });\n});</code></pre>\n<p>Test the API contract, not internal implementation details. Use a real (test) database rather than mocking the DB layer \u2014 an integration test that mocks the database isn't really testing integration.</p>`\n    },\n    {\n      q:\"Mock vs Stub vs Spy \u2014 what's the difference?\",\n      a:`<p>All three are \"test doubles,\" but they answer different questions about the code under test.</p>\n<ul>\n<li><strong>Stub:</strong> replaces a function with a fake that returns hardcoded values. You don't care how it was called \u2014 you just need controlled, predictable output. <code>jest.fn().mockResolvedValue(data)</code></li>\n<li><strong>Spy:</strong> wraps the real function, lets it actually execute, but records how it was called so you can assert on that afterward while keeping real behavior. <code>jest.spyOn(obj, 'method')</code></li>\n<li><strong>Mock:</strong> pre-programs expectations up front; the test fails if the function isn't called exactly as specified. Stricter than a spy. <code>jest.fn().mockImplementation(...)</code></li>\n</ul>\n<pre><code>// Stub an external service\njest.mock('../services/email');\nsendEmail.mockResolvedValue({ success: true });\n\n// Spy on a real method, then restore it\nconst spy = jest.spyOn(logger, 'error');\nawait handleRequest(badRequest);\nexpect(spy).toHaveBeenCalledWith('Validation failed', expect.any(Object));\nspy.mockRestore();\n\n// Mock with sequenced return values\nconst mockDb = jest.fn()\n  .mockResolvedValueOnce({ id: 1 })\n  .mockResolvedValueOnce({ id: 2 })\n  .mockRejectedValueOnce(new Error('DB down'));</code></pre>\n<p>Rule of thumb: stub when you only care about output, spy when you want real behavior plus verification, mock when the interaction itself (that a call happened, with what args, how many times) is the thing under test.</p>`\n    },\n    {\n      q:\"How do you test a component that fetches data asynchronously with React Testing Library?\",\n      a:`<p>Use <code>findBy*</code> queries, which retry until an element appears (up to a timeout), for anything that only exists after an async update. Mock the network at the network layer with MSW (Mock Service Worker) rather than mocking <code>fetch</code> directly &mdash; MSW intercepts real requests, so the component's actual fetch code runs unmodified.</p>\n<pre>const server = setupServer(\n  http.get(\"/api/user/1\", () =&gt; HttpResponse.json({ name: \"Dana\", role: \"admin\" }))\n);\nbeforeAll(() =&gt; server.listen());\nafterEach(() =&gt; server.resetHandlers());\nafterAll(() =&gt; server.close());\n\ntest(\"shows user data after loading\", async () =&gt; {\n  render(&lt;UserProfile userId=\"1\" /&gt;);\n  expect(screen.getByRole(\"status\")).toBeInTheDocument(); // loading state first\n\n  expect(await screen.findByText(/dana/i)).toBeInTheDocument(); // waits for it\n  expect(screen.queryByRole(\"status\")).not.toBeInTheDocument(); // loading is gone\n});\n\ntest(\"shows an error on failure\", async () =&gt; {\n  server.use(http.get(\"/api/user/1\", () =&gt; HttpResponse.error()));\n  render(&lt;UserProfile userId=\"1\" /&gt;);\n  expect(await screen.findByText(/something went wrong/i)).toBeInTheDocument();\n});</pre>\n<p><strong>Common follow-ups</strong></p>\n<ul><li><strong>Why not just mock fetch with jest.mock?</strong> It couples the test to implementation details (exactly how the component calls fetch) instead of behavior, and doesn't catch request-shape bugs (wrong URL, missing header) the way a real intercepted request does.</li></ul>`\n    },\n    {\n      q:\"What is the 'Testing Trophy,' and how does it change what you test?\",\n      a:`<p>Kent C. Dodds's Testing Trophy reprioritizes the classic testing pyramid: instead of \"mostly unit tests,\" it weights integration tests as the biggest layer, because they give the best confidence-to-cost ratio for a typical UI app.</p>\n<div class=\"diagram\" data-sd=\"testing-3\"><svg viewBox=\"0 0 800 240\" width=\"800\" xmlns=\"http://www.w3.org/2000/svg\" role=\"img\" aria-label=\"Testing trophy: static, unit, integration (largest), end-to-end\">\n<polygon points=\"330,20 470,20 450,58 350,58\" fill=\"var(--gold-soft)\" stroke=\"var(--gold)\"/><text x=\"400\" y=\"44\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"11\" fill=\"var(--text)\" font-weight=\"600\">E2E</text>\n<polygon points=\"300,62 500,62 520,142 280,142\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/><text x=\"400\" y=\"106\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"12\" fill=\"var(--text)\" font-weight=\"600\">Integration</text>\n<polygon points=\"330,146 470,146 460,178 340,178\" fill=\"var(--panel)\" stroke=\"var(--border)\"/><text x=\"400\" y=\"166\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"11\" fill=\"var(--text)\" font-weight=\"600\">Unit</text>\n<rect x=\"290\" y=\"184\" width=\"220\" height=\"30\" rx=\"6\" fill=\"var(--panel-2)\" stroke=\"var(--border)\"/><text x=\"400\" y=\"204\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"10.5\" fill=\"var(--text)\">Static: TypeScript, ESLint</text>\n<text x=\"540\" y=\"42\"  font-family=\"var(--mono)\" font-size=\"10\" fill=\"var(--muted)\">Playwright: a few critical user journeys</text>\n<text x=\"540\" y=\"100\"  font-family=\"var(--mono)\" font-size=\"10\" fill=\"var(--muted)\">RTL + MSW, Supertest + real DB container:</text><text x=\"540\" y=\"114\"  font-family=\"var(--mono)\" font-size=\"10\" fill=\"var(--accent)\">most confidence per unit of effort</text>\n<text x=\"540\" y=\"166\"  font-family=\"var(--mono)\" font-size=\"10\" fill=\"var(--muted)\">pure logic, reducers, utils</text>\n<text x=\"540\" y=\"202\"  font-family=\"var(--mono)\" font-size=\"10\" fill=\"var(--muted)\">catches typos and type errors for free</text>\n<text x=\"10\" y=\"100\"  font-family=\"var(--sans)\" font-size=\"10.5\" fill=\"var(--text)\">The more your tests</text><text x=\"10\" y=\"116\"  font-family=\"var(--sans)\" font-size=\"10.5\" fill=\"var(--text)\">resemble real use,</text><text x=\"10\" y=\"132\"  font-family=\"var(--sans)\" font-size=\"10.5\" fill=\"var(--text)\">the more confidence</text><text x=\"10\" y=\"148\"  font-family=\"var(--sans)\" font-size=\"10.5\" fill=\"var(--text)\">they give you.</text>\n</svg></div>\n\n<ul>\n<li><strong>Static analysis (base):</strong> TypeScript + ESLint catch a whole class of bugs before a test even runs &mdash; zero cost, ongoing payoff.</li>\n<li><strong>Unit tests:</strong> pure functions, reducers, utilities. Fast and isolated, but low confidence about how real user flows behave.</li>\n<li><strong>Integration tests (the largest slice):</strong> components rendered with their children, hooks, and mocked network calls &mdash; this is what most React Testing Library tests are. Best confidence-to-cost ratio.</li>\n<li><strong>E2E (top, smallest slice):</strong> Playwright/Cypress driving a real browser through full user flows. High confidence, but slow and more prone to flakiness &mdash; reserve it for critical paths like login, checkout, and signup.</li>\n</ul>\n<p>The interview-ready version: don't chase 100% coverage &mdash; focus on testing user-observable behavior at the integration level, with a thin layer of E2E on top for the paths that would hurt most in production.</p>`\n    },\n    {\n      q:\"What typically causes flaky frontend tests, and how do you eliminate them?\",\n      a:`<ul>\n<li><strong>Use auto-waiting queries/locators:</strong> React Testing Library's <code>findBy*</code>, and Playwright/Cypress locators, retry automatically &mdash; avoid a manual <code>sleep()</code>/fixed <code>wait()</code>, which is either too short (flaky) or too long (slow).</li>\n<li><strong>Deterministic data:</strong> seed fixtures rather than depending on shared or production data that can change between runs.</li>\n<li><strong>Mock time:</strong> anything time-based should use <code>jest.useFakeTimers()</code> (or an E2E framework's clock API) instead of real timers racing the test.</li>\n<li><strong>Isolate tests:</strong> each test sets up and tears down its own state &mdash; one test's leftover state should never leak into the next.</li>\n<li><strong>Mock the network:</strong> MSW gives deterministic responses; a real network call is a source of nondeterminism you don't need in a unit/integration test.</li>\n<li><strong>Retry once, fix always:</strong> a CI retry can absorb a one-off flake, but track the flake rate per test and treat a rising rate as a bug to fix, not noise to suppress.</li>\n</ul>`\n    },\n    {\n      q:\"RTL query priority \u2014 which query should you reach for first, and why?\",\n      a:`<p>RTL's queries aren't interchangeable \u2014 they exist in a deliberate priority order that mirrors how real users and assistive technology perceive the page.</p>\n      <table class=\"cmp\">\n        <tr><th>Priority</th><th>Query</th><th>Use when</th></tr>\n        <tr><td>1</td><td><code>getByRole</code></td><td>Almost always \u2014 mirrors how screen readers traverse the DOM by ARIA role</td></tr>\n        <tr><td>2</td><td><code>getByLabelText</code></td><td>Form inputs tied to a <code>&lt;label&gt;</code> \u2014 also enforces that the label/input association actually exists</td></tr>\n        <tr><td>3</td><td><code>getByText</code></td><td>Non-interactive visible text \u2014 paragraphs, headings, static copy</td></tr>\n        <tr><td>4</td><td><code>getByTestId</code></td><td>Last resort \u2014 nothing else uniquely identifies the element; not visible to real users</td></tr>\n      </table>\n      <p>Every <code>getBy*</code> also has a <code>findBy*</code> (async, waits for the element) and a <code>queryBy*</code> (returns <code>null</code> instead of throwing) variant, so the priority order applies regardless of which timing variant you need.</p>\n      <p><strong>Common follow-ups</strong></p>\n      <ul>\n        <li><strong>Why is <code>getByTestId</code> ranked last, not banned?</strong> It couples the test to implementation rather than to what a user can see, so refactors that don't change behavior can still break the test. It's fine as an escape hatch when no accessible query can uniquely identify the element.</li>\n        <li><strong>Why does <code>getByLabelText</code> outrank <code>getByText</code>?</strong> Because it verifies real accessibility wiring \u2014 if the label isn't correctly associated with the input, the query fails, catching an actual a11y bug instead of just finding text on the page.</li>\n      </ul>`\n    },\n    {\n      q:\"What does userEvent give you that fireEvent does not?\",\n      a:`<p><code>fireEvent</code> dispatches a single synthetic DOM event directly. <code>userEvent</code> simulates the full sequence of events a real user interaction actually produces.</p>\n      <pre>// fireEvent \u2014 one synthetic event, nothing else\nfireEvent.change(input, { target: { value: 'hello' } });\n\n// userEvent \u2014 the realistic sequence a browser would fire\nconst user = userEvent.setup();\nawait user.type(input, 'hello');\n// dispatches: focus \u2192 keydown \u2192 keypress \u2192 input \u2192 keyup, per character</pre>\n      <p>For example, a real click fires <code>pointerdown</code>, <code>mousedown</code>, <code>pointerup</code>, <code>mouseup</code>, then <code>click</code> in order. <code>fireEvent.click</code> skips straight to firing <code>click</code> alone. That gap matters for components relying on focus management, hover state, or keyboard event handlers that <code>fireEvent</code>'s shortcut never triggers \u2014 <code>userEvent</code> catches bugs that <code>fireEvent</code> tests miss entirely.</p>\n      <p><strong>Common follow-ups</strong></p>\n      <ul>\n        <li><strong>Should you ever still use fireEvent?</strong> It's fine for firing low-level, one-off DOM events that don't correspond to a real user gesture (like a raw <code>scroll</code> or <code>resize</code> event) \u2014 for anything a user does with mouse or keyboard, <code>userEvent</code> is the better default.</li>\n      </ul>`\n    },\n    {\n      q:\"Spot the bugs: what's wrong with this RTL test, and how would you fix it?\",\n      a:`<pre>test('user can submit form', () =&gt; {\n  render(&lt;LoginForm onSubmit={jest.fn()} /&gt;);\n  const emailInput = screen.getByLabelText('Email');\n  const submitButton = screen.getByRole('button', { name: /submit/i });\n\n  fireEvent.change(emailInput, { target: { value: 'test@example.com' } });\n  fireEvent.click(submitButton);\n\n  expect(emailInput.value).toBe('test@example.com');\n});</pre>\n      <p>This test has five separate problems, all common enough to show up in real code review:</p>\n      <ul>\n        <li><strong>Wrong target element.</strong> If the label isn't correctly associated with the input, <code>getByLabelText('Email')</code> can resolve to the <code>&lt;label&gt;</code> itself rather than the <code>&lt;input&gt;</code>, and firing a change event on a label does nothing.</li>\n        <li><strong>fireEvent instead of userEvent.</strong> <code>fireEvent.change</code>/<code>fireEvent.click</code> bypass the realistic browser event sequence \u2014 prefer <code>await userEvent.type(...)</code> and <code>await userEvent.click(...)</code>.</li>\n        <li><strong>No <code>await</code> on async work.</strong> If <code>onSubmit</code> is async or triggers a state update, the test doesn't wait for it to settle before asserting \u2014 needs <code>await</code>, and <code>waitFor</code> if the assertion depends on something appearing later.</li>\n        <li><strong>Asserting the wrong thing.</strong> Checking that the input still holds the value you just typed proves almost nothing. The test should verify <code>expect(onSubmit).toHaveBeenCalledWith(...)</code> \u2014 that the callback actually fired with the right data.</li>\n        <li><strong>No verification of the actual side effect.</strong> The test never checks that submission produced a visible result (a success message, a cleared loading state) \u2014 always assert on the post-submission UI, not just that a function was called.</li>\n      </ul>\n      <p><strong>Common follow-ups</strong></p>\n      <ul>\n        <li><strong>Rewrite it correctly \u2014 what would the fixed version look like?</strong> Use <code>userEvent</code> to type into the input found via <code>getByRole('textbox', { name: /email/i })</code>, click submit the same way, then assert <code>expect(handleSubmit).toHaveBeenCalledWith(expect.objectContaining({ email: 'test@example.com' }))</code> instead of reading the input's DOM value.</li>\n      </ul>`\n    },\n    {\n      q:\"In React Testing Library, what is the difference between getBy, findBy and queryBy?\",\n      a:`<table class=\"cmp\">\n<tr><th>Variant</th><th>Behaviour</th><th>Use it for</th></tr>\n<tr><td><code>getBy*</code></td><td>Synchronous. Returns the element or <strong>throws</strong> if none (or more than one) matches.</td><td>Asserting something is on screen right now.</td></tr>\n<tr><td><code>queryBy*</code></td><td>Synchronous. Returns the element or <strong><code>null</code></strong>; does not throw when missing.</td><td>Asserting something is <em>absent</em>: <code>expect(screen.queryByText('Error')).not.toBeInTheDocument()</code>.</td></tr>\n<tr><td><code>findBy*</code></td><td>Asynchronous. Returns a promise that resolves when the element appears (default timeout 1000 ms), rejects otherwise.</td><td>Waiting for something that appears after async work (fetch, timers, state updates).</td></tr>\n</table>\n<ul>\n<li>There are <code>All</code> variants (<code>getAllBy*</code>, <code>findAllBy*</code>, <code>queryAllBy*</code>) for multiple matches.</li>\n<li><code>findBy*</code> is <code>getBy*</code> wrapped in <code>waitFor</code>, so you rarely need <code>waitFor</code> for a simple appearance.</li>\n<li>Do not use <code>getBy*</code> to assert absence (it throws) or <code>queryBy*</code> to assert presence (you lose the helpful error message).</li>\n</ul>\n<pre>render(&lt;UserCard id=\"1\" /&gt;);\nexpect(screen.getByText(/loading/i)).toBeInTheDocument();          // there now\nconst name = await screen.findByRole('heading', { name: 'Jane Doe' }); // appears later\nexpect(screen.queryByText(/loading/i)).not.toBeInTheDocument();     // gone</pre>`\n    },\n    {\n      q:\"What is the difference between act() and waitFor() in React Testing Library?\",\n      a:`<ul>\n<li><strong><code>act()</code></strong> makes sure all React updates triggered inside it (state changes, effects, scheduled work) are flushed before you make assertions. RTL wraps its render, <code>userEvent</code> and <code>fireEvent</code> calls in <code>act</code> for you, so you rarely call it by hand. The \"not wrapped in act(...)\" warning usually means a state update happened outside a tracked event, typically async work that finished after the test moved on.</li>\n<li><strong><code>waitFor(callback)</code></strong> repeatedly runs a callback (polling plus a <code>MutationObserver</code>) until it stops throwing or a timeout is reached. Use it when the UI reaches the expected state <em>eventually</em>. Async queries (<code>findBy*</code>) already use it.</li>\n</ul>\n<p><strong>Rules of thumb:</strong></p>\n<ul>\n<li>Prefer <code>await screen.findBy...</code> for \"appears later\".</li>\n<li>Put <strong>one assertion or a related group</strong> inside <code>waitFor</code>, and never side effects (like a click), because the callback can run many times.</li>\n<li>Do not wrap <code>userEvent</code> calls in <code>act</code>; <code>await user.click(...)</code> is enough.</li>\n<li>Do not use fixed <code>setTimeout</code> waits; they are slow and flaky.</li>\n<li>Use <code>renderHook</code> and <code>act</code> together for testing custom hooks whose state changes are triggered from the test.</li>\n</ul>\n<pre>const user = userEvent.setup();\nrender(&lt;SearchBox /&gt;);\nawait user.type(screen.getByRole('searchbox'), 'shoes');\nawait waitFor(() =&gt; expect(mockSearch).toHaveBeenCalledWith('shoes'));   // eventually true\nexpect(await screen.findByText('12 results')).toBeInTheDocument();</pre>`\n    }\n  ]\n}\n];\n\n// ---------- render ----------\nconst navList = document.getElementById('navList');\nconst content = document.getElementById('content');\nlet activeId = DATA[0].id;\nconst STORAGE_KEY = 'react-training-reviewed-v1';\nconst PREFS_KEY = 'react-training-prefs-v1';\n\nfunction readJSON(key, fallback){ try{ const raw = localStorage.getItem(key); return raw ? JSON.parse(raw) : fallback; }catch(e){ return fallback; } }\nfunction writeJSON(key, val){ try{ localStorage.setItem(key, JSON.stringify(val)); }catch(e){} }\n\nlet reviewed = readJSON(STORAGE_KEY, {});\nlet prefs = Object.assign({ summaries:true, coreOnly:false, hideDone:false, shuffle:false }, readJSON(PREFS_KEY, {}));\nfunction savePrefs(){ writeJSON(PREFS_KEY, prefs); }\nfunction qKey(sectionId, qIdx){ return sectionId + ':' + qIdx; }\n\n// ---- derived data: plain text, one-line summaries, core set, reading time\nconst stripTags = (s)=> s.replace(/<pre[\\s\\S]*?<\\/pre>/g,' ').replace(/<svg[\\s\\S]*?<\\/svg>/g,' ').replace(/<[^>]+>/g,' ')\n  .replace(/&nbsp;/g,' ').replace(/&amp;/g,'&').replace(/&lt;/g,'<').replace(/&gt;/g,'>').replace(/&mdash;/g,'\u2014').replace(/&rarr;/g,'\u2192').replace(/&[a-z]+;/g,' ')\n  .replace(/\\s+/g,' ').trim();\nconst INDEX = [];\nDATA.forEach(section=>{\n  section.questions.forEach((item, qIdx)=>{\n    // Summary: leading prose (no badges, tables, code or diagrams), whole sentences until it says something useful.\n    const prose = stripTags(item.a.replace(/<span class=\"tbadge\">[\\s\\S]*?<\\/span>/,'').replace(/<table[\\s\\S]*?<\\/table>/g,' ').replace(/<div class=\"diagram\">[\\s\\S]*?<\\/div>/g,' '));\n    const sentences = prose.split(/(?<=[.!?])\\s+(?=[A-Z0-9\"'(])/);\n    let summary = '';\n    for(const s of sentences){ if(summary.length >= 90) break; summary = (summary ? summary + ' ' : '') + s; }\n    summary = summary.replace(/^(Common follow-ups|Model answer:|Answer:)\\s*/i, '');\n    if(summary.length > 240) summary = summary.slice(0, 237).replace(/\\s+\\S*$/,'') + '\u2026';\n    const text = stripTags(item.q + ' ' + item.a);\n    INDEX.push({ section, qIdx, item, summary, text: text.toLowerCase(), words: text.split(' ').length });\n  });\n});\nconst CORE = new Set();\nconst review = DATA.find(s=>s.id==='review');\nif(review) review.questions.forEach(q=>{ (q.a.match(/data-go=\"[^\"]+\"/g)||[]).forEach(m=>CORE.add(m.slice(9,-1))); });\nconst entry = (sid, i)=> INDEX.find(e=>e.section.id===sid && e.qIdx===i);\nfunction minutes(words){ return Math.max(1, Math.round(words / 200)); }\n\n// ---- totals and progress\nfunction totalQuestions(){ return INDEX.length; }\nfunction reviewedCount(){ return INDEX.filter(e=>reviewed[qKey(e.section.id,e.qIdx)]).length; }\nfunction sectionDone(section){ return section.questions.filter((_,i)=>reviewed[qKey(section.id,i)]).length; }\nfunction updateProgress(){\n  const total = totalQuestions(), done = reviewedCount();\n  document.getElementById('progressNum').textContent = done + ' / ' + total;\n  document.getElementById('progressFill').style.width = (total ? (done/total*100) : 0) + '%';\n}\n\n// ---- nav\nfunction renderNav(){\n  navList.innerHTML = '';\n  DATA.forEach((section, idx)=>{\n    const done = sectionDone(section), total = section.questions.length;\n    const btn = document.createElement('button');\n    btn.className = 'nav-item' + (section.id===activeId && !searchTerm ? ' active':'') + (done===total ? ' complete':'');\n    btn.innerHTML = '<span class=\"nav-num\">' + String(idx+1).padStart(2,'0') + '</span><span class=\"nav-label\">' + section.title + '</span><span class=\"nav-count\" title=\"' + done + ' of ' + total + ' reviewed\">' + (done ? done + '/' : '') + total + '</span>';\n    btn.addEventListener('click', ()=>{ clearSearch(false); activeId = section.id; hideBack(); renderNav(); renderContent(); window.scrollTo(0,0); });\n    navList.appendChild(btn);\n  });\n}\n\n// ---- cards\nfunction highlight(html, terms){\n  if(!terms.length) return html;\n  let out = html;\n  terms.forEach(t=>{\n    const safe = t.replace(/[.*+?^$(){}|[\\]\\\\]/g,'\\\\$&');\n    out = out.replace(new RegExp('(^|>)([^<]*)', 'g'), (m, a, b)=> a + b.replace(new RegExp('(' + safe + ')', 'ig'), '<mark>$1</mark>'));\n  });\n  return out;\n}\nfunction buildCard(e, opts){\n  opts = opts || {};\n  const key = qKey(e.section.id, e.qIdx);\n  const isDone = !!reviewed[key];\n  const card = document.createElement('div');\n  card.className = 'qcard' + (opts.open ? ' open' : '');\n  card.dataset.q = e.qIdx;\n  card.dataset.sec = e.section.id;\n  const core = CORE.has(key);\n  const chips = (opts.showSection ? '<span class=\"qsec\">' + e.section.title + '</span>' : '') + (core ? '<span class=\"qcore\" title=\"Core: linked from the 2-Hour Review\">\u2605 core</span>' : '');\n  const showSummary = prefs.summaries && e.section.id !== 'review' && e.summary;\n  const qhead = document.createElement('div');\n  qhead.className = 'qhead';\n  qhead.innerHTML =\n    '<span class=\"qcheck ' + (isDone?'done':'') + '\" role=\"checkbox\" aria-checked=\"' + isDone + '\" tabindex=\"0\" title=\"Mark reviewed (x)\"></span>' +\n    '<span class=\"qmain\"><span class=\"qtext ' + (isDone?'done-text':'') + '\">' + highlight(e.item.q, opts.terms || []) + '</span>' +\n    (chips ? '<span class=\"qchips\">' + chips + '</span>' : '') +\n    (showSummary ? '<span class=\"qsum\">' + highlight(e.summary.replace(/</g,'&lt;'), opts.terms || []) + '</span>' : '') + '</span>' +\n    '<span class=\"qchevron\">\u203a</span>';\n  const checkEl = qhead.querySelector('.qcheck');\n  const toggleDone = (ev)=>{ ev.stopPropagation(); setDone(card, !reviewed[key]); };\n  checkEl.addEventListener('click', toggleDone);\n  checkEl.addEventListener('keydown', (ev)=>{ if(ev.key===' '||ev.key==='Enter'){ ev.preventDefault(); toggleDone(ev); } });\n  qhead.addEventListener('click', ()=>{ card.classList.toggle('open'); setFocus(card, false); });\n  const body = document.createElement('div');\n  body.className = 'qbody';\n  body.innerHTML = e.item.a;\n  card.appendChild(qhead);\n  card.appendChild(body);\n  return card;\n}\nfunction setDone(card, val){\n  const key = qKey(card.dataset.sec, Number(card.dataset.q));\n  reviewed[key] = val;\n  writeJSON(STORAGE_KEY, reviewed);\n  const c = card.querySelector('.qcheck'), t = card.querySelector('.qtext');\n  c.classList.toggle('done', val); c.setAttribute('aria-checked', val); t.classList.toggle('done-text', val);\n  updateProgress(); renderNav();\n}\n\n// ---- section view\nfunction toolbarHTML(section, shown){\n  const words = section.questions.reduce((n,_,i)=> n + entry(section.id,i).words, 0);\n  const coreN = section.questions.filter((_,i)=>CORE.has(qKey(section.id,i))).length;\n  return '<div class=\"toolbar\">' +\n    '<span class=\"tmeta\">~' + minutes(words) + ' min full read' + (coreN && section.id!=='review' ? ' \u00b7 ' + coreN + ' core' : '') + (shown !== section.questions.length ? ' \u00b7 showing ' + shown : '') + '</span>' +\n    '<span class=\"tgroup\"><button data-act=\"flash\" class=\"flash-btn\">\u25b6 Flashcards</button><button data-act=\"expand\">Expand all</button><button data-act=\"collapse\">Collapse all</button></span>' +\n    '<span class=\"tgroup\">' +\n      '<label><input type=\"checkbox\" data-pref=\"summaries\"' + (prefs.summaries?' checked':'') + '> Summaries</label>' +\n      (CORE.size ? '<label><input type=\"checkbox\" data-pref=\"coreOnly\"' + (prefs.coreOnly?' checked':'') + '> \u2605 Core only</label>' : '') +\n      '<label><input type=\"checkbox\" data-pref=\"hideDone\"' + (prefs.hideDone?' checked':'') + '> Hide reviewed</label>' +\n    '</span></div>';\n}\nfunction renderContent(forceIdx){\n  content.innerHTML = '';\n  const section = DATA.find(s=>s.id===activeId);\n  const idx = DATA.findIndex(s=>s.id===activeId);\n  const visible = section.questions.map((_,i)=>i).filter(i=>{\n    if(i === forceIdx || section.id === 'review') return true;\n    const key = qKey(section.id, i);\n    if(prefs.coreOnly && !CORE.has(key)) return false;\n    if(prefs.hideDone && reviewed[key]) return false;\n    return true;\n  });\n  const head = document.createElement('div');\n  head.className = 'section-head';\n  head.innerHTML = '<span class=\"num\">' + String(idx+1).padStart(2,'0') + '</span><h2>' + section.title + '</h2><span class=\"count\">' + sectionDone(section) + ' / ' + section.questions.length + ' reviewed</span>';\n  content.appendChild(head);\n  const tb = document.createElement('div');\n  tb.innerHTML = toolbarHTML(section, visible.length);\n  content.appendChild(tb.firstChild);\n  if(!visible.length){\n    const empty = document.createElement('p');\n    empty.className = 'empty';\n    empty.textContent = prefs.coreOnly ? 'No core questions in this section with the current filters. Untick \"Core only\" to see all.' : 'Everything here is reviewed. Untick \"Hide reviewed\" to see it again.';\n    content.appendChild(empty);\n  }\n  visible.forEach(i=> content.appendChild(buildCard(entry(section.id, i), { open: section.id === 'review' })));\n  const pager = document.createElement('div');\n  pager.className = 'pager';\n  const prev = DATA[idx-1], next = DATA[idx+1];\n  pager.innerHTML = (prev ? '<button data-goto=\"' + prev.id + '\">\u2190 ' + prev.title + '</button>' : '<span></span>') + (next ? '<button data-goto=\"' + next.id + '\">' + next.title + ' \u2192</button>' : '<span></span>');\n  content.appendChild(pager);\n  focusIdx = -1;\n}\n\n// ---- search\nlet searchTerm = '';\nconst searchInput = document.getElementById('searchInput');\nfunction renderSearch(){\n  const terms = searchTerm.toLowerCase().split(/\\s+/).filter(t=>t.length > 1);\n  const hits = INDEX.filter(e=> terms.every(t=> e.text.includes(t)))\n    .map(e=>({ e, score: terms.reduce((n,t)=> n + (e.item.q.toLowerCase().includes(t) ? 10 : 1), 0) }))\n    .sort((a,b)=> b.score - a.score);\n  content.innerHTML = '';\n  const head = document.createElement('div');\n  head.className = 'section-head';\n  head.innerHTML = '<span class=\"num\">\u2315</span><h2>Search</h2><span class=\"count\">' + hits.length + ' result' + (hits.length===1?'':'s') + '</span>';\n  content.appendChild(head);\n  if(hits.length){\n    const tb = document.createElement('div'); tb.className = 'toolbar';\n    tb.innerHTML = '<span class=\"tmeta\">' + (hits.length > 80 ? 'showing the top 80' : 'best matches first') + '</span><span class=\"tgroup\"><button data-act=\"flash\" class=\"flash-btn\">\u25b6 Flashcards from these results</button></span>';\n    content.appendChild(tb);\n  }\n  if(!hits.length){\n    const p = document.createElement('p'); p.className = 'empty'; p.textContent = 'No questions match \"' + searchTerm + '\". Try fewer or shorter words.'; content.appendChild(p);\n  }\n  hits.slice(0, 80).forEach(h=> content.appendChild(buildCard(h.e, { showSection:true, terms })));\n  focusIdx = -1;\n  renderNav();\n}\nlet searchTimer;\nsearchInput.addEventListener('input', ()=>{\n  clearTimeout(searchTimer);\n  searchTimer = setTimeout(()=>{\n    searchTerm = searchInput.value.trim();\n    if(searchTerm.length >= 2){ hideBack(); renderSearch(); window.scrollTo(0,0); }\n    else if(!searchTerm){ renderNav(); renderContent(); }\n  }, 140);\n});\nfunction clearSearch(rerender){\n  if(!searchTerm && !searchInput.value) return;\n  searchTerm = ''; searchInput.value = '';\n  if(rerender !== false){ renderNav(); renderContent(); }\n}\n\n// ---- navigation between questions, with a Back button\nconst backBtn = document.getElementById('backBtn');\nlet returnTo = null;\nfunction hideBack(){ returnTo = null; backBtn.hidden = true; }\nfunction gotoQuestion(id, qIdx, fromLink){\n  if(!DATA.find(s=>s.id===id)) return;\n  if(fromLink){\n    returnTo = { id: activeId, search: searchTerm, y: window.scrollY };\n    const from = searchTerm ? 'search' : DATA.find(s=>s.id===activeId).title;\n    backBtn.textContent = '\u2190 Back to ' + from;\n    backBtn.hidden = false;\n  }\n  searchTerm = ''; searchInput.value = '';\n  activeId = id;\n  const n = (qIdx !== undefined && !isNaN(qIdx)) ? qIdx : undefined;\n  renderNav();\n  renderContent(n);\n  const card = n !== undefined ? content.querySelector('.qcard[data-q=\"' + n + '\"]') : null;\n  if(card){ card.classList.add('open'); setFocus(card, true); }\n  else window.scrollTo(0,0);\n}\nbackBtn.addEventListener('click', ()=>{\n  if(!returnTo) return;\n  const r = returnTo; hideBack();\n  if(r.search){ searchTerm = r.search; searchInput.value = r.search; renderSearch(); }\n  else { activeId = r.id; renderNav(); renderContent(); }\n  requestAnimationFrame(()=> window.scrollTo(0, r.y));\n});\nwindow.addEventListener('message', (e)=>{\n  if(e.data && e.data.type==='goto-section') gotoQuestion(e.data.id, e.data.qIdx, false);\n});\ncontent.addEventListener('click', (e)=>{\n  const a = e.target.closest('a[data-go]');\n  if(a){ e.preventDefault(); const [id, q] = a.dataset.go.split(':'); gotoQuestion(id, Number(q), true); return; }\n  const g = e.target.closest('button[data-goto]');\n  if(g){ activeId = g.dataset.goto; hideBack(); renderNav(); renderContent(); window.scrollTo(0,0); return; }\n  const act = e.target.closest('button[data-act]');\n  if(act){\n    if(act.dataset.act === 'flash'){ startDeck(deckFromView(), searchTerm ? 'Search: ' + searchTerm : DATA.find(s=>s.id===activeId).title); return; }\n    content.querySelectorAll('.qcard').forEach(c=> c.classList.toggle('open', act.dataset.act === 'expand'));\n  }\n});\ncontent.addEventListener('change', (e)=>{\n  const p = e.target.dataset && e.target.dataset.pref;\n  if(!p) return;\n  prefs[p] = e.target.checked; savePrefs();\n  if(searchTerm) renderSearch(); else renderContent();\n});\n\n// ---- keyboard: / search, j/k move, o or Enter open, x reviewed, n/p section, Esc\nlet focusIdx = -1;\nfunction cards(){ return Array.from(content.querySelectorAll('.qcard')); }\nfunction setFocus(card, scroll){\n  cards().forEach(c=> c.classList.remove('focused'));\n  card.classList.add('focused');\n  focusIdx = cards().indexOf(card);\n  if(scroll) card.scrollIntoView({ block:'start', behavior:'smooth' });\n}\ndocument.addEventListener('keydown', (e)=>{\n  if(fc.open){ fcKey(e); return; }\n  const typing = /INPUT|TEXTAREA|SELECT/.test(document.activeElement.tagName);\n  if(e.key === 'Escape'){ if(searchTerm || searchInput.value){ clearSearch(); } searchInput.blur(); return; }\n  if(typing || e.metaKey || e.ctrlKey || e.altKey) return;\n  const list = cards();\n  if(e.key === '/'){ e.preventDefault(); searchInput.focus(); searchInput.select(); return; }\n  if(e.key === 'j' || e.key === 'k'){\n    if(!list.length) return;\n    focusIdx = Math.max(0, Math.min(list.length - 1, focusIdx + (e.key === 'j' ? 1 : -1)));\n    setFocus(list[focusIdx], true); return;\n  }\n  const cur = list[focusIdx];\n  if((e.key === 'o' || e.key === 'Enter') && cur){ cur.classList.toggle('open'); return; }\n  if(e.key === 'x' && cur){ setDone(cur, !reviewed[qKey(cur.dataset.sec, Number(cur.dataset.q))]); return; }\n  if(e.key === 'n' || e.key === 'p'){\n    if(searchTerm) return;\n    const i = DATA.findIndex(s=>s.id===activeId) + (e.key === 'n' ? 1 : -1);\n    if(DATA[i]){ activeId = DATA[i].id; hideBack(); renderNav(); renderContent(); window.scrollTo(0,0); }\n  }\n});\n\n\n// ---- flashcards: question first, say the answer, reveal, rate yourself\nconst fcEl = document.getElementById('fc');\nconst fc = { open:false, deck:[], i:0, revealed:false, got:0, missed:[], again:new Map(), title:'', source:[] };\nfunction shuffleArr(a){ for(let i=a.length-1;i>0;i--){ const j=Math.floor(Math.random()*(i+1)); const t=a[i]; a[i]=a[j]; a[j]=t; } return a; }\nfunction startDeck(entries, title){\n  if(!entries.length) return;\n  fc.source = entries.slice();\n  fc.deck = prefs.shuffle ? shuffleArr(entries.slice()) : entries.slice();\n  fc.i = 0; fc.revealed = false; fc.got = 0; fc.missed = []; fc.again = new Map(); fc.title = title; fc.open = true;\n  fcEl.querySelector('#fcShuffle').checked = !!prefs.shuffle;\n  fcEl.hidden = false; document.body.classList.add('fc-lock');\n  renderFc();\n  fcEl.querySelector('.fc-dialog').focus();\n}\nfunction closeFc(){\n  fc.open = false; fcEl.hidden = true; document.body.classList.remove('fc-lock');\n  if(searchTerm) renderSearch(); else renderContent();\n}\nfunction markReviewed(key, val){ reviewed[key] = val; writeJSON(STORAGE_KEY, reviewed); updateProgress(); renderNav(); }\nfunction renderFc(){\n  const n = fc.deck.length;\n  fcEl.querySelector('.fc-title').textContent = fc.title;\n  fcEl.querySelector('.fc-count').textContent = (fc.i < n ? (fc.i + 1) : n) + ' / ' + n + (fc.got ? ' \u00b7 ' + fc.got + ' got it' : '') + (fc.missed.length ? ' \u00b7 ' + new Set(fc.missed).size + ' to revisit' : '');\n  fcEl.querySelector('.fc-bar i').style.width = (n ? fc.i / n * 100 : 0) + '%';\n  const body = fcEl.querySelector('.fc-body');\n  const actions = fcEl.querySelector('.fc-actions');\n  if(fc.i >= n){\n    const uniqMissed = Array.from(new Set(fc.missed));\n    body.innerHTML = '<div class=\"fc-done\"><h3>Deck finished</h3><p>' + fc.got + ' marked \"got it\" (and ticked as reviewed) \u00b7 ' + uniqMissed.length + ' to revisit.</p>' +\n      (uniqMissed.length ? '<p class=\"fc-sub\">To revisit:</p><ul>' + uniqMissed.map(e=>'<li>' + e.item.q + '</li>').join('') + '</ul>' : '<p class=\"fc-sub\">Nothing missed. Well done.</p>') + '</div>';\n    actions.innerHTML = (uniqMissed.length ? '<button data-fc=\"missed\" class=\"primary\">Practise the ' + uniqMissed.length + ' missed</button>' : '') +\n      '<button data-fc=\"restart\">Restart deck</button><button data-fc=\"close\">Close <kbd>Esc</kbd></button>';\n    return;\n  }\n  const e = fc.deck[fc.i];\n  const key = qKey(e.section.id, e.qIdx);\n  body.innerHTML = '<div class=\"qchips\"><span class=\"qsec\">' + e.section.title + '</span>' + (CORE.has(key) ? '<span class=\"qcore\">\u2605 core</span>' : '') + (reviewed[key] ? '<span class=\"qsec\">reviewed</span>' : '') + '</div>' +\n    '<h3 class=\"fc-q\">' + e.item.q + '</h3>' +\n    (fc.revealed ? '<div class=\"qbody fc-a\">' + e.item.a + '</div>' : '<p class=\"fc-prompt\">Answer it out loud first: the main point, one example, one trade-off. Then reveal.</p>');\n  body.scrollTop = 0;\n  actions.innerHTML = fc.revealed\n    ? '<button data-fc=\"prev\" title=\"Previous (\u2190)\">\u2190</button><button data-fc=\"again\" class=\"again\">Again <kbd>1</kbd></button><button data-fc=\"got\" class=\"primary\">Got it <kbd>2</kbd></button>'\n    : '<button data-fc=\"prev\" title=\"Previous (\u2190)\">\u2190</button><button data-fc=\"reveal\" class=\"primary\">Reveal answer <kbd>Space</kbd></button><button data-fc=\"skip\">Skip <kbd>\u2192</kbd></button>';\n}\nfunction fcAction(act){\n  const n = fc.deck.length, e = fc.deck[fc.i];\n  if(act === 'close') return closeFc();\n  if(act === 'restart') return startDeck(fc.source, fc.title);\n  if(act === 'missed') return startDeck(Array.from(new Set(fc.missed)), fc.title + ' \u00b7 missed');\n  if(fc.i >= n && act !== 'prev') return;\n  if(act === 'reveal'){ fc.revealed = !fc.revealed; }\n  else if(act === 'got'){ if(!fc.revealed){ fc.revealed = true; } else { markReviewed(qKey(e.section.id, e.qIdx), true); fc.got++; fc.i++; fc.revealed = false; } }\n  else if(act === 'again'){\n    if(!fc.revealed){ fc.revealed = true; }\n    else {\n      const k = qKey(e.section.id, e.qIdx), times = fc.again.get(k) || 0;\n      fc.missed.push(e);\n      if(times < 2){ fc.deck.push(e); fc.again.set(k, times + 1); }   // comes back later in this session\n      fc.i++; fc.revealed = false;\n    }\n  }\n  else if(act === 'skip'){ fc.i++; fc.revealed = false; }\n  else if(act === 'prev'){ if(fc.i > 0){ fc.i--; fc.revealed = false; } }\n  renderFc();\n}\nfcEl.addEventListener('click', (e)=>{\n  if(e.target === fcEl) return closeFc();\n  const b = e.target.closest('button[data-fc]');\n  if(b) return fcAction(b.dataset.fc);\n  const a = e.target.closest('a[data-go]');\n  if(a){ e.preventDefault(); closeFc(); const p = a.dataset.go.split(':'); gotoQuestion(p[0], Number(p[1]), true); }\n});\nfcEl.querySelector('#fcShuffle').addEventListener('change', (e)=>{\n  prefs.shuffle = e.target.checked; savePrefs();\n  const keep = fc.i + (fc.revealed ? 1 : 0);\n  const rest = fc.deck.slice(keep);\n  if(prefs.shuffle) shuffleArr(rest);\n  fc.deck = fc.deck.slice(0, keep).concat(rest);\n  renderFc();\n});\nfunction fcKey(e){\n  if(e.key === 'Escape'){ e.preventDefault(); closeFc(); return; }\n  if(e.target && e.target.id === 'fcShuffle') return;\n  const map = { ' ':'reveal', 'Enter':'reveal', '1':'again', '2':'got', 'ArrowRight':'skip', 'ArrowLeft':'prev' };\n  const act = map[e.key];\n  if(!act) return;\n  if(e.target && e.target.tagName === 'BUTTON' && (e.key === ' ' || e.key === 'Enter')) return;   // let a focused button click itself\n  e.preventDefault();\n  if(fc.i >= fc.deck.length && act !== 'prev') return;\n  fcAction(act);\n}\nfunction deckFromView(){ return cards().map(c=> entry(c.dataset.sec, Number(c.dataset.q))); }\ndocument.getElementById('fcCore').addEventListener('click', ()=>{\n  if(CORE.size) startDeck(Array.from(CORE).map(k=>{ const p = k.split(':'); return entry(p[0], Number(p[1])); }).filter(Boolean), 'All \u2605 core questions');\n  else startDeck(INDEX.filter(e=>e.section.id!=='quickref'), 'All questions');\n});\n\ndocument.getElementById('resetBtn').addEventListener('click', ()=>{\n  if(!confirm('Clear all reviewed checkmarks?')) return;\n  reviewed = {}; writeJSON(STORAGE_KEY, reviewed);\n  renderNav(); renderContent(); updateProgress();\n});\n\ndocument.getElementById('heroMeta').textContent = DATA.length + ' sections \u00b7 ' + totalQuestions() + ' questions' + (CORE.size ? ' \u00b7 ' + CORE.size + ' marked \u2605 core (linked from the 2-Hour Review)' : '');\ndocument.getElementById('fcCore').textContent = CORE.size ? '\u25b6 Flashcards: all \u2605 core questions' : '\u25b6 Flashcards: all ' + INDEX.filter(e=>e.section.id!=='quickref').length + ' questions';\nrenderNav();\nrenderContent();\nupdateProgress();\n</script>\n</body>\n</html>\n"}