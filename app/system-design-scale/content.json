{"html": "<!DOCTYPE html>\n<html lang=\"en\">\n<head>\n<meta charset=\"UTF-8\">\n<title>System Design at Scale \u2014 Interview Prep</title>\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1\">\n<link rel=\"preconnect\" href=\"https://fonts.googleapis.com\">\n<link rel=\"preconnect\" href=\"https://fonts.gstatic.com\" crossorigin>\n<link href=\"https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500;600;700&family=IBM+Plex+Sans:wght@400;500;600;700&display=swap\" rel=\"stylesheet\">\n<style>\n  :root{\n    --bg:#F6F4EF;\n    --panel:#FFFFFF;\n    --panel-2:#FBF9F4;\n    --border:#E4E0D4;\n    --text:#211F1B;\n    --muted:#716B5E;\n    --accent:#2F7A72;\n    --accent-ink:#F3FBF9;\n    --accent-soft:#E3F1EE;\n    --gold:#B8792F;\n    --gold-soft:#F5E9D8;\n    --code-bg:#EFECE2;\n    --danger:#B5502F;\n    --shadow: 0 1px 2px rgba(30,26,18,0.05);\n    --mono: 'IBM Plex Mono', ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;\n    --sans: 'IBM Plex Sans', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;\n  }\n  @media (prefers-color-scheme: dark){\n    :root:not([data-theme=\"light\"]){\n      --bg:#15181A;\n      --panel:#1B1F21;\n      --panel-2:#202426;\n      --border:#2C3133;\n      --text:#E9E6DE;\n      --muted:#9A968A;\n      --accent:#5FBFB2;\n      --accent-ink:#0B1716;\n      --accent-soft:#1E3230;\n      --gold:#D9A15C;\n      --gold-soft:#332918;\n      --code-bg:#20262A;\n      --danger:#D98866;\n      --shadow: 0 1px 2px rgba(0,0,0,0.4);\n    }\n  }\n  :root[data-theme=\"dark\"]{\n    --bg:#15181A;\n    --panel:#1B1F21;\n    --panel-2:#202426;\n    --border:#2C3133;\n    --text:#E9E6DE;\n    --muted:#9A968A;\n    --accent:#5FBFB2;\n    --accent-ink:#0B1716;\n    --accent-soft:#1E3230;\n    --gold:#D9A15C;\n    --gold-soft:#332918;\n    --code-bg:#20262A;\n    --danger:#D98866;\n    --shadow: 0 1px 2px rgba(0,0,0,0.4);\n  }\n\n  *{box-sizing:border-box;}\n  html,body{margin:0;padding:0;}\n  body{\n    background:var(--bg);\n    color:var(--text);\n    font-family:var(--sans);\n    line-height:1.55;\n    -webkit-font-smoothing:antialiased;\n  }\n  a{color:var(--accent);}\n\n  .app{\n    display:flex;\n    min-height:100vh;\n  }\n\n  /* ---------- Sidebar ---------- */\n  .sidebar{\n    width:260px;\n    flex:0 0 260px;\n    border-right:1px solid var(--border);\n    padding:28px 16px 24px;\n    position:sticky;\n    top:0;\n    height:100vh;\n    overflow-y:auto;\n  }\n  .brand{\n    font-family:var(--mono);\n    font-size:13px;\n    letter-spacing:0.02em;\n    color:var(--muted);\n    padding:0 8px 18px;\n    border-bottom:1px solid var(--border);\n    margin-bottom:14px;\n  }\n  .brand b{color:var(--text);font-weight:600;}\n  .nav-item{\n    display:flex;\n    align-items:center;\n    justify-content:space-between;\n    gap:10px;\n    width:100%;\n    text-align:left;\n    padding:10px 10px;\n    border-radius:8px;\n    border:1px solid transparent;\n    background:transparent;\n    color:var(--text);\n    font-family:var(--sans);\n    font-size:14.5px;\n    cursor:pointer;\n    margin-bottom:2px;\n  }\n  .nav-item:hover{background:var(--panel-2);}\n  .nav-item.active{\n    background:var(--accent-soft);\n    border-color:var(--accent);\n    color:var(--text);\n    font-weight:600;\n  }\n  .nav-num{\n    font-family:var(--mono);\n    font-size:12px;\n    color:var(--muted);\n    width:18px;\n    flex:0 0 18px;\n  }\n  .nav-item.active .nav-num{color:var(--accent);}\n  .nav-label{flex:1;}\n  .nav-count{\n    font-family:var(--mono);\n    font-size:11px;\n    color:var(--muted);\n    background:var(--panel-2);\n    border:1px solid var(--border);\n    padding:1px 6px;\n    border-radius:999px;\n  }\n  .progress-box{\n    margin-top:20px;\n    padding:14px 12px;\n    border:1px solid var(--border);\n    border-radius:10px;\n    background:var(--panel-2);\n  }\n  .progress-box .label{\n    font-family:var(--mono);\n    font-size:11px;\n    color:var(--muted);\n    margin-bottom:8px;\n  }\n  .progress-track{\n    height:6px;\n    border-radius:999px;\n    background:var(--border);\n    overflow:hidden;\n  }\n  .progress-fill{\n    height:100%;\n    background:var(--accent);\n    width:0%;\n    transition:width .25s ease;\n  }\n  .progress-num{\n    margin-top:8px;\n    font-family:var(--mono);\n    font-size:12px;\n    color:var(--text);\n  }\n  .reset-btn{\n    margin-top:10px;\n    width:100%;\n    font-family:var(--sans);\n    font-size:12px;\n    color:var(--muted);\n    background:transparent;\n    border:1px solid var(--border);\n    border-radius:7px;\n    padding:6px 8px;\n    cursor:pointer;\n  }\n  .reset-btn:hover{color:var(--danger);border-color:var(--danger);}\n\n  /* ---------- Main ---------- */\n  .main{\n    flex:1;\n    min-width:0;\n    padding:36px 40px 80px;\n    max-width:1200px;\n  }\n  .hero{\n    margin-bottom:34px;\n  }\n  .hero .eyebrow-free{\n    font-family:var(--mono);\n    font-size:13px;\n    color:var(--accent);\n    margin:0 0 10px;\n  }\n  .hero h1{\n    font-family:var(--mono);\n    font-size:30px;\n    line-height:1.25;\n    margin:0 0 12px;\n    font-weight:600;\n    letter-spacing:-0.01em;\n  }\n  .hero p{\n    color:var(--muted);\n    max-width:62ch;\n    margin:0 0 6px;\n    font-size:15px;\n  }\n  .hero .source-note{\n    font-size:13px;\n    color:var(--muted);\n    border-left:2px solid var(--border);\n    padding-left:12px;\n    margin-top:16px;\n  }\n\n  .section-head{\n    display:flex;\n    align-items:baseline;\n    gap:10px;\n    margin:44px 0 16px;\n    padding-bottom:10px;\n    border-bottom:1px solid var(--border);\n  }\n  .section-head .num{\n    font-family:var(--mono);\n    color:var(--accent);\n    font-size:14px;\n  }\n  .section-head h2{\n    font-family:var(--mono);\n    font-size:19px;\n    margin:0;\n    font-weight:600;\n  }\n  .section-head .count{\n    margin-left:auto;\n    font-family:var(--mono);\n    font-size:12px;\n    color:var(--muted);\n  }\n\n  .qcard{\n    border:1px solid var(--border);\n    border-radius:10px;\n    background:var(--panel);\n    margin-bottom:12px;\n    overflow:hidden;\n  }\n  .qhead{\n    display:flex;\n    align-items:flex-start;\n    gap:12px;\n    padding:14px 16px;\n    cursor:pointer;\n    user-select:none;\n  }\n  .qcheck{\n    margin-top:3px;\n    flex:0 0 auto;\n    width:18px;height:18px;\n    border-radius:5px;\n    border:1.5px solid var(--border);\n    display:flex;align-items:center;justify-content:center;\n    cursor:pointer;\n    background:var(--panel);\n  }\n  .qcheck.done{\n    background:var(--accent);\n    border-color:var(--accent);\n  }\n  .qcheck.done::after{\n    content:\"\u2713\";\n    color:var(--accent-ink);\n    font-size:12px;\n    font-weight:700;\n  }\n  .qtext{\n    flex:1;\n    font-size:15px;\n    font-weight:500;\n  }\n  .qtext.done-text{color:var(--muted);text-decoration:line-through;text-decoration-color:var(--border);}\n  .qchevron{\n    flex:0 0 auto;\n    color:var(--muted);\n    font-family:var(--mono);\n    font-size:13px;\n    transform:rotate(0deg);\n    transition:transform .15s ease;\n    margin-top:2px;\n  }\n  .qcard.open .qchevron{transform:rotate(90deg);}\n  .qbody{\n    display:none;\n    padding:2px 16px 20px 46px;\n    border-top:1px solid var(--border);\n    font-size:14.5px;\n  }\n  .qcard.open .qbody{display:block; padding-top:14px;}\n  .qbody p{margin:0 0 12px;}\n  .qbody ul{margin:0 0 12px; padding-left:20px;}\n  .qbody li{margin-bottom:6px;}\n  .qbody strong{color:var(--text);}\n  code{\n    font-family:var(--mono);\n    background:var(--code-bg);\n    padding:1px 5px;\n    border-radius:4px;\n    font-size:0.92em;\n  }\n  pre{\n    font-family:var(--mono);\n    background:var(--code-bg);\n    padding:12px 14px;\n    border-radius:8px;\n    overflow-x:auto;\n    font-size:13px;\n    margin:0 0 14px;\n    border:1px solid var(--border);\n  }\n  .diagram{\n    background:var(--panel-2);\n    border:1px solid var(--border);\n    border-radius:8px;\n    padding:14px;\n    margin:0 0 14px;\n    overflow-x:auto;\n  }\n  .diagram svg{display:block; margin:0 auto; max-width:100%; height:auto;}\n  .tag{\n    display:inline-block;\n    font-family:var(--mono);\n    font-size:10.5px;\n    color:var(--gold);\n    background:var(--gold-soft);\n    border:1px solid var(--gold);\n    border-radius:5px;\n    padding:1px 6px;\n    margin-bottom:10px;\n  }\n  table.cmp{\n    width:100%;\n    border-collapse:collapse;\n    margin:0 0 14px;\n    font-size:13.5px;\n  }\n  table.cmp th, table.cmp td{\n    border:1px solid var(--border);\n    padding:7px 10px;\n    text-align:left;\n    vertical-align:top;\n  }\n  table.cmp th{\n    background:var(--panel-2);\n    font-family:var(--mono);\n    font-size:12px;\n    font-weight:600;\n  }\n\n  ::selection{background:var(--accent-soft);}\n\n  @media (max-width: 820px){\n    .sidebar{\n      width:100%;\n      flex:none;\n      height:auto;\n      position:relative;\n      border-right:none;\n      border-bottom:1px solid var(--border);\n    }\n    .app{flex-direction:column;}\n    .main{padding:24px 18px 60px; max-width:100%;}\n  }\n</style>\n</head>\n<body>\n<div class=\"app\">\n  <nav class=\"sidebar\" id=\"sidebar\">\n    <div class=\"brand\"><b>System Design</b><br>At scale \u2014 interview prep</div>\n    <div id=\"navList\"></div>\n    <div class=\"progress-box\">\n      <div class=\"label\">REVIEWED</div>\n      <div class=\"progress-track\"><div class=\"progress-fill\" id=\"progressFill\"></div></div>\n      <div class=\"progress-num\" id=\"progressNum\">0 / 0</div>\n      <button class=\"reset-btn\" id=\"resetBtn\">Reset progress</button>\n    </div>\n  </nav>\n\n  <main class=\"main\">\n    <div class=\"hero\">\n      <p class=\"eyebrow-free\">Prep guide</p>\n      <h1>Design a full-stack app for millions of users</h1>\n      <p>A senior-architect-ready answer, end to end \u2014 requirements, a back-of-envelope capacity estimate, web framework choice, auth, edge/CDN/gateway, service architecture, data layer, caching, async messaging, deployment, reliability, security, and how to pace all of it in a 30\u201345 minute interview.</p>\n      <div class=\"source-note\">12 sections \u00b7 click a question to expand it \u00b7 check items off as you review them (saved in this browser only)</div>\n    </div>\n    <div id=\"content\"></div>\n  </main>\n</div>\n\n\n<script>\nconst DATA = [\n{\n  id:\"framing\", title:\"Framing the Problem\",\n  questions:[\n    {\n      q:\"How do you open when asked to \\\"design a full-stack app that serves millions of users\\\"?\",\n      a:`<p>Treat the prompt as deliberately underspecified and spend the first few minutes narrowing it, out loud, before any design work starts. Two categories of questions matter: functional (what does the app actually do \u2014 pick one or two core workflows to design deeply rather than trying to cover everything shallowly) and non-functional (how many users, what \"millions\" means concretely, read-heavy or write-heavy, global or single-region, latency and availability targets, what data is sensitive enough to need special handling). Anchoring on a concrete example \u2014 \"let's say this is a member portal, the core flows are browsing content, checking account status, and one write-heavy action like submitting a claim\" \u2014 gives every later decision something real to be justified against instead of floating in the abstract.</p>\n      <ul>\n        <li><strong>Functional scope:</strong> pick 2\u20133 core user workflows and say so explicitly \u2014 designing everything shallowly is worse than designing the important 20% deeply.</li>\n        <li><strong>Non-functional targets:</strong> concrete numbers for scale (10M registered / 1M daily active is a reasonable anchor), availability (99.9% vs 99.99% changes the entire reliability story), and latency (sub-200ms API responses vs \"reasonably fast\").</li>\n        <li><strong>Read vs. write ratio:</strong> most consumer apps are 90%+ reads \u2014 this single fact justifies caching and read replicas before anything else does.</li>\n        <li><strong>Time budget:</strong> roughly 5 minutes on requirements, 10\u201315 on high-level architecture, 15\u201320 going deep on 2\u20133 components the interviewer steers you toward, 5 on trade-offs and what you'd cut for an MVP.</li>\n      </ul>`\n    },\n    {\n      q:\"How would you size the system with a back-of-envelope estimate?\",\n      a:`<p>A rough capacity estimate is what turns \"millions of users\" from a scary phrase into a specific number of servers, a specific database size, and a specific caching strategy \u2014 and it's a strong signal on its own, independent of the final numbers being exactly right. The goal is order-of-magnitude accuracy in under two minutes, not precision.</p>\n      <pre><code>// Assume: 10M registered users, 10% daily active (DAU) = 1M DAU\n// Each active user makes ~20 requests/day (page views + API calls)\n\nAverage requests/sec = (1,000,000 users &times; 20 requests) / 86,400 sec\n                      &asymp; 231 req/sec\n\n// Traffic is never flat \u2014 plan for peak, not average.\n// A typical peak-to-average ratio for a consumer app is 3-5x.\nPeak req/sec &asymp; 231 &times; 5 &asymp; 1,150 req/sec\n\n// Storage: assume each user generates 50KB of data over their lifetime\n// (profile, activity history, documents)\nTotal storage &asymp; 10,000,000 &times; 50KB &asymp; 500GB\n\n// growing at, say, 500K new users/month &times; 50KB &asymp; 25GB/month\n// \u2014 comfortably within a single well-provisioned PostgreSQL instance\n// for years, which is the point: this justifies NOT sharding on day one.</code></pre>\n      <p><strong>Good interview signal:</strong> the exact multiplier matters less than showing you know average traffic and peak traffic are different numbers, and that peak is what you provision for. A storage estimate this small is itself a finding: it tells you a single primary database with read replicas is enough for a long time, and sharding would be solving a problem you don't have yet.</p>`\n    }\n  ]\n},\n{\n  id:\"client\", title:\"Client Layer & Web Framework\",\n  questions:[\n    {\n      q:\"Which web framework would you choose, and why?\",\n      a:`<p>Next.js is the default-strong answer for a consumer-facing full-stack app today, and the reason to give isn't \"it's popular\" \u2014 it's that it lets each page pick its own rendering strategy instead of forcing the whole app into one model. A marketing page can be statically generated at build time; a product page that changes daily can use ISR to stay fast while staying fresh; a logged-in dashboard that must be current on every load uses SSR; and a page with one slow, non-critical section can stream that section in via Suspense while the rest of the page is already interactive.</p>\n      <ul>\n        <li><strong>Next.js (App Router)</strong> \u2014 the right default: per-route rendering strategies, React Server Components to ship less client JS, built-in image/font optimization, and a mature deployment story on Vercel or self-hosted via Node. <strong>Partial Prerendering (PPR)</strong> is the newer capability worth naming: a single route serves a static shell instantly from the edge and streams the dynamic, per-user holes into it \u2014 previously you had to pick one mode for the whole page.</li>\n        <li><strong>React Router v7</strong> \u2014 worth being precise here, because this is a common stale answer: Remix v2's framework features were folded into React Router v7, so \"Remix vs. Next.js\" as a framing is outdated. React Router v7 in framework mode is the loader/action model, and it's a strong fit for heavily form/mutation-driven apps. (Remix v3 is a separate, ground-up project \u2014 not the thing most teams mean when they say \"Remix.\")</li>\n        <li><strong>A plain SPA (Vite + React Router in library mode)</strong> \u2014 reasonable for an internal tool or admin dashboard where SEO doesn't matter and everything is behind auth anyway; wrong choice for a public-facing product where first-load performance and SEO both matter.</li>\n        <li><strong>Mobile clients</strong> \u2014 React Native (code/logic sharing with the web team) for most product teams, or fully native when a feature needs the top tier of platform integration or performance.</li>\n      </ul>\n      <p><strong>One correctness detail worth knowing cold:</strong> as of Next.js 15, <code>fetch</code> requests and route handlers are <em>no longer cached by default</em> \u2014 caching is opt-in. Teams upgrading from 13/14 often carry a mental model where data was implicitly cached, which is exactly the kind of assumption that produces either stale data or a surprise origin-load spike after an upgrade.</p>`\n    },\n    {\n      q:\"REST, GraphQL, or tRPC for the API contract?\",\n      a:`<p>This is a \"depends on the org, not the technology\" answer, and saying so is the strong signal \u2014 naming one winner unconditionally is the weaker answer. REST is the safe, boring default: cacheable by URL, understood by every tool and every engineer, trivial to version. GraphQL earns its complexity when the client genuinely needs to shape its own queries \u2014 a mobile app and a web app with very different data needs hitting the same backend, or a product with deeply nested, client-driven views where REST would mean either over-fetching or a combinatorial explosion of endpoints. tRPC is the pragmatic middle ground specifically when both ends are TypeScript in the same monorepo \u2014 end-to-end type safety with none of GraphQL's schema/resolver ceremony, at the cost of tight coupling to that one stack.</p>\n      <p><strong>Common follow-up \u2014 when do you actually reach for GraphQL?</strong> Start with REST, reach for GraphQL only once \"the frontend needs a slightly different shape of this data on three different screens\" has actually happened more than once. And name the costs you're taking on with it: per-request query cost analysis and depth limiting (or a malicious/careless query fans out into thousands of backend calls), persisted queries if you want CDN cacheability back, and N+1 resolution via DataLoader as a default rather than an optimization.</p>`\n    }\n  ]\n},\n{\n  id:\"auth\", title:\"Auth & Identity\",\n  questions:[\n    {\n      q:\"Session cookies or JWTs \u2014 and where does auth actually get checked?\",\n      a:`<p>Session cookies (an opaque ID, server-side session store, typically Redis) and JWTs (a signed, self-contained token) trade off in exactly one place: JWTs avoid a server-side lookup on every request by putting the claims in the token itself, at the cost of being hard to revoke before they expire \u2014 a compromised JWT is valid until it naturally expires unless you build a denylist, which reintroduces the server-side check you were trying to avoid. For a system with millions of users, a hybrid is the pragmatic default:</p>\n      <div class=\"diagram\">\n      <svg viewBox=\"0 0 780 272\" xmlns=\"http://www.w3.org/2000/svg\" style=\"width:100%;max-width:780px\">\n        <text x=\"14\" y=\"20\" font-family=\"var(--mono)\" font-size=\"9.5\" fill=\"var(--muted)\">1 \u00b7 LOGIN (once)</text>\n        <rect x=\"14\" y=\"28\" width=\"104\" height=\"36\" rx=\"7\" fill=\"var(--panel-2)\" stroke=\"var(--border)\"/>\n        <text x=\"66\" y=\"50\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">Client</text>\n        <path d=\"M122 46 L198 46\" stroke=\"var(--muted)\" stroke-width=\"1.5\" marker-end=\"url(#sdAuthM)\"/>\n        <text x=\"160\" y=\"39\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">credentials</text>\n        <rect x=\"202\" y=\"28\" width=\"128\" height=\"36\" rx=\"7\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n        <text x=\"266\" y=\"50\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">Auth service</text>\n        <path d=\"M334 46 L406 46\" stroke=\"var(--accent)\" stroke-width=\"1.5\" marker-end=\"url(#sdAuthA)\"/>\n        <rect x=\"410\" y=\"20\" width=\"176\" height=\"22\" rx=\"6\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n        <text x=\"498\" y=\"35\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--text)\">access token &middot; 15 min &middot; signed</text>\n        <rect x=\"410\" y=\"48\" width=\"176\" height=\"22\" rx=\"6\" fill=\"var(--gold-soft)\" stroke=\"var(--gold)\"/>\n        <text x=\"498\" y=\"63\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--text)\">refresh token &middot; 30 d &middot; stored</text>\n\n        <line x1=\"14\" y1=\"88\" x2=\"766\" y2=\"88\" stroke=\"var(--border)\" stroke-dasharray=\"3\"/>\n\n        <text x=\"14\" y=\"110\" font-family=\"var(--mono)\" font-size=\"9.5\" fill=\"var(--muted)\">2 \u00b7 EVERY REQUEST (hot path \u2014 no datastore hit)</text>\n        <rect x=\"14\" y=\"120\" width=\"104\" height=\"36\" rx=\"7\" fill=\"var(--panel-2)\" stroke=\"var(--border)\"/>\n        <text x=\"66\" y=\"142\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">Client</text>\n        <path d=\"M122 138 L198 138\" stroke=\"var(--accent)\" stroke-width=\"1.5\" marker-end=\"url(#sdAuthA)\"/>\n        <text x=\"160\" y=\"131\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">+ access</text>\n        <rect x=\"202\" y=\"120\" width=\"160\" height=\"36\" rx=\"7\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n        <text x=\"282\" y=\"136\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"10.5\" fill=\"var(--text)\">API Gateway</text>\n        <text x=\"282\" y=\"150\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">verify signature only</text>\n        <path d=\"M366 138 L438 138\" stroke=\"var(--accent)\" stroke-width=\"1.5\" marker-end=\"url(#sdAuthA)\"/>\n        <text x=\"402\" y=\"131\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">+ identity hdr</text>\n        <rect x=\"442\" y=\"120\" width=\"144\" height=\"36\" rx=\"7\" fill=\"var(--panel-2)\" stroke=\"var(--border)\"/>\n        <text x=\"514\" y=\"142\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">Internal service</text>\n        <text x=\"600\" y=\"142\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--accent)\">&larr; stateless, fast</text>\n\n        <line x1=\"14\" y1=\"176\" x2=\"766\" y2=\"176\" stroke=\"var(--border)\" stroke-dasharray=\"3\"/>\n\n        <text x=\"14\" y=\"198\" font-family=\"var(--mono)\" font-size=\"9.5\" fill=\"var(--muted)\">3 \u00b7 EVERY 15 MIN (cold path \u2014 revocation happens here)</text>\n        <rect x=\"14\" y=\"208\" width=\"104\" height=\"36\" rx=\"7\" fill=\"var(--panel-2)\" stroke=\"var(--border)\"/>\n        <text x=\"66\" y=\"230\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">Client</text>\n        <path d=\"M122 226 L198 226\" stroke=\"var(--gold)\" stroke-width=\"1.5\" marker-end=\"url(#sdAuthG)\"/>\n        <text x=\"160\" y=\"219\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">+ refresh</text>\n        <rect x=\"202\" y=\"208\" width=\"128\" height=\"36\" rx=\"7\" fill=\"var(--gold-soft)\" stroke=\"var(--gold)\"/>\n        <text x=\"266\" y=\"230\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">Auth service</text>\n        <path d=\"M334 226 L406 226\" stroke=\"var(--gold)\" stroke-width=\"1.5\" marker-end=\"url(#sdAuthG)\"/>\n        <rect x=\"410\" y=\"208\" width=\"176\" height=\"36\" rx=\"7\" fill=\"var(--panel-2)\" stroke=\"var(--border)\"/>\n        <text x=\"498\" y=\"224\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"10.5\" fill=\"var(--text)\">Token store (Redis)</text>\n        <text x=\"498\" y=\"237\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">revoked? rotate + reissue</text>\n        <text x=\"600\" y=\"230\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--gold)\">&larr; kill switch</text>\n        <defs>\n          <marker id=\"sdAuthM\" markerWidth=\"8\" markerHeight=\"8\" refX=\"6\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L6,3 L0,6 z\" fill=\"var(--muted)\"/></marker>\n          <marker id=\"sdAuthA\" markerWidth=\"8\" markerHeight=\"8\" refX=\"6\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L6,3 L0,6 z\" fill=\"var(--accent)\"/></marker>\n          <marker id=\"sdAuthG\" markerWidth=\"8\" markerHeight=\"8\" refX=\"6\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L6,3 L0,6 z\" fill=\"var(--gold)\"/></marker>\n        </defs>\n      </svg>\n      </div>\n      <p>The shape above is what makes the hybrid work: the 15-minute access token means ~99% of requests are verified with a signature check at the gateway and never touch a datastore, while the refresh exchange is a natural, frequent checkpoint where revocation, rotation, and re-authorization actually get enforced. The worst case for a stolen session is one access-token lifetime, not 30 days.</p>\n      <ul>\n        <li><strong>Where it terminates:</strong> validate at the API Gateway so a bad or expired token never reaches an internal service \u2014 internal services trust a verified identity header the gateway attaches, they don't re-verify the token themselves.</li>\n        <li><strong>Refresh token rotation:</strong> every refresh issues a new refresh token and invalidates the old one. If an already-used refresh token is presented again, that's a strong signal it was stolen \u2014 revoke the whole family and force re-login.</li>\n        <li><strong>Storage:</strong> refresh token in an <code>HttpOnly</code>, <code>Secure</code>, <code>SameSite</code> cookie, never in <code>localStorage</code>, where any XSS can read it.</li>\n        <li><strong>Multi-tenant SaaS:</strong> tenant ID belongs in the token's claims, checked at the gateway, so a leaked or forged request can't cross tenant boundaries even if a downstream service has a bug.</li>\n      </ul>`\n    },\n    {\n      q:\"What does modern authentication look like beyond username and password?\",\n      a:`<p>A 2026-current answer should not stop at \"passwords plus MFA.\" <strong>Passkeys (WebAuthn)</strong> are the direction of travel and are supported across every major platform and browser: the user's device holds a private key, the server holds only a public key, and authentication is a signed challenge \u2014 which makes the entire category of credential-stuffing, password-reuse, and phishing attacks structurally impossible rather than merely mitigated. There is no shared secret on the server to leak.</p>\n      <ul>\n        <li><strong>Practical rollout:</strong> almost nobody goes passkey-only immediately \u2014 the realistic path is passkeys as the preferred method with an existing fallback (password + TOTP, or magic link), then watching adoption before considering removing the fallback. Account recovery is the genuinely hard part and deserves to be named: a device-bound credential means \"I lost my phone\" has to be designed for deliberately, not left to a password-reset email.</li>\n        <li><strong>MFA that's actually worth having:</strong> TOTP or WebAuthn, not SMS \u2014 SIM-swap attacks make SMS the weakest commonly-deployed second factor, and saying so is a signal you've thought past the checkbox.</li>\n        <li><strong>Buy vs. build:</strong> for most teams this is bought, not built \u2014 Auth0/Okta and Cognito are the incumbents, with Clerk and WorkOS the common current choices for new builds (WorkOS specifically when enterprise SSO/SCIM is a sales requirement). The build-it-yourself case is narrow: unusual compliance constraints, or auth itself being the product.</li>\n        <li><strong>Authorization is the harder half:</strong> authentication answers \"who is this,\" authorization answers \"what may they touch,\" and the second is where real systems leak. Centralized policy (an OPA-style policy engine, or a ReBAC/Zanzibar-style relationship model for anything with sharing and nested permissions) beats per-endpoint <code>if</code> statements that drift apart over time.</li>\n      </ul>`\n    }\n  ]\n},\n{\n  id:\"edge\", title:\"Edge, CDN & API Gateway\",\n  questions:[\n    {\n      q:\"What sits between the client and your services?\",\n      a:`<p>Two distinct layers, often conflated in an interview answer but worth naming separately: a CDN handles static, cacheable content close to the user (JS/CSS bundles, images, fonts, and \u2014 with the right cache-control headers \u2014 public HTML and API responses); an API Gateway handles everything that actually needs to reach your backend, and is where cross-cutting concerns get enforced exactly once instead of duplicated in every service.</p>\n      <ul>\n        <li><strong>CDN responsibilities:</strong> versioned static assets with long <code>max-age</code> + <code>immutable</code>; selectively cached public API/HTML responses with short TTLs and <code>stale-while-revalidate</code>; edge rendering for pages that are mostly the same across users.</li>\n        <li><strong>API Gateway responsibilities:</strong> TLS termination, authentication (validating the access token from the section above), rate limiting per user/IP/API key, request routing to the right backend service, and request/response logging with a correlation ID attached to every request for tracing.</li>\n      </ul>\n      <pre><code>// Illustrative gateway route config (Kong-style) \u2014 the shape matters\n// more than the specific product: authn + rate limit + route, in that order\nroutes:\n  - path: /api/v1/claims\n    service: claims-service\n    plugins:\n      - jwt-auth          # reject before it reaches the service\n      - rate-limiting:\n          minute: 60\n          policy: sliding-window\n      - correlation-id      # attach a request ID for tracing downstream</code></pre>`\n    },\n    {\n      q:\"How do you version an API without breaking existing clients?\",\n      a:`<p>The constraint that drives everything here: once a mobile app is in the wild, you do not control when clients upgrade. Some percentage of users will be running a nine-month-old build, so \"we'll just change the endpoint\" is never available to you the way it is for a web-only product where the client ships with every deploy.</p>\n      <ul>\n        <li><strong>Default to additive, non-breaking change.</strong> Adding a field is safe; removing one, renaming one, or narrowing a type is not. A surprising amount of API evolution can happen forever without a version bump if the team treats \"never remove, never repurpose\" as a rule.</li>\n        <li><strong>When you do need a break, version in the URL</strong> (<code>/api/v2/claims</code>) for anything public \u2014 it's greppable in logs, trivially routable at the gateway, and obvious to a client developer reading a stack trace. Header-based versioning is cleaner in theory and worse in practice for debuggability. Internal service-to-service APIs can be looser, since both sides deploy together.</li>\n        <li><strong>Run both versions side by side, with a deprecation clock.</strong> Announce the sunset date, emit a <code>Deprecation</code>/<code>Sunset</code> header, and \u2014 the part teams skip \u2014 instrument per-version traffic so you can actually see whether anyone is still calling v1 before you delete it. Deleting an endpoint you <em>believe</em> is unused is how you find out it wasn't.</li>\n        <li><strong>GraphQL versions differently:</strong> the schema evolves continuously, fields get <code>@deprecated</code> rather than a v2 being cut, and field-level usage analytics tell you when a deprecated field is safe to remove. That's a genuine advantage of the model, worth naming if GraphQL came up earlier.</li>\n      </ul>`\n    },\n    {\n      q:\"How do you make write endpoints safe to retry?\",\n      a:`<p>Any network call can fail <em>after</em> the server committed but <em>before</em> the response got back \u2014 the client sees a timeout and cannot distinguish \"it didn't happen\" from \"it happened and I didn't hear about it.\" If the client retries a non-idempotent write, the user gets charged twice. This is not an edge case; at millions of requests it happens constantly, and it's the single most common source of duplicate-charge incidents.</p>\n      <p>The fix is an <strong>idempotency key</strong>: the client generates a unique key per logical operation (not per attempt) and sends it as a header. The server stores it with a unique constraint alongside the result of the first successful execution, and a repeat of the same key returns the stored result instead of re-executing.</p>\n      <pre><code>POST /api/v1/payments\nIdempotency-Key: 8f14e45f-ea8d-4b21-9e55-7a1c0e3d2b6a\n\n-- server side, in one transaction:\nINSERT INTO idempotency_keys (key, endpoint, status)\nVALUES ($1, 'POST /payments', 'in_progress')\nON CONFLICT (key) DO NOTHING;\n-- 0 rows affected => this key was already used:\n--   completed  -> return the stored response, do not re-execute\n--   in_progress -> the original is still running; return 409, let the client back off</code></pre>\n      <ul>\n        <li><strong>Scope the uniqueness per endpoint</strong>, not globally, and set an expiry (24h is typical) so the table doesn't grow forever.</li>\n        <li><strong>Store enough of the response to replay it</strong> \u2014 status code and body \u2014 otherwise a retry succeeds but the client gets nothing useful back.</li>\n        <li><strong>This pairs with retries, it doesn't replace them:</strong> the resilience section's retry-with-backoff advice is only safe on writes <em>because</em> this exists. Naming that link is the senior version of the answer.</li>\n      </ul>`\n    }\n  ]\n},\n{\n  id:\"services\", title:\"Service Architecture\",\n  questions:[\n    {\n      q:\"Monolith or microservices \u2014 and how do you decide when to split?\",\n      a:`<p>Start with a modular monolith \u2014 a single deployable with clear internal module boundaries (auth, billing, notifications as separate packages/directories with disciplined interfaces between them) \u2014 and split a module out into its own service only once a specific, concrete reason forces it: the module needs an independently faster deploy cadence than the rest of the app, a genuinely different scaling profile (a notification worker that needs 50 instances while the core API needs 5), or a different team needs to own its release cycle without coordinating with everyone else. Splitting by default, before any of those pressures exist, buys network calls, distributed tracing, and data-consistency complexity in exchange for nothing.</p>\n      <p>The modular-monolith's internal boundaries are what make a later split cheap \u2014 if auth already only talks to the rest of the app through a clean interface, extracting it is a deployment change, not a rewrite. A monorepo (Nx, Turborepo) is a separate axis entirely from this decision \u2014 repo structure, backend runtime, and frontend runtime (microfrontends) are three independent choices that get conflated in conversation.</p>\n      <p><strong>The failure mode to name out loud:</strong> a \"distributed monolith\" \u2014 services split at the deployment layer but still sharing a database, or still requiring lockstep releases because their contracts are entangled. That combination is strictly worse than either extreme: all of the network and operational complexity of microservices, none of the independence they're supposed to buy. If two services must always deploy together, they are one service that happens to run on two machines.</p>`\n    },\n    {\n      q:\"What is a BFF (Backend-for-Frontend), and when do you add one?\",\n      a:`<p>A BFF is a thin service that sits between a specific client (web, iOS, a partner API) and the backend services, shaped around exactly what that client needs \u2014 aggregating three internal service calls into one response, or reshaping a payload so the mobile app doesn't have to do that work on a slow connection. It earns its place once different clients start needing meaningfully different shapes of the same data; before that point, every client hitting the same general-purpose API is simpler and one less thing to deploy and own.</p>\n      <p>The discipline that keeps a BFF healthy: it owns <em>shaping</em>, not business rules. The moment domain logic starts accumulating in the BFF, you've built a second place where the rules live and they will drift apart from the services that own them.</p>`\n    },\n    {\n      q:\"How do service boundaries relate to team structure?\",\n      a:`<p>This is the question that separates a senior engineer's answer from an architect's. <strong>Conway's Law</strong> observes that a system's structure tends to mirror the communication structure of the organization that built it \u2014 so if you draw service boundaries that cut across team boundaries, every change to that service needs two teams to coordinate, and the architecture will quietly deform until it matches the org chart anyway.</p>\n      <ul>\n        <li><strong>Design the boundary so one team owns it end to end</strong> \u2014 the service, its data, its on-call, its deploys. A service owned by two teams has no owner.</li>\n        <li><strong>The \"inverse Conway manoeuvre\"</strong> is the deliberate version: if you want a particular architecture, structure the teams to match it first, and let the architecture follow.</li>\n        <li><strong>This is also the real microservices argument.</strong> The honest reason most organizations succeed with microservices isn't technical scaling \u2014 it's that independent deployability lets 30 teams ship without a shared release train. At 8 engineers, you don't have that problem, which is why the modular monolith is the right default at that size regardless of how much traffic you have.</li>\n        <li><strong>Cognitive load is the sizing constraint:</strong> the right number of services is the number one team can actually hold in their heads and operate at 3am \u2014 not a number derived from domain modelling in the abstract.</li>\n      </ul>`\n    }\n  ]\n},\n{\n  id:\"data\", title:\"Data Layer\",\n  questions:[\n    {\n      q:\"SQL or NoSQL \u2014 how do you actually decide?\",\n      a:`<p>The honest answer is \"most core application data is relational, and PostgreSQL is a strong default even at large scale\" \u2014 reach for NoSQL when a specific access pattern genuinely doesn't fit rows and joins, not because \"NoSQL scales better\" (a myth; a well-indexed, properly-replicated PostgreSQL instance handles far more scale than most systems ever reach).</p>\n      <table class=\"cmp\">\n        <tr><th></th><th>Reach for it when</th></tr>\n        <tr><td><strong>PostgreSQL (relational)</strong></td><td>Data has real relationships, you need transactions/ACID guarantees, or the access patterns aren't fully known yet \u2014 the default for core application data.</td></tr>\n        <tr><td><strong>DynamoDB / key-value</strong></td><td>Access is almost always \"get by known key,\" at massive scale, with predictable low-latency reads \u2014 a session store, a shopping cart, a feature-flag lookup.</td></tr>\n        <tr><td><strong>Elasticsearch / OpenSearch</strong></td><td>Full-text search, faceted filtering, relevance ranking \u2014 never the source of truth, always synced from the real database.</td></tr>\n        <tr><td><strong>A vector store (pgvector, etc.)</strong></td><td>The product has an AI/RAG feature needing similarity search over embeddings.</td></tr>\n        <tr><td><strong>Columnar / OLAP (ClickHouse, BigQuery)</strong></td><td>Analytical queries scanning millions of rows \u2014 keep these off the transactional database entirely rather than letting one slow dashboard query degrade the product.</td></tr>\n      </table>\n      <p><strong>The \"one database per service\" rule and its cost:</strong> in a microservices design, services owning their own data is what makes them independently deployable \u2014 but it's also what costs you joins. A query that was a two-table join in the monolith becomes an API call plus in-memory correlation. If you find yourself needing that constantly, the boundary is wrong, not the rule.</p>`\n    },\n    {\n      q:\"How do you scale the database as write and read volume grow?\",\n      a:`<p>In order of how early you reach for them: indexing correctly first (the single highest-leverage, lowest-cost fix), then read replicas (routes the 90%+ read traffic most apps have away from the primary, which stays dedicated to writes), then connection pooling (PgBouncer) so thousands of app-server connections don't exhaust the database's own connection limit, and only once all of that is saturated, sharding (splitting data across multiple database instances by a key like <code>user_id</code> hash) \u2014 a much bigger operational commitment that a system sized like the estimate above rarely justifies from day one.</p>\n      <p><strong>Common follow-up \u2014 what do you give up once you shard?</strong> Cross-shard queries and transactions become genuinely hard, and re-sharding later (changing the shard key or count) is a data migration on the hardest difficulty setting. Naming that trade-off explicitly is a stronger signal than presenting sharding as a free scaling lever, and it's exactly why it's named last on this list rather than jumped to first.</p>`\n    },\n    {\n      q:\"Read replicas introduce lag \u2014 how do you handle read-your-own-writes?\",\n      a:`<p>This is the follow-up that catches people who name read replicas without having operated them. Replication is asynchronous: the primary commits and acknowledges the write immediately, and the change reaches replicas milliseconds to seconds later. If the user updates their profile and the very next page load reads from a replica, they see their old data and conclude the save failed.</p>\n      <div class=\"diagram\">\n      <svg viewBox=\"0 0 780 250\" xmlns=\"http://www.w3.org/2000/svg\" style=\"width:100%;max-width:780px\">\n        <rect x=\"14\" y=\"30\" width=\"118\" height=\"44\" rx=\"8\" fill=\"var(--panel-2)\" stroke=\"var(--border)\"/>\n        <text x=\"73\" y=\"50\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">App server</text>\n        <text x=\"73\" y=\"65\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">write</text>\n        <path d=\"M136 52 L216 52\" stroke=\"var(--accent)\" stroke-width=\"2\" marker-end=\"url(#sdLagA)\"/>\n        <text x=\"176\" y=\"44\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--accent)\">1. commit</text>\n        <rect x=\"220\" y=\"26\" width=\"150\" height=\"52\" rx=\"8\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n        <text x=\"295\" y=\"48\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11.5\" fill=\"var(--text)\">Primary</text>\n        <text x=\"295\" y=\"64\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">accepts all writes</text>\n\n        <path d=\"M374 58 L446 92\" stroke=\"var(--gold)\" stroke-width=\"1.5\" stroke-dasharray=\"4\" marker-end=\"url(#sdLagG)\"/>\n        <path d=\"M374 66 L446 152\" stroke=\"var(--gold)\" stroke-width=\"1.5\" stroke-dasharray=\"4\" marker-end=\"url(#sdLagG)\"/>\n        <text x=\"418\" y=\"80\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--gold)\">2. async replication</text>\n\n        <rect x=\"450\" y=\"74\" width=\"150\" height=\"44\" rx=\"8\" fill=\"var(--panel-2)\" stroke=\"var(--border)\"/>\n        <text x=\"525\" y=\"94\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">Replica A</text>\n        <text x=\"525\" y=\"108\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">lag ~10\u2013500 ms</text>\n        <rect x=\"450\" y=\"134\" width=\"150\" height=\"44\" rx=\"8\" fill=\"var(--panel-2)\" stroke=\"var(--border)\"/>\n        <text x=\"525\" y=\"154\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">Replica B</text>\n        <text x=\"525\" y=\"168\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">lag ~10\u2013500 ms</text>\n\n        <path d=\"M604 96 L672 96\" stroke=\"var(--muted)\" stroke-width=\"1.5\" marker-end=\"url(#sdLagM)\"/>\n        <text x=\"678\" y=\"99\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--muted)\">reads</text>\n\n        <rect x=\"14\" y=\"196\" width=\"752\" height=\"42\" rx=\"8\" fill=\"var(--gold-soft)\" stroke=\"var(--gold)\" stroke-dasharray=\"4\"/>\n        <text x=\"28\" y=\"214\" font-family=\"var(--mono)\" font-size=\"9.5\" fill=\"var(--gold)\">THE BUG:</text>\n        <text x=\"96\" y=\"214\" font-family=\"var(--sans)\" font-size=\"10.5\" fill=\"var(--text)\">user saves profile (1) &rarr; next page load reads Replica A before (2) lands &rarr; sees stale data, thinks the save failed</text>\n        <text x=\"96\" y=\"230\" font-family=\"var(--sans)\" font-size=\"10.5\" fill=\"var(--muted)\">Any read issued inside the lag window can observe a state older than a write the same user already completed.</text>\n        <defs>\n          <marker id=\"sdLagA\" markerWidth=\"8\" markerHeight=\"8\" refX=\"6\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L6,3 L0,6 z\" fill=\"var(--accent)\"/></marker>\n          <marker id=\"sdLagG\" markerWidth=\"8\" markerHeight=\"8\" refX=\"6\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L6,3 L0,6 z\" fill=\"var(--gold)\"/></marker>\n          <marker id=\"sdLagM\" markerWidth=\"8\" markerHeight=\"8\" refX=\"6\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L6,3 L0,6 z\" fill=\"var(--muted)\"/></marker>\n        </defs>\n      </svg>\n      </div>\n      <p>Three standard fixes, in increasing order of precision:</p>\n      <ul>\n        <li><strong>Route reads to the primary for a short window after a write</strong> (e.g. that user's next few seconds, tracked in the session). Crude, trivially implemented, and covers the overwhelming majority of real cases.</li>\n        <li><strong>Sticky routing per user</strong> \u2014 that user's session reads from one specific replica, so they at least see monotonic state rather than bouncing between replicas at different lag positions.</li>\n        <li><strong>Wait for the write position (LSN)</strong> \u2014 the write returns the primary's log sequence number, and subsequent reads either wait for a replica to catch up to that LSN or fall back to the primary. Most precise, most plumbing.</li>\n      </ul>\n      <p><strong>The point to make explicitly:</strong> this is what \"eventual consistency\" costs in practice \u2014 not an abstract CAP-theorem discussion, but a concrete UX bug you have to design around. And it's per-workflow: a \"likes\" counter can be stale for ten seconds and nobody cares; a bank balance immediately after a transfer cannot.</p>`\n    },\n    {\n      q:\"How do you change a database schema at scale with zero downtime?\",\n      a:`<p>At millions of rows and a deploy that rolls out gradually, a schema change is a distributed-systems problem, not a migration script. Two constraints drive everything: <strong>old and new application code run simultaneously</strong> during any rolling deploy, and <strong>a lock on a large table is an outage</strong>.</p>\n      <p>The answer is the <strong>expand\u2013contract</strong> (parallel change) pattern \u2014 never change a column, always add and remove in separate deploys:</p>\n      <pre><code>// Renaming users.name -> users.full_name, safely:\n\n// Deploy 1 \u2014 EXPAND: add the new column, nullable. Old code unaffected.\nALTER TABLE users ADD COLUMN full_name text;\n\n// Deploy 2 \u2014 write to both. Backfill old rows in batches, not one statement:\nUPDATE users SET full_name = name WHERE id BETWEEN $1 AND $2;  // chunked, throttled\n\n// Deploy 3 \u2014 read from full_name, keep writing both (safe rollback point)\n\n// Deploy 4 \u2014 CONTRACT: nothing reads or writes the old column anymore\nALTER TABLE users DROP COLUMN name;</code></pre>\n      <ul>\n        <li><strong>Every lock is a hazard.</strong> <code>CREATE INDEX CONCURRENTLY</code>, not <code>CREATE INDEX</code>; adding a <code>NOT NULL</code> column with a default rewrites the table on older Postgres versions; adding a foreign key takes a lock that blocks writes while it validates \u2014 add it <code>NOT VALID</code>, then <code>VALIDATE CONSTRAINT</code> separately.</li>\n        <li><strong>Set a lock timeout</strong> (<code>SET lock_timeout = '3s'</code>) on migrations. Without it, a migration that can't acquire its lock queues behind a long-running query and then blocks every subsequent query behind <em>itself</em> \u2014 a brief lock contention turns into a full outage.</li>\n        <li><strong>Backfills are batched and throttled</strong>, with a pause between chunks, so replication lag doesn't blow out while millions of rows rewrite.</li>\n        <li><strong>The rollback question:</strong> each step must be independently revertible. Expand\u2013contract is valuable precisely because there's no point where rolling back the app breaks the database.</li>\n      </ul>`\n    }\n  ]\n},\n{\n  id:\"caching\", title:\"Caching Strategy\",\n  questions:[\n    {\n      q:\"What does caching look like across the whole stack, end to end?\",\n      a:`<div class=\"diagram\">\n      <svg viewBox=\"0 0 800 196\" xmlns=\"http://www.w3.org/2000/svg\" style=\"width:100%;max-width:800px\">\n        <text x=\"400\" y=\"26\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11.5\" fill=\"var(--muted)\">A request only reaches the database if every layer to its left misses</text>\n        <rect x=\"18\" y=\"52\" width=\"136\" height=\"58\" rx=\"10\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n        <text x=\"86\" y=\"77\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11.5\" fill=\"var(--text)\">Browser cache</text>\n        <text x=\"86\" y=\"94\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">~0 ms</text>\n        <rect x=\"174\" y=\"52\" width=\"136\" height=\"58\" rx=\"10\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n        <text x=\"242\" y=\"77\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11.5\" fill=\"var(--text)\">CDN edge</text>\n        <text x=\"242\" y=\"94\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">~10\u201330 ms</text>\n        <rect x=\"330\" y=\"52\" width=\"136\" height=\"58\" rx=\"10\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n        <text x=\"398\" y=\"77\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11.5\" fill=\"var(--text)\">API Gateway</text>\n        <text x=\"398\" y=\"94\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">response cache</text>\n        <rect x=\"486\" y=\"52\" width=\"136\" height=\"58\" rx=\"10\" fill=\"var(--gold-soft)\" stroke=\"var(--gold)\"/>\n        <text x=\"554\" y=\"77\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11.5\" fill=\"var(--text)\">Redis</text>\n        <text x=\"554\" y=\"94\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">~1 ms</text>\n        <rect x=\"642\" y=\"52\" width=\"140\" height=\"58\" rx=\"10\" fill=\"var(--panel-2)\" stroke=\"var(--border)\"/>\n        <text x=\"712\" y=\"77\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11.5\" fill=\"var(--text)\">Database</text>\n        <text x=\"712\" y=\"94\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">~1\u201310 ms + load</text>\n        <path d=\"M156 81 L170 81\" stroke=\"var(--muted)\" stroke-width=\"1.5\" marker-end=\"url(#sdCacheM)\"/>\n        <path d=\"M312 81 L326 81\" stroke=\"var(--muted)\" stroke-width=\"1.5\" marker-end=\"url(#sdCacheM)\"/>\n        <path d=\"M468 81 L482 81\" stroke=\"var(--muted)\" stroke-width=\"1.5\" marker-end=\"url(#sdCacheM)\"/>\n        <path d=\"M624 81 L638 81\" stroke=\"var(--muted)\" stroke-width=\"1.5\" marker-end=\"url(#sdCacheM)\"/>\n        <text x=\"86\" y=\"132\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">static &middot; immutable</text>\n        <text x=\"242\" y=\"132\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">public &middot; SWR</text>\n        <text x=\"398\" y=\"132\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">non-personalized</text>\n        <text x=\"554\" y=\"132\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">hot &middot; computed</text>\n        <text x=\"712\" y=\"132\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">source of truth</text>\n        <text x=\"400\" y=\"168\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"10.5\" fill=\"var(--muted)\">Each layer is cheaper and closer to the user than the one behind it \u2014 every hit removes both a round-trip and a unit of backend load.</text>\n        <defs><marker id=\"sdCacheM\" markerWidth=\"8\" markerHeight=\"8\" refX=\"6\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L6,3 L0,6 z\" fill=\"var(--muted)\"/></marker></defs>\n      </svg>\n      </div>\n      <p>The strongest way to answer this is the waterfall above: a request only reaches the database once it has missed at every faster layer in front of it, and each layer exists because it's progressively cheaper and closer to the user than the one behind it. Browser cache and the CDN handle anything static or public; the API Gateway can cache whole responses for cacheable, non-personalized endpoints; Redis sits in front of the database for computed aggregates, session data, and anything read far more often than it changes; and the database's own shared buffers are the last line before disk I/O.</p>\n      <ul>\n        <li><strong>Invalidation is the actually hard part</strong> \u2014 a short TTL is the simple, safe default (bounded staleness, no explicit invalidation logic); explicit invalidation-on-write is fresher but means finding every code path that changes the underlying data.</li>\n        <li><strong>Cache stampede</strong> \u2014 when a hot key expires, many concurrent requests can miss simultaneously and hammer the database at once; fix with a lock so only one request repopulates the cache, or <code>stale-while-revalidate</code> so everyone gets the stale value while one background request refreshes it.</li>\n        <li><strong>The metric that tells you it's working</strong> is cache hit ratio per layer. An unmeasured cache is an assumption: a 40% CDN hit ratio usually means a cache key varying on something it shouldn't (a tracking query param, a <code>Vary</code> header that's too broad), and that's invisible until someone looks.</li>\n        <li><strong>Design for the cache being empty.</strong> After a Redis failover or a deploy that changes key prefixes, every request misses at once. If the database can't survive that, the cache isn't an optimization \u2014 it's a load-bearing dependency with no failure plan.</li>\n      </ul>`\n    }\n  ]\n},\n{\n  id:\"async\", title:\"Async & Messaging\",\n  questions:[\n    {\n      q:\"What moves off the request/response path, and how?\",\n      a:`<p>Anything that doesn't need to complete before the user gets a response \u2014 sending a notification, generating a document, updating a search index, calling a slow third-party API \u2014 goes through a queue instead of holding an HTTP request open. The producer publishes an event and returns immediately; one or more consumers process it independently, on their own schedule, and a failure in one consumer never blocks the request that triggered it or any other consumer.</p>\n      <ul>\n        <li><strong>Queue vs. pub/sub:</strong> a queue (SQS) is point-to-point \u2014 one worker per message, good for distributing units of work across a pool; pub/sub (SNS, or a topic generally) is one-to-many \u2014 good for \"several unrelated systems all need to react to this event.\" They combine constantly: publish once to a topic, fan out to several queues, one per consumer.</li>\n        <li><strong>Delivery semantics:</strong> most brokers give you at-least-once, which means duplicates are not an exception \u2014 they're normal operation. Consumers must be idempotent; effectively-once is something you build with idempotency keys at the consumer, not something the broker hands you.</li>\n        <li><strong>Ordering is not free:</strong> strict ordering typically costs you parallelism (a single consumer per ordered partition/group). Ask whether you need global ordering or only per-entity ordering \u2014 the latter is usually enough, and partitioning by entity ID gets it cheaply.</li>\n        <li><strong>Failure handling:</strong> a dead-letter queue catches messages that exceed their retry limit, so they stop blocking the main queue instead of being silently dropped. DLQ depth is an alertable signal, and a replay path out of it is part of the design, not an afterthought.</li>\n      </ul>`\n    },\n    {\n      q:\"How do you keep data consistent across services without a distributed transaction?\",\n      a:`<p>The Saga pattern: a sequence of local transactions, one per service, where every step has a matching compensating transaction that undoes its work if a later step fails. There is no cross-service rollback available \u2014 you don't undo, you move <em>forward</em> into a corrected state.</p>\n      <div class=\"diagram\">\n      <svg viewBox=\"0 0 790 250\" xmlns=\"http://www.w3.org/2000/svg\" style=\"width:100%;max-width:790px\">\n        <text x=\"14\" y=\"22\" font-family=\"var(--mono)\" font-size=\"9.5\" fill=\"var(--accent)\">FORWARD PATH &rarr;</text>\n        <rect x=\"14\" y=\"34\" width=\"150\" height=\"46\" rx=\"8\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n        <text x=\"89\" y=\"54\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">1 &middot; Create order</text>\n        <text x=\"89\" y=\"69\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">order svc &middot; ok</text>\n        <path d=\"M168 57 L196 57\" stroke=\"var(--accent)\" stroke-width=\"1.5\" marker-end=\"url(#sdSagaA)\"/>\n        <rect x=\"200\" y=\"34\" width=\"150\" height=\"46\" rx=\"8\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n        <text x=\"275\" y=\"54\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">2 &middot; Charge payment</text>\n        <text x=\"275\" y=\"69\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">payment svc &middot; ok</text>\n        <path d=\"M354 57 L382 57\" stroke=\"var(--accent)\" stroke-width=\"1.5\" marker-end=\"url(#sdSagaA)\"/>\n        <rect x=\"386\" y=\"34\" width=\"150\" height=\"46\" rx=\"8\" fill=\"var(--panel-2)\" stroke=\"var(--danger)\" stroke-width=\"2\"/>\n        <text x=\"461\" y=\"54\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">3 &middot; Reserve stock</text>\n        <text x=\"461\" y=\"69\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--danger)\">inventory svc &middot; FAILS</text>\n        <path d=\"M540 57 L568 57\" stroke=\"var(--border)\" stroke-width=\"1.5\" stroke-dasharray=\"3\"/>\n        <rect x=\"572\" y=\"34\" width=\"150\" height=\"46\" rx=\"8\" fill=\"none\" stroke=\"var(--border)\" stroke-dasharray=\"4\"/>\n        <text x=\"647\" y=\"54\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--muted)\">4 &middot; Schedule ship</text>\n        <text x=\"647\" y=\"69\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">never runs</text>\n\n        <path d=\"M461 86 L461 110\" stroke=\"var(--danger)\" stroke-width=\"2\" marker-end=\"url(#sdSagaD)\"/>\n        <text x=\"472\" y=\"104\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--danger)\">trigger compensation</text>\n\n        <text x=\"14\" y=\"140\" font-family=\"var(--mono)\" font-size=\"9.5\" fill=\"var(--gold)\">&larr; COMPENSATION PATH (runs in reverse)</text>\n        <rect x=\"200\" y=\"152\" width=\"150\" height=\"46\" rx=\"8\" fill=\"var(--gold-soft)\" stroke=\"var(--gold)\"/>\n        <text x=\"275\" y=\"172\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">Refund payment</text>\n        <text x=\"275\" y=\"187\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">undoes step 2</text>\n        <rect x=\"14\" y=\"152\" width=\"150\" height=\"46\" rx=\"8\" fill=\"var(--gold-soft)\" stroke=\"var(--gold)\"/>\n        <text x=\"89\" y=\"172\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">Cancel order</text>\n        <text x=\"89\" y=\"187\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">undoes step 1</text>\n        <path d=\"M386 175 L356 175\" stroke=\"var(--gold)\" stroke-width=\"1.5\" marker-end=\"url(#sdSagaG)\"/>\n        <path d=\"M196 175 L170 175\" stroke=\"var(--gold)\" stroke-width=\"1.5\" marker-end=\"url(#sdSagaG)\"/>\n\n        <text x=\"14\" y=\"228\" font-family=\"var(--sans)\" font-size=\"10.5\" fill=\"var(--muted)\">Each compensation is business logic you write, not a database rollback \u2014 and it must be idempotent and retried until it succeeds,</text>\n        <text x=\"14\" y=\"242\" font-family=\"var(--sans)\" font-size=\"10.5\" fill=\"var(--muted)\">because a failed compensation leaves the system in exactly the inconsistent state the saga existed to prevent.</text>\n        <defs>\n          <marker id=\"sdSagaA\" markerWidth=\"8\" markerHeight=\"8\" refX=\"6\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L6,3 L0,6 z\" fill=\"var(--accent)\"/></marker>\n          <marker id=\"sdSagaG\" markerWidth=\"8\" markerHeight=\"8\" refX=\"6\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L6,3 L0,6 z\" fill=\"var(--gold)\"/></marker>\n          <marker id=\"sdSagaD\" markerWidth=\"8\" markerHeight=\"8\" refX=\"6\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L6,3 L0,6 z\" fill=\"var(--danger)\"/></marker>\n        </defs>\n      </svg>\n      </div>\n      <ul>\n        <li><strong>Orchestration vs. choreography:</strong> an orchestrator (Temporal, Step Functions) holds the flow explicitly in one place \u2014 readable, debuggable, easy to add steps to, at the cost of a central component. Choreography has each service react to the previous one's event \u2014 loosely coupled and fine for 3\u20134 steps, but the overall flow exists nowhere in the code and becomes untraceable as it grows. Default to orchestration once a workflow has more than a few steps or needs to be observable.</li>\n        <li><strong>Sagas are not atomic, and you must say so:</strong> there is a window where the order exists and the payment is already charged. The system is temporarily inconsistent by design, and the UX has to account for that (\"processing\u2026\"), not pretend the operation was instantaneous.</li>\n      </ul>`\n    },\n    {\n      q:\"How does a service publish an event reliably when it also has to write to its own database?\",\n      a:`<p>This is the <strong>dual-write problem</strong>, and it's the most common silent data-loss bug in event-driven systems. Writing to the database and publishing to the broker are two separate systems with no shared transaction \u2014 if the process crashes between them, the database has the order and the rest of the world never hears about it. No amount of retrying fixes this, because the crash destroyed the intent to publish.</p>\n      <div class=\"diagram\">\n      <svg viewBox=\"0 0 790 236\" xmlns=\"http://www.w3.org/2000/svg\" style=\"width:100%;max-width:790px\">\n        <rect x=\"14\" y=\"30\" width=\"252\" height=\"130\" rx=\"10\" fill=\"none\" stroke=\"var(--accent)\" stroke-width=\"2\" stroke-dasharray=\"5\"/>\n        <text x=\"140\" y=\"50\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"9.5\" fill=\"var(--accent)\">ONE LOCAL TRANSACTION</text>\n        <rect x=\"34\" y=\"62\" width=\"212\" height=\"38\" rx=\"7\" fill=\"var(--panel-2)\" stroke=\"var(--border)\"/>\n        <text x=\"140\" y=\"85\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"10\" fill=\"var(--text)\">INSERT INTO orders ...</text>\n        <rect x=\"34\" y=\"108\" width=\"212\" height=\"38\" rx=\"7\" fill=\"var(--gold-soft)\" stroke=\"var(--gold)\"/>\n        <text x=\"140\" y=\"131\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"10\" fill=\"var(--text)\">INSERT INTO outbox ...</text>\n        <text x=\"140\" y=\"176\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"10\" fill=\"var(--muted)\">Both commit, or neither does.</text>\n\n        <path d=\"M270 95 L316 95\" stroke=\"var(--muted)\" stroke-width=\"1.5\" marker-end=\"url(#sdOutM)\"/>\n        <rect x=\"320\" y=\"60\" width=\"150\" height=\"70\" rx=\"10\" fill=\"var(--panel-2)\" stroke=\"var(--border)\"/>\n        <text x=\"395\" y=\"84\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">PostgreSQL</text>\n        <text x=\"395\" y=\"102\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">orders</text>\n        <text x=\"395\" y=\"117\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--gold)\">outbox (unpublished)</text>\n\n        <path d=\"M474 95 L520 95\" stroke=\"var(--gold)\" stroke-width=\"1.5\" marker-end=\"url(#sdOutG)\"/>\n        <text x=\"497\" y=\"87\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--gold)\">poll / CDC</text>\n        <rect x=\"524\" y=\"60\" width=\"118\" height=\"70\" rx=\"10\" fill=\"var(--gold-soft)\" stroke=\"var(--gold)\"/>\n        <text x=\"583\" y=\"90\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">Relay</text>\n        <text x=\"583\" y=\"106\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">Debezium</text>\n        <path d=\"M646 95 L688 95\" stroke=\"var(--accent)\" stroke-width=\"1.5\" marker-end=\"url(#sdOutA)\"/>\n        <rect x=\"692\" y=\"60\" width=\"84\" height=\"70\" rx=\"10\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n        <text x=\"734\" y=\"90\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">Broker</text>\n        <text x=\"734\" y=\"106\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">Kafka/SNS</text>\n\n        <rect x=\"320\" y=\"184\" width=\"456\" height=\"40\" rx=\"8\" fill=\"var(--panel-2)\" stroke=\"var(--border)\"/>\n        <text x=\"334\" y=\"201\" font-family=\"var(--sans)\" font-size=\"10.5\" fill=\"var(--text)\">A crash anywhere to the right of the commit loses nothing \u2014 the event is already durable in the outbox</text>\n        <text x=\"334\" y=\"216\" font-family=\"var(--sans)\" font-size=\"10.5\" fill=\"var(--muted)\">and the relay picks it up on restart. The cost: at-least-once delivery, so consumers must be idempotent.</text>\n        <defs>\n          <marker id=\"sdOutM\" markerWidth=\"8\" markerHeight=\"8\" refX=\"6\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L6,3 L0,6 z\" fill=\"var(--muted)\"/></marker>\n          <marker id=\"sdOutG\" markerWidth=\"8\" markerHeight=\"8\" refX=\"6\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L6,3 L0,6 z\" fill=\"var(--gold)\"/></marker>\n          <marker id=\"sdOutA\" markerWidth=\"8\" markerHeight=\"8\" refX=\"6\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L6,3 L0,6 z\" fill=\"var(--accent)\"/></marker>\n        </defs>\n      </svg>\n      </div>\n      <p>The <strong>Transactional Outbox</strong> collapses two writes into one: the event row goes into an <code>outbox</code> table inside the same transaction as the business write, so they commit atomically. A separate relay \u2014 either polling the table, or reading the write-ahead log via change-data-capture (Debezium) \u2014 publishes unpublished rows and marks them sent.</p>\n      <p><strong>The trade you're accepting:</strong> if the relay crashes between \"delivered to broker\" and \"marked as published,\" the event is published twice. That's the at-least-once guarantee again, and it's why consumer idempotency is a hard requirement rather than a nice-to-have. What you get in exchange is the guarantee that matters more: the event is <em>never silently lost</em>.</p>`\n    }\n  ]\n},\n{\n  id:\"deploy\", title:\"Deployment & Infrastructure\",\n  questions:[\n    {\n      q:\"What does the path from a merged PR to production look like?\",\n      a:`<p>Code is packaged into a container image, pushed to a registry, and deployed by an orchestrator (ECS or Kubernetes) that handles scheduling, health checks, and restarts \u2014 with the infrastructure itself defined as code (Terraform or CloudFormation) so an environment can be reproduced or torn down deterministically rather than hand-configured. The pipeline promotes a build through environments in order \u2014 dev &rarr; staging &rarr; production \u2014 gated by automated tests at minimum, and often a manual approval before the final production step.</p>\n      <ul>\n        <li><strong>Rollout strategy:</strong> canary (a small % of traffic first, ramp up while watching error rate and latency against a control group) for anything with real user-facing risk; blue-green when instant rollback matters more than gradual exposure; rolling updates as the default for routine, lower-risk deploys.</li>\n        <li><strong>Separate deploy from release.</strong> This is the senior refinement: ship the code dark behind a feature flag, then turn it on for a cohort independently of the deploy. Rollback becomes a config change measured in seconds rather than a redeploy measured in minutes, and the blast radius of a bad feature is a flag toggle instead of an incident.</li>\n        <li><strong>Migrations deploy separately from code</strong>, and always in the expand direction first (see the schema-migration question) \u2014 a deploy that ships a migration and the code depending on it in the same step has no safe rollback.</li>\n        <li><strong>Why this matters at \"millions of users\" scale specifically:</strong> a bad deploy at this scale isn't a minor incident \u2014 canary plus flags is what turns \"one engineer's mistake\" into \"a blip affecting 1% of traffic for two minutes.\"</li>\n      </ul>`\n    }\n  ]\n},\n{\n  id:\"reliability\", title:\"Reliability & Observability\",\n  questions:[\n    {\n      q:\"How do you design for things going wrong, not just for the happy path?\",\n      a:`<p>Every outbound call between services gets the same layered defense, applied in a specific order \u2014 each layer catches what the one inside it couldn't:</p>\n      <div class=\"diagram\">\n      <svg viewBox=\"0 0 700 260\" xmlns=\"http://www.w3.org/2000/svg\" style=\"width:100%;max-width:700px\">\n        <rect x=\"14\" y=\"20\" width=\"672\" height=\"226\" rx=\"14\" fill=\"var(--gold-soft)\" stroke=\"var(--gold)\" opacity=\"0.9\"/>\n        <text x=\"30\" y=\"40\" font-family=\"var(--mono)\" font-size=\"9.5\" fill=\"var(--gold)\">BULKHEAD &mdash; caps how much of your capacity this dependency can ever consume</text>\n        <rect x=\"40\" y=\"50\" width=\"620\" height=\"180\" rx=\"12\" fill=\"var(--panel)\" stroke=\"var(--danger)\"/>\n        <text x=\"56\" y=\"70\" font-family=\"var(--mono)\" font-size=\"9.5\" fill=\"var(--danger)\">CIRCUIT BREAKER &mdash; after N failures, stop calling entirely for a cooldown</text>\n        <rect x=\"66\" y=\"80\" width=\"568\" height=\"136\" rx=\"10\" fill=\"var(--panel-2)\" stroke=\"var(--accent)\"/>\n        <text x=\"82\" y=\"100\" font-family=\"var(--mono)\" font-size=\"9.5\" fill=\"var(--accent)\">RETRY &mdash; exponential backoff + jitter, bounded attempts</text>\n        <rect x=\"92\" y=\"110\" width=\"516\" height=\"92\" rx=\"9\" fill=\"var(--panel)\" stroke=\"var(--muted)\"/>\n        <text x=\"108\" y=\"130\" font-family=\"var(--mono)\" font-size=\"9.5\" fill=\"var(--muted)\">TIMEOUT &mdash; never wait forever; bound every single attempt</text>\n        <rect x=\"120\" y=\"142\" width=\"460\" height=\"46\" rx=\"8\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n        <text x=\"350\" y=\"163\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"12\" fill=\"var(--text)\">the actual outbound call</text>\n        <text x=\"350\" y=\"179\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--muted)\">payment API &middot; inventory service &middot; third-party webhook</text>\n        <text x=\"350\" y=\"246\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"10.5\" fill=\"var(--muted)\">Outside all of it: a FALLBACK &mdash; cached value, sensible default, or degraded mode \u2014 so the user gets something, not an error.</text>\n      </svg>\n      </div>\n      <p>Order matters, and getting it wrong makes things worse rather than better: a long timeout combined with aggressive retries multiplies load on a dependency that's already struggling, and a circuit breaker with no fallback simply converts \"slow\" into \"broken\" faster. For availability specifically: Multi-AZ (redundant infrastructure across physically separate datacenters in the same region, with automatic failover) covers the failure mode that actually happens most often; multi-region is reserved for when a full-region outage is genuinely unacceptable, given the real cost and consistency trade-offs it adds.</p>\n      <ul>\n        <li><strong>Retries are only safe on idempotent operations</strong> \u2014 which is exactly why the idempotency-key question above is a prerequisite for this one, not a separate topic.</li>\n        <li><strong>SLOs and error budgets</strong> turn \"how reliable should this be\" from a vague aspiration into a number the team is accountable to, and a concrete trigger for when to prioritize reliability work over features.</li>\n        <li><strong>Golden signals</strong> (latency, traffic, errors, saturation) are what every service's dashboard should be built around \u2014 CPU and memory are useful for debugging but rarely tell you a user is actually affected.</li>\n        <li><strong>Load shedding</strong> is the layer people forget: when saturated, deliberately reject low-priority traffic fast rather than degrading every request equally. Serving 90% of users well beats serving 100% of them unusably slowly.</li>\n      </ul>`\n    },\n    {\n      q:\"How do you know something is wrong before a user reports it?\",\n      a:`<p>Three complementary signals, not one: logs for deep-diving a specific incident after the fact, metrics for cheap, always-on alerting on trends, and distributed traces for following one slow or failed request across every service it touched. A trace context (trace ID + parent span ID) generated at the very first entry point and propagated on every outbound call \u2014 as the W3C <code>traceparent</code> header, or in a message's metadata for async paths \u2014 is what lets the full picture reassemble later even though no single service ever saw the whole request. OpenTelemetry is the vendor-neutral way to instrument this once and export anywhere.</p>\n      <ul>\n        <li><strong>Alert on symptoms, not causes.</strong> \"Checkout success rate dropped below 99%\" catches failure modes nobody predicted; \"CPU above 80%\" fires constantly without a user ever noticing. Every alert should map to a runbook and to something a user would actually feel.</li>\n        <li><strong>Stamp every log line with the trace ID</strong>, so \"why was this request slow\" (the trace) and \"what exactly happened inside that span\" (the logs) are one pivot apart instead of a timestamp-matching exercise across services.</li>\n        <li><strong>Watch the client, not just the server.</strong> Server-side p99 looks perfect while a JS error blocks checkout for every Safari user \u2014 real-user monitoring is the only place that failure is visible.</li>\n      </ul>`\n    }\n  ]\n},\n{\n  id:\"security\", title:\"Security\",\n  questions:[\n    {\n      q:\"What does defense-in-depth look like for a system at this scale?\",\n      a:`<p>No single control is trusted to be the only thing standing between an attacker and the data \u2014 each layer assumes the ones around it could fail. Encryption in transit (TLS everywhere, including internal service-to-service traffic via a service mesh's mTLS) and at rest (database and object storage encryption, keys managed by a dedicated service like KMS, never embedded in application config); secrets in a dedicated manager, never in environment variables checked into source or baked into an image; least-privilege IAM so a compromised service can only reach what it specifically needs, not the whole account; a WAF at the edge filtering common attack patterns before they reach the application; and rate limiting doing double duty as both a reliability control and an abuse/credential-stuffing defense.</p>\n      <ul>\n        <li>Input validation and output encoding at every trust boundary \u2014 never trust that a request reaching an internal service has already been validated by whatever called it.</li>\n        <li><strong>Supply chain is now a first-class attack surface</strong>, and a 2026-current answer should say so: pinned/lockfiled dependencies, automated vulnerability scanning in CI, image scanning before push, and an SBOM for anything shipped to customers. Most teams' largest quantity of untrusted code is their <code>node_modules</code>, not their own source.</li>\n        <li>For a system handling sensitive data specifically (health, financial, PII): audit logging on every access to that data, not just on writes \u2014 \"who looked at this record and when\" is often a compliance requirement, not an optional nice-to-have. Pair it with data retention and deletion policies, since GDPR/CCPA make \"we keep everything forever\" a liability rather than an asset.</li>\n        <li><strong>Assume breach:</strong> the design question isn't only \"how do we keep them out\" but \"when a service is compromised, how far can they get, and how fast do we notice.\" That reframing is what separates a security checklist from a security posture.</li>\n      </ul>`\n    }\n  ]\n},\n{\n  id:\"together\", title:\"Putting It All Together\",\n  questions:[\n    {\n      q:\"How does everything above fit together into one diagram?\",\n      a:`<div class=\"diagram\">\n      <svg viewBox=\"0 0 820 380\" xmlns=\"http://www.w3.org/2000/svg\" style=\"width:100%;max-width:820px\">\n        <rect x=\"14\" y=\"24\" width=\"150\" height=\"42\" rx=\"8\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n        <text x=\"89\" y=\"50\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11.5\" fill=\"var(--text)\">Web / Mobile</text>\n        <rect x=\"14\" y=\"92\" width=\"150\" height=\"42\" rx=\"8\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n        <text x=\"89\" y=\"112\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">CDN / Edge</text>\n        <text x=\"89\" y=\"126\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8\" fill=\"var(--muted)\">static &middot; SWR &middot; WAF</text>\n        <rect x=\"14\" y=\"160\" width=\"150\" height=\"42\" rx=\"8\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n        <text x=\"89\" y=\"180\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">API Gateway</text>\n        <text x=\"89\" y=\"194\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8\" fill=\"var(--muted)\">authn &middot; rate limit</text>\n\n        <rect x=\"224\" y=\"112\" width=\"144\" height=\"42\" rx=\"8\" fill=\"var(--gold-soft)\" stroke=\"var(--gold)\"/>\n        <text x=\"296\" y=\"138\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">Member service</text>\n        <rect x=\"224\" y=\"166\" width=\"144\" height=\"42\" rx=\"8\" fill=\"var(--gold-soft)\" stroke=\"var(--gold)\"/>\n        <text x=\"296\" y=\"192\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">Claims service</text>\n        <rect x=\"224\" y=\"220\" width=\"144\" height=\"42\" rx=\"8\" fill=\"var(--gold-soft)\" stroke=\"var(--gold)\"/>\n        <text x=\"296\" y=\"246\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">Document service</text>\n\n        <rect x=\"426\" y=\"84\" width=\"150\" height=\"40\" rx=\"8\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n        <text x=\"501\" y=\"109\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">Redis cache</text>\n        <rect x=\"426\" y=\"140\" width=\"150\" height=\"40\" rx=\"8\" fill=\"var(--panel-2)\" stroke=\"var(--border)\"/>\n        <text x=\"501\" y=\"165\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">Postgres primary</text>\n        <rect x=\"426\" y=\"196\" width=\"150\" height=\"40\" rx=\"8\" fill=\"var(--panel-2)\" stroke=\"var(--border)\"/>\n        <text x=\"501\" y=\"215\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">Read replicas</text>\n        <text x=\"501\" y=\"229\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8\" fill=\"var(--muted)\">lag-aware reads</text>\n        <rect x=\"426\" y=\"252\" width=\"150\" height=\"40\" rx=\"8\" fill=\"var(--gold-soft)\" stroke=\"var(--gold)\"/>\n        <text x=\"501\" y=\"277\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">Queue + outbox</text>\n        <rect x=\"628\" y=\"252\" width=\"158\" height=\"40\" rx=\"8\" fill=\"var(--gold-soft)\" stroke=\"var(--gold)\"/>\n        <text x=\"707\" y=\"277\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">Async workers</text>\n\n        <rect x=\"620\" y=\"24\" width=\"172\" height=\"96\" rx=\"10\" fill=\"none\" stroke=\"var(--muted)\" stroke-width=\"1.5\" stroke-dasharray=\"4\"/>\n        <text x=\"706\" y=\"46\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">Observability</text>\n        <text x=\"706\" y=\"64\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">logs &middot; metrics &middot; traces</text>\n        <text x=\"706\" y=\"80\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">SLOs &middot; alerts</text>\n        <text x=\"706\" y=\"100\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8\" fill=\"var(--muted)\">every layer reports here</text>\n\n        <path d=\"M89 68 L89 90\" stroke=\"var(--accent)\" stroke-width=\"1.5\" marker-end=\"url(#sdArchA)\"/>\n        <path d=\"M89 136 L89 158\" stroke=\"var(--accent)\" stroke-width=\"1.5\" marker-end=\"url(#sdArchA)\"/>\n        <path d=\"M168 178 L220 136\" stroke=\"var(--muted)\" stroke-width=\"1.5\" marker-end=\"url(#sdArchM)\"/>\n        <path d=\"M168 181 L220 187\" stroke=\"var(--muted)\" stroke-width=\"1.5\" marker-end=\"url(#sdArchM)\"/>\n        <path d=\"M168 184 L220 238\" stroke=\"var(--muted)\" stroke-width=\"1.5\" marker-end=\"url(#sdArchM)\"/>\n        <path d=\"M372 130 L422 108\" stroke=\"var(--muted)\" stroke-width=\"1.5\" marker-end=\"url(#sdArchM)\"/>\n        <path d=\"M372 140 L422 158\" stroke=\"var(--muted)\" stroke-width=\"1.5\" marker-end=\"url(#sdArchM)\"/>\n        <path d=\"M372 196 L422 210\" stroke=\"var(--muted)\" stroke-width=\"1.5\" marker-end=\"url(#sdArchM)\"/>\n        <path d=\"M372 244 L422 268\" stroke=\"var(--gold)\" stroke-width=\"1.5\" marker-end=\"url(#sdArchG)\"/>\n        <path d=\"M580 272 L624 272\" stroke=\"var(--gold)\" stroke-width=\"1.5\" marker-end=\"url(#sdArchG)\"/>\n        <path d=\"M501 182 L501 194\" stroke=\"var(--muted)\" stroke-width=\"1.5\" stroke-dasharray=\"3\" marker-end=\"url(#sdArchM)\"/>\n        <text x=\"512\" y=\"192\" font-family=\"var(--mono)\" font-size=\"8\" fill=\"var(--muted)\">replication</text>\n\n        <text x=\"14\" y=\"330\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--accent)\">NORTH-SOUTH</text>\n        <text x=\"110\" y=\"330\" font-family=\"var(--sans)\" font-size=\"10\" fill=\"var(--muted)\">client &rarr; edge &rarr; gateway &rarr; service</text>\n        <text x=\"330\" y=\"330\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--gold)\">ASYNC</text>\n        <text x=\"385\" y=\"330\" font-family=\"var(--sans)\" font-size=\"10\" fill=\"var(--muted)\">service &rarr; outbox &rarr; queue &rarr; worker</text>\n        <text x=\"14\" y=\"352\" font-family=\"var(--sans)\" font-size=\"10\" fill=\"var(--muted)\">Deliberately absent at this scale: sharded database, service mesh, multi-region active-active. Naming what you left out is as strong a signal as what you included.</text>\n        <defs>\n          <marker id=\"sdArchA\" markerWidth=\"8\" markerHeight=\"8\" refX=\"6\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L6,3 L0,6 z\" fill=\"var(--accent)\"/></marker>\n          <marker id=\"sdArchM\" markerWidth=\"8\" markerHeight=\"8\" refX=\"6\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L6,3 L0,6 z\" fill=\"var(--muted)\"/></marker>\n          <marker id=\"sdArchG\" markerWidth=\"8\" markerHeight=\"8\" refX=\"6\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L6,3 L0,6 z\" fill=\"var(--gold)\"/></marker>\n        </defs>\n      </svg>\n      </div>\n      <p>The diagram is what this whole design converges on: client traffic hits the CDN and gateway first (edge caching, auth, rate limiting), fans out to independently-scalable services, each reading through Redis before Postgres and writing to the primary with replicas absorbing read load, anything non-blocking leaving via the outbox and queue to async workers, and every layer reporting into one observability stack rather than each keeping an island of logs nobody correlates.</p>`\n    },\n    {\n      q:\"How do you reason about cost, not just correctness?\",\n      a:`<p>Cost is where a senior answer becomes an architect answer, because every design decision above has a bill attached and most candidates never mention it. The architecture that is technically elegant and 4&times; the necessary spend is not a good architecture.</p>\n      <ul>\n        <li><strong>Know roughly where the money goes.</strong> For a system of this shape it's usually, in order: compute (always-on instances sized for peak), managed database (the primary plus every replica, often the single largest line), data transfer (cross-AZ and egress \u2014 quietly enormous and almost never estimated), then storage, which is typically cheap by comparison.</li>\n        <li><strong>Cross-AZ traffic is billed.</strong> A chatty microservice mesh where every call crosses an availability zone converts your architecture diagram directly into a monthly invoice \u2014 one more reason a modular monolith is cheaper than it looks.</li>\n        <li><strong>Match the commitment to the workload:</strong> reserved or savings-plan pricing for the steady baseline, on-demand for the variable layer, spot for interruptible batch/async work. Autoscaling that actually scales <em>down</em> at night is often the fastest meaningful saving available.</li>\n        <li><strong>Caching is a cost control, not only a latency control.</strong> Every cache hit is a database query you didn't pay for and capacity you didn't have to provision \u2014 which is frequently the more persuasive argument to a business stakeholder than the millisecond savings.</li>\n        <li><strong>Make it visible:</strong> tag resources by team/service so spend is attributable, and treat a cost regression the way you'd treat a latency regression. Unattributed cloud spend never gets optimized, because nobody owns it.</li>\n      </ul>`\n    },\n    {\n      q:\"How would you test a system like this before it ships?\",\n      a:`<p>The testing strategy has to match the architecture \u2014 unit tests prove a function works and tell you nothing about whether two services still agree on a payload shape.</p>\n      <ul>\n        <li><strong>Contract tests</strong> (Pact-style) are the highest-value addition once services are separate: the consumer declares what it expects, the provider's CI verifies it still honours that. This catches the specific failure that integration environments catch too late and unit tests never catch at all \u2014 a provider shipping a breaking change that its consumers only discover in production.</li>\n        <li><strong>Load testing before launch, not after the incident</strong> \u2014 k6 or Gatling against a production-like environment, with three distinct shapes: a steady soak test (do memory and connections leak over hours?), a spike test (what happens at 5&times; instantly \u2014 does autoscaling react fast enough?), and a stress test to find the actual breaking point so you know where the cliff is rather than discovering it on your busiest day.</li>\n        <li><strong>Test the failure paths deliberately.</strong> Kill a replica, blackhole a dependency, inject latency \u2014 the circuit breakers and fallbacks designed above are untested code until something actually fails. Chaos experiments in staging, and a regular game day, are how you find out your retry logic amplifies load instead of dampening it.</li>\n        <li><strong>Verify the backups restore.</strong> An untested backup is not a backup. Restore time is the number that matters during an incident, and most teams have never measured theirs.</li>\n      </ul>`\n    },\n    {\n      q:\"What would you build for an MVP vs. design in from day one?\",\n      a:`<p>Some things are genuinely fine to defer because retrofitting them later is a bounded, well-understood amount of work: read replicas, sharding, a service mesh, and multi-region can all be added once real traffic proves they're needed, without a rewrite. Other things are expensive or impossible to retrofit cleanly, and belong in the design from day one even for an MVP:</p>\n      <ul>\n        <li><strong>Service boundaries and API contracts</strong> \u2014 a bad boundary calcifies fast once other things depend on it.</li>\n        <li><strong>Auth and the security posture around sensitive data</strong> \u2014 retrofitting security is where breaches come from.</li>\n        <li><strong>Basic observability</strong> \u2014 you cannot debug production on a system you were never watching; cheap to add early, very expensive to be missing mid-incident.</li>\n        <li><strong>Idempotency on write endpoints</strong> \u2014 adding it after duplicate charges have already happened means a data-cleanup project on top of the code change.</li>\n        <li><strong>A schema-migration discipline</strong> \u2014 the first time you need a zero-downtime change is the wrong time to invent the process.</li>\n      </ul>`\n    },\n    {\n      q:\"How do you pace this as a 30\u201345 minute interview answer?\",\n      a:`<p>Roughly: 5 minutes clarifying requirements and stating the capacity estimate, 10\u201315 minutes on the high-level architecture \u2014 walk left to right through client &rarr; edge &rarr; services &rarr; data, at a level where every box gets one sentence, not a paragraph \u2014 then let the interviewer steer the remaining 15\u201320 minutes into 2\u20133 components they want depth on (usually caching, the database, or reliability), and close with 5 minutes on trade-offs: what you'd cut for an MVP, what it costs to run, and the one or two things you're least confident about and would want to validate with load testing before shipping. Ending on that last, honest note \u2014 naming your own uncertainty \u2014 reads as seniority, not weakness.</p>\n      <p><strong>Two habits that separate strong candidates:</strong> narrate the <em>why</em> behind every box as you draw it (\"read replicas here because we established this is 90% reads\"), so the design visibly follows from the requirements rather than appearing fully formed; and state trade-offs as trade-offs rather than presenting each choice as obviously correct. \"I'd use Postgres here, and the thing I'm giving up is\u2026\" is a stronger sentence than any amount of confident box-drawing.</p>`\n    }\n  ]\n}\n];\n// ---------- render ----------\nconst navList = document.getElementById('navList');\nconst content = document.getElementById('content');\nlet activeId = DATA[0].id;\nconst STORAGE_KEY = 'system-design-scale-reviewed-v1';\n\nfunction loadReviewed(){\n  try{\n    const raw = localStorage.getItem(STORAGE_KEY);\n    return raw ? JSON.parse(raw) : {};\n  }catch(e){ return {}; }\n}\nfunction saveReviewed(obj){\n  try{ localStorage.setItem(STORAGE_KEY, JSON.stringify(obj)); }catch(e){}\n}\nlet reviewed = loadReviewed();\n\nfunction totalQuestions(){\n  return DATA.reduce((n,s)=>n+s.questions.length,0);\n}\nfunction reviewedCount(){\n  return Object.values(reviewed).filter(Boolean).length;\n}\nfunction updateProgress(){\n  const total = totalQuestions();\n  const done = reviewedCount();\n  document.getElementById('progressNum').textContent = done + ' / ' + total;\n  document.getElementById('progressFill').style.width = (total? (done/total*100):0) + '%';\n}\n\nfunction renderNav(){\n  navList.innerHTML = '';\n  DATA.forEach((section, idx)=>{\n    const btn = document.createElement('button');\n    btn.className = 'nav-item' + (section.id===activeId ? ' active':'');\n    btn.innerHTML = `<span class=\"nav-num\">${String(idx+1).padStart(2,'0')}</span><span class=\"nav-label\">${section.title}</span><span class=\"nav-count\">${section.questions.length}</span>`;\n    btn.addEventListener('click', ()=>{ activeId = section.id; renderNav(); renderContent(); });\n    navList.appendChild(btn);\n  });\n}\n\nfunction qKey(sectionId, qIdx){ return sectionId + ':' + qIdx; }\n\nfunction renderContent(pendingQIdx){\n  content.innerHTML = '';\n  const section = DATA.find(s=>s.id===activeId);\n  const idx = DATA.findIndex(s=>s.id===activeId);\n  const head = document.createElement('div');\n  head.className = 'section-head';\n  head.innerHTML = `<span class=\"num\">${String(idx+1).padStart(2,'0')}</span><h2>${section.title}</h2><span class=\"count\">${section.questions.length} question${section.questions.length>1?'s':''}</span>`;\n  content.appendChild(head);\n\n  section.questions.forEach((item, qIdx)=>{\n    const key = qKey(section.id, qIdx);\n    const isDone = !!reviewed[key];\n    const card = document.createElement('div');\n    card.className = 'qcard';\n\n    const qhead = document.createElement('div');\n    qhead.className = 'qhead';\n    qhead.innerHTML = `\n      <span class=\"qcheck ${isDone?'done':''}\"></span>\n      <span class=\"qtext ${isDone?'done-text':''}\">${item.q}</span>\n      <span class=\"qchevron\">\u203a</span>\n    `;\n    const checkEl = qhead.querySelector('.qcheck');\n    const textEl = qhead.querySelector('.qtext');\n    checkEl.addEventListener('click', (e)=>{\n      e.stopPropagation();\n      reviewed[key] = !reviewed[key];\n      saveReviewed(reviewed);\n      checkEl.classList.toggle('done');\n      textEl.classList.toggle('done-text');\n      updateProgress();\n    });\n    qhead.addEventListener('click', ()=>{ card.classList.toggle('open'); });\n\n    const body = document.createElement('div');\n    body.className = 'qbody';\n    body.innerHTML = item.a;\n\n    card.appendChild(qhead);\n    card.appendChild(body);\n    content.appendChild(card);\n  });\n\n  if(typeof pendingQIdx === 'number'){\n    const target = content.children[pendingQIdx + 1];\n    if(target){\n      target.classList.add('open');\n      target.scrollIntoView({ behavior:'smooth', block:'start' });\n    }\n  }\n}\n\ndocument.getElementById('resetBtn').addEventListener('click', ()=>{\n  reviewed = {};\n  saveReviewed(reviewed);\n  renderContent();\n  updateProgress();\n});\n\nwindow.addEventListener('message', (e)=>{\n  if(e.data && e.data.type==='goto-section' && DATA.find(s=>s.id===e.data.id)){\n    activeId = e.data.id;\n    renderNav();\n    renderContent(typeof e.data.qIdx === 'number' && !Number.isNaN(e.data.qIdx) ? e.data.qIdx : undefined);\n  }\n});\n\nrenderNav();\nrenderContent();\nupdateProgress();\n</script>\n</body>\n</html>\n"}