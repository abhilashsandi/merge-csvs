{"html": "<!DOCTYPE html>\n<html lang=\"en\">\n<head>\n<meta charset=\"UTF-8\">\n<title>Optimize at Every Layer \u2014 Interview Prep</title>\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1\">\n<link rel=\"preconnect\" href=\"https://fonts.googleapis.com\">\n<link rel=\"preconnect\" href=\"https://fonts.gstatic.com\" crossorigin>\n<link href=\"https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500;600;700&family=IBM+Plex+Sans:wght@400;500;600;700&display=swap\" rel=\"stylesheet\">\n<style>\n  :root{\n    --bg:#F6F4EF;\n    --panel:#FFFFFF;\n    --panel-2:#FBF9F4;\n    --border:#E4E0D4;\n    --text:#211F1B;\n    --muted:#716B5E;\n    --accent:#2F7A72;\n    --accent-ink:#F3FBF9;\n    --accent-soft:#E3F1EE;\n    --gold:#B8792F;\n    --gold-soft:#F5E9D8;\n    --code-bg:#EFECE2;\n    --danger:#B5502F;\n    --shadow: 0 1px 2px rgba(30,26,18,0.05);\n    --mono: 'IBM Plex Mono', ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;\n    --sans: 'IBM Plex Sans', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;\n  }\n  @media (prefers-color-scheme: dark){\n    :root:not([data-theme=\"light\"]){\n      --bg:#15181A;\n      --panel:#1B1F21;\n      --panel-2:#202426;\n      --border:#2C3133;\n      --text:#E9E6DE;\n      --muted:#9A968A;\n      --accent:#5FBFB2;\n      --accent-ink:#0B1716;\n      --accent-soft:#1E3230;\n      --gold:#D9A15C;\n      --gold-soft:#332918;\n      --code-bg:#20262A;\n      --danger:#D98866;\n      --shadow: 0 1px 2px rgba(0,0,0,0.4);\n    }\n  }\n  :root[data-theme=\"dark\"]{\n    --bg:#15181A;\n    --panel:#1B1F21;\n    --panel-2:#202426;\n    --border:#2C3133;\n    --text:#E9E6DE;\n    --muted:#9A968A;\n    --accent:#5FBFB2;\n    --accent-ink:#0B1716;\n    --accent-soft:#1E3230;\n    --gold:#D9A15C;\n    --gold-soft:#332918;\n    --code-bg:#20262A;\n    --danger:#D98866;\n    --shadow: 0 1px 2px rgba(0,0,0,0.4);\n  }\n\n  *{box-sizing:border-box;}\n  html,body{margin:0;padding:0;}\n  body{\n    background:var(--bg);\n    color:var(--text);\n    font-family:var(--sans);\n    line-height:1.55;\n    -webkit-font-smoothing:antialiased;\n  }\n  a{color:var(--accent);}\n\n  .app{\n    display:flex;\n    min-height:100vh;\n  }\n\n  /* ---------- Sidebar ---------- */\n  .sidebar{\n    width:260px;\n    flex:0 0 260px;\n    border-right:1px solid var(--border);\n    padding:28px 16px 24px;\n    position:sticky;\n    top:0;\n    height:100vh;\n    overflow-y:auto;\n  }\n  .brand{\n    font-family:var(--mono);\n    font-size:13px;\n    letter-spacing:0.02em;\n    color:var(--muted);\n    padding:0 8px 18px;\n    border-bottom:1px solid var(--border);\n    margin-bottom:14px;\n  }\n  .brand b{color:var(--text);font-weight:600;}\n  .nav-item{\n    display:flex;\n    align-items:center;\n    justify-content:space-between;\n    gap:10px;\n    width:100%;\n    text-align:left;\n    padding:10px 10px;\n    border-radius:8px;\n    border:1px solid transparent;\n    background:transparent;\n    color:var(--text);\n    font-family:var(--sans);\n    font-size:14.5px;\n    cursor:pointer;\n    margin-bottom:2px;\n  }\n  .nav-item:hover{background:var(--panel-2);}\n  .nav-item.active{\n    background:var(--accent-soft);\n    border-color:var(--accent);\n    color:var(--text);\n    font-weight:600;\n  }\n  .nav-num{\n    font-family:var(--mono);\n    font-size:12px;\n    color:var(--muted);\n    width:18px;\n    flex:0 0 18px;\n  }\n  .nav-item.active .nav-num{color:var(--accent);}\n  .nav-label{flex:1;}\n  .nav-count{\n    font-family:var(--mono);\n    font-size:11px;\n    color:var(--muted);\n    background:var(--panel-2);\n    border:1px solid var(--border);\n    padding:1px 6px;\n    border-radius:999px;\n  }\n  .progress-box{\n    margin-top:20px;\n    padding:14px 12px;\n    border:1px solid var(--border);\n    border-radius:10px;\n    background:var(--panel-2);\n  }\n  .progress-box .label{\n    font-family:var(--mono);\n    font-size:11px;\n    color:var(--muted);\n    margin-bottom:8px;\n  }\n  .progress-track{\n    height:6px;\n    border-radius:999px;\n    background:var(--border);\n    overflow:hidden;\n  }\n  .progress-fill{\n    height:100%;\n    background:var(--accent);\n    width:0%;\n    transition:width .25s ease;\n  }\n  .progress-num{\n    margin-top:8px;\n    font-family:var(--mono);\n    font-size:12px;\n    color:var(--text);\n  }\n  .reset-btn{\n    margin-top:10px;\n    width:100%;\n    font-family:var(--sans);\n    font-size:12px;\n    color:var(--muted);\n    background:transparent;\n    border:1px solid var(--border);\n    border-radius:7px;\n    padding:6px 8px;\n    cursor:pointer;\n  }\n  .reset-btn:hover{color:var(--danger);border-color:var(--danger);}\n\n  /* ---------- Main ---------- */\n  .main{\n    flex:1;\n    min-width:0;\n    padding:36px 40px 80px;\n    max-width:1200px;\n  }\n  .hero{\n    margin-bottom:34px;\n  }\n  .hero .eyebrow-free{\n    font-family:var(--mono);\n    font-size:13px;\n    color:var(--accent);\n    margin:0 0 10px;\n  }\n  .hero h1{\n    font-family:var(--mono);\n    font-size:30px;\n    line-height:1.25;\n    margin:0 0 12px;\n    font-weight:600;\n    letter-spacing:-0.01em;\n  }\n  .hero p{\n    color:var(--muted);\n    max-width:62ch;\n    margin:0 0 6px;\n    font-size:15px;\n  }\n  .hero .source-note{\n    font-size:13px;\n    color:var(--muted);\n    border-left:2px solid var(--border);\n    padding-left:12px;\n    margin-top:16px;\n  }\n\n  .section-head{\n    display:flex;\n    align-items:baseline;\n    gap:10px;\n    margin:44px 0 16px;\n    padding-bottom:10px;\n    border-bottom:1px solid var(--border);\n  }\n  .section-head .num{\n    font-family:var(--mono);\n    color:var(--accent);\n    font-size:14px;\n  }\n  .section-head h2{\n    font-family:var(--mono);\n    font-size:19px;\n    margin:0;\n    font-weight:600;\n  }\n  .section-head .count{\n    margin-left:auto;\n    font-family:var(--mono);\n    font-size:12px;\n    color:var(--muted);\n  }\n\n  .qcard{\n    border:1px solid var(--border);\n    border-radius:10px;\n    background:var(--panel);\n    margin-bottom:12px;\n    overflow:hidden;\n  }\n  .qhead{\n    display:flex;\n    align-items:flex-start;\n    gap:12px;\n    padding:14px 16px;\n    cursor:pointer;\n    user-select:none;\n  }\n  .qcheck{\n    margin-top:3px;\n    flex:0 0 auto;\n    width:18px;height:18px;\n    border-radius:5px;\n    border:1.5px solid var(--border);\n    display:flex;align-items:center;justify-content:center;\n    cursor:pointer;\n    background:var(--panel);\n  }\n  .qcheck.done{\n    background:var(--accent);\n    border-color:var(--accent);\n  }\n  .qcheck.done::after{\n    content:\"\u2713\";\n    color:var(--accent-ink);\n    font-size:12px;\n    font-weight:700;\n  }\n  .qtext{\n    flex:1;\n    font-size:15px;\n    font-weight:500;\n  }\n  .qtext.done-text{color:var(--muted);text-decoration:line-through;text-decoration-color:var(--border);}\n  .qchevron{\n    flex:0 0 auto;\n    color:var(--muted);\n    font-family:var(--mono);\n    font-size:13px;\n    transform:rotate(0deg);\n    transition:transform .15s ease;\n    margin-top:2px;\n  }\n  .qcard.open .qchevron{transform:rotate(90deg);}\n  .qbody{\n    display:none;\n    padding:2px 16px 20px 46px;\n    border-top:1px solid var(--border);\n    font-size:14.5px;\n  }\n  .qcard.open .qbody{display:block; padding-top:14px;}\n  .qbody p{margin:0 0 12px;}\n  .qbody ul{margin:0 0 12px; padding-left:20px;}\n  .qbody li{margin-bottom:6px;}\n  .qbody strong{color:var(--text);}\n  code{\n    font-family:var(--mono);\n    background:var(--code-bg);\n    padding:1px 5px;\n    border-radius:4px;\n    font-size:0.92em;\n  }\n  pre{\n    font-family:var(--mono);\n    background:var(--code-bg);\n    padding:12px 14px;\n    border-radius:8px;\n    overflow-x:auto;\n    font-size:13px;\n    margin:0 0 14px;\n    border:1px solid var(--border);\n  }\n  .diagram{\n    background:var(--panel-2);\n    border:1px solid var(--border);\n    border-radius:8px;\n    padding:14px;\n    margin:0 0 14px;\n    overflow-x:auto;\n  }\n  .diagram svg{display:block; margin:0 auto; max-width:100%; height:auto;}\n  .tag{\n    display:inline-block;\n    font-family:var(--mono);\n    font-size:10.5px;\n    color:var(--gold);\n    background:var(--gold-soft);\n    border:1px solid var(--gold);\n    border-radius:5px;\n    padding:1px 6px;\n    margin-bottom:10px;\n  }\n  table.cmp{\n    width:100%;\n    border-collapse:collapse;\n    margin:0 0 14px;\n    font-size:13.5px;\n  }\n  table.cmp th, table.cmp td{\n    border:1px solid var(--border);\n    padding:7px 10px;\n    text-align:left;\n    vertical-align:top;\n  }\n  table.cmp th{\n    background:var(--panel-2);\n    font-family:var(--mono);\n    font-size:12px;\n    font-weight:600;\n  }\n\n  ::selection{background:var(--accent-soft);}\n\n  @media (max-width: 820px){\n    .sidebar{\n      width:100%;\n      flex:none;\n      height:auto;\n      position:relative;\n      border-right:none;\n      border-bottom:1px solid var(--border);\n    }\n    .app{flex-direction:column;}\n    .main{padding:24px 18px 60px; max-width:100%;}\n  }\n</style>\n</head>\n<body>\n<div class=\"app\">\n  <nav class=\"sidebar\" id=\"sidebar\">\n    <div class=\"brand\"><b>Performance</b><br>Optimize at every layer</div>\n    <div id=\"navList\"></div>\n    <div class=\"progress-box\">\n      <div class=\"label\">REVIEWED</div>\n      <div class=\"progress-track\"><div class=\"progress-fill\" id=\"progressFill\"></div></div>\n      <div class=\"progress-num\" id=\"progressNum\">0 / 0</div>\n      <button class=\"reset-btn\" id=\"resetBtn\">Reset progress</button>\n    </div>\n  </nav>\n\n  <main class=\"main\">\n    <div class=\"hero\">\n      <p class=\"eyebrow-free\">Prep guide</p>\n      <h1>Optimize at every layer</h1>\n      <p>A senior-architect-ready answer to \"how would you optimize this system\" \u2014 client, network/edge, API/gateway, application server, caching, database, async/messaging, and the observability loop that tells you what to fix first, closing with a latency-numbers reference and a full-stack lever summary.</p>\n      <div class=\"source-note\">10 sections \u00b7 click a question to expand it \u00b7 check items off as you review them (saved in this browser only)</div>\n    </div>\n    <div id=\"content\"></div>\n  </main>\n</div>\n\n\n<script>\nconst DATA = [\n{\n  id:\"overview\", title:\"The Complete Flow\",\n  questions:[\n    {\n      q:\"What's the one-page version of this entire answer?\",\n      a:`<p>The fastest way to hold the whole answer in your head: this is the same ten stages the rest of this page is organized into, compressed into one flow \u2014 the diagnostic loop first, then top-down through the stack a request actually travels, closing with the numbers worth knowing cold.</p>\n      <div style=\"max-width:660px; margin:18px auto 4px;\">\n    <div style=\"display:flex; gap:18px; align-items:stretch;\">\n      <div style=\"display:flex; flex-direction:column; align-items:center; flex:0 0 36px;\">\n        <div style=\"width:36px; height:36px; border-radius:50%; background:var(--gold-soft); border:2px solid var(--gold); display:flex; align-items:center; justify-content:center; font-family:var(--mono); font-weight:700; color:var(--gold); font-size:14px; flex-shrink:0;\">1</div>\n        <div style=\"width:2px; flex:1; background:var(--border); margin-top:4px;\"></div>\n      </div>\n      <div style=\"flex:1; padding-bottom:24px; min-width:0;\">\n        <h4 style=\"margin:3px 0 6px; font-family:var(--sans); font-size:15.5px; font-weight:700; color:var(--text);\">The Optimization Mindset</h4>\n        <p style=\"margin:0 0 9px; font-size:13.5px; color:var(--muted); line-height:1.55; max-width:600px;\">Never optimize by intuition: measure \u2192 profile \u2192 fix the single biggest bottleneck \u2192 validate against the same metric \u2192 repeat. Rank fixes by impact \u00d7 reach against a user-facing metric, not by what looks technically interesting, and gate performance in CI so it never regresses silently next sprint.</p>\n        <div style=\"display:flex; flex-wrap:wrap; gap:6px;\"><span style=\"font-family:var(--mono); font-size:10.5px; padding:2px 9px; border-radius:99px; background:var(--panel-2); border:1px solid var(--border); color:var(--muted); white-space:nowrap;\">measure \u2192 profile \u2192 fix \u2192 validate</span><span style=\"font-family:var(--mono); font-size:10.5px; padding:2px 9px; border-radius:99px; background:var(--panel-2); border:1px solid var(--border); color:var(--muted); white-space:nowrap;\">RUM vs. synthetic</span><span style=\"font-family:var(--mono); font-size:10.5px; padding:2px 9px; border-radius:99px; background:var(--panel-2); border:1px solid var(--border); color:var(--muted); white-space:nowrap;\">budgets in CI</span></div>\n      </div>\n    </div>\n    <div style=\"display:flex; gap:18px; align-items:stretch;\">\n      <div style=\"display:flex; flex-direction:column; align-items:center; flex:0 0 36px;\">\n        <div style=\"width:36px; height:36px; border-radius:50%; background:var(--gold-soft); border:2px solid var(--gold); display:flex; align-items:center; justify-content:center; font-family:var(--mono); font-weight:700; color:var(--gold); font-size:14px; flex-shrink:0;\">2</div>\n        <div style=\"width:2px; flex:1; background:var(--border); margin-top:4px;\"></div>\n      </div>\n      <div style=\"flex:1; padding-bottom:24px; min-width:0;\">\n        <h4 style=\"margin:3px 0 6px; font-family:var(--sans); font-size:15.5px; font-weight:700; color:var(--text);\">Client & Browser</h4>\n        <p style=\"margin:0 0 9px; font-size:13.5px; color:var(--muted); line-height:1.55; max-width:600px;\">Core Web Vitals \u2014 LCP, INP, CLS \u2014 each have a distinct, mostly non-overlapping fix. The React Compiler now auto-memoizes in React 19, so audit the bundle and profile before hand-optimizing. Keep the main thread free with compositor-only animation (transform/opacity) and yielded chunks for heavy work.</p>\n        <div style=\"display:flex; flex-wrap:wrap; gap:6px;\"><span style=\"font-family:var(--mono); font-size:10.5px; padding:2px 9px; border-radius:99px; background:var(--panel-2); border:1px solid var(--border); color:var(--muted); white-space:nowrap;\">Core Web Vitals</span><span style=\"font-family:var(--mono); font-size:10.5px; padding:2px 9px; border-radius:99px; background:var(--panel-2); border:1px solid var(--border); color:var(--muted); white-space:nowrap;\">React Compiler</span><span style=\"font-family:var(--mono); font-size:10.5px; padding:2px 9px; border-radius:99px; background:var(--panel-2); border:1px solid var(--border); color:var(--muted); white-space:nowrap;\">main thread vs. compositor</span></div>\n      </div>\n    </div>\n    <div style=\"display:flex; gap:18px; align-items:stretch;\">\n      <div style=\"display:flex; flex-direction:column; align-items:center; flex:0 0 36px;\">\n        <div style=\"width:36px; height:36px; border-radius:50%; background:var(--gold-soft); border:2px solid var(--gold); display:flex; align-items:center; justify-content:center; font-family:var(--mono); font-weight:700; color:var(--gold); font-size:14px; flex-shrink:0;\">3</div>\n        <div style=\"width:2px; flex:1; background:var(--border); margin-top:4px;\"></div>\n      </div>\n      <div style=\"flex:1; padding-bottom:24px; min-width:0;\">\n        <h4 style=\"margin:3px 0 6px; font-family:var(--sans); font-size:15.5px; font-weight:700; color:var(--text);\">Network & Edge</h4>\n        <p style=\"margin:0 0 9px; font-size:13.5px; color:var(--muted); line-height:1.55; max-width:600px;\">Long-lived immutable caching for versioned static assets; short TTL plus stale-while-revalidate for cacheable dynamic responses. HTTP/3 and Brotli/zstd matter most on lossy mobile networks; edge rendering and speculative prefetch remove a round trip for the pages that qualify.</p>\n        <div style=\"display:flex; flex-wrap:wrap; gap:6px;\"><span style=\"font-family:var(--mono); font-size:10.5px; padding:2px 9px; border-radius:99px; background:var(--panel-2); border:1px solid var(--border); color:var(--muted); white-space:nowrap;\">CDN cache policy</span><span style=\"font-family:var(--mono); font-size:10.5px; padding:2px 9px; border-radius:99px; background:var(--panel-2); border:1px solid var(--border); color:var(--muted); white-space:nowrap;\">HTTP/3</span><span style=\"font-family:var(--mono); font-size:10.5px; padding:2px 9px; border-radius:99px; background:var(--panel-2); border:1px solid var(--border); color:var(--muted); white-space:nowrap;\">edge rendering</span></div>\n      </div>\n    </div>\n    <div style=\"display:flex; gap:18px; align-items:stretch;\">\n      <div style=\"display:flex; flex-direction:column; align-items:center; flex:0 0 36px;\">\n        <div style=\"width:36px; height:36px; border-radius:50%; background:var(--gold-soft); border:2px solid var(--gold); display:flex; align-items:center; justify-content:center; font-family:var(--mono); font-weight:700; color:var(--gold); font-size:14px; flex-shrink:0;\">4</div>\n        <div style=\"width:2px; flex:1; background:var(--border); margin-top:4px;\"></div>\n      </div>\n      <div style=\"flex:1; padding-bottom:24px; min-width:0;\">\n        <h4 style=\"margin:3px 0 6px; font-family:var(--sans); font-size:15.5px; font-weight:700; color:var(--text);\">API & Gateway Layer</h4>\n        <p style=\"margin:0 0 9px; font-size:13.5px; color:var(--muted); line-height:1.55; max-width:600px;\">Rate limit and paginate with cursors at the edge, before a request ever reaches an application server. Batch N+1 fan-out with DataLoader \u2014 the fix is eliminating round trips, not shrinking any single query.</p>\n        <div style=\"display:flex; flex-wrap:wrap; gap:6px;\"><span style=\"font-family:var(--mono); font-size:10.5px; padding:2px 9px; border-radius:99px; background:var(--panel-2); border:1px solid var(--border); color:var(--muted); white-space:nowrap;\">rate limiting</span><span style=\"font-family:var(--mono); font-size:10.5px; padding:2px 9px; border-radius:99px; background:var(--panel-2); border:1px solid var(--border); color:var(--muted); white-space:nowrap;\">N+1 / DataLoader</span><span style=\"font-family:var(--mono); font-size:10.5px; padding:2px 9px; border-radius:99px; background:var(--panel-2); border:1px solid var(--border); color:var(--muted); white-space:nowrap;\">cursor pagination</span></div>\n      </div>\n    </div>\n    <div style=\"display:flex; gap:18px; align-items:stretch;\">\n      <div style=\"display:flex; flex-direction:column; align-items:center; flex:0 0 36px;\">\n        <div style=\"width:36px; height:36px; border-radius:50%; background:var(--gold-soft); border:2px solid var(--gold); display:flex; align-items:center; justify-content:center; font-family:var(--mono); font-weight:700; color:var(--gold); font-size:14px; flex-shrink:0;\">5</div>\n        <div style=\"width:2px; flex:1; background:var(--border); margin-top:4px;\"></div>\n      </div>\n      <div style=\"flex:1; padding-bottom:24px; min-width:0;\">\n        <h4 style=\"margin:3px 0 6px; font-family:var(--sans); font-size:15.5px; font-weight:700; color:var(--text);\">Application Server</h4>\n        <p style=\"margin:0 0 9px; font-size:13.5px; color:var(--muted); line-height:1.55; max-width:600px;\">Never block Node's single event loop with synchronous CPU-bound work \u2014 offload to a worker pool and keep the loop free for I/O. Scale horizontally on the signal that actually reflects saturation for the workload (often queue depth or event-loop lag, not raw CPU).</p>\n        <div style=\"display:flex; flex-wrap:wrap; gap:6px;\"><span style=\"font-family:var(--mono); font-size:10.5px; padding:2px 9px; border-radius:99px; background:var(--panel-2); border:1px solid var(--border); color:var(--muted); white-space:nowrap;\">event-loop lag</span><span style=\"font-family:var(--mono); font-size:10.5px; padding:2px 9px; border-radius:99px; background:var(--panel-2); border:1px solid var(--border); color:var(--muted); white-space:nowrap;\">worker_threads</span><span style=\"font-family:var(--mono); font-size:10.5px; padding:2px 9px; border-radius:99px; background:var(--panel-2); border:1px solid var(--border); color:var(--muted); white-space:nowrap;\">horizontal autoscaling</span></div>\n      </div>\n    </div>\n    <div style=\"display:flex; gap:18px; align-items:stretch;\">\n      <div style=\"display:flex; flex-direction:column; align-items:center; flex:0 0 36px;\">\n        <div style=\"width:36px; height:36px; border-radius:50%; background:var(--gold-soft); border:2px solid var(--gold); display:flex; align-items:center; justify-content:center; font-family:var(--mono); font-weight:700; color:var(--gold); font-size:14px; flex-shrink:0;\">6</div>\n        <div style=\"width:2px; flex:1; background:var(--border); margin-top:4px;\"></div>\n      </div>\n      <div style=\"flex:1; padding-bottom:24px; min-width:0;\">\n        <h4 style=\"margin:3px 0 6px; font-family:var(--sans); font-size:15.5px; font-weight:700; color:var(--text);\">Caching Layer</h4>\n        <p style=\"margin:0 0 9px; font-size:13.5px; color:var(--muted); line-height:1.55; max-width:600px;\">Pick the layer that matches the data's real scope \u2014 in-process for per-instance, Redis for shared state, CDN for content that's the same across users. Almost every real caching failure is invalidation, not the mechanism: stampedes, a cache key missing a dimension the response actually varies on, or a thundering herd on a cold restart.</p>\n        <div style=\"display:flex; flex-wrap:wrap; gap:6px;\"><span style=\"font-family:var(--mono); font-size:10.5px; padding:2px 9px; border-radius:99px; background:var(--panel-2); border:1px solid var(--border); color:var(--muted); white-space:nowrap;\">right layer for the job</span><span style=\"font-family:var(--mono); font-size:10.5px; padding:2px 9px; border-radius:99px; background:var(--panel-2); border:1px solid var(--border); color:var(--muted); white-space:nowrap;\">cache stampede</span><span style=\"font-family:var(--mono); font-size:10.5px; padding:2px 9px; border-radius:99px; background:var(--panel-2); border:1px solid var(--border); color:var(--muted); white-space:nowrap;\">cold-start herd</span></div>\n      </div>\n    </div>\n    <div style=\"display:flex; gap:18px; align-items:stretch;\">\n      <div style=\"display:flex; flex-direction:column; align-items:center; flex:0 0 36px;\">\n        <div style=\"width:36px; height:36px; border-radius:50%; background:var(--gold-soft); border:2px solid var(--gold); display:flex; align-items:center; justify-content:center; font-family:var(--mono); font-weight:700; color:var(--gold); font-size:14px; flex-shrink:0;\">7</div>\n        <div style=\"width:2px; flex:1; background:var(--border); margin-top:4px;\"></div>\n      </div>\n      <div style=\"flex:1; padding-bottom:24px; min-width:0;\">\n        <h4 style=\"margin:3px 0 6px; font-family:var(--sans); font-size:15.5px; font-weight:700; color:var(--text);\">Database</h4>\n        <p style=\"margin:0 0 9px; font-size:13.5px; color:var(--muted); line-height:1.55; max-width:600px;\">EXPLAIN ANALYZE's estimated-vs-actual row gap tells you stale statistics from a genuinely missing index. Scale throughput with read replicas, batched writes, and partitioning \u2014 in that order \u2014 well before reaching for a bigger box.</p>\n        <div style=\"display:flex; flex-wrap:wrap; gap:6px;\"><span style=\"font-family:var(--mono); font-size:10.5px; padding:2px 9px; border-radius:99px; background:var(--panel-2); border:1px solid var(--border); color:var(--muted); white-space:nowrap;\">EXPLAIN ANALYZE</span><span style=\"font-family:var(--mono); font-size:10.5px; padding:2px 9px; border-radius:99px; background:var(--panel-2); border:1px solid var(--border); color:var(--muted); white-space:nowrap;\">read replicas</span><span style=\"font-family:var(--mono); font-size:10.5px; padding:2px 9px; border-radius:99px; background:var(--panel-2); border:1px solid var(--border); color:var(--muted); white-space:nowrap;\">batched writes</span></div>\n      </div>\n    </div>\n    <div style=\"display:flex; gap:18px; align-items:stretch;\">\n      <div style=\"display:flex; flex-direction:column; align-items:center; flex:0 0 36px;\">\n        <div style=\"width:36px; height:36px; border-radius:50%; background:var(--gold-soft); border:2px solid var(--gold); display:flex; align-items:center; justify-content:center; font-family:var(--mono); font-weight:700; color:var(--gold); font-size:14px; flex-shrink:0;\">8</div>\n        <div style=\"width:2px; flex:1; background:var(--border); margin-top:4px;\"></div>\n      </div>\n      <div style=\"flex:1; padding-bottom:24px; min-width:0;\">\n        <h4 style=\"margin:3px 0 6px; font-family:var(--sans); font-size:15.5px; font-weight:700; color:var(--text);\">Async & Messaging</h4>\n        <p style=\"margin:0 0 9px; font-size:13.5px; color:var(--muted); line-height:1.55; max-width:600px;\">Batch consumption amortizes fixed per-operation overhead across many items \u2014 often the single biggest throughput win available. Autoscale workers off queue depth, but watch oldest-message age too: depth alone looks healthy right up until a poison message is quietly blocking everything behind it.</p>\n        <div style=\"display:flex; flex-wrap:wrap; gap:6px;\"><span style=\"font-family:var(--mono); font-size:10.5px; padding:2px 9px; border-radius:99px; background:var(--panel-2); border:1px solid var(--border); color:var(--muted); white-space:nowrap;\">batch processing</span><span style=\"font-family:var(--mono); font-size:10.5px; padding:2px 9px; border-radius:99px; background:var(--panel-2); border:1px solid var(--border); color:var(--muted); white-space:nowrap;\">queue depth + age</span><span style=\"font-family:var(--mono); font-size:10.5px; padding:2px 9px; border-radius:99px; background:var(--panel-2); border:1px solid var(--border); color:var(--muted); white-space:nowrap;\">backpressure</span></div>\n      </div>\n    </div>\n    <div style=\"display:flex; gap:18px; align-items:stretch;\">\n      <div style=\"display:flex; flex-direction:column; align-items:center; flex:0 0 36px;\">\n        <div style=\"width:36px; height:36px; border-radius:50%; background:var(--gold-soft); border:2px solid var(--gold); display:flex; align-items:center; justify-content:center; font-family:var(--mono); font-weight:700; color:var(--gold); font-size:14px; flex-shrink:0;\">9</div>\n        <div style=\"width:2px; flex:1; background:var(--border); margin-top:4px;\"></div>\n      </div>\n      <div style=\"flex:1; padding-bottom:24px; min-width:0;\">\n        <h4 style=\"margin:3px 0 6px; font-family:var(--sans); font-size:15.5px; font-weight:700; color:var(--text);\">Observability & Validation</h4>\n        <p style=\"margin:0 0 9px; font-size:13.5px; color:var(--muted); line-height:1.55; max-width:600px;\">Golden signals say which service is unhealthy; a distributed trace says exactly why one specific request was slow. Validate any fix with load testing before it ships \u2014 profiling in isolation misses failure modes that only appear under real concurrency.</p>\n        <div style=\"display:flex; flex-wrap:wrap; gap:6px;\"><span style=\"font-family:var(--mono); font-size:10.5px; padding:2px 9px; border-radius:99px; background:var(--panel-2); border:1px solid var(--border); color:var(--muted); white-space:nowrap;\">golden signals</span><span style=\"font-family:var(--mono); font-size:10.5px; padding:2px 9px; border-radius:99px; background:var(--panel-2); border:1px solid var(--border); color:var(--muted); white-space:nowrap;\">distributed tracing</span><span style=\"font-family:var(--mono); font-size:10.5px; padding:2px 9px; border-radius:99px; background:var(--panel-2); border:1px solid var(--border); color:var(--muted); white-space:nowrap;\">load testing</span></div>\n      </div>\n    </div>\n    <div style=\"display:flex; gap:18px; align-items:stretch;\">\n      <div style=\"display:flex; flex-direction:column; align-items:center; flex:0 0 36px;\">\n        <div style=\"width:36px; height:36px; border-radius:50%; background:var(--gold-soft); border:2px solid var(--gold); display:flex; align-items:center; justify-content:center; font-family:var(--mono); font-weight:700; color:var(--gold); font-size:14px; flex-shrink:0;\">10</div>\n        \n      </div>\n      <div style=\"flex:1; padding-bottom:2px; min-width:0;\">\n        <h4 style=\"margin:3px 0 6px; font-family:var(--sans); font-size:15.5px; font-weight:700; color:var(--text);\">Reference Numbers & Summary</h4>\n        <p style=\"margin:0 0 9px; font-size:13.5px; color:var(--muted); line-height:1.55; max-width:600px;\">Internalize the latency ladder \u2014 memory beats disk by roughly 100,000\u00d7, a same-region hop beats cross-continent by roughly 100\u00d7 \u2014 so a design can be sanity-checked in your head. Close by naming one concrete lever per layer, proof the whole stack was covered systematically.</p>\n        <div style=\"display:flex; flex-wrap:wrap; gap:6px;\"><span style=\"font-family:var(--mono); font-size:10.5px; padding:2px 9px; border-radius:99px; background:var(--panel-2); border:1px solid var(--border); color:var(--muted); white-space:nowrap;\">latency numbers</span><span style=\"font-family:var(--mono); font-size:10.5px; padding:2px 9px; border-radius:99px; background:var(--panel-2); border:1px solid var(--border); color:var(--muted); white-space:nowrap;\">one lever per layer</span></div>\n      </div>\n    </div>\n      </div>\n      <p><strong>How to use this:</strong> narrate it top to bottom when asked \"how would you optimize this system\" \u2014 start with the loop in step 1, not with a specific fix, then walk the stack layer by layer and let the interviewer stop you wherever they want depth. Every step below is its own fully worked answer in the sections that follow.</p>\n      <p><strong>The two pages are the same layers, asked two different questions.</strong> This page's flow answers \"how would you design it\"; the performance-optimization page's flow answers \"how would you make it fast\" \u2014 for the same stack, layer by layer. Worth having both answers ready for the same box in the diagram, since an interviewer switching from a design question to \"okay, and how would you optimize that\" mid-conversation is common. The AWS column is what \"implement this on AWS specifically\" looks like for each row \u2014 the same mapping the AWS reference architecture question in the interview-prep Cloud track draws out in full.</p>\n      <div style=\"overflow-x:auto;\">\n      <table class=\"cmp\" style=\"min-width:680px;\">\n        <tr><th>Layer</th><th>Design decision</th><th>Optimization lever</th><th>AWS implementation</th></tr>\n        <tr><td><strong>Client</strong></td><td>Per-route rendering strategy (SSG/ISR/SSR/PPR), REST vs. GraphQL contract</td><td>Core Web Vitals, React Compiler, compositor-only animation</td><td>Amplify Hosting or S3 + CloudFront; AppSync if GraphQL</td></tr>\n        <tr><td><strong>Network / CDN</strong></td><td>What sits at the edge \u2014 CDN + cache-control headers</td><td>Cache policy, HTTP/3, edge rendering, speculative prefetch</td><td>CloudFront, Route 53, AWS WAF, Lambda@Edge</td></tr>\n        <tr><td><strong>API / Gateway</strong></td><td>Auth termination, rate limiting, versioning, idempotency keys</td><td>Rate-limit enforcement, N+1 batching (DataLoader), cursor pagination</td><td>API Gateway, Cognito, Application Load Balancer</td></tr>\n        <tr><td><strong>Services / app tier</strong></td><td>Modular monolith vs. microservices, BFF, team-shaped boundaries</td><td>Event-loop hygiene, worker-pool offload, horizontal autoscaling</td><td>ECS + Fargate or EKS; Lambda for event-driven pieces</td></tr>\n        <tr><td><strong>Data / database</strong></td><td>SQL vs. NoSQL, CAP-theorem trade-off, replication, sharding, vertical partitioning</td><td><code>EXPLAIN ANALYZE</code>, indexing, read replicas, batched writes</td><td>RDS/Aurora (Multi-AZ + read replicas), DynamoDB, Aurora Limitless for sharding</td></tr>\n        <tr><td><strong>Caching</strong></td><td>Multi-layer waterfall (browser &rarr; CDN &rarr; gateway &rarr; Redis &rarr; DB), invalidation strategy</td><td>Right layer for the job, stampede protection, cold-start herd</td><td>ElastiCache (Redis), CloudFront, DAX for DynamoDB</td></tr>\n        <tr><td><strong>Async / messaging</strong></td><td>Queue vs. pub/sub, Saga + compensating transactions, transactional outbox</td><td>Batch processing, queue-depth + age autoscaling, backpressure</td><td>SQS, SNS, EventBridge, Step Functions for saga orchestration</td></tr>\n        <tr><td><strong>Deployment / scaling</strong></td><td>Vertical vs. horizontal scaling, load balancing, canary/blue-green</td><td>Horizontal autoscaling trigger, perf budgets enforced in CI</td><td>EC2/ECS Auto Scaling, ALB/NLB, CodeDeploy, CodePipeline</td></tr>\n        <tr><td><strong>Reliability / observability</strong></td><td>Timeout &rarr; retry &rarr; circuit breaker &rarr; bulkhead &rarr; fallback, SLOs, Multi-AZ</td><td>Golden signals, distributed tracing, load-testing a fix before it ships</td><td>CloudWatch, X-Ray, Route 53 health checks, Multi-AZ RDS</td></tr>\n      </table>\n      </div>`\n    }\n  ]\n},\n{\n  id:\"mindset\", title:\"The Optimization Mindset\",\n  questions:[\n    {\n      q:\"What's the process, before touching any specific layer?\",\n      a:`<div class=\"diagram\">\n      <svg viewBox=\"0 0 720 186\" xmlns=\"http://www.w3.org/2000/svg\" style=\"width:100%;max-width:720px\">\n        <rect x=\"14\" y=\"30\" width=\"146\" height=\"52\" rx=\"9\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n        <text x=\"87\" y=\"52\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"12\" fill=\"var(--text)\">1 &middot; Measure</text>\n        <text x=\"87\" y=\"68\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">RUM &middot; user-facing metric</text>\n        <path d=\"M164 56 L188 56\" stroke=\"var(--accent)\" stroke-width=\"1.5\" marker-end=\"url(#poLoopA)\"/>\n        <rect x=\"192\" y=\"30\" width=\"146\" height=\"52\" rx=\"9\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n        <text x=\"265\" y=\"52\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"12\" fill=\"var(--text)\">2 &middot; Profile</text>\n        <text x=\"265\" y=\"68\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">find the real bottleneck</text>\n        <path d=\"M342 56 L366 56\" stroke=\"var(--accent)\" stroke-width=\"1.5\" marker-end=\"url(#poLoopA)\"/>\n        <rect x=\"370\" y=\"30\" width=\"160\" height=\"52\" rx=\"9\" fill=\"var(--gold-soft)\" stroke=\"var(--gold)\"/>\n        <text x=\"450\" y=\"52\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"12\" fill=\"var(--text)\">3 &middot; Fix ONE thing</text>\n        <text x=\"450\" y=\"68\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">the biggest one only</text>\n        <path d=\"M534 56 L558 56\" stroke=\"var(--gold)\" stroke-width=\"1.5\" marker-end=\"url(#poLoopG)\"/>\n        <rect x=\"562\" y=\"30\" width=\"144\" height=\"52\" rx=\"9\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n        <text x=\"634\" y=\"52\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"12\" fill=\"var(--text)\">4 &middot; Validate</text>\n        <text x=\"634\" y=\"68\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">same metric as step 1</text>\n        <path d=\"M634 86 L634 118 L87 118 L87 90\" stroke=\"var(--muted)\" stroke-width=\"1.5\" fill=\"none\" stroke-dasharray=\"4\" marker-end=\"url(#poLoopM)\"/>\n        <text x=\"360\" y=\"134\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--muted)\">repeat \u2014 fixing one bottleneck routinely exposes the next one underneath it</text>\n        <text x=\"360\" y=\"164\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"10.5\" fill=\"var(--muted)\">Skipping step 1 is guessing. Skipping step 4 means you never learn whether the fix worked or the traffic simply changed.</text>\n        <defs>\n          <marker id=\"poLoopA\" markerWidth=\"8\" markerHeight=\"8\" refX=\"6\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L6,3 L0,6 z\" fill=\"var(--accent)\"/></marker>\n          <marker id=\"poLoopG\" markerWidth=\"8\" markerHeight=\"8\" refX=\"6\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L6,3 L0,6 z\" fill=\"var(--gold)\"/></marker>\n          <marker id=\"poLoopM\" markerWidth=\"8\" markerHeight=\"8\" refX=\"6\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L6,3 L0,6 z\" fill=\"var(--muted)\"/></marker>\n        </defs>\n      </svg>\n      </div>\n      <p>Measure, profile, fix the single biggest bottleneck, validate against the metric you started from, repeat \u2014 never optimize by intuition, and never fix more than one thing before re-measuring. This loop matters more than any individual technique below: an interviewer who sees a candidate reach for \"add a cache\" or \"add an index\" before \"how would you know that's actually the problem\" is seeing the weaker answer, regardless of whether the specific fix turns out to be correct.</p>\n      <ul>\n        <li><strong>Amdahl's Law, informally:</strong> the maximum speedup from optimizing one part is capped by how much time that part actually accounts for \u2014 shaving 90% off a step that's 5% of total latency moves the needle far less than a 20% improvement to the step that's 60% of it.</li>\n        <li><strong>RUM vs. synthetic:</strong> real-user monitoring (field data, actual devices and networks) is what production performance actually is; synthetic/lab tools (Lighthouse, local profiling) are reproducible and great for iterating on a fix, but a fast dev laptop on fiber hides exactly the problems a mid-range Android on 4G will surface. Use synthetic to develop a fix, RUM to confirm it mattered.</li>\n        <li><strong>Percentiles, never averages.</strong> An average latency of 200ms is compatible with 5% of users waiting three seconds. p75 is what Core Web Vitals grade on; p99 is where the angriest users live. An average hides both.</li>\n      </ul>`\n    },\n    {\n      q:\"With five things all looking slow, how do you decide what to fix first?\",\n      a:`<p>Rank by impact &times; how many users it affects, divided by effort to fix \u2014 and impact should be measured against a user-facing metric (a Core Web Vital, an API p95, a checkout completion rate), not an internal one like CPU utilization, which can be perfectly healthy while users experience real pain. A cheap fix with a huge blast radius (a missing database index hit by every request) always outranks an expensive fix with a narrow one (hand-optimizing a rarely-visited settings page), regardless of which one is more technically interesting.</p>\n      <p><strong>The framing that lands well in an interview:</strong> tie it to a business metric wherever one exists. \"This page is 400ms slower\" is an engineering statement; \"this page is 400ms slower and it's the top of the checkout funnel\" is a prioritization argument, and the second one is what gets the work scheduled.</p>`\n    },\n    {\n      q:\"How do you stop performance regressions from landing in the first place?\",\n      a:`<p>Everything else on this page is remediation \u2014 this is the part that stops you doing it again in six months, and it's the answer most candidates never reach. Performance regresses by default: every sprint adds a dependency, a tracking script, a component. Without a gate, the only feedback loop is a user complaining.</p>\n      <ul>\n        <li><strong>Bundle-size budgets enforced in CI.</strong> A hard ceiling per route that fails the build, not a dashboard someone might look at. The most common real-world regression is a single <code>import</code> that quietly pulls in a 300KB library, and a CI check catches it in the PR where it's a one-line fix rather than a month later when it's load-bearing.</li>\n        <li><strong>Lighthouse CI on key routes</strong> with asserted thresholds, run per-PR against a preview deploy \u2014 catches render-blocking resources and image regressions before merge.</li>\n        <li><strong>Field data alerting, not just lab data.</strong> Alert on p75 LCP/INP from RUM by route, so a real regression pages someone even when it only reproduces on real devices.</li>\n        <li><strong>Track backend latency as an SLO with an error budget</strong>, so \"the API got 15% slower\" is a tracked, owned number rather than something noticed anecdotally.</li>\n        <li><strong>The cultural half:</strong> a budget nobody can override becomes a budget everyone routes around. Make exceeding it a deliberate, documented decision with an owner and an expiry, not a silent one.</li>\n      </ul>`\n    }\n  ]\n},\n{\n  id:\"client\", title:\"Client & Browser\",\n  questions:[\n    {\n      q:\"What are Core Web Vitals, and what actually moves each one?\",\n      a:`<div class=\"diagram\">\n      <svg viewBox=\"0 0 780 250\" xmlns=\"http://www.w3.org/2000/svg\" style=\"width:100%;max-width:780px\">\n        <line x1=\"120\" y1=\"42\" x2=\"756\" y2=\"42\" stroke=\"var(--border)\" stroke-width=\"1.5\"/>\n        <text x=\"120\" y=\"30\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">0s</text>\n        <text x=\"279\" y=\"30\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">1s</text>\n        <text x=\"438\" y=\"30\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">2s</text>\n        <text x=\"597\" y=\"30\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">3s</text>\n        <text x=\"740\" y=\"30\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">4s</text>\n        <line x1=\"120\" y1=\"36\" x2=\"120\" y2=\"48\" stroke=\"var(--border)\"/>\n        <line x1=\"279\" y1=\"36\" x2=\"279\" y2=\"48\" stroke=\"var(--border)\"/>\n        <line x1=\"438\" y1=\"36\" x2=\"438\" y2=\"48\" stroke=\"var(--border)\"/>\n        <line x1=\"597\" y1=\"36\" x2=\"597\" y2=\"48\" stroke=\"var(--border)\"/>\n\n        <text x=\"14\" y=\"76\" font-family=\"var(--mono)\" font-size=\"10\" fill=\"var(--accent)\">LCP</text>\n        <rect x=\"120\" y=\"62\" width=\"60\" height=\"20\" rx=\"4\" fill=\"var(--panel-2)\" stroke=\"var(--border)\"/>\n        <text x=\"150\" y=\"76\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8\" fill=\"var(--muted)\">TTFB</text>\n        <rect x=\"182\" y=\"62\" width=\"120\" height=\"20\" rx=\"4\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\" opacity=\"0.6\"/>\n        <text x=\"242\" y=\"76\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8\" fill=\"var(--text)\">FCP</text>\n        <rect x=\"304\" y=\"62\" width=\"130\" height=\"20\" rx=\"4\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n        <text x=\"369\" y=\"76\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8\" fill=\"var(--text)\">hero image paints</text>\n        <line x1=\"438\" y1=\"56\" x2=\"438\" y2=\"90\" stroke=\"var(--accent)\" stroke-width=\"2\" stroke-dasharray=\"3\"/>\n        <text x=\"446\" y=\"78\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--accent)\">2.5s target</text>\n        <text x=\"14\" y=\"92\" font-family=\"var(--mono)\" font-size=\"8\" fill=\"var(--muted)\">loading</text>\n\n        <text x=\"14\" y=\"134\" font-family=\"var(--mono)\" font-size=\"10\" fill=\"var(--gold)\">INP</text>\n        <circle cx=\"560\" cy=\"127\" r=\"5\" fill=\"var(--gold)\"/>\n        <text x=\"560\" y=\"112\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">user clicks</text>\n        <rect x=\"568\" y=\"118\" width=\"54\" height=\"18\" rx=\"4\" fill=\"var(--gold-soft)\" stroke=\"var(--gold)\"/>\n        <text x=\"595\" y=\"131\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8\" fill=\"var(--text)\">200ms</text>\n        <path d=\"M626 127 L660 127\" stroke=\"var(--gold)\" stroke-width=\"1.5\" marker-end=\"url(#poCwvG)\"/>\n        <text x=\"666\" y=\"131\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">next paint</text>\n        <text x=\"14\" y=\"150\" font-family=\"var(--mono)\" font-size=\"8\" fill=\"var(--muted)\">responsiveness</text>\n        <text x=\"120\" y=\"131\" font-family=\"var(--sans)\" font-size=\"10\" fill=\"var(--muted)\">measured on every interaction, not just the first \u2014 the worst one is your score</text>\n\n        <text x=\"14\" y=\"192\" font-family=\"var(--mono)\" font-size=\"10\" fill=\"var(--danger)\">CLS</text>\n        <rect x=\"120\" y=\"172\" width=\"150\" height=\"22\" rx=\"4\" fill=\"var(--panel-2)\" stroke=\"var(--border)\"/>\n        <text x=\"195\" y=\"187\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">content</text>\n        <path d=\"M278 183 L308 183\" stroke=\"var(--danger)\" stroke-width=\"1.5\" marker-end=\"url(#poCwvD)\"/>\n        <text x=\"293\" y=\"176\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8\" fill=\"var(--danger)\">ad loads</text>\n        <rect x=\"316\" y=\"162\" width=\"150\" height=\"16\" rx=\"4\" fill=\"var(--danger)\" opacity=\"0.25\" stroke=\"var(--danger)\"/>\n        <text x=\"391\" y=\"174\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8\" fill=\"var(--text)\">injected banner</text>\n        <rect x=\"316\" y=\"182\" width=\"150\" height=\"22\" rx=\"4\" fill=\"var(--panel-2)\" stroke=\"var(--danger)\" stroke-dasharray=\"3\"/>\n        <text x=\"391\" y=\"197\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">content \u2014 shifted down</text>\n        <text x=\"480\" y=\"188\" font-family=\"var(--sans)\" font-size=\"10\" fill=\"var(--muted)\">the user was reading, or about to tap, the thing that moved</text>\n        <text x=\"14\" y=\"208\" font-family=\"var(--mono)\" font-size=\"8\" fill=\"var(--muted)\">stability</text>\n\n        <text x=\"14\" y=\"238\" font-family=\"var(--sans)\" font-size=\"10.5\" fill=\"var(--muted)\">Three different problems on three different timescales \u2014 which is why \"make the site faster\" is a weaker answer than naming which vital is failing and why.</text>\n        <defs>\n          <marker id=\"poCwvG\" markerWidth=\"8\" markerHeight=\"8\" refX=\"6\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L6,3 L0,6 z\" fill=\"var(--gold)\"/></marker>\n          <marker id=\"poCwvD\" markerWidth=\"8\" markerHeight=\"8\" refX=\"6\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L6,3 L0,6 z\" fill=\"var(--danger)\"/></marker>\n        </defs>\n      </svg>\n      </div>\n      <table class=\"cmp\">\n        <tr><th>Metric</th><th>Target (p75)</th><th>Primary levers</th></tr>\n        <tr><td><strong>LCP</strong> \u2014 Largest Contentful Paint</td><td>&le; 2.5s</td><td>Fast TTFB, <code>fetchpriority=\"high\"</code> + preload on the hero image, eliminate render-blocking CSS/JS, avoid lazy-loading anything above the fold</td></tr>\n        <tr><td><strong>INP</strong> \u2014 Interaction to Next Paint</td><td>&le; 200ms</td><td>Break up long main-thread tasks, yield between chunks, cut unnecessary re-renders, keep heavy work off the interaction path</td></tr>\n        <tr><td><strong>CLS</strong> \u2014 Cumulative Layout Shift</td><td>&le; 0.1</td><td>Reserve space with <code>aspect-ratio</code> or explicit dimensions, <code>size-adjust</code> on fallback fonts, never inject content above existing content</td></tr>\n      </table>\n      <p><strong>Two details that signal you've actually shipped this work:</strong> INP replaced FID in 2024 and is a much harder metric \u2014 FID only measured input <em>delay</em> on the first interaction, while INP measures the full click-to-paint duration across every interaction in the session and reports roughly the worst. Apps that scored well on FID routinely fail INP. And all three grade at the <strong>75th percentile of real users</strong>, so a lab score on your laptop is not a pass.</p>`\n    },\n    {\n      q:\"How do you optimize a React app's rendering performance specifically?\",\n      a:`<p>Work top-down from the biggest lever, not straight to memoization: audit the bundle first (route-level code splitting and tree-shakeable imports usually dwarf any component-level fix), cut client-side JS with Server Components for anything that doesn't need interactivity, then profile with React DevTools to find components that actually re-render too often \u2014 and only then reach for memoization.</p>\n      <p><strong>The 2026 update that changes this answer: the React Compiler.</strong> It is now the recommended default for new React 19 applications, and it auto-memoizes components and values at build time by analysing which values actually change. In a compiled codebase, hand-written <code>useMemo</code>/<code>useCallback</code>/<code>React.memo</code> is largely unnecessary and often counterproductive \u2014 the compiler does it more accurately and without the reviewer overhead. Saying \"I'd check whether the project is on the compiler before hand-optimizing anything\" is a stronger, more current answer than reciting the manual-memoization rules.</p>\n      <ul>\n        <li><strong>If the compiler is enabled:</strong> stop hand-memoizing; write idiomatic code and let it do the work. The remaining manual cases are narrow \u2014 values crossing a boundary the compiler can't see, or expensive computations you want cached across renders for reasons other than reference equality.</li>\n        <li><strong>If it isn't (an older codebase):</strong> the old rules still apply \u2014 <code>React.memo</code> around a component that re-renders often with unchanged props, <code>useMemo</code> around a genuinely expensive computation (not an object literal), <code>useCallback</code> when a function feeds a memoized child. Wrapping everything by default adds comparison overhead for no benefit, and is a well-known AI-generated-code smell.</li>\n        <li><strong>Concurrent features are about perceived performance:</strong> <code>useTransition</code> marks an update as interruptible so typing stays responsive while an expensive list re-renders; <code>useDeferredValue</code> lets an expensive subtree lag behind a fast-changing input. Neither makes the render faster \u2014 they change what the user waits on, which is usually what actually matters.</li>\n        <li><strong>Virtualize long lists</strong> (<code>react-window</code>, TanStack Virtual) and keep stable, unique <code>key</code>s \u2014 never an array index on a reorderable list.</li>\n      </ul>`\n    },\n    {\n      q:\"How do you keep the main thread free for user input?\",\n      a:`<p>To answer this well you have to be able to draw the browser's rendering pipeline, because the entire argument rests on which stages a given property forces the browser to redo:</p>\n      <div class=\"diagram\">\n      <svg viewBox=\"0 0 800 292\" xmlns=\"http://www.w3.org/2000/svg\" style=\"width:100%;max-width:800px\">\n        <rect x=\"80\" y=\"54\" width=\"130\" height=\"44\" rx=\"8\" fill=\"var(--panel-2)\" stroke=\"var(--border)\"/>\n        <text x=\"145\" y=\"81\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">JavaScript</text>\n        <rect x=\"224\" y=\"54\" width=\"130\" height=\"44\" rx=\"8\" fill=\"var(--panel-2)\" stroke=\"var(--border)\"/>\n        <text x=\"289\" y=\"81\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">Style</text>\n        <rect x=\"368\" y=\"54\" width=\"130\" height=\"44\" rx=\"8\" fill=\"var(--panel-2)\" stroke=\"var(--border)\"/>\n        <text x=\"433\" y=\"81\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">Layout</text>\n        <rect x=\"512\" y=\"54\" width=\"130\" height=\"44\" rx=\"8\" fill=\"var(--panel-2)\" stroke=\"var(--border)\"/>\n        <text x=\"577\" y=\"81\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">Paint</text>\n        <rect x=\"656\" y=\"54\" width=\"130\" height=\"44\" rx=\"8\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n        <text x=\"721\" y=\"81\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">Composite</text>\n        <path d=\"M212 76 L220 76\" stroke=\"var(--muted)\" stroke-width=\"1.5\" marker-end=\"url(#poPipeM)\"/>\n        <path d=\"M356 76 L364 76\" stroke=\"var(--muted)\" stroke-width=\"1.5\" marker-end=\"url(#poPipeM)\"/>\n        <path d=\"M500 76 L508 76\" stroke=\"var(--muted)\" stroke-width=\"1.5\" marker-end=\"url(#poPipeM)\"/>\n        <path d=\"M644 76 L652 76\" stroke=\"var(--muted)\" stroke-width=\"1.5\" marker-end=\"url(#poPipeM)\"/>\n\n        <path d=\"M80 40 L642 40\" stroke=\"var(--danger)\" stroke-width=\"1.5\"/>\n        <path d=\"M80 40 L80 48 M642 40 L642 48\" stroke=\"var(--danger)\" stroke-width=\"1.5\"/>\n        <text x=\"361\" y=\"32\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--danger)\">MAIN THREAD \u2014 competes with your JS and with user input</text>\n        <path d=\"M656 40 L786 40\" stroke=\"var(--accent)\" stroke-width=\"1.5\"/>\n        <path d=\"M656 40 L656 48 M786 40 L786 48\" stroke=\"var(--accent)\" stroke-width=\"1.5\"/>\n        <text x=\"721\" y=\"32\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--accent)\">COMPOSITOR</text>\n\n        <text x=\"14\" y=\"140\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--danger)\">width, top, height</text>\n        <rect x=\"368\" y=\"128\" width=\"418\" height=\"20\" rx=\"5\" fill=\"var(--danger)\" opacity=\"0.25\" stroke=\"var(--danger)\"/>\n        <text x=\"577\" y=\"142\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--text)\">re-runs layout + paint + composite &mdash; every frame</text>\n\n        <text x=\"14\" y=\"176\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--gold)\">background-color</text>\n        <rect x=\"512\" y=\"164\" width=\"274\" height=\"20\" rx=\"5\" fill=\"var(--gold-soft)\" stroke=\"var(--gold)\"/>\n        <text x=\"649\" y=\"178\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--text)\">re-runs paint + composite</text>\n\n        <text x=\"14\" y=\"212\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--accent)\">transform, opacity</text>\n        <rect x=\"656\" y=\"200\" width=\"130\" height=\"20\" rx=\"5\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n        <text x=\"721\" y=\"214\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--text)\">composite only</text>\n\n        <text x=\"14\" y=\"252\" font-family=\"var(--sans)\" font-size=\"10.5\" fill=\"var(--muted)\">Bar length is the work per frame. Only the bottom row runs off the main thread \u2014 which is why a <tspan font-family=\"var(--mono)\" fill=\"var(--accent)\">transform</tspan> animation stays at 60fps</text>\n        <text x=\"14\" y=\"268\" font-family=\"var(--sans)\" font-size=\"10.5\" fill=\"var(--muted)\">while JavaScript is busy, and a <tspan font-family=\"var(--mono)\" fill=\"var(--danger)\">width</tspan> animation stutters the moment anything else needs the main thread.</text>\n        <defs><marker id=\"poPipeM\" markerWidth=\"8\" markerHeight=\"8\" refX=\"6\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L6,3 L0,6 z\" fill=\"var(--muted)\"/></marker></defs>\n      </svg>\n      </div>\n      <pre><code>/* expensive \u2014 layout + paint + composite on every frame */\n.card:hover { width: 320px; }\n\n/* cheap \u2014 compositor only, main thread stays free */\n.card:hover { transform: scale(1.05); }</code></pre>\n      <p>For genuinely heavy computation (parsing a large dataset, image processing), move it off the main thread entirely with a Web Worker. For work that must stay on the main thread but shouldn't block input, break it into chunks and <strong>yield between them</strong>:</p>\n      <pre><code>// modern, INP-aware: yield to the browser so pending input runs first\nasync function processAll(items) {\n  for (const chunk of chunks(items, 50)) {\n    processChunk(chunk);\n    // scheduler.yield() resumes with higher priority than setTimeout(0),\n    // so the loop doesn't get starved after letting input through\n    await scheduler.yield();\n  }\n}</code></pre>\n      <ul>\n        <li><strong>Any task over 50ms is a \"long task\"</strong> and is what INP is actually measuring \u2014 the fix is almost always chunking plus yielding, not making the work itself faster.</li>\n        <li><code>scheduler.postTask()</code> for explicitly prioritized work, <code>requestIdleCallback</code> for genuinely deferrable background work (analytics flushes, prefetch warming).</li>\n        <li><strong>Also a perf trap:</strong> animating <code>box-shadow</code> or <code>filter</code> forces repaints \u2014 animate a pre-rendered shadow on a pseudo-element's <code>opacity</code> instead. And <code>will-change: transform</code> promotes an element to its own layer ahead of time, but every layer costs memory \u2014 use it on the few elements that actually animate, not defensively.</li>\n      </ul>`\n    },\n    {\n      q:\"What about the assets themselves \u2014 images, fonts, bundles?\",\n      a:`<p>Images are almost always the largest bytes on a page and the usual LCP element, so they're the first place to look. Serve at the resolution actually displayed (<code>next/image</code> or <code>srcset</code>/<code>sizes</code> \u2014 never a desktop-sized file to a phone), in a modern format (AVIF with a WebP fallback; AVIF typically wins 20\u201330% over WebP at equivalent quality), lazy-load everything below the fold and explicitly <em>not</em> the hero (a lazy-loaded LCP image is a classic self-inflicted regression), and always set dimensions so the space is reserved before it loads.</p>\n      <ul>\n        <li><strong>Fonts:</strong> <code>font-display: swap</code>, preload only the one critical face, subset to the characters actually used, and use <code>size-adjust</code>/<code>ascent-override</code> on the fallback so the swap doesn't shift layout. Self-host rather than pulling from a third-party origin \u2014 it removes a DNS+TLS round trip on the critical path.</li>\n        <li><strong>Bundles:</strong> tree-shakeable imports (<code>import { debounce } from 'lodash-es'</code>, never the whole package), route-level splitting, and a bundle analyzer run regularly \u2014 the recurring finding is a full charting, date, or icon library pulled in for one small piece of UI.</li>\n        <li><strong>Third-party scripts are usually the worst offender and the least examined.</strong> Tag managers, chat widgets, and analytics run on the main thread, block interaction, and are outside your build's control entirely. Audit them on a schedule, load them <code>async</code>/deferred, and consider moving the heavy ones off-thread (Partytown) \u2014 a chat widget nobody opens should not be costing every visitor 300ms of INP.</li>\n      </ul>`\n    }\n  ]\n},\n{\n  id:\"network\", title:\"Network & Edge\",\n  questions:[\n    {\n      q:\"What's the caching policy at the CDN, and does the transport protocol matter?\",\n      a:`<p>Versioned static assets get a long <code>max-age</code> plus <code>immutable</code> \u2014 they never need revalidation because a content change means a new URL. Public, cacheable HTML/API responses get a short TTL plus <code>stale-while-revalidate</code>, serving the stale copy instantly while a background request refreshes it, so no user ever waits on a cache miss.</p>\n      <pre><code>// versioned bundle \u2014 never revalidate\nCache-Control: public, max-age=31536000, immutable\n\n// public HTML \u2014 fresh-ish, but never make the user wait for it\nCache-Control: public, max-age=0, s-maxage=60, stale-while-revalidate=300\n\n// personalized response \u2014 never store in a shared cache\nCache-Control: private, no-store</code></pre>\n      <ul>\n        <li><strong>Protocols:</strong> HTTP/2 multiplexing (many requests over one connection) is table stakes; HTTP/3 over QUIC additionally removes transport-level head-of-line blocking and survives network switches, which matters most on lossy mobile links. Both are usually a CDN toggle rather than an application change.</li>\n        <li><strong>Compression:</strong> Brotli beats gzip by roughly 15\u201320% at equivalent settings. <strong>zstd</strong> is now broadly supported and worth enabling where your CDN offers it \u2014 comparable ratios to Brotli at meaningfully faster compression, which matters for dynamic responses compressed per-request rather than once at build time.</li>\n        <li><strong>The metric that tells you any of this is working is cache hit ratio.</strong> A hit ratio well under 90% on static assets almost always means the cache key is varying on something it shouldn't \u2014 a tracking query parameter, an over-broad <code>Vary</code> header, a missing normalization rule. That's invisible until someone actually looks at it.</li>\n      </ul>`\n    },\n    {\n      q:\"When does edge rendering actually help \u2014 and what else can the edge do?\",\n      a:`<p>For SSR pages that are mostly the same across users \u2014 a product page, a marketing page \u2014 running the render at an edge location close to the user (Cloudflare Workers, Vercel Edge Functions) removes the round trip to a single origin region, without the cost and complexity of true multi-region infrastructure. It's the wrong tool for personalized, database-heavy renders, where the edge function ends up making the same slow round trip to the origin database anyway and you've added a hop for nothing.</p>\n      <p><strong>The stronger 2026 framing is that the edge is no longer only about rendering.</strong> A few things belong there specifically because they're cheap and latency-sensitive:</p>\n      <ul>\n        <li><strong>Auth/redirect checks at the edge</strong> \u2014 bounce an unauthenticated request before it consumes an origin round trip at all.</li>\n        <li><strong>A/B assignment and feature-flag evaluation</strong> \u2014 decided at the edge so the user gets a single, correctly-variant response rather than a flash of the wrong one after hydration.</li>\n        <li><strong>Partial Prerendering</strong> (Next.js) is the pattern that makes this concrete: a static shell served instantly from the edge, with the dynamic per-user holes streamed in \u2014 previously you had to choose one rendering mode for the entire route.</li>\n        <li><strong>Speculative navigation:</strong> the Speculation Rules API lets you declaratively prefetch or fully <em>prerender</em> the next likely page, so a click lands on an already-rendered document. It's the closest thing to free instant navigation available today \u2014 with the obvious caveat that prerendering pages nobody visits wastes their bandwidth and your origin capacity, so scope the rules to high-confidence links.</li>\n      </ul>\n      <pre><code>&lt;script type=\"speculationrules\"&gt;\n{ \"prerender\": [{\n    \"where\": { \"selector_matches\": \".product-card a\" },\n    \"eagerness\": \"moderate\"\n}] }\n&lt;/script&gt;</code></pre>`\n    }\n  ]\n},\n{\n  id:\"api\", title:\"API & Gateway Layer\",\n  questions:[\n    {\n      q:\"How do you protect and speed up the API layer itself?\",\n      a:`<p>Rate limiting (token bucket to allow bursts, sliding window for accuracy) protects capacity from any single client, enforced at the edge/gateway so a rejected request never reaches an application server. Keep-alive connection reuse avoids a fresh TCP+TLS handshake on every internal call. Payload size matters directly: paginate list endpoints with cursors rather than offsets (an <code>OFFSET 100000</code> makes the database walk and discard 100,000 rows), and return only the fields the client needs.</p>\n      <ul>\n        <li><strong>Compress responses</strong> and stop sending fields nobody reads \u2014 the cheapest API optimization is usually deleting data from the response shape.</li>\n        <li><strong>Timeouts on every hop</strong>, tuned so an inner timeout is shorter than the outer one that depends on it. Mismatched timeouts mean the caller gives up while the callee keeps working, burning capacity on a result nobody will read.</li>\n      </ul>`\n    },\n    {\n      q:\"How do you avoid N+1 request patterns?\",\n      a:`<div class=\"diagram\">\n      <svg viewBox=\"0 0 760 236\" xmlns=\"http://www.w3.org/2000/svg\" style=\"width:100%;max-width:760px\">\n        <text x=\"14\" y=\"24\" font-family=\"var(--mono)\" font-size=\"9.5\" fill=\"var(--danger)\">NAIVE &mdash; 1 + N round trips</text>\n        <rect x=\"14\" y=\"34\" width=\"112\" height=\"150\" rx=\"8\" fill=\"var(--panel-2)\" stroke=\"var(--border)\"/>\n        <text x=\"70\" y=\"112\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">Resolver</text>\n        <rect x=\"248\" y=\"34\" width=\"112\" height=\"150\" rx=\"8\" fill=\"var(--panel-2)\" stroke=\"var(--border)\"/>\n        <text x=\"304\" y=\"112\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">Database</text>\n        <path d=\"M130 50 L244 50\" stroke=\"var(--accent)\" stroke-width=\"1.5\" marker-end=\"url(#poNplusA)\"/>\n        <text x=\"187\" y=\"45\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8\" fill=\"var(--muted)\">SELECT posts</text>\n        <path d=\"M130 76 L244 76\" stroke=\"var(--danger)\" stroke-width=\"1.2\" marker-end=\"url(#poNplusD)\"/>\n        <path d=\"M130 96 L244 96\" stroke=\"var(--danger)\" stroke-width=\"1.2\" marker-end=\"url(#poNplusD)\"/>\n        <path d=\"M130 116 L244 116\" stroke=\"var(--danger)\" stroke-width=\"1.2\" marker-end=\"url(#poNplusD)\"/>\n        <path d=\"M130 136 L244 136\" stroke=\"var(--danger)\" stroke-width=\"1.2\" marker-end=\"url(#poNplusD)\"/>\n        <path d=\"M130 156 L244 156\" stroke=\"var(--danger)\" stroke-width=\"1.2\" marker-end=\"url(#poNplusD)\"/>\n        <text x=\"187\" y=\"174\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8\" fill=\"var(--danger)\">SELECT user WHERE id=? &times; N</text>\n        <rect x=\"14\" y=\"196\" width=\"346\" height=\"26\" rx=\"6\" fill=\"var(--danger)\" opacity=\"0.18\" stroke=\"var(--danger)\"/>\n        <text x=\"187\" y=\"213\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"10\" fill=\"var(--text)\">100 posts &rarr; 101 queries</text>\n\n        <line x1=\"384\" y1=\"20\" x2=\"384\" y2=\"222\" stroke=\"var(--border)\" stroke-dasharray=\"3\"/>\n\n        <text x=\"406\" y=\"24\" font-family=\"var(--mono)\" font-size=\"9.5\" fill=\"var(--accent)\">BATCHED &mdash; 2 round trips</text>\n        <rect x=\"406\" y=\"34\" width=\"112\" height=\"150\" rx=\"8\" fill=\"var(--panel-2)\" stroke=\"var(--border)\"/>\n        <text x=\"462\" y=\"104\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">Resolver</text>\n        <text x=\"462\" y=\"120\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8\" fill=\"var(--accent)\">+ DataLoader</text>\n        <rect x=\"634\" y=\"34\" width=\"112\" height=\"150\" rx=\"8\" fill=\"var(--panel-2)\" stroke=\"var(--border)\"/>\n        <text x=\"690\" y=\"112\" text-anchor=\"middle\" font-family=\"var(--sans)\" font-size=\"11\" fill=\"var(--text)\">Database</text>\n        <path d=\"M522 62 L630 62\" stroke=\"var(--accent)\" stroke-width=\"1.5\" marker-end=\"url(#poNplusA)\"/>\n        <text x=\"576\" y=\"56\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8\" fill=\"var(--muted)\">SELECT posts</text>\n        <path d=\"M522 146 L630 146\" stroke=\"var(--accent)\" stroke-width=\"2\" marker-end=\"url(#poNplusA)\"/>\n        <text x=\"576\" y=\"140\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8\" fill=\"var(--accent)\">WHERE id = ANY($1)</text>\n        <text x=\"576\" y=\"166\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8\" fill=\"var(--muted)\">all N ids, one query</text>\n        <rect x=\"406\" y=\"196\" width=\"340\" height=\"26\" rx=\"6\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n        <text x=\"576\" y=\"213\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"10\" fill=\"var(--text)\">100 posts &rarr; 2 queries</text>\n        <defs>\n          <marker id=\"poNplusA\" markerWidth=\"8\" markerHeight=\"8\" refX=\"6\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L6,3 L0,6 z\" fill=\"var(--accent)\"/></marker>\n          <marker id=\"poNplusD\" markerWidth=\"8\" markerHeight=\"8\" refX=\"6\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L6,3 L0,6 z\" fill=\"var(--danger)\"/></marker>\n        </defs>\n      </svg>\n      </div>\n      <p>The N+1 problem \u2014 fetching a list, then making one additional call per item for related data \u2014 appears at every layer: a REST client calling <code>/posts</code> then <code>/users/:id</code> per post, or a GraphQL resolver naively re-querying per field. DataLoader collects every <code>.load(id)</code> issued synchronously within one tick, deduplicates the ids, and flushes them as a single batched query \u2014 turning 101 round trips into 2.</p>\n      <ul>\n        <li><strong>Two rules that make it work:</strong> create the loader per request (its cache must never span users), and return results in exactly the order the ids arrived \u2014 DataLoader matches positionally, not by value.</li>\n        <li><strong>It's the round trips, not the rows.</strong> 100 queries of 1ms each is 100ms of mostly-waiting; one query returning 100 rows is ~2ms. The fix is about eliminating latency multiplication, which is also why the same pattern matters for HTTP calls between services, not just database access.</li>\n        <li><strong>gRPC vs. REST between internal services:</strong> the binary protocol and native streaming meaningfully reduce per-call overhead at high volume, at the cost of being harder to debug and less universally tooled \u2014 a real trade-off worth naming rather than assuming one is strictly superior.</li>\n      </ul>`\n    }\n  ]\n},\n{\n  id:\"server\", title:\"Application Server\",\n  questions:[\n    {\n      q:\"What does Node.js-specific performance hygiene look like?\",\n      a:`<div class=\"diagram\">\n      <svg viewBox=\"0 0 760 228\" xmlns=\"http://www.w3.org/2000/svg\" style=\"width:100%;max-width:760px\">\n        <text x=\"14\" y=\"24\" font-family=\"var(--mono)\" font-size=\"9.5\" fill=\"var(--danger)\">BLOCKING &mdash; one CPU-bound call freezes every concurrent request</text>\n        <text x=\"14\" y=\"52\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--muted)\">main thread</text>\n        <rect x=\"110\" y=\"36\" width=\"60\" height=\"24\" rx=\"4\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n        <text x=\"140\" y=\"52\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8\" fill=\"var(--text)\">req A</text>\n        <rect x=\"172\" y=\"36\" width=\"420\" height=\"24\" rx=\"4\" fill=\"var(--danger)\" opacity=\"0.3\" stroke=\"var(--danger)\"/>\n        <text x=\"382\" y=\"52\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--text)\">heavyComputation() &mdash; 800ms, synchronous</text>\n        <rect x=\"594\" y=\"36\" width=\"60\" height=\"24\" rx=\"4\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n        <text x=\"624\" y=\"52\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8\" fill=\"var(--text)\">req B</text>\n        <text x=\"14\" y=\"82\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--muted)\">queued</text>\n        <rect x=\"172\" y=\"68\" width=\"420\" height=\"18\" rx=\"4\" fill=\"var(--panel-2)\" stroke=\"var(--danger)\" stroke-dasharray=\"3\"/>\n        <text x=\"382\" y=\"81\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--danger)\">reqs B, C, D, E &hellip; all waiting \u2014 none of them asked for the expensive work</text>\n\n        <line x1=\"14\" y1=\"106\" x2=\"746\" y2=\"106\" stroke=\"var(--border)\" stroke-dasharray=\"3\"/>\n\n        <text x=\"14\" y=\"130\" font-family=\"var(--mono)\" font-size=\"9.5\" fill=\"var(--accent)\">OFFLOADED &mdash; event loop stays free</text>\n        <text x=\"14\" y=\"158\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--muted)\">main thread</text>\n        <rect x=\"110\" y=\"142\" width=\"60\" height=\"24\" rx=\"4\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n        <text x=\"140\" y=\"158\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8\" fill=\"var(--text)\">req A</text>\n        <rect x=\"174\" y=\"142\" width=\"56\" height=\"24\" rx=\"4\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n        <text x=\"202\" y=\"158\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8\" fill=\"var(--text)\">req B</text>\n        <rect x=\"234\" y=\"142\" width=\"56\" height=\"24\" rx=\"4\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n        <text x=\"262\" y=\"158\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8\" fill=\"var(--text)\">req C</text>\n        <rect x=\"294\" y=\"142\" width=\"56\" height=\"24\" rx=\"4\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n        <text x=\"322\" y=\"158\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8\" fill=\"var(--text)\">req D</text>\n        <rect x=\"354\" y=\"142\" width=\"56\" height=\"24\" rx=\"4\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n        <text x=\"382\" y=\"158\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"8\" fill=\"var(--text)\">req E</text>\n        <text x=\"420\" y=\"158\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--accent)\">&hellip; served normally throughout</text>\n        <text x=\"14\" y=\"194\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--muted)\">worker</text>\n        <rect x=\"172\" y=\"178\" width=\"420\" height=\"24\" rx=\"4\" fill=\"var(--gold-soft)\" stroke=\"var(--gold)\"/>\n        <text x=\"382\" y=\"194\" text-anchor=\"middle\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--text)\">heavyComputation() &mdash; same 800ms, different thread</text>\n        <path d=\"M172 170 L172 176\" stroke=\"var(--gold)\" stroke-width=\"1.5\" marker-end=\"url(#poEvtG)\"/>\n        <text x=\"620\" y=\"194\" font-family=\"var(--mono)\" font-size=\"8.5\" fill=\"var(--muted)\">result &rarr; callback</text>\n        <defs><marker id=\"poEvtG\" markerWidth=\"8\" markerHeight=\"8\" refX=\"6\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L6,3 L0,6 z\" fill=\"var(--gold)\"/></marker></defs>\n      </svg>\n      </div>\n      <p>Node runs JavaScript on a single thread, so any synchronous CPU-heavy work blocks the event loop entirely \u2014 freezing every request being served concurrently, not just the one that triggered it. That asymmetry is the whole point: one user requesting a report degrades the experience for everyone else on that instance.</p>\n      <pre><code>// blocks the event loop \u2014 every other request waits\napp.get('/report', (req, res) =&gt; {\n  const result = heavyComputation(data); // synchronous, CPU-bound\n  res.json(result);\n});\n\n// offloaded \u2014 event loop stays free for other requests\nconst worker = new Worker('./report-worker.js', { workerData: data });\nworker.on('message', (result) =&gt; res.json(result));</code></pre>\n      <ul>\n        <li><strong>Use a worker pool, not a worker per request</strong> \u2014 spawning a thread costs tens of milliseconds and unbounded spawning is its own outage. <code>piscina</code> is the usual answer.</li>\n        <li><strong>Measure it rather than guessing:</strong> event-loop lag is the single most diagnostic Node metric there is (<code>perf_hooks</code> <code>monitorEventLoopDelay</code>). Rising lag with flat CPU means blocking work; it should be on the dashboard next to the golden signals.</li>\n        <li><strong>Watch for accidental blocking</strong> beyond obvious computation: large <code>JSON.parse</code>/<code>stringify</code>, synchronous <code>fs</code> calls, and a pathological regex (catastrophic backtracking) all block just as effectively.</li>\n        <li><strong>One process uses one core</strong> \u2014 run one container replica per core and let the orchestrator schedule them, which is generally simpler to operate than the built-in <code>cluster</code> module.</li>\n      </ul>`\n    },\n    {\n      q:\"How does horizontal scaling and memory management fit in?\",\n      a:`<p>Stateless app servers (session state in Redis, never in-process) are what make horizontal autoscaling straightforward \u2014 the orchestrator can add or remove instances purely on real load, with no instance holding state that vanishes when it's replaced. Scale on the signal that reflects saturation for your workload: CPU is the default, but request queue depth or event-loop lag is usually the better trigger for an I/O-bound Node service, which can be badly saturated at 40% CPU.</p>\n      <ul>\n        <li><strong>Autoscaling has to react faster than traffic arrives.</strong> If a container takes 90 seconds to become healthy and your spike arrives in 30, the scaling policy is decorative \u2014 either pre-warm ahead of known peaks or keep enough headroom to absorb the ramp.</li>\n        <li><strong>Memory leaks look like slow degradation, not a crash</strong> \u2014 unbounded in-process caches and un-removed event listeners are the two usual culprits. <code>process.memoryUsage()</code> is the quick check; heap snapshots taken minutes apart and diffed are how you find what's actually retained.</li>\n        <li><strong>Tune the GC only after profiling confirms it's the problem.</strong> Raising <code>--max-old-space-size</code> to stop OOM crashes is usually treating a symptom; a leak grows to fill whatever ceiling you set.</li>\n      </ul>`\n    }\n  ]\n},\n{\n  id:\"caching\", title:\"Caching Layer\",\n  questions:[\n    {\n      q:\"Redis, CDN, or in-process \u2014 which cache for which job?\",\n      a:`<p>Three layers with different scope and lifetime. An in-process cache (a <code>Map</code> or LRU in application memory) is the fastest possible option but is per-instance and vanishes on restart \u2014 fine for small, cheap-to-recompute values, and ideal request-scoped. Redis is shared across every instance, survives individual restarts, and is the default for session data, computed aggregates, and anything read far more often than it changes. The CDN caches at the edge, closest to the user, for content that's the same across many users.</p>\n      <ul>\n        <li><strong>The subtle in-process problem at scale:</strong> with 20 instances you have 20 independent caches with independent TTLs, so a user's consecutive requests can land on instances holding different versions of the same value. That inconsistency is invisible in development with one instance running.</li>\n        <li><strong>Redis is a network hop, not free.</strong> ~1ms per call means a request making 30 sequential Redis calls has spent 30ms on cache lookups \u2014 pipeline or <code>MGET</code> them rather than looping.</li>\n      </ul>`\n    },\n    {\n      q:\"What actually breaks a caching layer in production?\",\n      a:`<p>Almost always invalidation, not the caching mechanism. The failure modes worth naming by name:</p>\n      <ul>\n        <li><strong>Stampede (dogpile):</strong> a hot key expires and every concurrent request misses simultaneously, hammering the database at once. Fix with a lock so only one request repopulates while others briefly wait, or serve stale while one background refresh runs.</li>\n        <li><strong>A cache key that doesn't vary by everything the response varies by</strong> \u2014 missing a user ID, locale, or tenant \u2014 silently serves one user's data to another. This is a security incident disguised as a performance win, and it's the reason cache keys deserve code review attention.</li>\n        <li><strong>Thundering herd on restart:</strong> after a Redis failover or a deploy that changes key prefixes, the cache is empty and 100% of traffic hits the origin at once. If the database can't survive that, your cache isn't an optimization \u2014 it's an undeclared hard dependency. Stagger warmup, and load-test with a cold cache at least once so you know.</li>\n        <li><strong>Caching the error.</strong> A 500 cached with a five-minute TTL turns a transient blip into a five-minute outage. Only cache successful responses, deliberately.</li>\n      </ul>`\n    }\n  ]\n},\n{\n  id:\"database\", title:\"Database\",\n  questions:[\n    {\n      q:\"What's the actual process for diagnosing a slow query?\",\n      a:`<p><code>EXPLAIN ANALYZE</code> shows what the planner chose and what actually happened when it ran. The single most useful signal on the page is the gap between estimated and actual row counts: a large gap means stale statistics (run <code>ANALYZE</code>), while accurate estimates paired with a bad plan \u2014 a sequential scan where an index scan should apply \u2014 points to a missing or unusable index. Add <code>BUFFERS</code> to see whether the data came from cache or disk, which distinguishes \"this query is inefficient\" from \"this query is cold.\"</p>\n      <ul>\n        <li><strong>Index the whole predicate, in the right order:</strong> a composite index on <code>(carrier_id, state, created_at DESC)</code> serves filtering and sorting in one pass; the same columns in the wrong order serve neither well. Build with <code>CONCURRENTLY</code> on a live table.</li>\n        <li><strong>Beyond the default B-tree:</strong> partial indexes for a frequently-filtered subset, covering indexes (<code>INCLUDE</code>) for index-only scans, GIN for JSONB/full-text/arrays.</li>\n        <li><strong>An index that can't be used</strong> is the common silent failure \u2014 wrapping the column in a function (<code>WHERE lower(email) = ...</code>) defeats a plain index unless you built the matching expression index.</li>\n        <li><strong>Rule out the pool before touching the query.</strong> Connections queuing for a PgBouncer slot present exactly as \"the query got slow\" from the application's side, while the query itself is unchanged. Check <code>pg_stat_statements</code> for total time (frequency &times; duration), not just the slowest single execution \u2014 a 5ms query run 10,000 times a minute costs more than a 2-second report run hourly.</li>\n      </ul>`\n    },\n    {\n      q:\"Beyond a single query, how do you scale database throughput?\",\n      a:`<p>Read replicas absorb read traffic away from the primary, which is the majority of load for most consumer apps \u2014 with the caveat that replication is asynchronous, so a read immediately following a write can observe stale data and the application has to handle that deliberately. Denormalization trades write complexity and storage for read speed, worth it once a specific join is proven hot. Partitioning splits a large table by a key (often time-based) so queries and maintenance touch only the relevant slice \u2014 and makes deleting old data a partition drop instead of a mass <code>DELETE</code>. Sharding spreads data across separate instances entirely: the biggest operational commitment on the list, and the last one to reach for.</p>\n      <ul>\n        <li><strong>Batch writes.</strong> A single multi-row <code>INSERT</code> or <code>COPY</code> is dramatically faster than N individual statements \u2014 same total rows, a fraction of the round trips and transaction overhead.</li>\n        <li><strong>Long transactions are a scaling problem, not just a slow query.</strong> In Postgres they hold back vacuum, causing table bloat that degrades every other query \u2014 keep transactions short and never hold one open across an external API call.</li>\n        <li><strong>Keep analytics off the transactional database.</strong> One unbounded dashboard query scanning millions of rows will degrade the product for everyone; replicate to a columnar store and let it run there.</li>\n      </ul>`\n    }\n  ]\n},\n{\n  id:\"async\", title:\"Async & Messaging\",\n  questions:[\n    {\n      q:\"How do you tune a queue-based system for throughput?\",\n      a:`<p>Batch processing (consuming and handling N messages at once instead of one at a time) amortizes fixed per-operation overhead across many items \u2014 a single batched database insert instead of N individual ones is frequently the largest throughput win available. Autoscale workers off queue depth rather than a fixed instance count, so capacity tracks real backlog. Backpressure \u2014 letting queue depth signal a slow consumer back to the producer instead of publishing blindly \u2014 is what stops a temporarily-slow consumer from becoming an outage.</p>\n      <ul>\n        <li><strong>Queue depth alone is the wrong alarm; watch <em>age</em> too.</strong> A queue of 10,000 messages draining in 30 seconds is healthy. A queue of 50 messages where the oldest is 20 minutes old means a poison message is blocking the consumer \u2014 depth looks fine while the system is broken.</li>\n        <li><strong>Separate queues by priority and by workload shape.</strong> A password-reset email stuck behind 50,000 queued analytics events is a support ticket; a slow consumer on its own queue only delays its own work.</li>\n        <li><strong>Batch size is a latency/throughput trade-off:</strong> bigger batches mean better throughput and worse per-message latency, plus a larger blast radius when one message in the batch fails. Make that choice deliberately rather than accepting a library default.</li>\n      </ul>`\n    }\n  ]\n},\n{\n  id:\"observability\", title:\"Observability & Validation\",\n  questions:[\n    {\n      q:\"How do you find the real bottleneck in a system with many services?\",\n      a:`<p>Golden signals (latency, traffic, errors, saturation) per service tell you <em>which</em> service is unhealthy; a distributed trace tells you <em>why</em> a specific request was slow, by showing the full span tree across every service it touched \u2014 frequently revealing the bottleneck isn't where you'd have guessed, but in a downstream call the \"slow\" service was merely waiting on. This is the mindset loop made concrete: metrics say something is wrong and roughly where, the trace says exactly which span in exactly which request, and that's what you profile.</p>\n      <ul>\n        <li><strong>Trace the slow requests specifically.</strong> Tail-based sampling keeps the traces that were slow or errored rather than a uniform random 1% \u2014 a random sample of a p99 problem mostly returns healthy requests.</li>\n        <li><strong>A flat span with a long duration and no children</strong> is where profiling starts: the time is inside that process, not in something it called.</li>\n        <li><strong>Continuous profiling</strong> (Pyroscope, or a cloud profiler) closes the last gap \u2014 it tells you which function inside that span burned the CPU, in production, without reproducing the problem locally.</li>\n      </ul>`\n    },\n    {\n      q:\"How do you validate a performance fix before it reaches production?\",\n      a:`<p>Profiling tells you what to change; load testing tells you whether the change survives contact with real concurrency. The distinction that matters is that single-request latency and behaviour under load are different properties \u2014 a fix can improve one and regress the other (adding a cache that serializes on a lock looks great in isolation and collapses at 500 concurrent requests).</p>\n      <ul>\n        <li><strong>Three test shapes, three different questions:</strong> a <em>soak</em> test over hours answers \"does anything leak?\"; a <em>spike</em> test answers \"does autoscaling react fast enough?\"; a <em>stress</em> test to the breaking point answers \"where is the cliff, and how does it fail when we reach it?\" \u2014 graceful degradation or total collapse.</li>\n        <li><strong>Test against production-like data volumes.</strong> A query plan on 10,000 seeded rows tells you nothing about the same query on 50 million; the planner will choose differently.</li>\n        <li><strong>k6 or Gatling</strong> for HTTP load, scripted as code and run in CI against a staging environment so the result is a tracked number over time, not a one-off exercise before launch.</li>\n        <li><strong>Close the loop in production anyway:</strong> ship behind a flag, compare the metric against a control cohort, and be willing to conclude the optimization didn't matter. Fixes that were obviously going to help and measurably didn't are common, and noticing that is the discipline the whole page is built on.</li>\n      </ul>`\n    }\n  ]\n},\n{\n  id:\"reference\", title:\"Reference Numbers & Summary\",\n  questions:[\n    {\n      q:\"What latency numbers should you just know, cold?\",\n      a:`<div class=\"diagram\">\n      <svg viewBox=\"0 0 780 262\" xmlns=\"http://www.w3.org/2000/svg\" style=\"width:100%;max-width:780px\">\n        <text x=\"14\" y=\"22\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--muted)\">bar length is logarithmic \u2014 each step right is roughly 10&times; slower</text>\n        <text x=\"14\" y=\"52\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--text)\">L1 cache</text>\n        <rect x=\"196\" y=\"40\" width=\"57\" height=\"16\" rx=\"3\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n        <text x=\"261\" y=\"52\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--muted)\">~1 ns</text>\n        <text x=\"14\" y=\"78\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--text)\">RAM</text>\n        <rect x=\"196\" y=\"66\" width=\"171\" height=\"16\" rx=\"3\" fill=\"var(--accent-soft)\" stroke=\"var(--accent)\"/>\n        <text x=\"375\" y=\"78\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--muted)\">~100 ns</text>\n        <text x=\"14\" y=\"104\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--text)\">SSD random read</text>\n        <rect x=\"196\" y=\"92\" width=\"353\" height=\"16\" rx=\"3\" fill=\"var(--gold-soft)\" stroke=\"var(--gold)\"/>\n        <text x=\"557\" y=\"104\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--muted)\">~150 &micro;s</text>\n        <text x=\"14\" y=\"130\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--text)\">Redis round trip</text>\n        <rect x=\"196\" y=\"118\" width=\"383\" height=\"16\" rx=\"3\" fill=\"var(--gold-soft)\" stroke=\"var(--gold)\"/>\n        <text x=\"587\" y=\"130\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--muted)\">~0.5 ms</text>\n        <text x=\"14\" y=\"156\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--text)\">Same-region hop</text>\n        <rect x=\"196\" y=\"144\" width=\"400\" height=\"16\" rx=\"3\" fill=\"var(--gold-soft)\" stroke=\"var(--gold)\"/>\n        <text x=\"604\" y=\"156\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--muted)\">~1 ms</text>\n        <text x=\"14\" y=\"182\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--text)\">DB query (indexed)</text>\n        <rect x=\"196\" y=\"170\" width=\"440\" height=\"16\" rx=\"3\" fill=\"var(--gold-soft)\" stroke=\"var(--gold)\"/>\n        <text x=\"644\" y=\"182\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--muted)\">~1\u201310 ms</text>\n        <text x=\"14\" y=\"208\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--text)\">Cross-continent RTT</text>\n        <rect x=\"196\" y=\"196\" width=\"519\" height=\"16\" rx=\"3\" fill=\"var(--danger)\" opacity=\"0.3\" stroke=\"var(--danger)\"/>\n        <text x=\"723\" y=\"208\" font-family=\"var(--mono)\" font-size=\"9\" fill=\"var(--muted)\">~120 ms</text>\n        <line x1=\"196\" y1=\"30\" x2=\"196\" y2=\"218\" stroke=\"var(--border)\"/>\n        <text x=\"14\" y=\"242\" font-family=\"var(--sans)\" font-size=\"10.5\" fill=\"var(--muted)\">Memory beats disk by ~100,000&times;; a same-region hop beats a cross-continent one by ~100&times;. That single pair of ratios is the entire</text>\n        <text x=\"14\" y=\"256\" font-family=\"var(--sans)\" font-size=\"10.5\" fill=\"var(--muted)\">justification for both caching and for putting infrastructure close to users \u2014 and it's why the speed of light is a real design constraint.</text>\n      </svg>\n      </div>\n      <p>These come up constantly as a sanity check on any estimate. The exact figures vary by hardware generation; the <em>relative</em> gaps are what's worth internalizing \u2014 they're what let you reject a proposed design in your head before drawing it, because \"that's 50 cross-region calls on the critical path\" becomes an immediate 5-second latency estimate rather than a vague concern.</p>`\n    },\n    {\n      q:\"One summary table for the whole stack \u2014 what's the lever at each layer?\",\n      a:`<p>A closing table like this is a strong way to end the answer, showing the whole stack was covered systematically rather than as a scattered list of individually-interesting facts.</p>\n      <table class=\"cmp\">\n        <tr><th>Layer</th><th>Primary lever</th><th>Diagnostic to reach for</th></tr>\n        <tr><td><strong>Client</strong></td><td>Core Web Vitals, code splitting, compositor-only animation, Server Components + React Compiler</td><td>RUM p75, Lighthouse, React DevTools Profiler</td></tr>\n        <tr><td><strong>Network / Edge</strong></td><td>CDN cache policy, HTTP/3, Brotli/zstd, edge rendering, speculative prefetch</td><td>Cache hit ratio, waterfall, TTFB by region</td></tr>\n        <tr><td><strong>API / Gateway</strong></td><td>Rate limiting, batching/DataLoader, cursor pagination, connection reuse</td><td>p95/p99 per endpoint, query count per request</td></tr>\n        <tr><td><strong>App server</strong></td><td>Non-blocking event loop, worker pool, horizontal autoscaling</td><td>Event-loop lag, heap growth, continuous profiler</td></tr>\n        <tr><td><strong>Cache</strong></td><td>Right layer for the job, stampede protection, correct cache keys</td><td>Hit ratio, origin load after a cold start</td></tr>\n        <tr><td><strong>Database</strong></td><td>Indexing, read replicas, pooling, partitioning, batched writes</td><td><code>EXPLAIN ANALYZE</code>, <code>pg_stat_statements</code></td></tr>\n        <tr><td><strong>Async</strong></td><td>Batching, autoscale-by-queue-depth, backpressure, priority queues</td><td>Queue depth <em>and</em> oldest-message age</td></tr>\n        <tr><td><strong>Everywhere</strong></td><td>Budgets in CI so it doesn't regress next sprint</td><td>Golden signals to find it, traces to explain it</td></tr>\n      </table>`\n    }\n  ]\n}\n];\n// ---------- render ----------\nconst navList = document.getElementById('navList');\nconst content = document.getElementById('content');\nlet activeId = DATA[0].id;\nconst STORAGE_KEY = 'performance-optimization-reviewed-v1';\n\nfunction loadReviewed(){\n  try{\n    const raw = localStorage.getItem(STORAGE_KEY);\n    return raw ? JSON.parse(raw) : {};\n  }catch(e){ return {}; }\n}\nfunction saveReviewed(obj){\n  try{ localStorage.setItem(STORAGE_KEY, JSON.stringify(obj)); }catch(e){}\n}\nlet reviewed = loadReviewed();\n\nfunction totalQuestions(){\n  return DATA.reduce((n,s)=>n+s.questions.length,0);\n}\nfunction reviewedCount(){\n  return Object.values(reviewed).filter(Boolean).length;\n}\nfunction updateProgress(){\n  const total = totalQuestions();\n  const done = reviewedCount();\n  document.getElementById('progressNum').textContent = done + ' / ' + total;\n  document.getElementById('progressFill').style.width = (total? (done/total*100):0) + '%';\n}\n\nfunction renderNav(){\n  navList.innerHTML = '';\n  DATA.forEach((section, idx)=>{\n    const btn = document.createElement('button');\n    btn.className = 'nav-item' + (section.id===activeId ? ' active':'');\n    btn.innerHTML = `<span class=\"nav-num\">${String(idx+1).padStart(2,'0')}</span><span class=\"nav-label\">${section.title}</span><span class=\"nav-count\">${section.questions.length}</span>`;\n    btn.addEventListener('click', ()=>{ activeId = section.id; renderNav(); renderContent(); });\n    navList.appendChild(btn);\n  });\n}\n\nfunction qKey(sectionId, qIdx){ return sectionId + ':' + qIdx; }\n\nfunction renderContent(pendingQIdx){\n  content.innerHTML = '';\n  const section = DATA.find(s=>s.id===activeId);\n  const idx = DATA.findIndex(s=>s.id===activeId);\n  const head = document.createElement('div');\n  head.className = 'section-head';\n  head.innerHTML = `<span class=\"num\">${String(idx+1).padStart(2,'0')}</span><h2>${section.title}</h2><span class=\"count\">${section.questions.length} question${section.questions.length>1?'s':''}</span>`;\n  content.appendChild(head);\n\n  section.questions.forEach((item, qIdx)=>{\n    const key = qKey(section.id, qIdx);\n    const isDone = !!reviewed[key];\n    const card = document.createElement('div');\n    card.className = 'qcard';\n\n    const qhead = document.createElement('div');\n    qhead.className = 'qhead';\n    qhead.innerHTML = `\n      <span class=\"qcheck ${isDone?'done':''}\"></span>\n      <span class=\"qtext ${isDone?'done-text':''}\">${item.q}</span>\n      <span class=\"qchevron\">\u203a</span>\n    `;\n    const checkEl = qhead.querySelector('.qcheck');\n    const textEl = qhead.querySelector('.qtext');\n    checkEl.addEventListener('click', (e)=>{\n      e.stopPropagation();\n      reviewed[key] = !reviewed[key];\n      saveReviewed(reviewed);\n      checkEl.classList.toggle('done');\n      textEl.classList.toggle('done-text');\n      updateProgress();\n    });\n    qhead.addEventListener('click', ()=>{ card.classList.toggle('open'); });\n\n    const body = document.createElement('div');\n    body.className = 'qbody';\n    body.innerHTML = item.a;\n\n    card.appendChild(qhead);\n    card.appendChild(body);\n    content.appendChild(card);\n  });\n\n  if(typeof pendingQIdx === 'number'){\n    const target = content.children[pendingQIdx + 1];\n    if(target){\n      target.classList.add('open');\n      target.scrollIntoView({ behavior:'smooth', block:'start' });\n    }\n  }\n}\n\ndocument.getElementById('resetBtn').addEventListener('click', ()=>{\n  reviewed = {};\n  saveReviewed(reviewed);\n  renderContent();\n  updateProgress();\n});\n\nwindow.addEventListener('message', (e)=>{\n  if(e.data && e.data.type==='goto-section' && DATA.find(s=>s.id===e.data.id)){\n    activeId = e.data.id;\n    renderNav();\n    renderContent(typeof e.data.qIdx === 'number' && !Number.isNaN(e.data.qIdx) ? e.data.qIdx : undefined);\n  }\n});\n\nrenderNav();\nrenderContent();\nupdateProgress();\n</script>\n</body>\n</html>\n"}